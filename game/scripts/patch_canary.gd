## patch_canary.gd —— 热更的**探针文件**：只用来回答一个问题，
## 「补丁挂上去之后，代码到底换没换」。
##
## ## 为什么需要它
##
## 2026-09-10 查出来：这条热更流水线从 09-02 建成起打的**每一个补丁都是白发的**。
## 下载成功、验签通过、SHA 对得上、`load_resource_pack()` 返回 true、状态记上了、
## 启动证明也过了 —— 每一步都报成功，而代码一个字节都没换。
##
## 原因是导出预设的 `script_export_mode`（Godot 4.5 新建预设默认 = 2，压缩二进制 token）：
## 导出时 `foo.gd` 被编译成 `foo.gdc`，另放一个 `foo.gd.remap` 指过去，**包里没有 `.gd`**。
## 补丁塞的是纯文本 `.gd`，挂上之后 `.gd` 是有了，但 `.remap` 还在、还指着基线那份 `.gdc`——
## 引擎按 remap 走，补丁那份没人看。现在预设已改成 0（纯文本），但**光靠「记得别改回去」
## 是靠不住的**，所以要有这个文件。
##
## ## 怎么用
##
## `tests/build_patch.gd` 打每个补丁时都会**重写**这个文件、把本次补丁号写进 `BUILD`。
## 发补丁时 `tools/build_patch.sh` 拿目标 tag 真导一个包、挂上刚打好的补丁、
## 让 `scripts/patch_probe.gd` 读这里的 `BUILD`：读回补丁号才算数，读回 0 就是没生效。
##
## **判据必须落在一个基线里已经存在的文件上** —— 补丁新加的路径在基线里没有 `.remap`，
## 纯文本那份会被正常加载，于是连坏掉的流水线也能过。这个文件因此必须**长期躺在仓库里**、
## 跟着每次全量发版进基线包，哪怕客户端一次都不读它。
##
## 仓库里这份永远是 0：只有补丁包里那份才带真的补丁号。
const BUILD := 0
