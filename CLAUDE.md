# CLAUDE.md —— Cell War 新版仓库协作规则

本仓库是《Cell War》新版电子化工程。动手改代码前先读 `docs/架构说明书.md`。

## 三条基本规则（用户定的，必须执行）

1. **开发日志**：发生重大变化、重要决策、程序关键点落地时，必须在 `docs/开发日志.md` 追加条目（日期 + 做了什么 + 为什么）。随做随写，不要等任务全部结束才补。
2. **可读性优先**：代码为"后续人工接手修改"而写——清晰命名、小函数、单一职责；注释解释"为什么"而不是"是什么"；宁可平直，不要炫技。
3. **架构说明书同步**：`docs/架构说明书.md` 必须与代码一致。新增/调整模块、目录、关键流程时，在同一次提交里更新它，让别的 AI 接手也能快速了解项目结构。

## 仓库约定

- 推送前先 `git pull --rebase`（用户也会自己往仓库推东西）。
- 提交信息说清楚"改了什么、为什么"。
