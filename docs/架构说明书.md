# Cell War 新版 · 架构说明书

> 本文档是项目结构的唯一权威说明，供 AI 与人类协作者快速接手。
> **铁律：架构层面的任何变化（新模块、目录调整、关键流程改动）必须在同一次提交里同步本文档。**

## 项目是什么

《Cell War》桌游（免疫细胞 vs 癌细胞）的电子化新版工程。**规则已修改，从零重建，不继承旧版代码**；引擎 Godot 4（GDScript）。

- 规则原文：`Cell War新版规则.md`（在仓库外，用户本地）
- 旧版 demo（旧规则，可作实现思路参考）：https://github.com/Kevinnb66699/SpringSense-2026-2027-Cell-War

## 当前状态（2026-08-26）

规则引擎 + AI + 平衡模拟器可用，36 项无头测试全绿。**尚无图形界面**——目前只能跑无头模拟。

规则文档仍有两块空白：**世界事件**和**卡牌**。原型里分别是「跳过」和「空白卡」，所有平衡数据都是在这个前提下测的。

## 目录结构

```
Cell-War/
├── CLAUDE.md              # 协作规则（Claude Code 自动加载）
├── AGENTS.md              # 其他 AI 工具入口
├── README.md
├── .gitignore
├── docs/
│   ├── 架构说明书.md        # 本文档
│   ├── 开发日志.md          # 重大变化与决策记录
│   ├── 规则电子化说明.md     # 规则歧义处的临时决策（每条都待团队确认）
│   ├── 平衡测试报告.md       # AI 互搏数据、问题诊断、推荐方案
│   └── 地图布局_127格.svg    # 棋盘与特殊组织位置（每格标了轴坐标，可与美术图逐格核对）
└── game/                  # Godot 工程
    ├── project.godot
    ├── scripts/
    │   ├── core/          # 规则引擎（不依赖任何 UI）
    │   └── ai/            # AI 决策
    └── tests/             # 无头脚本（测试 / 模拟 / 复盘）
```

## 核心设计：问答桥

引擎**不知道**谁在做决定。需要玩家抉择时，引擎调 `game.ask(pid, req)` 抛出一个「问题 + 选项列表」，由**桥**（`CWBridge` 子类）返回选中的下标。

```
CWGame ──ask(req)──> CWBridge ──> 启发式 AI / 将来的 UI / 将来的联机对端
       <──选项下标──
```

这样同一套规则代码可以驱动 AI 互搏、人机对战、热座、联机，互不干扰。**新增交互时不要在引擎里直接读 UI，一律走 ask()。**

`req` 的形状：`{ kind, pid, prompt, options: [{label, data}], (tag) }`
`kind` 取值：`setup_place` / `action` / `attack_target` / `differentiate` / `remodel_target` / `revive` / `confirm`

## 模块地图

### 规则引擎 `game/scripts/core/`

| 文件 | 职责 | 注意事项 |
|---|---|---|
| [cw_data.gd](../game/scripts/core/cw_data.gd) | 静态数据：棋盘几何、特殊组织位置、规则常量、名称表 | 全是 `const`/`static`，无状态。当前棋盘为**半径 6 / 127 格**；换地图只改这里的 `BOARD_RADIUS` 与 `CORES/MARROWS/VESSELS`，其余代码不写死尺寸 |
| [cw_tuning.gd](../game/scripts/core/cw_tuning.gd) | 平衡旋钮。默认值 = 规则原文；`recommended()` = 当前推荐方案 | 平衡测试只改这里，**不要改引擎代码** |
| [cw_game.gd](../game/scripts/core/cw_game.gd) | 对局状态 + 通用工具 + 主循环 + 胜负判定 | 各流程模块持有它的强引用，**对局结束必须调 `dispose()` 断环**，否则内存泄漏 |
| [cw_setup.gd](../game/scripts/core/cw_setup.gd) | 开局：建棋盘、铺初始癌组织、抽癌细胞种类、落子 | 初始癌区目前是「自中央格向外铺」的固定布局 |
| [cw_world.gd](../game/scripts/core/cw_world.gd) | 世界回合 S/E 两阶段结算 | 结算顺序即规则顺序，改动前先对照规则文档 |
| [cw_turn.gd](../game/scripts/core/cw_turn.gd) | 玩家回合循环：反复「选行动→执行」直到结束回合 | 有 `MAX_ACTIONS_PER_TURN` 死循环保护 |
| [cw_actions.gd](../game/scripts/core/cw_actions.gd) | 主动技能、移动、攻击结算 | `build_options()` 生成合法行动（含费用校验），`execute()` 执行 |
| [cw_cards.gd](../game/scripts/core/cw_cards.gd) | 卡牌系统 | **目前是桩**：卡池未定义，抽卡只得到空白卡 |
| [cw_bridge.gd](../game/scripts/core/cw_bridge.gd) | 问答桥基类 | 默认永远选第 0 项 |

### AI `game/scripts/ai/`

| 文件 | 职责 |
|---|---|
| [heuristic_bridge.gd](../game/scripts/ai/heuristic_bridge.gd) | 启发式 AI，双方阵营都能打。不使用随机数，保证同种子可复现 |

### 测试与工具 `game/tests/`

| 文件 | 用途 | 运行方式 |
|---|---|---|
| [headless_test.gd](../game/tests/headless_test.gd) | 36 项单元/回归测试 | `--script res://tests/headless_test.gd` |
| [balance_variants.gd](../game/tests/balance_variants.gd) | 多方案 × 多人数胜率矩阵 | `--script res://tests/balance_variants.gd` |
| [balance_sim.gd](../game/tests/balance_sim.gd) | 单配置详细统计（含癌细胞种类表现） | `--script res://tests/balance_sim.gd` |
| [dump_game.gd](../game/tests/dump_game.gd) | 导出单局完整日志，人工复盘用 | `--script res://tests/dump_game.gd` |

命令模板（本机 Godot 路径）：

```bash
"D:/Godot/Godot_v4.5-stable_win64.exe/Godot_v4.5-stable_win64_console.exe" --headless --path game --script res://tests/headless_test.gd
```

## 关键约定（改代码前必读）

1. **能量用「十分能量」整数存储**：`30` = 3.0 能量。避免浮点误差，保证回放/联机确定性。显示用 `CWData.fmt()`。
2. **所有随机数必须走 `game.rng`**。用了别的随机源，确定性测试（同种子两局哈希一致）会立刻抓到。
3. **不要给 `CWGame` 加名为 `log` 的方法**——GDScript 的内置数学函数 `log()` 不能被覆盖，会导致整个类编译失败。日志方法叫 `log_msg()`。
4. **新增 `class_name` 脚本后必须先 `--import` 再跑测试**，否则报 "Identifier not declared"。
5. **进入一格的所有效果都在 `CWActions.enter_tile()` 里**（定殖/净化/特殊组织收取/标记刷新）。移动、血管传送、复活落位共用它——加新效果加在这一处，不要散落各处。
6. **新增规则状态字段时，要同步更新 `CWGame.state_hash()`**，否则确定性测试会漏检。
7. **平衡数值改动走 `CWTuning`**，不要直接改 `CWData` 常量——`CWData` 是规则原文的记录。
8. **不要在代码或测试里写死棋盘尺寸**（`61`/`127`/`41`……）。一律引用 `CWData.TOTAL_TILES`、
   `CWData.BOARD_RADIUS`、`CWData.CANCER_WIN_WEIGHTED` 等常量。2026-08-27 从 61 格换到 127 格时，
   4 处写死的数字全部变成了假失败，排查成本远高于一开始就引用常量。
9. **改棋盘尺寸后必须重跑平衡**：收入公式挂钩「健康组织格数」，棋盘面积一变，
   低保/封顶的相对位置就全变了——61 格调好的数值搬到 127 格会让癌胜率从 51% 掉到 10%。

## 关键决策记录

决策的时间线见 [开发日志](开发日志.md)；本节只保留「现在为什么是这样」的最终结论。

- **引擎与 UI 用问答桥解耦**：旧版验证过的架构，同一套规则可驱动 AI/人机/联机。
- **平衡数值抽成 CWTuning 旋钮**：规则平衡有大量未定项，需要不改代码就能批量试方案。默认值保持规则原文，让「按规则跑」永远是一行代码的事。
- **规则歧义处一律先拍临时决策、记进文档再实现**：见 [规则电子化说明](规则电子化说明.md)，每条都标了待团队确认。
- **确定性优先**：所有随机走 `game.rng` + `state_hash()` + 同种子重放测试。这是将来做联机（确定性锁步）和 AI 模拟打分的前提，代价很小但补不回来。
