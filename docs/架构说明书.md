# Cell War 新版 · 架构说明书

> 本文档是项目结构的唯一权威说明，供 AI 与人类协作者快速接手。
> **铁律：架构层面的任何变化（新模块、目录调整、关键流程改动）必须在同一次提交里同步本文档。**

## 项目是什么

《Cell War》桌游（免疫细胞 vs 癌细胞）的电子化新版工程。**规则已修改，从零重建，不继承旧版代码**；引擎 Godot 4（GDScript）。

- 规则原文（PRD）：`Cell_War PRD.md`（在仓库外，队友随时改，动规则相关代码前先重读比对；旧的《Cell War新版规则.md》已删除——2026-08-28 团队决定，它停在 61 格版）
- 旧版 demo（旧规则，可作实现思路参考）：https://github.com/Kevinnb66699/SpringSense-2026-2027-Cell-War

## 当前状态（2026-08-29）

引擎已**全量对齐 PRD**（差异清单见 [PRD差异对照](PRD差异对照.md)），**564 项无头测试全绿**。

**规则**：127 格棋盘、15 格初始癌区、S/E 两阶段完整结算、四种真实癌细胞及其八条专属技能、
四种免疫分化、【E-微环境压迫】/「坏死」/【I-各司其职】、能量损失五步管线、两个 E 类胜利判定。

**流程是状态机**（`CWGame.pending()` / `step()` / `snapshot()` / `restore()`）——
推进到哪一步是数据不是调用栈，AI 因此能从任意局面往前推演。详见「核心设计：流程状态机」。

**远端的 `refactor/elm-update` 分支不合并**（团队 2026-08-28 定）：那是验证
「msg 驱动 / 状态机」方向的原型分支，方向已由 9868ca3 在 main 以 `cw_*` 形态落地。
分支分叉早于 PRD 对齐、没有下述全部界面，后续开发一律基于 main。

**图形**：棋盘渲染、格子点选与高亮、掷骰骰子、主菜单「活体棋盘」、开场三拍、
右侧竖条 HUD、行动栏（按钮不消失只变暗）、手牌抽屉、暂停菜单、退出确认、结算屏。
免疫五种、癌症四种的细胞精灵都已接线，并带**六帧静息呼吸动画**
（2026-08-29 接入 `assets/art/cells/anim/` 的九张表图：`Sprite2D.hframes=6`、
6 帧/秒、相位按细胞编号错开；主菜单装饰与侧栏等静态场合仍用单帧图）。
从主菜单能一路玩到底，**全程不切场景**。

**卡牌：66/66 全部可结算**（2026-08-29 四片落齐）：身份按 PRD 落地
（`cw_card_data.gd`，由 `tools/gen_card_data.py` 生成），抽卡按等级/分期选池、剔除同名技能。
`cw_card_fx.gd` 住着 20 张无选择卡、11 张需中途选择的卡（结算中途走 `game.ask`，
抽卡链路因此是**协程**，详见「询问桥」）、15 张修饰卡（自我修饰挂 `cell["mods"]`，
全局修饰复用 `events["active"]`，口径 #57~#64）与 **21 张永久技能 + 装备位**
（打出即装备 `cell["equipped"]`、「技 N」计数、死亡不掉；效果按 equipped 被动查询，
「每行动回合/每世界回合第一次」用 `fx_turn`/`fx_round` 闸门，口径 #65~#70）。
**真人已能打**（方案甲：左键点手牌 → 棋盘高亮目标 → 点格结算；
右键弃置走确认条；无目标卡补一拍确认）。事件卡结算弹带效果说明的通报。
**AI 也会用卡了**（2026-08-29 深夜）：启发式桥按「出牌免费、只看时机」打分出牌，
能量宽裕时抽卡；另有静态估值 + 扁平蒙特卡洛桥可选（默认参数单决策约 135ms，
见 AI 模块表与「下一步」三）。「任意时间」时机已于 2026-08-30 由团队取消
（口径 #72）：六张卡（细胞膜修复/缺氧适应/PD-L1/DNA损伤修复/BCL-2/癌症干性）
一律改为**己方回合可用 + 生效时长「下一次」**，打断窗口不做了。

**世界事件全量落地**（2026-08-28 深夜，W1~W8 定案后）：`cw_world_fx.gd` 在
3/6/10/15/20/25/30 回合等权随机抽取（同局不重复），18 个事件全部实装。
持续效果住在 `game.events["active"]` 修饰器容器里，结算点用 `game.event_stacks(名)`
查询——**这就是卡牌修饰框架（对照 5.1 #26）的底座**，卡牌侧将来塞条目即可复用。
容器整体进快照与 `state_hash`。边界口径已全部定案（2026-08-29：双重触发按**三档**结算、
紊乱回合末**方案A 同时返回**、免疫伪装按 PRD **大成功并给成功**），清单见对照 §六。

⚠ **平衡：有卡版已重跑**（2026-08-29 深夜，数值一个没动）。启发式互搏
4/6 人局癌方仍 100%（但免疫从 3.0 撑到 3.7 回合）；**免疫换蒙特卡洛后打到
55% 胜**——「癌方必胜」相当一部分是 AI 强度的假象；双方都上蒙特卡洛则
癌 90/免 10，结构性优势仍在但不再是铁板。完整对照表与三条读法见
[平衡方案_PRD版](平衡方案_PRD版.md) 第六节。团队定的**先不调数值**不变；
真要调，建议先把基准 AI 定为「双方蒙特卡洛（小参数）」。2 人局不再作为平衡目标。

**两处美术占位**（代码里都标了）：固化癌组织的硬化外壳（现用压暗色标）、
癌变血管（与健康血管共用一张图）。癌细胞精灵 2026-08-28 已接线，占位图形随之删除。

**对局配置面板已上线**（2026-08-29 深夜）：人数 2/4/6、阵营（含观战）、
AI 强度（普通/较强=蒙特卡洛），「开始对局」后弹出；**悬停格子详情**与
**被动技能悬浮框**同日跟上（设计稿都在「界面小块」画布，见开发日志）。
**界面小块已全数上线**（2026-08-29 深夜，一天七件）：对局配置面板、
悬停格子详情、被动技能悬浮框、对局日志面板（L 键）、规则速查、
**存档读档**（暂停「保存并退出」↔ 主菜单「继续对局」，存档=流程快照、
只在 pending 边界可写）、设置（AI 节奏 / 掷骰动画）。主菜单五项全亮。
暂停菜单内的规则/设置入口已接（2026-08-30，页压页复用同款页面类）。
剩下的是演出级润色：事件卡飞卡动画。

## 下一步：三条路（2026-08-28 团队记录）

按当前的依赖关系排，**第 1 条是主线**，2、3 可以并行插队。

### 一、卡牌效果（主线）—— ✅ 2026-08-29 全部完成

66/66 张卡可结算，四片落地：20 无选择 → 11 需中途选择（协程化 + game.ask）
→ 15 修饰批（mods 框架）→ 21 永久技能 + 装备位（equipped 被动查询 +
fx_turn/fx_round 闸门）。待团队过目的口径集中在 规则电子化说明 #51~#73。
**数值修正有一条通则**（#73，2026-08-30 定案）：同一数值受多个效果影响时，
基准值 → 卡牌按**打出先后**逐张结算（永久技能算作卡牌，与即时卡同队）→ 世界事件最后；
减免只降不升。**能量损失不走这条**，它有 PRD 自己的五步管线。
即时卡与永久技能靠 `cell["play_n"]` 这把每细胞的时序尺比先后。

**主线已无待办**：原先挂着的「任意时间」打断窗口于 2026-08-30 由团队取消
（口径 #72，改为己方回合可用 + 「下一次」时长）。引擎侧本就只允许己方回合打手牌，
时长侧只有【缺氧适应】需实改，且团队顺势重写了它的卡面：两个半句并成一面
一次性护盾「下一次【微环境压迫】或癌细胞技能造成的能量损失 -1」（压迫的免压特判随之删除）。
**PRD 卡面的「使用时机：任意时间」待队友改字。**

### 二、平衡

**有卡版基线已重跑**（2026-08-29 深夜，见上面 ⚠ 段与
[平衡方案_PRD版](平衡方案_PRD版.md) 第六节）。下一步是团队的：
① 看重跑数据，定「平衡实验的基准 AI」（建议双方蒙特卡洛小参数——等强、
确定性、100 局 7 分钟）；② 还要不要按方案丙动数值（AI 变强后幅度预期变小）。
手上已有的东西：消融数据、`CWTuning` 旋钮、模拟脚本，随时能跑网格。

### 三、AI —— 三步走完成两步半（2026-08-29 深夜）

1. ✅ **会用卡**（启发式桥）：出牌免费所以只评「时机」——直接效果有目标就打、
   增益看马上用不用得上；代谢耦联/选格子等中途询问给出真答案，不再一律答 0 弃权
2. ✅ **静态估值函数**（`cw_eval.gd`）：地盘按胜利判据口径加权是主导项，
   另计固化进度、能量、手牌/装备、记忆/等级、战线距离。零和（免疫视角=取负）
3. ✅ **扁平蒙特卡洛**（`monte_carlo_bridge.gd`）：逐候选「快照→试走→粗跑→估值→回滚」。
   实测默认参数（rollouts=2 / horizon=40）单决策约 135ms、2 人整局 3.4s——
   同种子的对照局里免疫从 3 回合撑到了 6 回合。**已接进真对局**（同日深夜）：
   对局配置面板的「AI 强度：较强」= CWUIBridge 开推演闸（enabled）
4. 真要更强再上 **ISMCTS**（手牌是隐藏信息，要 determinization；骰子/抽卡要机会节点；
   4~6 人不是两人零和）

**注意**：平衡测试用的 AI 要**快且一致**（用启发式桥或调小蒙特卡洛参数），
人机对战用的 AI 可以慢。两者不必是同一个 —— 硬要用一个只会两头不讨好。

## 目录结构

```
Cell-War/
├── CLAUDE.md              # 协作规则（Claude Code 自动加载）
├── AGENTS.md              # 其他 AI 工具入口
├── README.md
├── .gitignore
├── docs/
│   ├── 架构说明书.md        # 本文档
│   ├── 开发日志.md          # 重大变化与决策记录
│   ├── 规则电子化说明.md     # 规则歧义处的临时决策（每条都待团队确认）
│   ├── 平衡测试报告.md       # AI 互搏数据、问题诊断、推荐方案
│   └── 地图布局_127格.svg    # 棋盘与特殊组织位置（每格标了轴坐标，可与美术图逐格核对）
└── game/                  # Godot 工程
    ├── project.godot      # 主场景 = scenes/Main.tscn；贴图过滤必须是最近邻（像素素材）
    ├── scenes/            # 场景（.tscn 静态布局，编辑器里可改）
    │   ├── Main.tscn      #   唯一入口：Camera2D + Board + MainMenu + Match（共用同一台相机与棋盘）
    │   ├── MainMenu.tscn  #   主菜单的静态部分：压暗层 / Logo / 菜单项 / 光晕 / 版本号
    │   ├── Match.tscn     #   对局子树：Match + UI（**没有自己的棋盘和相机**，不能单独运行）
    │   ├── Board.tscn     #   棋盘
    │   └── Tissue.tscn    #   单个组织块（Sprite2D）
    │                      #   Match.tscn 的 UI 层：Panel 竖条 / ActionBar 行动栏 /
    │                      #   Hand 手牌 / Toast 骰子旁的说明 / Pause 暂停菜单
    ├── assets/
    │   ├── art/           # 组织块贴图 32×34（健康/癌变 × 普通/核心/骨髓 + 血管）
    │   │   └── cells/     #   细胞精灵：免疫 24/32px（普通/B/T/巨噬/树突）、
    │   │                  #   癌症 32px（黑色素瘤/印戒/骨肉瘤）+ 小细胞肺癌 16px
    │   ├── fonts/         # 点阵字体 + 许可证，字号规则见该目录 README
    │   └── shaders/       # dice.gdshader 骰子 / silhouette.gdshader 格子高亮剪影
    ├── scripts/
    │   ├── core/          # 规则引擎（不依赖任何 UI）
    │   ├── ai/            # AI 决策
    │   └── ui/            # 表现层（渲染 / 将来的菜单与 HUD）
    └── tests/             # 无头脚本（测试 / 模拟 / 复盘）
```

## 核心设计：问答桥

引擎**不知道**谁在做决定。需要玩家抉择时，引擎调 `game.ask(pid, req)` 抛出一个「问题 + 选项列表」，由**桥**（`CWBridge` 子类）返回选中的下标。

```
CWGame ──ask(req)──────> CWBridge ──> 启发式 AI / 蒙特卡洛 AI / 人类 UI / 将来的联机对端
       <──选项下标──
       ──show_roll(…)──> CWBridge ──> 掷骰动画（引擎 await 到播完）
       ──show_result(…)─> CWBridge ──> 一句话通报（不 await，提示自己淡掉）
```

`show_roll()` 是同一条桥上的**单向通知**：引擎先用 `rng` 掷出结果，再把结果送给表现层去演，
并 `await` 到动画播完才继续结算。**表现层无权决定结果**，这是约定 #11。
它**广播给所有桥并按对象去重** —— AI 掷的骰旁观者也得看见，热座共用一个 UI 桥时只演一次。

签名：`show_roll(reason, value, sides, pid, at)`。`at` 是这次掷骰指向的格子
（攻击给目标格，突变/抗体给发动者所在格）—— 骰子要落在棋盘上那一格旁边，
坐标只有引擎知道。`pid` 可用于显示「谁掷的」，**但不用来给骰子染色**：
骰面颜色已经表示结果档位，再叠阵营色会两套语义打架（决策「甲」）。

`show_result(text, at)`（由 `CWGame.announce()` 广播，同样按对象去重）是掷骰的**结算说明**——
"攻击大成功"、"突变：无事发生"。**文字由引擎给**：点数怎么判读是规则，
表现层照着点数自己再判一遍等于把规则抄了第二份，改一处就会对不上。
不 `await`，因为提示自己会淡掉，不该卡住结算。

这样同一套规则代码可以驱动 AI 互搏、人机对战、热座、联机，互不干扰。**新增交互时不要在引擎里直接读 UI，一律走 ask()。**

`req` 的形状：`{ kind, pid, prompt, options: [{label, data}], (tag) }`
`kind` 取值：流程状态机的顶层询问 `setup_place` / `immune_revive` / `revive` / `action`，
加上卡牌结算的**中途选择**（2026-08-29 随「需中途选择」批引入，tag=卡名）：
`free_move`（走一步/停）/ `pick_cell`（选细胞）/ `pick_tile`（选格）/ `pick`（按钮多选一）。
中途选择里「可以不做」的询问**下标 0 恒为停止/放弃**——基类桥永远答 0，天然安全。
由此抽卡链路是协程：`draw → resolve_event → enter_tile → collect_special` 一路 await
到 `pending()`/`step()`；**漏 await 的调用会脱离结算顺序**，复现测试会当场炸
（各文件头注有清单，新调用点照抄 await）。

## 核心设计：流程状态机（2026-08-28）

**对局推进到哪一步是数据，不是调用栈。**

```
game.pending()          # 返回「轮到谁、要做什么决定、有哪些选项」；空 = 对局结束
game.step(choice)       # 落子并推进到下一个决策点
game.snapshot()         # 整份状态（含 rng.state）
game.restore(snap)      # 原样放回去
```

`pending()` 和 `step()` 都是协程，但**只在两种情况下真正挂起**：表现层要演出（掷骰动画），
或卡牌结算中途经 `game.ask` 追问且作答的是人类桥。同步桥（AI/测试）下 await 立即完成，
快照→落子→回滚的评估循环依旧零挂起。

**为什么要这样。** 原来的写法是 `run_game()` 一个循环跑到底，中间 `await bridge.ask()`。
那样「现在推进到哪儿」存在**调用栈**里（哪个函数、循环跑到第几个玩家），而调用栈没法快照。
AI 要评估「我走这一步会怎样」，需要的是：快照 → 落一步 → 继续跑几步 → 回滚，反复几千次。
所以流程位置必须搬进 `flow` 这个字典，推进必须是一次函数调用。

**三条随之而来的约定（2026-08-29 随「需中途选择」批修订）：**

1. **行动的入口全是顶层选项。** 分化、裂解（要不要顺带净化）、血行转移落点、跃进方向
   全摊平成独立选项，参数直接写在 `data` 里——AI 选行动时看到的就是原子行动。
   少数卡在**结算里**的后续决定（趋化募集的走位、代谢耦联的数额……）经 `game.ask` 追问；
   「可以不做」的追问下标 0 恒为停止/放弃，基类桥答 0 天然安全。
2. **`advance()` 路上不掷骰、不演出。** 唯一可能挂起的是自动结算连锁里的中途询问
   （踩骨髓抽到要选择的事件卡这类），且只有人类桥作答时真挂起。
3. **随机数发生器的 state 属于快照。** 少了它，同一步走两遍会掷出不同的骰子，推演就没有意义。
   挂起只发生在等人类作答的那一瞬，快照永远取在 pending 边界上——那时没有悬着的协程。

`run_game()` 退化成一个薄壳：`while pending(): step(await ask(...))`。
AI 互搏、平衡模拟、无头测试都只要「跑完一局」，用它就够。

实测：单步 **0.106 ms**，一局 4 人局约 292 步 / 31 ms。3 秒的思考预算约合 **2.8 万步推演**。
`snapshot()` 深拷贝一次约 **0.16 ms** —— 真要跑到十万次量级时，
这里应该换成「落子 / 悔子」（make-unmake）而不是整份复制，但那要先有回滚日志。

`stop_at` 字段可以让 `advance()` 在某个阶段开头停下（测试和工具用，`""` = 不停）。

## 模块地图

### 规则引擎 `game/scripts/core/`

| 文件 | 职责 | 注意事项 |
|---|---|---|
| [cw_data.gd](../game/scripts/core/cw_data.gd) | 静态数据：棋盘几何、特殊组织位置、规则常量、名称表 | 全是 `const`/`static`，无状态。当前棋盘为**半径 6 / 127 格**；换地图只改这里的 `BOARD_RADIUS` 与 `CORES/MARROWS/VESSELS`，其余代码不写死尺寸 |
| [cw_tuning.gd](../game/scripts/core/cw_tuning.gd) | 平衡旋钮。**默认值 = PRD**；`split_income()` 是验证人数缩放的实验档 | 平衡测试只改这里，**不要改引擎代码**。当前 PRD 数值下平衡是崩的，见 [平衡方案_PRD版](平衡方案_PRD版.md) |
| [cw_game.gd](../game/scripts/core/cw_game.gd) | 对局状态 + **流程状态机**（`pending`/`step`/`advance`）+ 快照回滚 + 能量损失管线 + 胜负判定 + **修饰条目 API**（`add_mod`/`mods_of`/`spend_mods`/`clear_mods`/`install_event`） | 各流程模块持有它的强引用，**对局结束必须调 `dispose()` 断环**。扣能量只能走 `settle_loss()` 那五步；护盾/减免修饰已挂在 `immune_hit`/`cancer_hit` 里（第⑤步），别在外面自己减。**全引擎只有这两个损失入口**，绕开管线的只有卡面明写的两处（细胞毒性增强 T 的 +1.0、突变自扣），盘点见规则说明 #78；【囊性护甲】走 `armor_cut()`，两条管线共用同一个每世界回合额度 |
| [cw_setup.gd](../game/scripts/core/cw_setup.gd) | 开局：`begin()` 建棋盘铺癌区抽种类，`place_options()`/`place()` 落子，`finish()` 原发灶 | 初始癌区目前是「自中央格向外铺」的固定布局（PRD 的 15 格） |
| [cw_world.gd](../game/scripts/core/cw_world.gd) | 世界回合结算：`round_start()` / 两处复活的 `*_options`+`revive_*` / `aerobic()` / `e_phase()` | **结算顺序逐条照抄 PRD**，改动前回去核对。最容易踩的是「增生在侵蚀之前」 |
| [cw_turn.gd](../game/scripts/core/cw_turn.gd) | 只剩回合的开场与收尾（`begin_turn`/`end_turn`） | **循环本身已经搬进 CWGame 的流程状态机**；这里保留 `MAX_ACTIONS_PER_TURN` 护栏常量。「本回合」时钟的修饰卡在 `end_turn` 过期 |
| [cw_damage.gd](../game/scripts/core/cw_damage.gd) | **伤害结算**：DamageEvent / DamageResult / LethalEvent，七阶段管线 | 按队友《攻击与伤害结算系统设计》实装（口径 #80）。**业务代码不许自己扣受击方能量**——包括「无视减伤」，那是带 `UNPREVENTABLE` 的事件。范围伤害走 `game.immune_hit_area()` / `cancer_hit_area()` 整批提交 |
| [cw_cost.gd](../game/scripts/core/cw_cost.gd) | **费用结算**：CostContext / CostModifier / CostQuote，`quote()` 纯查询报价、`commit()` 原子扣费 | 按队友《费用结算系统设计》实装（口径 #79）。修饰登记在 `TEMPLATES`（语义阶段 + 条件谓词 + 消耗策略），卡牌/永久技能/世界事件三种来源统一收集。免费额度进竞争组，一次最多消耗一个 |
| [cw_actions.gd](../game/scripts/core/cw_actions.gd) | 主动技能、移动、攻击结算 | `build_options()` 生成合法行动（含费用校验），`execute()` 执行。`enter_tile()`/`collect_special()` 是**协程**（踩骨髓可能抽到要中途选择的事件卡），调用点一律 await。迁移选项单独抽成 `immune_move_options()`——【全身免疫动员】复用同一份合法性与定价。**费用全部外包给 `CWCost`**：本文件只算**基准价**（`_move_base_cost`，含免疫等级与细胞自带技能）并建上下文，谁减多少一概不管。`_move_cost_mod()` / `_skill_move_cost()` / `_purge_cost()` 都是 `CWCost.quote()` 的薄壳；`_do_move()` / `_do_homing()` / `_do_jump()` / `_do_lyse()` 走 `CWCost.commit()` |
| [cw_card_data.gd](../game/scripts/core/cw_card_data.gd) | 66 张卡的**身份**：卡名 / 类别 / 各池权重 | **由 `tools/gen_card_data.py` 从 PRD 生成，不要手改**。不含效果 |
| [cw_card_fx.gd](../game/scripts/core/cw_card_fx.gd) | 卡牌**效果**：全部事件 + 全部即时技能（需中途选择批经 `game.ask` 追问；修饰批在 play 里挂条目，触发散在各挂接点）+ 永久技能的「打出即装备」入口 | **66/66 全部住进来了**（永久技能的效果按 `equipped` 散在各挂接点，见 cw_game/cw_actions 行）。卡牌伤害不算「攻击」——`immune_hit(attack=false)`；能量数值一律过 `_amp()`（【信号放大】）；事件结算自己发带效果说明的通报（`_evt()`）；「可以不做」的追问下标 0 恒为停止 |
| [cw_world_fx.gd](../game/scripts/core/cw_world_fx.gd) | **世界事件**：抽取（等权随机、同局不重复）、18 个事件的结算、持续效果的修饰器容器 | 状态在 `game.events`（进快照/哈希）；结算点查 `game.event_stacks(名)`。**卡牌修饰框架（#26）复用这套容器**，别另起炉灶——已住进来的有【基质稳定】【TGF-β释放】【TNF-α局部炎症】。要区分「这条是世界事件还是卡牌挂的」用 `is_world_event()`，**别拿 `DURATION` 表当判据**（那张表只列世界事件，卡牌条目会误判成「被双重触发的本回合类事件」）|
| [cw_cards.gd](../game/scripts/core/cw_cards.gd) | 抽卡：选池 → 剔除同名 → 按权重抽 | `draw()` 是**协程**（事件卡立即结算、结算可能中途发问），调用点一律 await。【事件】立即弃置不进手牌，未实现的事件兜底喊「效果待实现」。手牌上限 `CWData.HAND_MAX = 8`；注意 I 级池只有 6 张技能，那一级实际到不了 8 |
| [cw_bridge.gd](../game/scripts/core/cw_bridge.gd) | 问答桥基类 | 默认永远选第 0 项 |
| [cw_save.gd](../game/scripts/core/cw_save.gd) | 存档读档：`game.snapshot()` 经 `var_to_str` 落盘（user://save.cw，单档位） | **只在 pending 边界写**（中途询问的协程栈不在快照里）；读回 = 同构对局 `restore()`，待决询问原样重现。日志不入档；版本号不认识就当没档 |

### AI `game/scripts/ai/`

| 文件 | 职责 |
|---|---|
| [heuristic_bridge.gd](../game/scripts/ai/heuristic_bridge.gd) | 启发式 AI，双方阵营都能打。不使用随机数，保证同种子可复现。**会用卡**（2026-08-29）：`_score_play` 按「出牌免费、只看时机」打分——永久技能到手就装、直接效果有目标就打、攻击增益等有仗可打、防御卡等敌人贴近、迁移折扣看旁边有没有路；能量宽裕时抽卡（免疫 ≥3.0、癌症 ≥4.5 且不贴脸）。也应答全部中途选择（free_move 按净化/捡资源/接敌打分、pick_cell 按波及面、pick_tile 不弃权且挑癌性邻格多的、代谢耦联富济贫+数额拉满、基因组不稳定按记忆与能量） |
| [cw_eval.gd](../game/scripts/ai/cw_eval.gd) | 静态估值：从某一阵营视角给局面打分（癌方优势取负即免疫视角，零和）。地盘按胜利判据口径（癌 1 / 固化 2）是主导项，另计固化进度、能量、手牌/装备、记忆/等级、免疫离战线距离 | **不是规则**：权重是 AI 的品味，不进 `cw_tuning`；只在蒙特卡洛截断 playout 末端用 |
| [monte_carlo_bridge.gd](../game/scripts/ai/monte_carlo_bridge.gd) | 扁平蒙特卡洛桥（继承启发式）：只接管 `action` 顶层询问，逐候选「快照 → 试走 → 按启发式粗跑 horizon 步 → 估值 → 回滚」，其余询问与中途选择沿用启发式 | 评估期间 `game.bridges` 整体换成同步启发式代打（人类桥绝不会在推演里被问到）、`sim_quiet` 静音日志；零随机数（playout 去相关靠烧 rng），同种子同答案。默认 rollouts=2 / horizon=40 ≈ 单决策 135ms（人机档）；平衡模拟嫌慢就调小或直接用启发式桥 |

### 表现层 `game/scripts/ui/`

| 文件 | 职责 | 注意事项 |
|---|---|---|
| [board.gd](../game/scripts/ui/board.gd) | 把棋盘画出来；`tile_center()` 轴坐标→像素、`hex_at()` 像素→轴坐标；`set_marks()` 设置格子高亮 | 尺寸与布点**只读 `CWData`**，本层不留第二份坐标（见约定 #10）。要把东西摆到某格上一律走 `tile_center()`；高亮是**淡入淡出 + 同心圆由内向外**揭示的（圆心取这批格子的重心）；`set_marks()` 每帧都会被调用，所以目标没变时必须什么都不做（节点要复用，否则补间永远走不完）。**深度一律走 `tile_z(格, above)`**（`tile_center()` 是顶面中心、比贴图中心高 4px，拿它当 z 用东西会掉到棋盘后面——骰子踩过）；点选一律走 `hex_at()` |
| [cw_view.gd](../game/scripts/ui/cw_view.gd) | 机位：菜单/对局两组参数、「看点→锚点→zoom」的反解、两组之间的插值 `blend()` | 全工程只有一台相机，两组参数**必须摆在一起**——开局过场就是在它们之间插值。对局机位 zoom 1.45 / 锚点 (348,270) 与右侧竖条 264px 是同一套推导。**过场插的是取景参数、zoom 走几何插值**——直接插 `camera.position` 会两端都对、中间取景游走 |
| [main.gd](../game/scripts/ui/main.gd) | 入口协调：主菜单 →「开始对局」→ 开场三拍 → 交还控制权；暂停菜单的「返回主菜单」；**对局结束 → 结算屏 → 再来一局 / 返回主菜单** | 过场时长常量**集中在这里**，别散进各个 Tween。**Control 的 `gui_input` 不会自动吃掉事件**：菜单项点击必须 `set_input_as_handled()`，否则那一下会漏到这里被当成「点一下跳过」，过场整个消失。相机与绽开是两段**前后相接**的动画，绝不能挂同一条时间轴（会把绽开整段跳掉，见开发日志） |
| [match.gd](../game/scripts/ui/match.gd) | 一局对局的装配与呈现：建引擎、注册桥、把状态画到棋盘上、演开场绽开 | 规则一行都不在这里。呈现是**每帧全量刷新**（`set_tissue()` 贴图没变即返回），所以画面不可能和状态对不上。细胞贴图**一律不缩放**（最近邻过滤，非整数倍会磨出锯齿），高度 16/24/32 三种，靠 `_set_cell_art()` 对齐脚底而不是中心。免疫的贴图每帧对一次（分化会改 `itype`），癌症的建节点时定一次（`ctype` 一局只读） |
| [ui_bridge.gd](../game/scripts/ui/ui_bridge.gd) | 表现层的询问桥：人类玩家的询问界面 + 掷骰演出 | **继承蒙特卡洛桥**（`enabled` 关着=启发式，对局配置面板的「AI 强度」拨它；默认关）：同一个对象注册给所有玩家，人类走界面、其余走 AI，演出按对象去重只演一遍。询问界面只有一条规则：选项 `data` 里**带 `to` 就进棋盘、不带就进按钮**，六种询问全覆盖。「迁移」是两段式（先点技能再点格子）且**切换式**——走完一步继续停在选目标格上，退出条件只有「主动退出 / 没有可达格 / 换人或换世界回合」（开关记着 pid + round_no）。引擎那边一步没动，是桥记住了上一次选的是迁移。**打牌走方案甲**（2026-08-29 定）：行动询问期间手牌手势直连 `_pick_hand()`——左键选卡进目标态（`cid` 目标高亮其所在格）、右键进弃置确认、目标态里点别的卡直接换卡；play/discard 仍不进行动栏。**双击=免确认**（2026-08-30）：手势第三元素带「免确认」旗，无目标卡跳过「确认打出」那拍，有目标的卡照旧进选格 |
| [action_bar.gd](../game/scripts/ui/action_bar.gd) | 底部行动栏：技能按钮 / 目标选择提示，两种形态照搬定稿 | 只有一个出口信号 `chosen(下标)`；取消不另设信号，它就是 `cancel_index` 指的那个按钮。**数字键 1..9 = 从左到右第几个按钮**（只在技能栏形态生效，数字垫灰底标在费用行前）。⚠ **横向余量很小**：最挤的 T细胞四技能占 346 / 361，加长任何标题或费用文字都可能顶出画布——`t_action_bar_width` 盯着这条。**不传 `cancel_index` 就表示这一问不能取消**，右键与 Esc 一律不响应（否则「选分化方向」时 Esc 会替玩家选中最后一项） |
| [hand.gd](../game/scripts/ui/hand.gd) | 左下角手牌抽屉：叠放、悬停抬起推开、抽卡飞入；**打出/弃置/双击手势**（左键/右键/双击信号，双击第二下不再重发单击）与选中态（半抬 34px + 其余压暗） | **一张卡同时只能有一条补间**：位置一律由 `_layout()` 统一算，`sync()` 只摆起点。两条补间同改 `position` 会「先瞬间到位再闪回去重播」 | 几个数互相咬死：`卡宽72 − 露出52 = 推开20`、`514 − 抬起86 + 112 = 540`。**间距随张数压缩**——固定 52 的话第 6 张就盖到行动栏上（手牌上限规则未定） |
| [toast.gd](../game/scripts/ui/toast.gd) | 骰子旁边那行字：掷骰时报「攻击/突变/抗体」，停稳后换成结算说明 | 文字一律由引擎给（`show_result`）。位置跟着骰子走——骰子刻意落在棋盘格上就是为了把「谁在打谁」和「结果」绑死，说明不能丢回屏幕角落。**避让对象是骰子的外框而不是格子中心**：骰子在屏幕上有一百多像素高，按格子中心摆会正好压在骰面上 |
| [pause_menu.gd](../game/scripts/ui/pause_menu.gd) | 对局中的暂停菜单（Esc）：主列表（含「保存并退出」，按 can_save 亮灭）/ 确认页两页，选中项带辉光 | **不属于左侧「面板槽」**，那是开局前的语法；这是覆盖层。暂停时 `get_tree().paused = true`（AI 间隔是 SceneTreeTimer，会跟着停），自身须为 `PROCESS_MODE_ALWAYS`。Esc 归属靠 `CWActionBar.can_cancel()` 判断，两边各自判断、不依赖事件传播顺序。「返回主菜单 / 退出游戏」要过确认页，且**默认停在「取消」上**。**规则速查/设置**（2026-08-30）：同款页面类再建一份挂在本节点下（继承 ALWAYS，暂停中照常收输入），子页开着时输入全数转给它、列表让位（盯 `visibility_changed`），Esc 只关子页不解除暂停；入口在菜单内部消化、不发 `chose` |
| [settle_screen.gd](../game/scripts/ui/settle_screen.gd) | 对局结束的结算屏（团队从四个方向里选的**丁**）：胜负宣告 + 终局盘面四个数 + 带胜利线刻度的加权条 + 两个出口 | **压暗只盖左边 696**：右侧竖条此刻是终局读数，玩家清单留在那儿，横幅只装竖条不显示的盘面数据。尺寸照搬方向稿（横幅 696×300、上下居中）。演出五拍约 1.5s，**中途任意点击/按键直接到位**；`_playing` 期间按键只用来跳过，不当成选择（防误触）。胜因**不用 `game.win_reason`**——那句自带「癌症胜利：」前缀，和 40px 标题重复，且四种结局句式不统一，改由 `_reason_text()` 按 `win_kind` 现写 |
| [match_panel.gd](../game/scripts/ui/match_panel.gd) | 右侧竖条：回合/阶段、胜负进度、玩家列表（悬停行浮出**已装备清单**）、免疫等级、钉底的「结束回合」 | 尺寸照搬定稿标注稿，**一个数都别改**：52 / 56 / 44×人数 / 44 / 52，6 人局合计 530 只余 10px——这套高度是按最挤的情况配平的。宽度必须与 `CWView.PANEL_WIDTH` 一致。技能悬浮框往面板左侧浮、没装备不浮，`_build`/`reset` 都要清悬停状态 |
| [tile_info.gd](../game/scripts/ui/tile_info.gd) | 悬停格子详情：停 0.25s 浮出（组织/固化/特殊/坏死/占据者），贴格摆、压右栏就翻左 | 内容 `describe()`、摆位 `place()`、宽度 `width_for()` 都是**纯函数**（无头测试直接核对，CWToast.place 同款打法）；宽度按最长行实测撑开（208 只是下限，定宽装不下「代谢核心 · 储量 x.x」）。悬停信号在 CWMatch start/teardown 成对开合（拆局约定）；节点压在暂停菜单下面 |
| [log_panel.gd](../game/scripts/ui/log_panel.gd) | 对局日志面板：L 键开关的左侧浮层，展示 `game.logs` | 定长 Label 池 + 「离底部多少行」偏移（0=跟随最新，上翻停住，滚回底继续跟）；行着色跟日志前缀语法（▶/★/☠/缩进）。`first_line()`/`line_color()` 纯函数直测；滚轮只在指着面板时翻页；`active` 由 CWMatch start/teardown 翻（对局外不响应 L）；开合走 `toggle()`（L 键与入口提示共用，开合重置回跟随）；标题行的 L 用 `CWStyle.keycap()` |
| [log_hint.gd](../game/scripts/ui/log_hint.gd) | 左上角「对局日志 L」常驻入口提示（定案A·2026-08-30）：灰字 + L 键帽（行动栏快捷键同款垫块），点击 = 按 L | 钉在 `CWLogPanel.RECT.position`——提示在哪面板就在哪展开，空间自洽；观战/亲手打都常驻。显隐归 CWMatch（开局亮、面板开着让位、拆局收起，`_process` 每帧对齐）；底板 1px 描边（全局 2px 在 22px 小件上太重）；键帽定尺寸（`CWStyle.keycap` 按 10px 字形带手工对中——点阵行框虚高，行框居中会偏） |
| [rules_page.gd](../game/scripts/ui/rules_page.gd) | 规则速查：主菜单与对局内暂停菜单共用的一页式总览（胜利/回合/判定/费用/固化/卡牌/快捷键） | **数字现读 CWData/CWTuning 默认值，不写第二份**——测试按常量比对。反击那半句只在 counter_dmg_on_fail>0 时显示（规则原文无反击）。Esc 走菜单路由；**右键关闭接在 gui_input**（本层 STOP 会把鼠标事件吃掉，到不了路由——覆盖层收鼠标手势都得这么接） |
| [cw_settings.gd](../game/scripts/ui/cw_settings.gd) | 玩家偏好静态存取：AI 节奏（快/标准/慢）、掷骰动画开关；user://settings.cfg 持久化 | **只放真的有东西可设的项**（音量等 BGM 进场再加）。`main._ready` 尽早 load；match 装配时读 ai_delay_ms，ui_bridge.show_roll 按 dice_anim 直接跳过演出 |
| [settings_page.gd](../game/scripts/ui/settings_page.gd) | 设置页（主菜单与暂停菜单共用）：两行值拨（视觉/键盘/拨值语汇同配置面板：固定位箭头只在焦点行亮、悬停发光，选中行标题带辉光） | 改动**即改即存即生效**，没有「取消」；行定义走函数不走常量（值的现状要从 CWSettings 读）；`_clicky` 抄自 config_panel（值行骨架仍不抽公共件） |
| [cw_style.gd](../game/scripts/ui/cw_style.gd) | 配色、字号、描边框，全工程一处 | 改配色改字号只改这里。字号只有 40/30/20/10 四档（点阵网格，见约定 #13；40 是结算屏的胜负宣告，一局只出现一次）。`box()` 的 `pad` 不传就是 0，描边会贴着字 |
| [main_menu.gd](../game/scripts/ui/main_menu.gd) | 主菜单：摆机位、摆装饰细胞、菜单项的选中/悬停/键鼠交互；「开始对局」先弹配置面板 | 构图照搬定稿原型，机位参数在 `CWView`。装饰细胞的格子坐标一律走 `board.tile_center()`。覆盖层（配置面板/退出确认）统一由本节点的 `_unhandled_input` 路由，谁开着路由给谁 |
| [config_panel.gd](../game/scripts/ui/config_panel.gd) | 对局配置：主菜单同一**面板槽**的左侧面板（人数 2/4/6、阵营含观战、AI 强度、随机种子） | 槽位语法照开局过场原型：菜单 0.30s 淡出 → 配置同位置 0.32s 淡入（不重叠），坐标按原型标注（标签 x120/值 x250/行距 42）；自带左暗罩。**「进入棋盘」变白按「最后动的设备」裁决**（Kevin 8-30 终稿）：键盘选到按钮=白；鼠标一旦划过按悬停算（悬停白/移开蓝），下一次键盘按键夺回——`_mouse_led` 一位裁判。菱形标始终跟键盘/悬停焦点（光晕用 Sprite2D 保证圆心，参数同 MainMenu Marker），**选中行标题带主菜单同款辉光**（CWPauseMenu.GLOW 四层）；拨值箭头位置固定（ARROW_R_X）、悬停转白发光。种子拨一下换一枚 → match_seed 可复现。**焦点默认在第一行**（停在按钮上玩家会以为不能改，Kevin 8-29）——行上回车=拨值，到按钮才开局；取值局间保留；`human_seat()` 观战返回 -1。UI 文案只用点阵字库有的字形（拨值箭头是 ASCII < >，字形覆盖测试盯着） |
| [dice.gd](../game/scripts/ui/dice.gd) | 掷骰演出（方案 D）。`play()` 是协程，动画播完才返回；`place_at()` 把它摆到棋盘某格上 | 结果由引擎传入，本层只负责演到那一面（见约定 #11）。翻滚随机用节点自己的 `randf()`，**不碰 `game.rng`**。按落地点 y 参与深度排序，与组织块同规则 |
| [dice.gdshader](../game/assets/shaders/dice.gdshader) | 片段着色器里对立方体解析求交（slab 法），Fresnel + 程序化环境反射 | 立方体有闭式解，**不需要 ray marching**；常量不能叫 `LIGHT`（内建量重名会编译失败） |
| [silhouette.gdshader](../game/assets/shaders/silhouette.gdshader) | 把贴图染成纯色、只留 alpha 轮廓，供格子高亮叠色用 | 输出白色、颜色由节点 `modulate` 给，所以**一份材质够全部高亮共用**。设计稿的高亮是 `lerp(原色, #30D1FA, 0.43)`，叠一层 alpha=0.43 的纯色剪影即逐像素等价 |

### 测试与工具 `game/tests/`

| 文件 | 用途 | 运行方式 |
|---|---|---|
| [headless_test.gd](../game/tests/headless_test.gd) | 单元/回归测试（含棋盘渲染坐标一致性、点选与 `tile_center()` 互逆、菜单字号点阵网格、掷骰演出与结算的交叉核对）。**项数只写在上面「当前状态」一处**，别在这里再抄一份 | `--script res://tests/headless_test.gd` |
| ⚠ 建棋盘一律走 `make_board()` | 脚本解析失败时 Godot 只打印错误、照样返回节点，于是整段测试不执行而 `fails` 仍是 0——会**假装通过**。`make_board()` 里显式确认脚本挂上了（判据必须用 `has_method()`，`get_script()` 在这种情况下照样非空） | — |
| [screenshot.gd](../game/tests/screenshot.gd) | 把场景渲染几帧存成 PNG，用来和设计稿逐像素比对（**不能加 --headless**）。着色器/叠色这类错无头测试查不出来，只能靠它 | `--script res://tests/screenshot.gd -- 输出.png [场景] [鼠标x,y] [秒数，可逗号分隔连拍] ["x,y@秒;key:动作名@秒;…"]` |
| [balance_variants.gd](../game/tests/balance_variants.gd) | 多方案 × 多人数胜率矩阵 | `--script res://tests/balance_variants.gd` |
| [balance_sim.gd](../game/tests/balance_sim.gd) | 单配置详细统计（含癌细胞种类表现） | `--script res://tests/balance_sim.gd` |
| [bench_mc.gd](../game/tests/bench_mc.gd) | 蒙特卡洛桥的手动基准：单次决策毫秒数、整局秒数（毫秒随机器浮动，不做断言） | `--script res://tests/bench_mc.gd` |
| [dump_game.gd](../game/tests/dump_game.gd) | 导出单局完整日志，人工复盘用 | `--script res://tests/dump_game.gd` |

命令模板（本机 Godot 路径）：

```bash
"D:/Godot/Godot_v4.5-stable_win64.exe/Godot_v4.5-stable_win64_console.exe" --headless --path game --script res://tests/headless_test.gd
```

## 关键约定（改代码前必读）

1. **能量用「十分能量」整数存储**：`30` = 3.0 能量。避免浮点误差，保证回放/联机确定性。显示用 `CWData.fmt()`。
2. **所有随机数必须走 `game.rng`**。用了别的随机源，确定性测试（同种子两局哈希一致）会立刻抓到。
3. **不要给 `CWGame` 加名为 `log` 的方法**——GDScript 的内置数学函数 `log()` 不能被覆盖，会导致整个类编译失败。日志方法叫 `log_msg()`。
4. **新增 `class_name` 脚本后必须先 `--import` 再跑测试**，否则报 "Identifier not declared"。
5. **进入一格的所有效果都在 `CWActions.enter_tile()` 里**（定殖/净化/特殊组织收取/标记刷新）。移动、血管传送、复活落位共用它——加新效果加在这一处，不要散落各处。
6. **新增规则状态字段时，要同步更新 `CWGame.state_hash()`**，否则确定性测试会漏检。
   2026-08-30 就踩了一次：口径 #73 把 `mods[].seq` / `equip_seq` 变成规则相关数据
   （移动费链按它排序），哈希却没跟上，于是两个**结算结果不同**的局面算出同一个哈希。
   反过来也有个坑：`cell["play_n"]` 这类**只增不减的计数器不要进哈希**——绝对值不影响
   任何结算（只有相对先后有意义，那已被各条目的 seq 记住），塞进去会打破
   「挂上修饰 → 消耗掉 = 回到原状态」这条测试地基。
7. **费用一律走 `CWCost`，报价与扣费分开**（口径 #79）。新卡要改费用，只在
   `CWCost.TEMPLATES` 里登记一条 `CostModifier`（语义阶段 + 条件 + 消耗策略），
   **不要**回到 `CWActions` 里手写 if。两条铁律：
   `quote()` 是**纯查询**——不扣能量、不消耗修饰、不烧闸门、不写日志、不碰 rng，
   所以 UI 每帧刷新、AI 枚举、快照推演都可以随便调；要写的闸门记进 `usage_marks`，
   等 `commit()` 才落笔。`commit()` 先**复验合法性**（`ctx["validate"]`，由动作层给，
   且必须与选项生成用的是同一份谓词），再按当前状态重新报价——调用方存的旧价钱只用来核对。
   **从复验到扣费之间不许出现 `await`**，否则中间的挂起会让复验白做。
8. **范围伤害必须走 `immune_hit_area()` / `cancer_hit_area()`**，不要自己 for 循环逐个
   调 `immune_hit`/`cancer_hit`。批次由事件的 `simultaneous_group` 决定，**不是**
   由「调用方碰巧塞进同一个数组」决定：`group == 0` 每个事件自成一批；批次号在**循环外**
   取一次给全体目标；主攻击与它的次级伤害（T 细胞无视减伤那一下）**必须同批**，
   拆开会让 BCL-2 先把能量拉回分期值、次级伤害再补一刀反而绕过免死。
9. **伤害一律走 `CWDamage`，读 `actual` 不读理论值**（口径 #80）。业务代码**不许**自己
   `target["energy"] -= v`——「无视减伤」也不行，那是带 `UNPREVENTABLE` 标签的伤害事件；
   自己扣的话，日志、BCL-2、死亡检查与统计会立刻分叉成两套。范围伤害必须走
   `game.immune_hit_area()` / `cancer_hit_area()` **整批提交**，边遍历边扣能量会让
   数组顺序改变结果。吸血、「造成 X 伤害后」、斩杀一律读 `DamageResult.actual`
   （目标实际失去多少），读理论伤害会在残血目标身上多回整整 1.0。
10. **平衡数值改动走 `CWTuning`**，不要直接改 `CWData` 常量——`CWData` 是规则原文的记录。
11. **不要在代码或测试里写死棋盘尺寸**（`61`/`127`/`41`……）。一律引用 `CWData.TOTAL_TILES`、
    `CWData.BOARD_RADIUS`、`CWData.CANCER_WIN_WEIGHTED` 等常量。2026-08-27 从 61 格换到 127 格时，
   4 处写死的数字全部变成了假失败，排查成本远高于一开始就引用常量。
12. **改棋盘尺寸后必须重跑平衡**：收入公式挂钩「健康组织格数」，棋盘面积一变，
    低保/封顶的相对位置就全变了——61 格调好的数值搬到 127 格会让癌胜率从 51% 掉到 10%。
13. **表现层不许自己存一份棋盘坐标**。规则层用轴坐标 `(q,r)`，渲染层用「行,列」下标，
    两套坐标之间只有 `board.gd` 的 `axial_to_rc()` 一个转换点，源头永远是 `CWData`。
    2026-08-27 合并渲染代码前，渲染层自己抄了一份特殊组织下标，地图改到第三版时没跟上——
    画出来还是 5 个骨髓、代谢核心还在半径 4。`headless_test.gd` 的 `t_board_view()` 专门防这个。
14. **表现层不许决定结果**。所有随机由引擎的 `rng` 先掷出，再经 `CWBridge.show_roll()`
    送给表现层去演；动画只能演到那个数上，不能反过来决定它。
    掷骰动画的翻滚随机走节点自己的 `randf()`，**绝不碰 `game.rng`**。
    `headless_test.gd` 的 `t_roll_hook()` 会把钩子收到的点数和日志里实际结算用的点数逐次交叉核对。
15. **界面的设计分辨率是 960×540，不要改**。它和界面原型的画布同尺寸，
    原型里的 `1cqw` 正好换算成 `9.6px`，所有定稿坐标能直接乘过来；
    而且 1080p 全屏恰好是整数 2 倍，点阵字和像素贴图都能整数放大不糊。
    拉伸模式是 `canvas_items` + `keep`。
16. **点阵字号只能取字体网格的整数倍**：中文（缝合像素 10px）取 10 / 20 / 30 / 40，
    Logo 用的 Silkscreen 取 8 的倍数。取别的值会被重采样磨出灰边。
    同理，20px 字号下 1 个字模像素 = 2 屏幕像素，**字间距和位移也要取 2 的倍数**。
    `headless_test.gd` 的 `t_main_menu()` 会扫描场景里所有 Label 盯住这一条。
17. **要不要给某个随机加动画，是纯表现层决定**。静默用 `roll_d6()/roll_d3()`，
    带演出用 `roll_shown(sides, reason, pid)`；两者消耗的 rng 完全相同，可随时互换，
    不影响同种子可复现。**不要为了加动画去改 `roll_d6/roll_d3` 的签名** ——
    GDScript 的 `await` 会沿调用链往上传染，那样每个既有调用点都得跟着改。


## 关键决策记录

决策的时间线见 [开发日志](开发日志.md)；本节只保留「现在为什么是这样」的最终结论。

- **引擎与 UI 用问答桥解耦**：旧版验证过的架构，同一套规则可驱动 AI/人机/联机。
- **平衡数值抽成 CWTuning 旋钮**：规则平衡有大量未定项，需要不改代码就能批量试方案。默认值保持规则原文，让「按规则跑」永远是一行代码的事。
- **规则歧义处一律先拍临时决策、记进文档再实现**：见 [规则电子化说明](规则电子化说明.md)，每条都标了待团队确认。
- **确定性优先**：所有随机走 `game.rng` + `state_hash()` + 同种子重放测试。这是将来做联机（确定性锁步）和 AI 模拟打分的前提，代价很小但补不回来。
