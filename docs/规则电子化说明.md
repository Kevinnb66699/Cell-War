# 规则电子化说明 —— 未决问题与临时决策

> 规则原文：`Cell_War PRD.md`（仓库外；旧的《Cell War新版规则.md》已于 2026-08-28 删除）。
> 本文档记录规则文档未覆盖/有歧义之处，以及**原型阶段的临时决策**。
> 标记说明：✅=规则文档已解决　⏳=临时决策，待团队确认　✂️=原型范围裁剪
>
> **改动任何一条临时决策时，必须同步修改对应引擎代码并在 docs/开发日志.md 记一笔。**

> ⚠ **本表大部分 ⏳ 条目早于 2026-08-28 的 PRD 全量对齐（9868ca3）**，其中一批已被
> PRD 明文推翻（至少：#4 叠格、#6 等级呼吸加成、#7 X 级门槛、#22 衰减 −1、#23 免疫不死、
> #24/#27 胜利判定时点、#26 毒素无上限、#36/#37 的胜利门槛与初始癌区数）。
> 逐条重审是一件待办的活——重审之前**以 PRD 与引擎现状为准**，别按本表旧结论改代码。

## 零、团队定案（2026-08-28 答复）

| # | 问题 | 定案 | 引擎现状 |
|---|------|------|----------|
| 38 | 癌细胞能否移进免疫细胞占据的格 | ✅ **不可以**（一格一细胞双向成立；免疫→癌细胞格的攻击是唯一例外） | 已符合：`cw_actions.gd` `_cancer_options()` 对任何有细胞的格直接跳过；【早期血行转移】【转移】落点同样排除 |
| 39 | 六个骨髓全癌化/占满时免疫复活不了 | ✅ **是有意的**——骨髓是真实的稀缺资源 | 已符合：`revive_options_immune()` 无合法格时记日志跳过，之后每个世界回合自动重试 |
| 40 | 免疫死亡罚停 | ✅ **1 回合**（死于第 N 回合 → 第 N+1 回合 S 阶段复活） | 已符合：`cw_game.gd` `respawn_round = round_no + 1`（`immune_respawn_delay` 旋钮默认 0） |
| 41 | 免疫池【代谢耦联】的「1 / 1.5 / 2」 | ✅ **使用者自选档位**：转移 1→接受方得 1.2、1.5→2、2→2.5，不是分期数值 | ✅ 已实现（2026-08-29）：先问方向再问数额，只有一种选择就不问；免疫/癌症两版共用一份实现（按自身阵营选队友）。支付口径见 #53 |
| 42 | 世界事件抽取规则（对照 W1） | ✅ **等权随机，同局不可重复**（抽过的事件本局不再出现；18 个事件 ≥ 7 个触发回合，够抽） | 已实现：`cw_world_fx.gd` `trigger()` 从 `events["pool"]` 等权抽取（走 `game.rng`），抽过即移除 |
| 44 | 【紊乱】传送细节（对照 W2） | ✅ 落地算「进入」；目的地**不选血管格**（已进 PRD）。返回边界 2026-08-29 定案**方案A「同时返回」**：全体视为同时归位——返回者的原位互不相同、互不阻挡；死亡不返回；主动移动过的照样返回；原位被复活/移动的细胞真占住才留在传送后位置；原位组织已变按正常「进入」结算（PRD 明写触发净化/定殖） | 已实现：`cw_world_fx.gd` `_chaos()`/`_chaos_return()`（先算清全部落点、再统一归位补「进入」结算） |
| 45 | 攻击判定修正口径（对照 W3） | ✅ **并给，不重掷**：【细胞毒】失败并给成功（PRD：5/6 成功、1/6 大成功）；【免疫伪装】**大成功并给成功**（PRD：1/3 失败、2/3 成功。2026-08-29 按新 PRD 改判，推翻 08-28 聊天答复的「并给失败」） | 已实现：`cw_actions.gd` `attack_outcome()`——d6 掷完改写判定，掷骰消耗不变（同种子可复现） |
| 46 | 【固化加速】门槛（对照 W4） | ✅ 计数**从 <2.0 涨到 ≥2.0** 时立即转化（骨样硬化 1→2.5 一步跨线也算） | 已实现：`cw_game.gd` `raise_solid()` 统一判门槛（E-固化与卡【基质硬化】共用）；触发时已 ≥2.0 的格不追溯——按「从 1 达到」的字面读，待团队复核 |
| 47 | 【免疫抑制因子】效果改文（对照 W5） | ✅ 新效果：本回合免疫净化组织花 0.2 能量且不获得抗原记忆；所有癌细胞失去 0.5；所有免疫细胞弃 1 张 | 已实现：净化费计入移动/裂解价签，`enter_tile`/`_do_lyse` 不给记忆，一次性部分在 `cw_world_fx.gd`。新效果文已写进 PRD（2026-08-28 23:56 版） |
| 48 | 事件的「弃 1 张牌」（对照 W6） | ✅ **随机弃**（走 `game.rng`） | 已实现：`cw_world_fx.gd` `_discard_random()`，【增殖抑制】【免疫抑制因子】共用 |
| 49 | 【双重触发】叠加语义（对照 W7） | ✅ 2026-08-29 定案**三档**：持续·数量类（营养输送/异常增殖/代谢加速/抗原变异/抗原暴露/基质阻隔/信号放大）**两份同时生效 2 回合**（强度×2）；持续·开关类（细胞毒/免疫伪装/迁移激活）**一份强度接力 4 回合**；本回合类全部 7 个**连续两个回合、每回合完整生效一遍**（08-28 的「3 回合」方案作废） | 已实现：`trigger()` 三档分派 + `on_round_start()` 第二回合重演。**实际排期下两个事件不会同时在场**（2026-08-30 核算，纠正早先相反的说法）：4 回合事件最早从第 6 回合起手（第 3 回合先抽到双重触发），活 6~9，第 10 回合正好接上。6→10 这段刚好卡满、零余量，改事件表就会重叠；容器是列表，真重叠也撑得住 |
| 50 | 【信号放大】翻倍范围（对照 W8） | ✅ **即时技能卡翻倍**（含【代谢耦联】的转移量），**永久技能卡与细胞自带技能不翻** | 已实现：`cw_card_fx.gd` `_amp()` 包住模块内全部能量增减。事件卡按「即时」同口径翻（待确认）；【代谢耦联】给出/接收两侧一起翻（✅ 2026-08-29 已实装） |
| 43 | 无氧呼吸取整（对照 四-D） | ✅ **也四舍五入到十分位**（与有氧一致；PRD 只给有氧写了取整） | 已改：`cw_world.gd` `_anaerobic()` 由向下取整改为四舍五入，测试补了除不尽用例（2.2/4=0.55→0.6） |

四条中 A/B/C（#38~40）引擎全部已是现行行为，一行代码没改；
相关回归测试在 `headless_test.gd`（一格一细胞、骨髓复活、罚停回合、无骨髓可复活分支）。

## 零·补 「需中途选择」批的实现口径（2026-08-29 实装，⏳ 待团队过目）

11 张要玩家在结算里做决定的卡（对照 §5.1）全部实装，PRD 没写死的细节按下表处理：

| # | 卡 | 实现口径 | 引擎位置 |
|---|------|----------|----------|
| 51 | 【全身免疫动员】的「各自可以立即迁移 1 次」 | ⏳ **费用照付、进癌细胞格照常触发攻击**——PRD 只说「可以迁移」没写免费（+1.5 就是路费）；每个细胞由它自己的玩家决定走/不走 | `cw_card_fx.gd` `_mobilization()`，复用 `immune_move_options()` 的合法性与定价 |
| 52 | 【炎症性趋化】的每步 0.2 | ⏳ 0.2 是**基准价，照过移动费修正**：免疫抑制的净化费、基质阻隔的翻倍、迁移激活的免费首移都适用（它就是「移动」）。可进健康/普通癌组织（固化不行），进癌细胞格=攻击（卡面明写） | `_chemotaxis_steps()` 走 `_move_cost_mod`，每步 `_do_move` 全套结算 |
| 53 | 【代谢耦联】支付侧口径 | ⏳ 转出按**费用**口径（`game.pay`，不能降至 0）、接收是纯获得；两侧数额都过【信号放大】（#50） | `_couple_tiers()` / `_couple()` |
| 54 | 【基质重塑】的「最多 2 格」 | ⏳ 「最多」= 上限不是义务，两段（拆固化、转健康）都可提前停；转健康的候选=**拆过的格自身+其相邻格**（拆过的格刚变普通癌组织，自己就是合法候选） | `_remodel()` / `_remodel_heal_cands()` |
| 55 | 【放疗】区域与坏死 | ⏳ 区域=**含起点的随机连通 15 格**（从边界随机生长，走 `game.rng`）；区域内癌性组织**含固化、含有细胞站着的**一律转健康；坏死沿用毒素那套倒计时（不供能、可被定殖、定殖时清除） | `_radiotherapy()`，`NECROSIS_RADIO=5` |
| 56 | 【基因组不稳定】的「第 20 世界回合起」 | ⏳ 按 `round_no >= 20` 判（恰与癌症Ⅲ期起点重合，但按卡面数字写死不跟分期表）；两掷**相同就不再问**；免费=不扣 0.5 也不占每回合突变次数 | `_genome_instability()`，复用 `apply_mutation()` |

另两条顺带口径：风暴类（炎症风暴/免疫风暴）与浸润的「癌组织」都按字面只指**普通癌组织**
（固化癌组织是另一个词，不转化、不可进入）；事件卡的中途选择是**强制的**（抽取后立即生效，
PRD 没给「放弃」——只有走位类给「停在这里」）。

## 零·补2 修饰批的实现口径（2026-08-29 实装，⏳ 待团队过目）

15 张修饰卡（免疫 10 / 癌症 5）全部实装：自我修饰挂 `cell["mods"]`（次数 + 本回合/
本世界回合/用尽三种时钟），全局修饰（基质稳定/TGF-β/TNF 冻结格）挂进世界事件容器。
PRD 没写死的细节按下表处理：

| # | 口径 | 内容 |
|---|------|------|
| 57 | 同名/多盾同时生效 | ⏳ 一次符合条件的结算里，所有在场的同类修饰**一起触发、各消耗一次**（两张「下一次损失 −1.5」= 同一个下一次上减 3.0，不排队；细胞膜修复 + I型干扰素同一次损失一起减） |
| 58 | 「攻击成功后」含大成功 | ⏳ 穿孔素-颗粒酶/补体级联的「攻击成功后」按「成功档及以上」读——大成功照触发（PRD 只有补体调理把两档分开写） |
| 59 | 补体调理自动重掷 | ⏳ 「可重新判定一次」严格不劣（第二次结果不会比失败更差），所以**不发问、自动重掷**；重掷后的骰面照样吃世界事件的并给。修饰骑在「下一次攻击」上：无论结果如何该次攻击就把它消耗掉 |
| 60 | 判定链顺序 | ⏳ 骰面 → 世界事件并给（细胞毒/免疫伪装，管的是骰面概率）→ 补体调理重掷 → 防御方 PD-L1 最后压一级（管的是「判定」）。【高亲和力克隆】不掷骰直接大成功——没有骰面可供世界事件并给，但 PD-L1 照压 |
| 61 | 移动费修正顺序 | ~~⏳ 卡牌修饰先改基准价（覆盖先于增减）→ 世界事件 → 翻倍~~ **已被 #73 的通则取代（2026-08-30）**：不再按「类别」排，改按**打出先后**排 |
| 62 | 减伤修饰的来源判别 | ⏳ 【DNA损伤修复】只挡免疫方**事件/技能**（毒素、抗体、即时卡、事件伤害），不挡普通攻击（那是 PD-L1 的领地）；【缺氧适应】**2026-08-30 卡面重写后**挡「癌细胞技能 **或** 微环境压迫」（技能 = 黏液破裂/乳酸酸化这类细胞技能与癌方即时卡）；世界事件与反弹仍不算。为此毒素/抗体的 immune_hit 改标 attack=false（对树突减半/巨噬吸血零影响——那两位用不了这两个技能） |
| 63 | TGF-β 的叠加与消耗 | ⏳ 「减少20%向下取整、可叠加」= 逐份 ×80% 向下取整到十分位；挂到**下一次**有氧结算整体消耗（left=2 保证活到下个 S 阶段） |
| 64 | 分期数值的取值时刻 | ⏳ 上皮—间质转化的次数在**打出时**取（它当场生成 N 次额度）；DNA损伤修复的减免量在**被击中时**取（护盾结算才用到数值）——都是「按当前世界回合取值」原则在各自结算点的落法 |

顺带一条：【缺氧适应】重写前有个「免压期间压迫根本不发生」的特判（避免误消耗
细胞膜修复这类护盾），重写后压迫一律走损失管线、由这面盾在管线里减 1.0，特判已删。
由此**挨一下压迫会把细胞膜修复等同场护盾一并消耗**（定案 #57「同一次损失把盾都用掉」）——
✅ **2026-08-30 团队确认这是有意为之**：「这本身就是一种战术」，**不给压迫开例外**。
玩家要自己权衡护盾留到哪一下损失上，别在 E 阶段前随手把盾挂满。

## 零·补3 永久技能批的实现口径（2026-08-29 实装，⏳ 待团队过目）

21 张唯一永久技能（免疫 17 / 癌症 4）随装备位全部实装：打出即装备
（`cell["equipped"]`，右侧竖条「技 N」计数），同名限一张由抽卡合法性把关，
**死亡不掉装备**（【癌症干性】以复活为触发，反证装备穿越生死）。
PRD 没写死的细节按下表处理：

| # | 口径 | 内容 |
|---|------|------|
| 65 | 移动费修正的完整顺序 | ~~⏳ 基准价 → 覆盖 → 减免叠加（共同下限 0.2）→ 净化费 → 加价 → 翻倍 → 绝对免费~~ **已被 #73 的通则取代（2026-08-30）**。仍然成立的两条：净化费是净化的钱不是迁移的钱（排在卡牌之后，减免砍不到）；巡航免费进普通癌组织仍要付 0.2 净化费 |
| 66 | 【免疫监视】的「自身相邻3格」 | ⏳ 按 **3 格范围**读（hex 距离 ≤3，含自身格）——「相邻」只有 6 格，说「3格」只能是半径。守护圈内健康组织不做增生判定（不掷骰）、不进侵蚀候选 |
| 67 | 【细胞毒性增强】T 的「不受减伤效果影响」 | ⏳ 那 1.0 绕开五步管线**直接扣**（不吃护甲/护盾，也不吃标记翻倍——它是独立小额直扣，可致死、可触发 BCL-2）。非 T 的每回合首次 +1.0 照常走管线第②步 |
| 68 | 【BCL-2抗凋亡】救的范围 | ⏳ 只救「**受到的**能量损失」（挂在损失管线的死亡检查上）：黏液破裂的自毁、突变的自扣不经过那条路，救不了；【吞噬体成熟】的处决不是能量损失，同样救不回（先免死再被处决的顺序按字面走） |
| 69 | 呼吸类「额外获得」与 TGF-β | ⏳ 代谢适应/自分泌生存信号/GLUT1 的加成在基准收入**之外**加，不吃 TGF-β 的 −20%（那句管的是有氧结算本身的所得） |
| 70 | 「攻击…时/后」闸门的消耗时机 | ⏳ 抗体亲和力成熟（首次攻击邻健康目标 +0.5）与抗原呈递强化（首次攻击未标记者→施标记）的闸门在攻击**发动**时消耗，判定失败也算攻过（加成/标记只在命中/目标存活时兑现）。呈递强化树突的双份标记对**光环**施加的标记同样生效（「自身施加的标记」不分来源） |
| 71 | 装备后不可主动弃置 | ⏳ 弃置权 PRD 只写给手牌（规则 3「**手牌**可随时弃置」），角色面板上的永久技能没有移除通道——全 66 张里唯一会离开面板的是【BCL-2抗凋亡】的「触发后本牌被弃置」，特例专门写明恰好反证默认不可弃。玩法上也无拆卸动机（无上限、同名限一、全是自益）。引擎照此：弃置选项只从手牌生成。将来若出「偷/封技能」类新卡需要可拆卸，先补 PRD 再动引擎 |

| 73 | **数值修正的结算顺序（新通则）** | ✅ **2026-08-30 团队定案（Entity_303 + Kevin）**：同一个数值受多个效果影响时，① **基准值**（行动本身 + 免疫等级 + 细胞自带技能）→ ② **卡牌效果，按打出/装备的先后逐张结算**（永久技能算作卡牌，与即时卡混在同一条队列）→ ③ **世界事件最后**。「降为 X」改写当前值；「-X」按各卡自己的「最低 Y」钳；「不消耗能量」改写为 0 且不受下限约束；**减免只降不升**（已经免费的不会被减免卡抬回 0.2）。**本条不适用于能量损失**——那边以 PRD 的五步管线为准。已实装：`_move_cost_mod` 走队列（`cell["play_n"]` 给即时卡与永久技能盖同一把时序戳），5 条断言含「同两张卡换顺序结果不同」 |
| 72 | 「任意时间」时机取消，改为己方回合 + 「下一次」时长 | ✅ **2026-08-30 团队定案（Kevin）**：打断窗口不做了。六张原「任意时间」卡（细胞膜修复 / 缺氧适应 / PD-L1表达 / DNA损伤修复 / BCL-2抗凋亡 / 癌症干性）**使用时机一律改为「己方回合」**，生效时长一律改为**「下一次」**（挂着等被触发，不随回合过期）。引擎侧本就只允许己方回合打手牌，故时机是零改动、**PRD 卡面待队友改字**；时长侧四张即时卡里三张（细胞膜修复/PD-L1表达/DNA损伤修复）早已是 `until=""`，唯一实改的是【缺氧适应】的减伤半句（原挂 round、世界回合末作废，与卡面「下一次」不符——已改 `""`）。**【缺氧适应】随即由团队重写卡面**（同日）：两个半句并成一面一次性护盾——「下一次【微环境压迫】**或**来自癌细胞技能造成的能量损失 -1」，「本世界回合免疫压迫」的说法作废。引擎照此：单条 `until=""` 修饰，`cancer_hit` 里 `skill or reason=="微环境压迫"` 时消耗，`_pressure()` 的免压特判删除。两张永久卡装备后即常驻被动，没有「时长」可改 |
| 74 | **【炎症趋化】是「改为 0.5」而不是「降为 0.5」** | ✅ **2026-08-30 团队定案 A（Kevin）**。2026-08-30 审查实测：先装【组织巡航】（首移免费 = 0）再打【炎症趋化】，费用被**抬回** 0.5，且卡与巡航的首移额度双双消耗——打一张打折卡反而多付 0.5。团队选择**保留改写语义、改卡面措辞**：「降为」→「**改为**」，抬价是打出顺序的一部分，要玩家自己权衡。X 级（基准 0.5）下「先 CXCR3 后炎症趋化 = 0.5 / 反过来 = 0.2」同理。⏳ **PRD 卡面待队友改字**；#73 的通则条目里「降为 X」这一类也应统一改称「**改为 X**」。同类卡【上皮—间质转化】（改为 0.2）目前抬不了价（癌方基准价最低就是 0.2），建议一并改字，行为零变化 |
| 75 | **免费额度省，限次折扣不省**（#73 的补充） | ✅ **2026-08-30 团队定案 D（Kevin，同日两版：先通用判据、后收窄为本条）**。起因：审查实测同时装【组织巡航】+【组织驻留】时，本回合第一次向健康组织移动会把**两个**「首次免费」额度一起烧掉，而报价上完全看不出第二个被浪费了。**定案（乙案）**：把费用**变 0** 的那几条（组织巡航首移 / 组织驻留 / 癌症干性 / 世界事件【迁移激活】）属于「免费额度」——这一次本来就免费就**不消耗**，额度留给后面真要花钱的移动；**限次折扣与改写类照旧消耗**（CXCR3趋化 / LFA-1黏附 / 炎症趋化 / 上皮—间质转化），只要目的地对得上、次数还有，哪怕这一次一分钱没减到，次数也照扣（此时日志会明写「本次未改变费用，次数仍消耗」，别让玩家不明不白少一次）。⚠ 实装上要分清**两件事**：「这张卡适不适用」（目的地/次数对不对，为假一律不消耗）与「它有没有真的改到钱」（只对免费额度那几条有意义）——合成一个判据的话，「打出【炎症趋化】却往健康组织走」和「打出【炎症趋化】但价钱本来就是 0.5」会被当成同一件事，而团队对这两者的裁定是相反的。本条不推翻定案 #57（同一次能量损失把同名护盾全用掉）——那是伤害管线，与移动费是两码事 |
| 76 | **【囊性护甲】改为「每世界回合第一次能量损失 -0.5」，不限来源** | ✅ **2026-08-30 团队定案 B（Kevin）**。旧卡面写「受到的能量损失」，实现也只挂在 `immune_hit` 上，于是世界事件那条管线（【免疫抑制因子】对全体癌细胞的 0.5）整个绕过了护甲——而 `immune_hit` 自己的注释还写着「任何来源都生效」，注释与实现打架。新卡面不限来源，引擎把护甲抽成 `CWGame.armor_cut()`，**两条伤害管线共用同一个每世界回合额度**。⏳ **PRD 卡面待队友改字**。⏳ 唯一没盖到的是【突变】第 3 面的自扣（不走管线，按 #68 的精神算「自己扣自己」而非「受到损失」）——**待团队确认**是否也要挡 |
| 77 | **把目标当场打死，【抗原呈递强化】的额度照样用掉**（#70 的延伸） | ✅ **2026-08-30 团队定案 C（Kevin）**：「按照口径 #70 来」。审查发现这个行为原先是 `and` 求值顺序的副产品（`first_this_round()` 有副作用，排在 `target["alive"]` 之前），而不是明写的选择。现按 #70「攻击**发动**即算攻过」写成两层判断，并补一句日志「目标已死亡，本世界回合的施加额度就此用掉」——旧版打死目标后额度消失且**零反馈**，玩家无从察觉 |
| 87 | **平衡基准：MC 互搏的绝对刻度不作为目标，36% 就地接受** | ✅ **2026-08-31 Kevin 定案「就这样」**，理由「实战中人类打不过用 MC 的癌细胞方」。规则对齐 PRD 后双方 MC 下癌胜率掉到 4 人 36% / 6 人 36%（带外），曾拟两条补偿杠杆，**均不采用**。**依据**：[平衡方案_PRD版 §8.5](平衡方案_PRD版.md) 实测 AI 强度对两个角色的价值极不对称 —— 免疫已是 MC 时把癌方也换成 MC 只值 **+10** 个点，癌方已是 MC 时把免疫换成 MC 值 **−85** 个点。癌方的最优策略（能量花光、一路铺格）启发式本来就在做，免疫要集中火力/挑净化顺序/攒记忆升级，那是搜索才找得到的。**所以 MC 互搏系统性高估免疫方**，同一套数值在真人局里癌方会强于 36%。⚠ **方向有数据、幅度没有**（那组是 n=20 小样本，且「真人比 MC 弱多少」一个数都没测过）。**推论**：MC 互搏此后只用作 A/B 对照（比较两套数值哪个更平），**绝对刻度不当靶子**；在拿到真人对局结果之前不要再用 MC 数字去拧数值。这也部分解决了一直悬着的「基准 AI」问题 |
| 86 | **E 阶段第 7~9 步的先后按 PRD 校正** | ✅ **2026-08-31 Kevin 定「8.3 按照 PRD 的顺序改」，已实装**。`CWWorldFx.round_end()` 原本把「其他 E 类效果」（【紊乱】返回原位）和「更新持续时间类状态」（世界事件倒计时/到期）合成一个函数，而且整体排在 `_clear_newborn()` **之后**；PRD 的第 7 步在第 9 步「移除新生」之前。**后果**：【紊乱】返回时【定殖】造出的癌组织会多背一个世界回合的「新生」，下个世界回合也固化不了。现拆成 `round_effects()`（第 7 步）与 `tick_durations()`（第 8 步），`e_phase()` 每一行都标上 PRD 步骤号。判据用**不变量**：跑完 `e_phase()` 盘面上不该剩任何「新生」标记 —— 已验证把顺序改回旧的这条会红 |
| 85 | **取消【原发灶】：开局不再有固化癌组织** | ✅ **2026-08-31 Kevin 定案「取消初始的固化癌组织」，已实装**。口径 #83 刚补记这条偏离，Kevin 当天即决定取消 —— 理由是它从来不在 PRD 里，而 PRD「游戏开始」第 6 条明写「所有癌组织固化计数初始为 0」。`CWData.SOLID_AT_CANCER_SPAWN` 改为 `false`，引擎与 PRD 就此一致。**代价**：癌方失去开局那次复活容错 —— 【复活】要落在固化癌组织上，而固化要癌细胞原地停留到计数满 3，可停留就会被免疫打死。**机制保留成旋钮没有删**：2026-08-27 实测把它关掉，「规则原文」在 2/4/6 人局癌胜率全部归零，它是平衡实验里分量最重的一根杠杆，而且测试要能验证「开回来时照旧生效」，否则哪天想开回来会发现代码早烂了而测试全绿 |
| 84 | **补上两条一直没跟上 PRD 的规则：攻击失败自损 0.5、6 人局行动顺序严格交替** | ✅ **2026-08-31 Kevin 定「8.1&8.2 都改」，已实装**。两条都是【落后】——PRD 一直这么写、引擎停在 61 格旧规则，详见 [PRD差异对照 §八](PRD差异对照.md)。**① 攻击失败自损**：PRD【迁移】写「1/3概率失败，不造成伤害，**自身-0.5能量**，免疫细胞移动后被癌细胞反弹，弹回原格」，而 61 格旧规则没有那半句，引擎的 `counter_dmg_on_fail` 就一直写死 0，注释还断言「规则原文为 0：癌方没有任何伤害手段 → 免疫细胞不可能死亡」——**这句是假话，而且正因为写得斩钉截铁，08-28 的全量比对读到这里就跳过去了**。现改为 `CWData.COUNTER_DMG_ON_FAIL := 5`，走 `cancer_hit` 而非直接扣能量，好让它和别的损失一样吃减伤/护盾/死亡检查；按口径 #62，反弹**不算**「癌细胞技能造成的损失」，【缺氧适应】挡不住。**② 6 人局行动顺序**：PRD 是免疫A—癌A—免疫B—癌B—免疫C—癌C（严格交替），引擎是 免A-癌A-**癌B-免B**-免C-癌C（61 格旧排法）。4 人局两版一致，**只看 4 人局发现不了**。⚠ 连带：口径 #23「免疫细胞无死亡途径」彻底作废（此前已被【E-微环境压迫】推翻一次）；规则速查页那半句跟着旋钮走，已补**正反两条**断言（只钉「显示了」的话，把条件式换成无脑拼接也照样绿）|
| 83 | **【原发灶】：癌细胞出生格开局即为固化癌组织 —— PRD 里没有这条** | ✅ **团队 2026-08-26 定案并实装**（`CWData.SOLID_AT_CANCER_SPAWN`、`CWSetup._place_primary_lesions()`），⏳ **PRD 待补条文**。PRD「游戏开始」第 6 条明写「**所有癌组织固化计数初始为 0**」，引擎却把每个癌症玩家的出生格直接设成固化癌组织（4 人局 2 格 / 6 人局 3 格）。**加它是为了破一个死循环**：【复活】要落在固化癌组织上，而固化要癌细胞原地停留到计数满 3，可停留就会被免疫打死 —— 开局送一格等于给癌方一次复活容错（复活会把固化格降级回癌组织，所以每格只救一次）。**这是引擎偏离 PRD 最重的一处**：[平衡测试报告](平衡测试报告.md) 实测把它单独关掉，「规则原文」在 2/4/6 人局癌方胜率**全部变成 0%**、平均 3 个世界回合结束 —— 棋盘从 61 扩到 127 格带来的改善其实全来自它。⚠ **本条 2026-08-31 才补记**：它 08-26 就实装了，但从没进过 [PRD差异对照](PRD差异对照.md)，是 Kevin 实测时问「为什么一开局就固化了两个癌组织」才暴露的。根因是历次比对都是**单向**的（拿 PRD 逐条问引擎），查不出「引擎有、PRD 没有」这一类 —— 反向比对还没做，见 PRD差异对照 §8.5 |
| 82 | **癌方占地单价 0.5→1.2，两个折扣同比例抬，初始癌组织改为按人数分档** | ✅ **2026-08-31 Kevin 拍板（「先按照你测出来的数值来」），引擎已实装，⏳ PRD 待改字**（改哪几句见 [PRD差异对照 §七](PRD差异对照.md)）。（引擎为平衡偏离 PRD 的还有 #83【原发灶】。）四个数：癌细胞移动至健康组织 `CANCER_MOVE_HEALTHY` 0.5→**1.2**、小细胞肺癌【极简胞浆】`SCLC_MOVE_HEALTHY` 0.3→**0.7**、黑色素瘤【伪足穿透】`PSEUDOPOD_COST` 0.2→**0.5**、初始癌组织 `INIT_CANCER_TILES` 固定 15 → **{2: 15, 4: 15, 6: 21}**。**根因**：癌细胞每移动到一格健康组织就【定殖】一格，所以移动费就是**占地单价**，而免疫【净化】一格要 1.0 —— 0.5 对 1.0 实测占地转化比 **4.6:1**、双方蒙特卡洛下癌方 93~100% 胜、对局 3.4 个世界回合就结束。抬到 1.2 后双方蒙特卡洛 4 人 **52%**、6 人 **53%**。**明确不引入**「无氧呼吸封顶」：那是 PRD 里没有的概念，而且在蒙特卡洛基准下加了反而把癌胜率砸到 10%（封顶掐的是领先方滚雪球，而会搜索的免疫本来就不让癌方滚起来）。免疫方一个数没动，胜利条件/回合上限/固化阈值/增生/侵蚀全没动。⚠ **一个连带影响**：【伪足穿透】抬到 0.5 之后，【上皮—间质转化】的「改为 0.2」对黑色素瘤从空操作变成真有用（0.5→0.2）—— 这是抬价**新造出来**的组合，已补测试（`t_card_mods` ②b），其中第一条断言钉的就是「伪足穿透必须比 EMT 贵」这个前提。⚠ **2 人局不在平衡目标内**（Kevin 同日明确），沿用 PRD 的 15 格，别拿它的胜率来提问题。数据与推导见 [平衡方案_PRD版 §八](平衡方案_PRD版.md) |
| 81 | **提交行动时要复验合法性；范围伤害的「同时」由事件字段决定** | ✅ **2026-08-31 按队友《费用与攻击/伤害系统检查报告》修正**，玩法结果无回归。三条：**① 费用提交的原子性**——`CWCost.commit()` 在报价**之前**先复验行动此刻仍然合法（`ctx["validate"]`，由动作层提供，且与选项生成**共用同一份谓词**），复验到扣费之间不许出现 `await`；另查「起点是否仍等于 `ctx["from"]`」。修之前 `commit()` 只重新报价、不复验，而文件头注释却写着「重新验证 → 重新报价」——**注释承诺了代码没做的事**。**② 范围伤害的批次**——`simultaneous_group` 从前写在循环里，同一次范围伤害的每个目标拿到不同批次号，而 `submit()` 又根本不读这个字段：真正表达「同时」的是「调用方恰好把它们塞进同一个数组」。现改为批次号在循环外取一次给全体目标，`submit()` 按 group 稳定分组逐组结算。四条语义：`group == 0` 各自独立成批 / 批次顺序按首次出现 / 禁止跨 `submit()` 追加同组 / **次级伤害归当前批**（T 细胞无视减伤那一下若与主攻击拆批，【BCL-2抗凋亡】会先把能量拉回分期值、次级伤害再补一刀，反而绕过免死）。**③ 死亡只在死亡阶段发生**——`_resolve_deaths()` 分三遍走（挑濒死 → 死亡替代 → 批量宣死），【吞噬体成熟】的斩杀从攻击流程挪进伤后触发队列（设计 §5.7），排在巨噬【吞噬】的吸血之前。780 项检查全绿 |
| 80 | **伤害结算改为事件化管线** | ✅ **2026-08-30 Kevin 定「以队友两份系统设计为准」**。新建 `cw_damage.gd`，七阶段：建立事件 → 替代/免疫 → 数值计算 → 批量提交 → 濒死与死亡替代 → 批量死亡 → 伤后触发。`immune_hit`/`cancer_hit` 退化成薄壳。**PRD 五步数学一字未改**，变的是它周围的结构。四条实际的行为变化：①**吸血与「造成 X 伤害后」读 `actual`（目标实际失去多少）而非理论伤害** —— 残血目标身上两者能差 1.0，这是队友文档抓出、我的全量审查漏掉的真 bug；②**范围伤害整批同时结算**（8 处 AoE 改走 `immune_hit_area`/`cancer_hit_area`，不再边遍历边扣能量边死人）；③一次性减免按 `ON_BENEFIT`：一组盾没把伤害压低就不消耗（**与定案 #57 正交**——#57 管「同名多条一起算」，这条管「这一组有没有起作用」，队友答复 b 已确认不冲突）；④【吞噬体成熟】改为**伤害后斩杀**，本次 `actual > 0` 才成立，且走 `LethalEvent`（按口径 #68 显式标成**不可阻止**，BCL-2 救不回）。【细胞毒性增强】T 细胞的无视减伤部分不再直接扣能量，改成带 `UNPREVENTABLE` 的次级事件——跳过数值减免，但日志、BCL-2、死亡检查照走 |
| 79 | **费用结算改用「语义阶段」，取代 #73 在费用侧的「打出先后」** | ✅ **2026-08-30 Kevin 定「以队友两份系统设计为准」**，队友答复确认 a「按最后那版，规定得更明确」。费用修饰按**语义阶段**排序：③基础值替换 →④固定加费 →⑤固定减费（含**该减费自己的**局部下限）→⑥倍增 →⑦倍减 →⑨免费豁免 →⑩行动硬下限 →⑪不可豁免附加费；来源顺序（角色被动→卡牌→技能→世界事件）与 applied_seq 只是**同阶段内的平局规则**。**连带作废**：口径 #73 在费用侧的「卡牌按打出先后逐张结算」、**定案 A**（【炎症趋化】的抬价——新阶序下「改为」只看基准值、且免费豁免排在它之后，抬价场景不复存在，卡面「改为/降为」的区分随之失去意义）、**定案 D 乙案**（限次折扣照扣——改为设计 §七.3 的 `ON_BENEFIT` 默认：没改变费用就不消耗）。**免费额度竞争**：同一竞争组一次最多消耗一个，选谁按**打出/装备先后**（队友答复 c：「本质竞争组优先级问题，按方便记忆可沿用打出先后顺序」，覆盖设计 §九「适用范围更窄者优先」的原文；来源仍分层在前，否则 applied_seq 恒为 0 的世界事件会永远抢在细胞自己的额度前面）。✅ 连带一条卡面改字**已于 2026-08-31 写进 PRD**：【组织驻留】【组织巡航】的「每个行动回合**第一次**」已改为「每个行动回合**可有一次**」（设计 §九.2 自陈此裁定依赖该改字）。实装：`cw_cost.gd`，713 项检查全绿 |
| 78 | **能量损失计算流程的全引擎确认** | ✅ **2026-08-30 审查盘点**（应 Kevin「请确认伤害相关计算流程明确」）。全引擎**只有两个损失入口**，都落在 PRD 五步管线 `CWGame.settle_loss(base, add, mult, div, cut)` 上：`immune_hit`（免疫来源，14 个调用点中的 8 个）与 `cancer_hit`（癌症/世界事件等中立来源，6 个）。②固定增加只在 `immune_hit` 有（攻击类修饰的额外伤害）；③倍增只有【标记】×2；④倍减只有树突【各司其职】（仅普通攻击）；⑤固定减免两条管线各自一批，**【囊性护甲】跨两条共用额度**（#76）。**绕开管线的只有两处，都是卡面明写**：【细胞毒性增强】T 细胞的 +1.0（「不受减伤效果影响」，#67）与【突变】第 3 面的自扣（#68）。此外直接写 `energy` 的地方只剩「支付费用」`pay()`、「管线内部落地」`_lose_energy()`、以及复活/免死/死亡三处**赋值**（不是损失）。结论：**没有第五条路，流程明确** |

另一条顺带：【细胞因子网络】已上膛就不重复上膛（同回合连打只算一发）。

## 一、已由规则文档解决

| # | 问题 | 结论 |
|---|------|------|
| ✅ | 棋盘格数 | 61 格（半径 4 蜂窝），7 初始癌组织 + 54 健康 |
| ✅ | 固化衰减量 | 每世界回合 -1 |

## 二、临时决策（⏳ 全部待团队确认）

### 内容空缺类（原 P0）

| # | 问题 | 临时决策 |
|---|------|----------|
| 1 | 世界事件内容为空 | ✅ **内容已交付且全量实装**（2026-08-28 晚进 PRD，当晚 18 个事件全部进引擎 `cw_world_fx.gd`；W1~W8 定案见 #42、#44~50，落地口径见 `PRD差异对照.md` §六） |
| 2 | 卡牌为空 | ✅ **早已过时**：66 张卡真名字/真类别/真权重（2026-08-28），效果已落地 31 张（20 无选择 + 11 需中途选择，2026-08-29）；剩 35 修饰卡、22 永久卡见对照 §5.1。仍成立的半句：**AI 不抽卡不打牌**，平衡数据仍是"无卡版"口径，等卡齐后重跑 |
| 3 | 特殊组织位置未定 | ✅ **已被 127 格新图取代**（2026-08-27 团队交图，#33/#34 定案）：3 核心 + 6 骨髓（内圈半径 3 交替 + 外缘 3）+ 血管 (±6,0)。本行的 61 格临时布局作废 |
| 4 | 一格能否叠细胞 | ⏳ **同阵营可叠，敌对阵营不可同格**（依据规则第 9/80 行措辞）。攻击时若目标格有多个癌细胞，攻击方选一个结算；只有目标格癌细胞全灭免疫才进格 |
| 5 | 2 人局 | ⏳ 补充 2 人行动顺序：免疫A-癌症A（电子版测试最小配置，非官方规则） |

### 文档硬伤类（原 P1，按字面实现）

| # | 问题 | 临时决策 |
|---|------|----------|
| 6 | III 级有氧呼吸 4→3 回落 | ⏳ 按字面实现：I=3 / II=4 / III=3 / X=3 |
| 7 | III 级 16-30 与 X 级 30+ 重叠 | ⏳ 30 记忆归 X 级（即 III=16~29，X=30+） |
| 8 | 自爆 vs "费用不能降至 0" | ⏳ 自爆是唯一例外：耗尽能量视为效果，细胞死亡进复活队列 |

### 歧义类（原 P2）

| # | 问题 | 临时决策 |
|---|------|----------|
| 9 | "经过"定义 | ⏳ =进入该格的瞬间（移动/传送/复活落位都算）。停留不重复触发定殖/收取；但特殊组织**产出的瞬间**，站在其上的细胞立即收取 |
| 10 | 能量精度与无氧呼吸取整 | ✅ 能量最小单位 0.1，引擎内部用整数"十分能量"存储；无氧呼吸除法**四舍五入到 0.1**（团队 2026-08-28 定，见 #43；如 1.4/3=0.467→0.5） |
| 11 | 侵蚀选格 | ⏳ 全局掷一次（2/3→1 格，1/3→2 格），从所有被包围连通块的合法格中**均匀随机**选取 |
| 12 | 癌细胞种类抽取 | ⏳ 每个癌症玩家独立随机抽取，同局**不重复**（便于平衡测试覆盖更多种类） |
| 13 | 血管传送细节 | ⏳ 强制传送（S 类自动）；两端同时有细胞则互换；若互换会导致敌对同格，则本次传送整体取消。传送落地算"进入"（触发定殖/净化/收取） |
| 14 | 树突状细胞与攻击 | ⏳ 不能攻击 ⇒ 不能迁移进癌细胞所在格（选项直接不提供） |
| 15 | 分化费用 | ⏳ 免费（0 能量），III 级解锁，每细胞一次，每种全阵营限一个 |
| 16 | 固着型固化点追踪 | ⏳ 每格分开记"普通固化点"和"固着点"；衰减只减普通点，固着点永不自然减少。固化判定看两者之和≥2。（同格同回合多细胞停留只 +1，任一为固着型则记固着点） |
| 17 | "回合"一词 | ⏳ 全部按**世界回合**理解（免疫逃逸/侵袭型前 2 次/基质重塑/基因不稳定/B 抗体 3 次/特殊组织产出周期）。所有次数标记在 S 阶段统一重置 |
| 18 | 抗原记忆来源 | ⏳ 只有"进入式净化"（含裂解后立刻触发的净化、传送落地净化）+1 记忆；细胞毒素/抗体反噬的组织转化不给记忆。记忆下限 0 |
| 19 | 攻击结算顺序 | ⏳ 基础伤害(1/2) → 若有【标记】则 ×2 并移除 → 若免疫逃逸可用则 -0.5。反弹/未杀死时费用不退还。B 抗体的能量损失**算**"免疫细胞造成的能量损失"（吃标记×2、被免疫逃逸减免） |
| 20 | 毒素/抗体反噬能否转化有癌细胞站着的格 | ⏳ 不能，只转化**无活体癌细胞**的癌组织（避免"癌细胞站在健康组织上"的非法状态） |
| 21 | 复活细节 | ⏳ 按行动顺序依次结算；可自愿放弃本回合复活；可复活到已有己方细胞的固化格（叠格规则）；落位算"进入" |
| 22 | 固化计数 | ⏳ 按**格**每回合 +1（不按细胞数叠加）；固化癌组织被裂解/复活降级为癌组织时计数清零 |

### 设计确认类（原 P3，按字面实现，只需团队点头）

| # | 问题 | 现状 |
|---|------|------|
| 23 | ~~免疫细胞无死亡途径（癌方无伤害手段）~~ | 🛑 **已作废，两次被推翻**：① 2026-08-28 PRD 引入【E-微环境压迫】，免疫细胞会被相邻癌性组织压死；② 2026-08-31 补实装 PRD 的「攻击失败自身-0.5能量」（口径 #84）。本条**曾被当成依据写进 `cw_tuning.gd` 的注释**，直接导致 08-28 的全量比对漏掉了 ②——作废的口径要就地划掉，别只在新条目里说「推翻了旧的」 |
| 24 | 免疫胜利 I 类立即判 / 癌症胜利 S 类回合开始判 | ⏳ 按字面实现（癌方达标后免疫有一整轮反应窗口） |
| 25 | "占格封复活"：杀光癌细胞+免疫站住全部固化格 → 立即获胜 | ⏳ 按字面实现 |
| 26 | T 细胞毒素无次数上限 | ⏳ 按字面实现（对比 B 抗体 3 次/世界回合） |
| 27 | 30 回合终局判定时点 | ⏳ 第 30 世界回合 E 类结算完毕后判定；癌症 S 胜利检查放在每回合 S 阶段最开头（世界事件之前） |

## 二·补 平衡测试引出的新问题（2026-08-26，数据见 平衡测试报告.md）

| # | 问题 | 状态 |
|---|------|------|
| 28 | **规则原文下癌症方胜率 0%**，平均 3.2 回合结束 | ⏳ 推荐方案已给出（四项改动），待团队确认后写回规则文档 |
| 29 | **人数缩放崩坏**：同一数值 2/4/6 人局为 88%/50%/3%。根因是【无氧呼吸】除以癌细胞数，导致癌方阵营总收入与人数无关，而免疫方总收入随人数线性增长 | ⏳ 推荐方案让两边都「按己方细胞数均分」，收敛到 50%/46% |
| 30 | **对局长度远短于规则预期**：推荐方案下平均 9~10 回合结束，120 局仅 1 局打到 30 回合 → 「30 回合终局判定」与「癌性组织 ≥1/3」两条规则形同虚设 | ⏳ 待团队决定期望的对局长度 |
| 31 | **【免疫胜利】不看癌组织面积**：哪怕癌组织铺满棋盘，只要癌细胞全灭且无固化组织，免疫立即获胜。加入向外增生后此问题更突出 | ⏳ 建议补一个面积条件 |
| 32 | 角色设定写「癌细胞前期强、免疫前期弱」，但推荐方案是开局均势、癌方稍逊 | ⏳ 要还原设定需继续加强癌方前期，同时补免疫后期成长手段 |

## 二·补2 127 格新地图引出的问题（2026-08-27，图见 [地图布局_127格.svg](地图布局_127格.svg)）

棋盘从半径 4（61 格）扩到半径 6（127 格），特殊组织按团队给的地图图片重新布点。
读图结果（轴坐标，中心 (0,0)）：

| 类型 | 数量 | 坐标 |
|---|---|---|
| 代谢核心（绿） | 3 | (0,-4) (4,0) (-4,4) |
| 骨髓（紫） | 4 | **(0,0)** (4,-4) (-4,0) (0,4) |
| 血管（青） | 2 | (6,0) (-6,0) |

规律：半径 4 那一环的 6 个「角」上，核心与骨髓**交替**排布；外加中央格 1 个骨髓、左右两个最远角一对血管。

| # | 问题 | 状态 |
|---|------|------|
| 33 | **骨髓数量对不上**：规则文本写「骨髓（5个）」，地图上只画了 4 个 | ✅ **团队 2026-08-27 答复：是有意调整的，不是漏画**。规则文本里的「5个」作废。#34 改版后**定为 6 个**（内圈 3 + 外缘 3）|
| 34 | **中央格被骨髓占用，与开局规则直接冲突**：规则同时要求「初始癌组织**必须包含**棋盘中央格」和「癌组织**不能与**特殊组织重合」，中央格一旦是特殊组织，这两条无法同时成立 | ✅ **团队 2026-08-27 定案，已实现**：拿掉中央骨髓，改在外缘边中格补 3 个（骨髓共 6 个）；同时内圈（3 核心 + 3 骨髓）从半径 4 收到半径 3，缩小中心空地。冲突随之消解——中央格变回普通格，两条约束都成立。布局见 [地图布局_127格.svg](地图布局_127格.svg) |
| 35 | 〔团队 2026-08-27：已知悉，暂不处理，先记录〕**血管破坏了地图的旋转对称**：核心/骨髓那 6 个环上点是**3 重旋转对称**（转 120° 完全重合），但血管在 (±6,0) 只有 **2 重对称**。两者叠加后整张图**没有任何旋转对称**，也就是说没有两个玩家的开局环境是等价的。另外两端不等价：(6,0) 旁 2 格是核心（能量），(-6,0) 旁 2 格是骨髓（卡牌） | ⏳ 若是有意的地形差异则无需改；若想要公平对称，血管应放在满足 3 重对称的一组位置上 |
| 36 | **规则文本仍是 61 格时代的数字**：「其余54枚组织」「癌性组织≥1/3」等需同步 | ✅ **团队 2026-08-27 答复：真实判定条件就按换算值执行**——癌方即时胜利门槛 ⌈2/3×127⌉=**85**、终局判定线 ⌈1/2×127⌉=**64**（代码已是这个值）。规则文本放到最后统一改 |
| 37 | **初始癌组织仍是 7 格**（占全盘 5.5%，61 格时代是 11.5%） | ✅ **已被 08-28「按 PRD 实现」定案覆盖：现值 15 格**（`INIT_CANCER_TILES`，9868ca3）。本行 08-27 的「维持 7 格」结论基于旧数值体系，其平衡担忧由 [平衡方案_PRD版](平衡方案_PRD版.md) 的新基线接管 |

## 三、原型范围裁剪（✂️ 非规则问题，纯开发排期）

- 初始 7 癌组织：原型固定用"中央格+第一环"默认布局（规则允许癌方自选，选择 UI 后续做）
- UI 为原型级：程序化构建界面、文字日志代替掷骰动画；场景化（.tscn 可视化编辑）与美术贴图接入放到玩法验证之后
- 世界事件、卡牌：见上表 #1 #2
