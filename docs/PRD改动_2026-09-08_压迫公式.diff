# PRD 改动：【E-微环境压迫】换算式 —— 按相邻组织类型加权（Kevin 2026-09-08）
#
# 起因：Kevin 连发两版。先给了「免伤格数 2 → 3」，紧接着改口「微环境压迫改成这样！」
#       并给了一条全新的加权式。**后一版覆盖前一版**，本文件记的是最终形态。
#
# 旧：损失 =（相邻癌性组织数 − 2）× 0.5   —— 癌组织与固化癌组织同权，健康组织不参与。
# 新：损失 = max(0, 相邻癌组织 + 相邻固化癌组织 × 2 − 相邻健康组织) × 0.5
#
#     三处变化：① 固化癌组织权重翻倍（占地之外再加一层压制）；
#               ② 相邻健康组织**反向抵消**——站在战线上（三癌三健康）不再掉能量，
#                  代价是深入癌区急剧变贵；③ 原来的「前 2 格免伤」阈值取消。
#
#     上下限：旧式 0 ~ 2.0。新式 0 ~ 6.0（六面固化）。同为六面癌组织时 2.0 → 3.0。
#
# ⚠ **线上正本与本条有一处不一致，需要 Kevin 在线上补**：截图里的公式写作
#   「能量损失 = max(0, 相邻癌组织 + 相邻固化癌组织 × 2 − 相邻健康组织)」，**没有 × 0.5**。
#   按字面读六面固化就是 −12.0（免疫每回合有氧收入才 2.0 左右，等于必死）。
#   2026-09-08 当面问过，Kevin 答「仍然 ×0.5」，本地正本与引擎都按 ×0.5 落。
#   线上那条公式末尾该补上 × 0.5，否则下一个照着线上读的人会实现成两倍。
#
# 坏死格：`tissue` 仍是 HEALTHY（坏死是叠在上面的计数），Kevin 2026-09-08 确认**按健康组织计**，
#         也就是照样抵消 1 点压迫。引擎因此只读 `tissue` 一个字段，不特判坏死。
#
# 改前 SHA-256: 8224f5cd63731f32e3156ef494130fa5a35972daf9670f5c0a471677f3d73f9c
# 改后 SHA-256: 9dc53e846bf336d421ea712bd796939f2a90c740b030c4d4046e0f70c17bcdd0
# 改前备份: Cell_War PRD.备份_2026-09-08_压迫公式前.md
#
# 引擎：CWData.PRESSURE_FREE_ADJ 删除（新式没有阈值，留着就是死常量）；
#       新增 PRESSURE_SOLID_W = 2 / PRESSURE_HEALTHY_W = -1，PRESSURE_PER_ADJ = 5 仍是那个 ×0.5。
#       算式只有一份（CWWorld.pressure_at），界面悬停详情与 AI 惜命都调它。
# 平衡：净效果是**给癌方加强**（固化权重翻倍、上限 2.0 → 6.0），但战线上反而变宽松。
#       现有标定全部作废——4 人局癌胜 68% 那个数是旧压迫下测的。

diff --git "a/Cell_War PRD.\345\244\207\344\273\275_2026-09-08_\345\216\213\350\277\253\345\205\254\345\274\217\345\211\215.md" b/Cell_War PRD.md
index 2567d41..13c90d5 100644
--- "a/Cell_War PRD.\345\244\207\344\273\275_2026-09-08_\345\216\213\350\277\253\345\205\254\345\274\217\345\211\215.md"	
+++ b/Cell_War PRD.md	
@@ -252,9 +252,11 @@ I类\-立即生效 S类\-世界回合开始时生效 E类\-世界回合结束时
 
 【E\-微环境压迫】：世界回合结束时，每个免疫细胞受到其相邻**癌性组织**造成的微环境压迫
 
-- 若相邻**癌性组织**数量超过2格，则该免疫细胞损失（相邻**癌性组织**数量－2）×0\.5能量
+能量损失 ＝ max(0, 相邻**癌组织**数 ＋ 相邻**固化癌组织**数 × 2 － 相邻**健康组织**数) × 0\.5
 
-- 相邻**癌性组织**不超过2格时，不造成能量损失。 
+- 相邻格只看**组织类型**：癌组织 +1、固化癌组织 +2、健康组织 －1；棋盘外的方向不计。
+
+- 坏死是叠在健康组织上的状态，坏死格仍按**健康组织**计（Kevin 2026-09-08 确认）。
 
 【E\-增生】没有免疫细胞存在的**健康组织**按「相邻癌性组织数x3%」概率被转为**癌组织**
 
