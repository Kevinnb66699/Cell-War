# PRD 改动：【局部吞噬】事件卡 → 即时技能（Kevin 2026-09-08）
#
# 起因：Kevin 直接给了改后的卡面。
#
# 旧：类别 事件 / 权重 2 / 「抽取后立即生效。随机抽取该卡的免疫细胞相邻1格没有细胞占据的
#     癌组织，将其转为健康组织，并获得1抗原记忆。」
# 新：类别 即时技能 / 权重 2 / 「随机将相邻1格没有细胞占据的癌组织，将其转为健康组织，
#     并获得1抗原记忆。」
#
# **变的是「谁来决定什么时候用」，不是效果本身** —— 效果一个字没改（仍是随机 1 格 + 1 记忆），
# 但它从「抽到就必须结算」变成「进手牌、由玩家挑时机打出」。
#
# 改前 SHA-256: b73312a2d2266a139bd18067e7d4ffc0362602047c62237cfc76fc49aa76ed9b
# 改后 SHA-256: df78715fac40edd88b97f109bbcfd3920b11f782ab20908d19d57ed3823f5bf2
# 改前备份: Cell_War PRD.备份_2026-09-08_局部吞噬前.md
#
# 引擎：卡表由 tools/gen_card_data.py 从 PRD 重生成（只动了那两行）。
#   CWCardFx：结算体从 resolve_event 的分派搬到 _resolve_played；
#   候选格抽成 _phagocytosis_targets()，选项层与结算层共用一份。
#
# 三处**连带后果**，都不是顺手改的：
#   ① **免疫卡池构成**：事件 20 → 19、即时 25 → 26。
#   ② **I 级手牌实际上限 6 → 7** —— 该级的技能池多了一张（手牌 8 的硬上限没动，
#      但 I 级本来够不到，实际能拿几张由技能池张数决定）。
#   ③ **「落空」这条路要挡在选项层**：它当事件卡时抽到就必须结算，相邻没有可转化的
#      癌组织时只能落空；改成打出之后，落空就是纯粹的坑（卡吃掉、什么也没发生），
#      所以 hand_options 里没有可转化邻格就**不出这张的选项**（同 TNF-α 那条的纪律）。
#      结算体里那条落空分支保留作兜底 —— 联机延迟、快照回滚后重放旧选择都可能走到。
#
# 不变的一条：**卡面明写的那 1 点抗原记忆照给**。play() 里 card_resolve_depth 会 +1
#   （打出的即时卡引发的净化不给记忆），但那条管的是「顺带造成的净化」，
#   与卡面明写的奖励是两件事 —— Kevin 2026-09-07 的口径。

diff --git "a/Cell_War PRD.\345\244\207\344\273\275_2026-09-08_\345\261\200\351\203\250\345\220\236\345\231\254\345\211\215.md" b/Cell_War PRD.md
index 760e7b8..5586712 100644
--- "a/Cell_War PRD.\345\244\207\344\273\275_2026-09-08_\345\261\200\351\203\250\345\220\236\345\231\254\345\211\215.md"	
+++ b/Cell_War PRD.md	
@@ -632,11 +632,11 @@ $能量=\frac{连通块癌组织个数^{0.3}\times 2.8+全图固化癌组织个
 
 【局部吞噬】
 
-类别：事件
+类别：即时技能
 
 权重：2
 
-效果：抽取后立即生效。随机抽取该卡的免疫细胞相邻1格没有细胞占据的癌组织，将其转为健康组织，并获得1抗原记忆。
+效果：随机将相邻1格没有细胞占据的癌组织，将其转为健康组织，并获得1抗原记忆。
 
 
 
