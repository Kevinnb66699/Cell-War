# PRD ↔ 引擎 差异对照

> 基准：`Cell_War PRD.md`（仓库外）。**2026-09-01 晚**按定案 B + 方案乙改了三处正文，改后 **SHA-256 `5ca71533301214ca5e1be50296932e14a28a6f95ec113cf5d0c8e333e68927c5`**（1530 行 / 37461 字节），
> 审核用 unified diff 见 [PRD改动_2026-09-01_定案B与乙.diff](PRD改动_2026-09-01_定案B与乙.diff)（改前 `844a9f73…`，1530 行 / 37315 字节）。
> 更早的基准：**2026-08-31 版**，40688 字节 / 1594 行
> **SHA-256 `94b0298771741534adefa983f67229221ef5a1ee5b69f7a4ff9c7f64668393e5`**（2026-09-01 团队改版 + 4 处笔误修回，1524 行 / LF 行尾；
> 上一版是 `7853db4b1b712ec1…`，1594 行 / CRLF —— **对比前先规范化行尾**）
> （最近一次改动：口径 #89 的三条收入护栏 —— 癌细胞初始能量 6、有氧下限 2、无氧上限 10。
> 更早：#88 给【迁移】补「每行动回合最多 3 次攻击」（39789 字节 / 1588 行，`f727bae9…`）；
> 再往前 39681 字节 / 1586 行 `2b0a3946…`。）
>
> **2026-08-31 Kevin 交代「记得修改 PRD」，本轮由我们直接改了 PRD 正文**，共 19 处：
> 〇.1 / 〇.2 / 〇.5 三条卡面改写、〇.3 新增一节通用结算规则、口径 #79 的两张卡卡面、
> §七 7.1~7.4 的四个平衡数值。改前已备份到本次会话的 scratchpad。
> 改后用 `tools/gen_card_data.py` 重新生成卡牌身份表，与仓库版**仍逐行一致**——
> 证明改字没碰坏卡池结构。
>
> 上一版基准（2026-08-28 23:56 版，36540 字节 / 1554 行）
> SHA-256 `844b956d09d600aeac048124ba36f3b1a94680ed0fd29ec3e46cb8a4e49185fe`。
> ⚠ **PRD 在仓库外、不受版本管理**，做不了真正的版本 diff。
> 每次同步先比校验和 —— 08-31 那次差点把同一版当成新版去查。
> 23:56 版相对世界事件版（36173 字节）只把 W 系列定案落成条文（抽取不重复、
> 紊乱除血管+返回触发净化/定殖、细胞毒 5/6+1/6、免疫伪装 1/3 失败、
> 免疫抑制因子新效果文、固化加速 ≥2、世界事件最长两回合），与引擎逐条一致。
> 世界事件版相对 18:55 版只把结尾的「暂无」换成 18 个世界事件（74 行），
> 其余逐行一致，卡牌身份重生成仍逐字节一致。
> 18:55 版相对 01:11 版**没有可检测的内容变化**：卡牌身份用 `tools/gen_card_data.py`
> 重生成后与仓库版逐字节一致；规则正文、角色设定、X 级卡池、癌症卡池与 01:11 版
> 留档逐行一致。I/II/III 级卡池的效果文本只有字数指纹可比（±3 字噪声内吻合），
> 不排除个别措辞微调——效果尚未实现，实现时以当前文本为准，所以不影响任何已写的代码。
> 对照：本仓库 `game/scripts/core/` 全部实现
> 用途：**这是一张工作清单**，不是决策记录。决策记录在 `规则电子化说明.md`，
> 改动记录在 `开发日志.md`。每修完一条，请在本表打勾并在开发日志记一笔。

差异分三类：
- **【落后】** 引擎没跟上文档，文档一直是这么写的 —— 直接改，不需要讨论
- **【文档改了】** PRD 这一版改了数值或规则 —— 直接改，不需要讨论
- **【引擎故意的】** 引擎为了平衡刻意偏离规则原文，记在 `规则电子化说明.md` 或
  `平衡测试报告.md` 里 —— **改之前要团队点头**，因为回到规则原文会推翻已验证的平衡

> **清账状态（2026-08-28）**：一、二两节已在 `9868ca3` 全部落地——团队定案
> 「按 PRD 实现，差异不再逐条讨论」，二的「要团队点头」随定案一并解决；
> 各表的引擎值列都是 **9868ca3 修复前**的快照，留作「改了什么」的记录——**现值已全部 = PRD**。三的重跑也跑完了，结果在
> [平衡方案_PRD版](平衡方案_PRD版.md)。**仍开放**：五（卡牌效果——31 张已落地：
> 20 张无选择批 + 11 张需中途选择批（2026-08-29）；剩 35 张修饰卡等 #26 接入、
> 22 张永久卡等 #27 装备位）。六（世界事件）已于 2026-08-28 深夜**全量进引擎**——
> W1~W8 均已答复（定案 #42、#44~50），落地口径已逐条确认（§六）。
> 四（A~D）与 5.2 也已定案（#38~41、#43）。

---

## ○′、2026-09-01 团队改版：差异结清与新增（口径 #94）

### 结清 —— PRD 追上了引擎，这几条不再是「偏离」

| 原偏离 | 结清方式 |
|---|---|
| 【无氧呼吸】10 点上限（口径 #92 改成 999） | PRD 删掉了那条说明，引擎不动 |
| 【S-有氧呼吸】下限 2.0 的夹取位置（口径 #89/#92 争议过） | PRD 改写成公式内的 `max{2, …}`，与实现一致 |
| 能量上限 15（口径 #92，此前是 PRD 之外的实验档） | PRD 正式写入（数值修正结算顺序第 9 阶段「溢出」） |
| 攻击 3 次/行动回合的条文位置（口径 #88） | PRD 挪到【攻击】小节，规则不变 |

### 新增 —— 引擎按新 PRD 改了，但有两处**语义后果**要团队确认

1. **【裂解】不再给抗原记忆、也不触发净化连锁。** 新文只说「转为健康组织」，
   删掉了旧文的「随后可立刻触发**【净化】**」。按字面实装的结果是：挂在净化上的
   【免疫记忆库】等永久技能，从此不会被裂解触发；T 细胞拆固化也不再涨等级。
   **如果本意是「仍算一次净化」，把那半句加回 PRD 即可，引擎改一行。**

2. **【抗体亲和力成熟】的伤害那一半失效了。** 基础【抗体】已经是 1.5，
   而这张卡写的是「伤害 1.0 → 1.5」—— 现在只剩「费用 1.0 → 0.5」还有作用。
   要么把卡面改成更高的数（如 2.0），要么承认它现在是一张纯减费卡。

### 仍然是刻意偏离（本次没动）

第七节那批占地单价/移动费的偏离原样保留 —— 它们是平衡测试定的，PRD 这次没碰。
⚠ 但**平衡基线已经作废**：改版后启发式互搏 4 人局癌胜 17%（改前 64%）、6 人局 9%，
第七节那些数字是在旧胜利线（64）和旧【增生】（每回合）下测的，要重测。

---

## 〇、✅ 已写进 PRD 的口径（2026-08-31 结清）

本节原本是「团队已拍板、引擎已实装、但 PRD 正文还写着旧文字」的待办清单。
**Kevin 2026-08-31 交代「记得修改 PRD」，已全部落字，本节转为记录。**

| 条 | 状态 |
|---|---|
| 〇.1 六张卡「任意时间」→「己方回合」 | ✅ 已改（8 处，含【细胞膜修复】【缺氧适应】各出现两次） |
| 〇.2 【缺氧适应】效果重写 | ✅ 已改（2 处） |
| 〇.3 新增「数值修正的结算顺序」通则 | ✅ 已写入，**但按口径 #79 的语义阶段重写过**，不是本节存档的旧草稿 |
| 〇.4 【炎症趋化】「降为」→「改为」 | 🛑 **撤回，不用改**（随定案 A 一起作废） |
| 〇.5 【囊性护甲】效果重写 | ✅ 已改 |

> **§七** 是同一性质的另一份清单（平衡数值，口径 #82），也已在同一次落字。
> **§八** 方向相反：PRD 一直这么写、引擎没跟上，2026-08-31 已全部改完引擎。

### 〇.1 ✅ 六张卡的「使用时机：任意时间」→「己方回合」（口径 #72）

细胞膜修复 / 缺氧适应 / PD-L1表达 / DNA损伤修复 / BCL-2抗凋亡 / 癌症干性。
打断窗口不做了，这六张改为己方回合打出、生效时长一律「下一次」。

### 〇.2 ✅ 【缺氧适应】效果整段重写（口径 #72）

旧：「本世界回合不受【微环境压迫】；下一次癌细胞技能造成的能量损失额外 -1.0」
新：**「下一次【微环境压迫】或来自癌细胞技能造成的能量损失 -1」**

### 〇.3 ✅ 通用结算规则新增一条：数值修正的结算顺序（按 **#79 的语义阶段**写入）

PRD 原本只有「能量损失计算顺序」五步管线，缺一条管其余数值（如移动费）的通则。
**2026-08-31 已补进 PRD 的「通用结算规则」一节**，写的是**口径 #79 的九个语义阶段**
（基准值 → 基准值替换 → 固定增加 → 固定减免 → 倍增 → 倍减 → 免费豁免 → 行动下限 →
不可豁免附加费），外加「次数与额度的消耗」两条，以及与「能量损失计算顺序」互相指认的一句。
⚠ **这段是新写的规范性条文，请团队过目**——它照的是引擎 `cw_cost.gd` 的实际行为。

> 🛑 **但下面这份草稿已经过时，别照抄进 PRD。** 它写的是口径 #73 的
> 「卡牌按**打出先后**逐张结算」，而 **口径 #79 已经在费用侧作废了这条**，
> 改成了**语义阶段**：③基础值替换 →④固定加费 →⑤固定减费（含该减费自己的局部下限）
> →⑥倍增 →⑦倍减 →⑨免费豁免 →⑩行动硬下限 →⑪不可豁免附加费；
> 来源顺序与打出先后**降级成同阶段内的平局规则**。
> 引擎（`cw_cost.gd`）跑的是 #79。**要入 PRD 的是 #79 那套，这份草稿得整段重写。**
> 重写时还要注意 #79 连带作废了定案 A 与定案 D 乙案，下面「两处修订」那段也一并失效。

以下为**存档**的旧草稿（口径 #73 版本，仅供追溯，勿用）：

> **通用结算规则 · 数值修正的结算顺序**
>
> 当同一个数值同时受到多个效果影响时，按以下顺序依次结算：
>
> 1. **基准值** —— 由该行动本身、细胞种类与免疫等级决定，含细胞自带技能（如【伪足穿透】【极简胞浆】）。
> 2. **卡牌效果** —— 按卡牌**打出的先后顺序**依次结算。永久技能自打出（装备）那一刻起加入这个顺序，此后每次结算都按它被打出的时刻排位。
> 3. **世界事件** —— 最后结算。
>
> 各类效果的结算方式：
>
> - 「**改为** X」：把当前数值**改写**为 X。**改写可能使数值升高** —— 若先前的效果已把它压到 X 以下，仍改写为 X。
> - 「-X」「减少 X」：在当前数值上**扣减**；卡面写有「最低 Y」的，扣减后不低于 Y。
> - 「不消耗能量」「免费」：把当前数值改写为 0，不受「最低 Y」限制。
> - **减免类效果不会使数值升高** —— 若当前数值已不高于该效果的下限，则保持不变。
> - **「免费」类效果**（把数值改写为 0）在**数值本来就已是 0** 时视为未结算，其额度**不予消耗**。
> - 其余效果（减免、改写）只要适用即结算：即便未能改变数值，其次数与每回合额度**照常消耗**。
>
> 本条**不适用于能量损失**：能量损失另有「能量损失计算顺序」的五步规则，以那一条为准。
>
> **示例**（免疫细胞向癌性组织迁移，基准 1.0）：先打【炎症趋化】（改为 0.5）、
> 再打【CXCR3趋化】（-0.5，最低 0.2）→ 0.5 → **0.2**；顺序反过来 →
> 1.0 → 0.5 → 被【炎症趋化】**改写回 0.5**（改为可以抬价，这是打出顺序要权衡的地方）。
> 若此时【基质阻隔】在场，最后再翻倍。

配套建议：在「能量损失计算顺序」那一节末尾补一句「本顺序优先于『数值修正的结算顺序』」，
两条通则互相指认，读到哪条都不会用错。

**两处修订（2026-08-30 审查后追加，口径 #74 / #75）**：

1. 原草稿把这一类写成「降为 X」，但实测【炎症趋化】会把已经更便宜的价钱**抬回** 0.5
   （先装【组织巡航】首移免费 = 0，再打【炎症趋化】→ 0.5）。团队定案保留改写语义、
   改称「**改为**」，见 〇.4。所以**卡面文字要改**，上一版草稿里「卡面文字不用改」那句作废。
2. 新增「**免费额度省、限次折扣不省**」两条（团队定案 D）：
   旧行为下同时装【组织巡航】+【组织驻留】，一次移动会把两个「本回合首次免费」一起烧掉。
   注意定案只覆盖「免费」类；减免与改写类即便没减到钱也照扣次数。

### 〇.4 ~~【炎症趋化】卡面：「降为 0.5」→「改为 0.5」（口径 #74）~~ —— 🛑 已撤回，不用改

**这一条随定案 A 一起作废，口径 #79 明写。** 原本的理由是「『改为』能抬价，
所以措辞要准确」；但 #79 换成语义阶段之后，「改为」只看**基准值**、
且免费豁免排在它**之后** —— **抬价场景不复存在**，「改为 / 降为」的区别失去意义。

新版 PRD（2026-08-31）里【炎症趋化】仍写「降为 0.5」、【上皮—间质转化】仍写
「改为 0.2」，**两者都不用动**。保留本条只是为了让看过旧版文档的人知道它被撤回了。

### 〇.5 ✅ 【囊性护甲】效果重写（口径 #76）

> 原：受到的能量损失 -0.5，每世界回合 1 次
> 新：**每世界回合第一次能量损失 -0.5**

去掉「受到的」，**不再限定来源**。旧文字下引擎只在免疫来源的伤害管线里实现了护甲，
世界事件（【免疫抑制因子】对全体癌细胞的 0.5）那条路整个绕过去了。改后两条管线共用同一个额度。

⏳ 顺带要问：【突变】第 3 面的**自扣** 1.0 算不算「能量损失」？引擎目前**不挡**
（按口径 #68 的精神，自扣不是「受到损失」）。若团队认为该挡，卡面和引擎都要再动一次。

---

## 一、必须改，无需讨论 —— ✅ 已全部落地（9868ca3）

### 1.1 数值

| # | 项 | PRD | 引擎当时的值（9868ca3 修复前） | 位置 | 类 |
|---|---|---|---|---|---|
| 1 | 初始癌组织 | **15** 格（+112 健康 = 127） | 7 | `cw_data.gd` `INIT_CANCER_TILES` | 落后（61 格时代的数，棋盘扩到 127 时没跟着改） |
| 2 | 无氧呼吸系数 | 癌 **0.4** / 固化 **1.0** | 0.2 / 0.5 | `cw_data.gd` `ANAEROBIC_PER_*` | 文档改了（旧规则是 0.5/1.2） |
| 3 | 有氧呼吸 | **(健康格数 − 坏死格数) ÷ 总格数 × 3**，四舍五入到十分位 | 按等级固定 `[3,4,3,3]` | `cw_data.gd` `AEROBIC_GAIN`、`cw_world.gd` `_aerobic()` | 文档改了 |
| 4 | 有氧呼吸的等级加成 | **删除**（II 级「改为 4 能量」、III 级「改为 3 能量」两条都没了） | 还在 | 同上 | 文档改了 |
| 5 | X 级门槛 | **31+** 抗原记忆（III = 16~30） | 30+ | `cw_data.gd` `LEVEL_MIN_MEMORY` | 文档改了（顺带解掉了说明 #7 的重叠歧义） |
| 6 | B 细胞【抗体】 | 每世界回合 **2** 次 | 3 | `cw_data.gd` `ANTIBODY_MAX_PER_ROUND` | 文档改了 |
| 7 | T 细胞【细胞毒素】 | 每世界回合 **3** 次 | 无上限 | `cw_actions.gd` `_do_toxin` | 文档补的（推翻说明 #26） |
| 8 | 【基因表达】 | 每回合最多 **3** 次（免疫与癌症都是） | 无上限 | `cw_actions.gd` `_do_draw` | 文档补的 |
| 9 | 巨噬【吞噬】净化回能 | **0.3** → 2026-09-03 定案①改为**不回能**（见 7.12） | 0.2 | `cw_data.gd` `MACRO_HEAL_PURIFY` | 文档改了；09-03 引擎与 PRD 同步改 |
| 10 | 巨噬【吞噬】攻击回能 | ⌈受击方损失 ÷ 2⌉ **随伤害变化** | 固定 0.5 | `cw_data.gd` `MACRO_HEAL_ATTACK` | 文档改了（从常数变成公式） |
| 11 | 免疫【复活】落点 | 玩家选任一**健康状态的骨髓格**且无细胞占据 | 全场随机健康格 | `cw_world.gd` `_immune_respawn()` | 落后 + 引擎的临时决策 |
| 12 | 免疫【复活】能量 | **1.0** | 2.0 | `cw_data.gd` `IMMUNE_RESPAWN_ENERGY` | 文档改了（旧规则 0） |
| 13 | 癌【复活】落点 | **未被细胞**占据的固化癌组织 | 未被**免疫**占据（允许叠己方） | `cw_world.gd` `_revive()` | 文档改了（配合 2.1 一格一细胞） |
| 14 | 【瓦伯格超速糖酵解】 | 无氧呼吸 **×110%**，向上取整到十分位 | 旧公式（连通块加成） | 未实现（旧 6 种里没有） | 文档改了 |
| 15 | 世界事件回合 | 3 / 6 / 10 / 15 / 20 / 25 / **30 止** | 3 / 6 / 10 / 15+5k **无上限** | `cw_data.gd` `is_world_event_round()` | 文档明确了终点 |

### 1.2 全新机制（引擎 0 处实现）

| # | 机制 | 内容 | 谁依赖它 |
|---|---|---|---|
| 16 | **【E-微环境压迫】** | E 阶段第 1 步。相邻癌性组织 > 2 格时，免疫细胞损失 (相邻数−2)×0.5 | 免疫卡【缺氧适应】【耗竭抵抗】；**也是癌方第一个稳定的伤害来源** |
| 17 | **「坏死」状态** | 该格不为免疫【有氧呼吸】供能，可被【定殖】；按格记剩余世界回合数 | T 细胞【细胞毒素】（**规则原文就有，一直是漏的**）2 回合；免疫卡【放疗】5 回合 |
| 18 | **【I-各司其职】** | 树突状细胞攻击只造成 1/2 伤害，向下取整到十分位 | 免疫卡【抗原呈递强化】 |
| 19 | **能量损失结算管线** | 固定 5 步：基础 → 固定增加 → 倍增 → 倍减 → 固定减免 | 几乎每一张带 ±N / ×2 的卡 |
| 20 | **抽卡合法性** | 手上已有的同名技能、已装备的同名永久技能从候选剔除，**其余卡权重不变**；无合法卡则不能抽 | 全部卡池 |
| 20b | **癌症卡的分期取值** | 卡池按世界回合分三期（1—9 / 10—19 / 20—30），**权重和效果数值都随期变**；永久技能按**当前**世界回合取值，不按装备时锁定 | 全部 17 张癌症卡 |

### 1.3 结构性差异

**2.1 一格只能站一个细胞** —— PRD 棋盘设定：「**一个组织内只能容纳一个细胞**」。
引擎现在是说明 #4「同阵营可叠，敌对不可同格」。这条推翻它，影响面比看起来大：
- `cw_setup.gd` 落子阶段
- `cw_world.gd` 复活落位（免疫、癌症两处）
- `match.gd` 的 `STACK_DX = 9.0` 错开渲染**整块作废**
- 卡【免疫增援】【肿瘤细胞募集】的传送落点判定
- 说明 #4 / #21 要改写

**2.2 E 阶段顺序变了，而且多两步**

| PRD | 当时的引擎 |
|---|---|
| 1 微环境压迫 | *缺* |
| 2 增生 | 2（顺序在侵蚀之后） |
| 3 侵蚀 | 1（顺序在增生之前） |
| 4 无氧呼吸 | 3 |
| 5 固化 | 4 |
| 6 固化计数衰减 | 5 |
| 7 其他 E 类 | *缺* |
| 8 更新持续状态、移除到期的「坏死」 | *缺* |
| 9 移除「新生」 | 6 |
| 10 **胜利条件检查** | 只在 ≥ 上限回合时判终局 |

**增生与侵蚀的先后是反的** —— 增生会改变「完全包围」的判定结果，顺序不同结果不同。

**2.3 S 阶段顺序**：PRD 是 特殊组织产出 → 血管传送 → **免疫复活** → **癌复活** → 有氧呼吸 → 其他 S。
引擎是 癌症胜利判定 → 世界事件 → 产出 → 血管 → **癌复活** → **免疫复活** → 有氧。
两处差异：① 癌症胜利判定要挪到 E 阶段（见 2.4）② 两边复活的先后是反的。

**2.4 两个胜利条件都改成 E 类** —— PRD 现在写的是【**E**-免疫胜利】【**E**-癌症胜利】，
统一在 E 阶段第 10 步检查。引擎是免疫胜利 I 类（净化后立即判，`cw_actions.gd:190`、
`cw_actions.gd:313`、`cw_game.gd:275` 三处）+ 癌症胜利 S 类（`cw_world.gd:16`）。
→ **说明 #24 与 #27 被官方推翻**，那三处即时判定要全部撤掉。

**2.5 【E-增生】正式进规则**（4%/相邻癌性组织）—— 引擎已实现，但作为旋钮
`proliferate_per_adjacent` **默认关闭**，只有 `CWTuning.recommended()` 打开。要改成默认值。
这是团队 2026-08-26 的提案被 PRD 正式采纳。

**2.6 癌细胞种类改成 4 种真实癌症** —— 见 `开发日志.md`。旧的 6 种
（BLAST/INVASIVE/REMODEL/ESCAPE/UNSTABLE/SESSILE）全部作废。
唯一能直接搬的是【囊性护甲】= 旧 `ESCAPE_REDUCTION`。
新增需求：新的格子状态「黏液」、固化计数要支持 **+1.5**（十分整数）、
「免疫无法攻击站在固化癌组织上的骨肉瘤」这条攻击合法性限制。

**2.7 【印戒-黏液破裂】范围扩大** —— 从「周围 2 格所有**健康组织**」改成
「自身所在格及周围 2 格所有**组织**」，并且**范围内免疫细胞受到 2 能量损失**。

---

## 二、引擎故意偏离，改之前要团队点头 —— ✅ 团队定案后已回到 PRD 值（9868ca3）

这几条是平衡测试的产物，回到 PRD 原值会推翻 `平衡测试报告.md` 里已验证的结论。

| # | 项 | PRD | 引擎当时的值（修复前） | 当时为什么这么定 |
|---|---|---|---|---|
| 21 | 固化阈值 | 计数达 **3** | 2 | 说明 #22 一线；127 格棋盘上 3 太慢 |
| 22 | 固化计数衰减 | 每回合 **−0.5** | −1 | `规则电子化说明.md` 里被标成「✅ 已解决 = −1」，**这一条其实从来就和规则原文不符** |
| 23 | 癌症胜利门槛 | ⌈1/2 × 127⌉ = **64** | 85 = ⌈2/3 × 127⌉ | 2026-08-26 定案：1/3 门槛对癌方过友好；但当时的基线是 61 格 |
| 24 | 30 回合终局线 | ⌊1/3 × 127⌋ = **42** | 64 = ⌈1/2 × 127⌉ | 同上 |
| 25 | 回合上限 | **30** | 20 | 2026-08-26 定案：实测平均 9~10 回合结束，30 从未触发 |

---

## 三、⚠ PRD 的新收入公式会让人数缩放问题复发

`平衡测试报告.md` 问题 #29 的根因是：**癌方阵营总收入与人数无关**（无氧呼吸除以连通块
内癌细胞数），**而免疫方阵营总收入随人数线性增长**。当时 2/4/6 人局的癌方胜率是
88% / 50% / 3%。修法是让两边都按己方细胞数均分。

PRD 这一版的新公式是：
- 免疫：每个细胞各得 (健康 − 坏死) ÷ 总格数 × 3 —— **不除以免疫细胞数**
- 癌症：连通块供能 ÷ 连通块内癌细胞数 —— **仍然除**

**结构和 #29 完全一样。** 用开局盘面（15 癌 / 112 健康 / 127 总）算阵营总收入：

| 人数 | 免疫方总收入 | 癌方总收入 | 比值 |
|---|---|---|---|
| 2 人（1/1） | 2.6 | 6.0 | 癌方 **2.3 倍** |
| 4 人（2/2） | 5.2 | 6.0 | 大致均势 |
| 6 人（3/3） | 7.8 | 6.0 | 免疫 **1.3 倍** |

4 人局是好的，2 人和 6 人会朝相反方向偏。这条**不该我单方面改**——是按 PRD 字面实现
然后重跑平衡测试拿数据说话，还是直接沿用引擎里已验证的均分方案，要团队定。
建议：**按字面实现，重跑 `tests/balance_sim.gd`，用新数据再谈。**

→ **已重跑（2026-08-28）**：比上表预测更糟——不是两头不均，是癌方 2/4/6 人局
全 100% 胜、4/6 人局平均 3 个世界回合结束。根因与救法见
[平衡方案_PRD版](平衡方案_PRD版.md)，本节留作背景。

---

## 四、需要团队回答的歧义

| # | 问题 |
|---|---|
| A | ✅ **已答复：不可以**（2026-08-28，定案见 规则电子化说明 #38）。原问题：旧规则有一条「癌细胞无法向免疫细胞占据的组织移动」，PRD 删掉了，反向没写。 |
| B | ✅ **已答复：就是「6 个骨髓格里健康且空的那些」，骨髓全癌化时复活不了是有意的**（2026-08-28，#39）。 |
| C | ✅ **已答复：罚停 1 回合**，与引擎现状一致（2026-08-28，#40）。背景：【E-微环境压迫】让免疫细胞真的会死了（说明 #23 作废）。 |
| D | ✅ **已答复：无氧呼吸也四舍五入到十分位**（2026-08-28，定案见 规则电子化说明 #43，引擎已改）。有氧本来就按 PRD 四舍五入。 |

---

## 五、卡牌系统（2026-08-28 两池齐了）

**规模**：66 张唯一卡 / 81 个池内条目（同一张卡可以出现在多个池里，权重不同）。

| 池 | 张数 | 事件 | 即时技能 | 永久技能 | 权重合计 |
|---|---|---|---|---|---|
| I 级 | 11 | 5 | 3 | 3 | 37 |
| II 级 | 14 | 3 | 8 | 3 | 38 |
| III 级 | 17 | 5 | 9 | 3 | 47 |
| X 级 | 22 | 4 | 9 | 9 | 50 |
| 癌症 | 17 | 6 | 7 | 4 | 分期 |

**两套池的结构不一样，引擎要分别对待：**
- **免疫**按抗原记忆等级切池，升级后下次抽卡直接用新池，不再抽旧池
- **癌症**不分等级，永远一个池，但**按世界回合分三期**（1—9 / 10—19 / 20—30），
  权重和效果数值都随期变。**永久技能按当前世界回合取值**，不在装备时锁定 ——
  也就是说同一张【GLUT1高表达】第 5 回合给 0.5、第 25 回合给 1.0，装备位得每回合重算

### 5.1 引擎需要的四样东西

| # | 能力 | 谁需要 |
|---|---|---|
| 26 | **修饰/触发框架** | **35 / 66 张**（免疫 26、癌症 9）。钩子：攻击判定前改概率、攻击成功后追加、净化时、有氧/无氧呼吸时、受伤时、迁移计费时、E 阶段结算时。时长：本次 / 本回合 / 本世界回合 / 永久。计数器：「每世界回合第一次」「接下来 N 次」「每个行动回合第一次」 |
| 27 | **装备位（角色面板）** | 全部 22 张永久技能（免疫 18、癌症 4）。PRD 只说「置于角色面板」，位置和交互要设计 |
| 28 | **抽卡合法性过滤** | 全部。手上已有同名技能 / 已装备同名永久技能 → 剔除候选，其余权重不变；无合法卡则不能抽 |
| 29 | **分期取值** | 全部 17 张癌症卡 |

剩下 31 张是「立即结算、只改数值和地块」，卡系统一有就能写。

**进展**：#28 ✅（9868ca3，`cw_cards.gd`）。2026-08-28 深夜第一片效果落地
（`cw_card_fx.gd`，333 项测试全绿）：11 张无选择事件卡抽到自动结算、9 张单目标
即时技能可打出（摊平成顶层选项，结算后弃置）、弃牌动作可用；`immune_hit` 增加
「非攻击」口径——卡牌伤害不吃树突减半/巨噬吸血，吃【标记】与【囊性护甲】。
2026-08-29 晚第二片落地：**11 张需中途选择的卡全部实装**
（趋化募集/效应细胞浸润/炎症风暴/免疫风暴/全身免疫动员/基因组不稳定/
炎症性趋化/代谢耦联×2/基质重塑/放疗）——结算里的决定走 `game.ask`
（kind：free_move / pick_cell / pick_tile / pick），抽卡链路因此改成协程
（draw→enter_tile→collect_special 一路 await 到流程状态机）；实现口径见
规则电子化说明 #51~#56（⏳ 待团队过目）。
2026-08-29 深夜第三片落地（494 项测试全绿）：**修饰批 15 张唯一卡全部实装**
（免疫 10：I型干扰素/补体调理/炎症趋化/细胞膜修复/CXCR3趋化/TNF-α局部炎症/
穿孔素-颗粒酶/补体级联/缺氧适应/高亲和力克隆；癌症 5：基质稳定/TGF-β释放/
PD-L1表达/DNA损伤修复/上皮—间质转化）——#26 框架兑现：自我修饰挂
`cell["mods"]`（次数 + 本回合/本世界回合/用尽三种时钟），全局修饰复用
`events["active"]`；口径见 规则电子化说明 #57~#64（⏳ 待团队过目）。
本表早前的「35 张修饰卡（免疫 26、癌症 9）」按**池内条目**计且把永久算了进去；
按唯一卡计：修饰事件+即时 = 15（已清零）。
2026-08-29 深夜第四片落地（538 项测试全绿）：**#27 装备位 + 21 张唯一永久技能全部实装**
（免疫 17、癌症 4；早前写 22 是笔误）——打出即装备（`cell["equipped"]`，
「技 N」计数），效果按 equipped 被动查询，「每行动回合/每世界回合第一次」
用 `fx_turn`/`fx_round` 闸门；#29 分期取值随之落地（GLUT1/RAS/BCL-2/干性按
当前回合取值）。口径见 规则电子化说明 #65~#70（⏳ 待团队过目）。
**66 张卡全部可结算（66/66）**。剩：**真人打牌界面已接**（方案甲）；
AI 策略未接（AI 抽到卡只握着不打）；「任意时间」时机等打断窗口。

**前置依赖**：第 1.2 节那三个全新机制（【E-微环境压迫】/「坏死」/【I-各司其职】）
是若干张卡的前提，必须先做。

### 5.2 文档里待澄清的一处

**【代谢耦联】** 出现在 III 级、X 级（免疫）和癌症卡池三处。免疫池的两处
效果文本里带了 `1 / 1.5 / 2` 和 `1.2 / 2 / 2.5` 的**分期数值**，但免疫池没有分期制度
（免疫按等级切池，癌症才按回合分期）。这两处该取哪一档？还是说免疫版本
应该按 III 级 / X 级各给一个固定值？

→ ✅ **已答复（2026-08-28）：使用者自选档位**——转移 1 / 1.5 / 2 对应接受方获得
1.2 / 2 / 2.5，不是分期数值。定案与癌症池同名卡的处理假设见 规则电子化说明 #41。

---

## 六、世界事件（2026-08-28 晚交付，18 个）

调度器早就有（3/6/10/15/20/25/30 回合），内容 2026-08-28 晚进 PRD、当晚全量进引擎。
按实现形态分两类：

- **即时/本回合 8 个**：紊乱、抗原丢失、固化加速、增殖抑制、免疫抑制因子、营养缺乏、
  细胞应激、双重触发（"本回合"类也要在回合末撤销效果）
- **持续 2 回合的修饰 10 个**：营养输送、异常增殖、代谢加速、细胞毒、免疫伪装、
  抗原变异、抗原暴露、基质阻隔、迁移激活、信号放大

**引擎需要的新东西——三样都已落地（2026-08-28 深夜，`cw_world_fx.gd`）**：
①抽取规则=定案 #42（等权随机、同局不可重复，`events["pool"]` 抽一个少一个）
②修饰器容器=`game.events["active"]`，条目 {name, left, stacks, data}，结算点一律问
`game.event_stacks(名)`——挂接点已铺：攻击判定、移动/打牌计费、卡牌能量放大、
增生、特殊组织产出/收取、固化门槛（新公用入口 `game.raise_solid()`）、抗原记忆、
血管奖励、紊乱传送/返回。**这就是 5.1 #26 要的框架**：卡牌修饰将来塞条目即可复用
③快照/`state_hash` 已带上 `events` 整体（池、生效条目、双重触发标记、各事件簿记）。

### 待澄清（W 系列）——2026-08-28 深夜全部答复并实现；23:56 版 PRD 已把定案落字
（抽取规则、紊乱除血管+返回触发净化/定殖、细胞毒 5/6+1/6、免疫抑制因子新效果文、
固化加速 >=2 都写进了 PRD，与引擎一致）

| # | 定案（编号见 规则电子化说明 §零） |
|---|---|
| W1 | ✅ 等权随机、同局不可重复（#42） |
| W2 | ✅ 落地算「进入」；目的地不选血管格（#44）。③返回边界 08-29 定案：**方案A 同时返回**（详见 #44 与下方第 1 条） |
| W3 | ✅ 并给不重掷：细胞毒 失败→成功；免疫伪装 大成功→**成功**（#45，08-29 按新 PRD 改判） |
| W4 | ✅ 从 <2.0 涨到 ≥2.0 立即转化，含骨样硬化一步跨线（#46） |
| W5 | ✅ 效果改文：净化花 0.2 且不获记忆；癌细胞 −0.5；免疫弃 1（#47，已进 PRD） |
| W6 | ✅ 随机弃（#48） |
| W7 | ✅ 三档：数量类叠两次 / 开关类接力 4 回合 / 本回合类连续两回合各一遍（#49 修订版） |
| W8 | ✅ 即时技能卡翻倍、永久技能卡不翻、细胞自带技能不翻（#50） |

### 落地口径（原「落地假设」——2026-08-29 已随讨论逐条确认为定案）

1. **W2③ 紊乱的回合末返回（定案：方案A「同时返回」）**：全部返回视为同时发生——
   先算清每个人的落点、再统一归位，返回者的原位互不相同、**互不阻挡**；
   死亡不返回；主动移动过的照样返回；原位被复活/移动的细胞真占住才留在传送后位置
   （连环挤占的极端情形再随机找合法空位）；原位组织已变按正常「进入」结算
   （PRD 已明写触发净化/定殖）。**返回本身也算一次「进入」**（特殊组织收取等照常）。
2. **W4 不追溯**：【固化加速】触发时计数已 ≥2.0 的格不立即转化（「从 1 达到」按跨线读）。
3. **W5 细则**：净化费 0.2 计入**价签**（移动/裂解选项按加价后金额校验与支付）；
   攻击进普通癌组织格同样加价，攻击失败不退（当作出手成本）；
   卡牌的「转为健康组织」不算细胞净化，不收费也本就不给记忆（说明 #18）。
4. **W7（定案：三档，#49 修订版）**：持续·数量类（营养输送/异常增殖/代谢加速/
   抗原变异/抗原暴露/基质阻隔/信号放大）**两份同时生效 2 回合**；
   持续·开关类（细胞毒/免疫伪装/迁移激活）**一份强度接力 4 回合**；
   本回合类全部 7 个（紊乱/抗原丢失/固化加速/增殖抑制/免疫抑制因子/营养缺乏/细胞应激）
   **变成连续两个回合、每回合完整生效一遍**——开关半句不再白给。
   第 3 回合起手的 4 回合事件会与第 6 回合的新事件短暂共存，容器天然支持。
5. **W8 边界**：事件卡（抽到立即结算）里的能量增减**也翻倍**（按「即时」口径）；
   【代谢耦联】给出方与接收方的数额一起翻（2026-08-29 已实装）；
   固化计数、抗原记忆、抽牌数不是能量，不翻。
6. **营养输送**只认 S 阶段的血管传送为「通过血管」；黑色素瘤【早期血行转移】是技能，不计。
7. **代谢加速**对「产出瞬间站在核心上」的收取同样翻倍（收取口径统一，不细分是否刚进入）。
8. **事件的「失去 X 能量」按中立来源结算**：走 `cancer_hit` 管线（可致死），
   不触发【标记】×2 /【囊性护甲】减免/巨噬吸血（那些挂在免疫来源的 `immune_hit` 上）。

### 沿用的旧假设（世界事件交付时就列过，继续有效）

【基质阻隔】翻倍适用于一切移动计费（技能移动也翻，0 翻倍仍是 0）；
【细胞应激】只对打出手牌收 0.5，事件卡与【基因表达】不受影响；【抗原丢失】只管「攻击」，
抗体/细胞毒素照常扣能量；【抗原暴露】对一切抗原记忆来源、每**次**获得 +1；
事件文本里的「回合」一律按世界回合理解（说明 #17）。

---

## 七、为平衡刻意偏离 PRD 的条目 —— ⏳ 已拍板已实装，PRD 待改字

**本节与 〇 节性质相同**（团队已拍板、引擎已实装、PRD 正文还写着旧数字），
单独成节是因为它是一整套平衡数值，放在一起便于一次改完。

**7.1~7.5：Kevin 2026-08-31 拍板（「先按照你测出来的数值来」），
`CWData` 常量已改成下面的新值，无头测试全绿；**同日 PRD 正文也已落字**。**
数据与推导见 [平衡方案_PRD版 §八](平衡方案_PRD版.md)，口径编号 #82。

**7.7 是另一回事**：2026-08-26 就定案实装的【原发灶】，一直没记进本文档，
2026-08-31 复核时才发现，**当天即由 Kevin 定案取消**（口径 #85）。
所以引擎当前为平衡偏离 PRD 的，只剩 7.1~7.4 这四处 —— 而它们现在也已写进 PRD，
**引擎与 PRD 就此完全一致**。

> 📌 下面每条都按**技能名／规则名 + 引擎常量**定位。
> PRD 正文（1462 行那版）不在仓库里，所以**没有逐字引用 PRD 原句** ——
> 真去改字时要先在 PRD 里定位到对应那一句，再照本节的新值改。

### 7.1 癌细胞移动至健康组织：0.5 → **1.2**

| | 值 | 引擎位置 |
|---|---|---|
| PRD 正文（待改） | 0.5 | —— |
| **引擎现值** | **1.2** | `CWData.CANCER_MOVE_HEALTHY = 12`（旋钮 `CWTuning.cancer_move_healthy`） |

**这是整套平衡的支点。** 癌细胞每移动到一格健康组织就【定殖】该格，
所以这个数就是「**占地单价**」；免疫【净化】一格是 1.0。
现行 0.5 对 1.0 的结果是实测占地转化比 **4.6 : 1**（癌方每局铺 68 格，免疫收 15 格）。

### 7.2 小细胞肺癌【极简胞浆】：0.3 → **0.7**

| | 值 | 引擎位置 |
|---|---|---|
| PRD 正文（待改） | 0.3（移动至健康组织**永久**降为 0.3） | —— |
| **引擎现值** | **0.7** | `CWData.SCLC_MOVE_HEALTHY = 7`（旋钮 `CWTuning.sclc_move_healthy`） |

按 7.1 同比例抬（0.3 / 0.5 ≈ 0.7 / 1.2），保持「小细胞比别的癌种便宜约四成」这个相对关系。

### 7.3 黑色素瘤【伪足穿透】：0.2 → **0.5**

| | 值 | 引擎位置 |
|---|---|---|
| PRD 正文（待改） | 0.2（目标健康组织邻接 ≥2 格癌性组织时；**无次数限制**） | —— |
| **引擎现值** | **0.5** | `CWData.PSEUDOPOD_COST = 5`（旋钮 `CWTuning.pseudopod_cost`） |

⚠ **一个连带影响（已实装并补了测试）**：【上皮—间质转化】的「改为 0.2」原先对黑色素瘤是**空操作**
（基准价本来就是 0.2，见 [定案D范围确认](定案D范围确认_2026-08-30.md) 顺手带上④）。
【伪足穿透】抬到 0.5 之后，**EMT 的 0.2 重新变得有意义** ——
黑色素瘤打 EMT 会从 0.5 降到 0.2。这是抬价**新造出来**的组合，
已补测试 `t_card_mods` ②b（第一条断言钉的就是「伪足穿透必须比 EMT 贵」这个前提，
谁把伪足穿透改回 0.2，那条会先红）。
⏳ **如果团队觉得这个组合太便宜**，可以把 EMT 一并抬（比如「改为 0.5」）——
那是另一条要改的 PRD 文字，目前**没改**。

### 7.4 初始癌组织：固定 15 格 → **按人数分（4 人 15 / 6 人 21）**

| | 值 | 引擎位置 |
|---|---|---|
| PRD 正文（待改） | 15 格，不随人数变化 | —— |
| **引擎现值** | **{2: 15, 4: 15, 6: 21}** | `CWData.INIT_CANCER_TILES` 已改成**字典**，取值走 `CWData.init_cancer_tiles(n)`；旋钮 `CWTuning.init_cancer_tiles = -1` 表示按表取 |

**这是唯一需要在 PRD 里新增「按人数取值」结构的一条**，其余都只是换数字。

理由：免疫方阵营总收入**随人数线性增长**（【S-有氧呼吸】每个免疫细胞各拿全额、
不按细胞数均分），癌方**不随人数变**（【E-无氧呼吸】连通块供能按块内癌细胞数均分）。
人越多免疫越强，得在开局布局上补回来。实测斜率约 **7 个百分点/格**（18 格 30% →
20 格 45% → 21 格 53% → 22 格 59%），刻度够细，是个好用的分人数杠杆。

> 队友那份 Rust 报告的 v12 也是这么做的（4 人 13 格 / 6 人 32 格）——**方向一致、数不一样**。
> 两套实现还没对齐，见 [平衡方案_PRD版 §七](平衡方案_PRD版.md)。

### 7.5 明确**不改**的（已按此实装，免得被顺手带上）

- **无氧呼吸不加封顶。** 候选甲曾用「每细胞每世界回合封顶 3.5」，
  但在蒙特卡洛基准下加了反而把癌胜率砸到 10%。候选乙**不引入这个 PRD 里没有的概念**。
- **免疫方一个数都不动**：迁移费、有氧呼吸系数、攻击伤害、等级门槛全按 PRD。
- **胜利条件、回合上限、固化阈值、【E-增生】、【E-侵蚀】全不动。**
- 癌细胞在**自家癌性组织**上移动的 0.2（`CANCER_MOVE_CANCEROUS`）**不动** ——
  那不是占地，抬它只会惩罚机动。

### 7.8 🔴 细胞能量上限 15.0（PRD 里没有这个概念）＋ 无氧呼吸上限放到 999.0

| | 内容 |
|---|---|
| **PRD**（无氧呼吸） | 「上式算出的数值**不超过10点**」；全文没有「细胞能量上限」这一概念 |
| **引擎** | `ENERGY_CAP_PER_ROUND := 150`（15.0）：E 阶段结算末把**所有存活细胞**的能量削到 15.0；同时 `ANAEROBIC_CAP := 9990`（999.0，形同不封） |
| **为什么** | 封「每回合进账」挡不住**跨回合囤积**。2026-08-31 试玩：免疫把癌组织压到 3 格，黑色素瘤下一回合掏出攒了几轮的 25.5 能量一口气占了 63 格，3 → 73 越过 64 的胜利线。**盘面地盘差不是领先，对方账上的能量才是** —— 所以改成封存量 |
| **实测** | 启发式 120 局：4 人 68% → **64%**，6 人 64% → **56%**。拆开测「只放开无氧、不加能量上限」是 68%（纹丝不动）→ 起作用的是能量上限，放开无氧是免费的 |
| **PRD 要加的字** | ①【无氧呼吸】那句的「不超过10点」需删除或改数；② 需新增一条通则，大意：「每个世界回合结算结束时，所有细胞的能量不超过 15 点，超出部分舍去」。**措辞待团队定稿后回写** |
| **状态** | ⏳ Kevin 2026-08-31 定「暂时改成这样，后续再调整」。`ANAEROBIC_CAP` 常量与 `CWTuning.anaerobic_cap` 旋钮**保留不删**（Kevin 明确要求），随时可用 `balance_scan -- ecap=100` 扫回对照 |

⚠ 两条副作用要记住：**① 对「稳扎稳打推进」几乎没影响** —— 试玩里癌症方 3 回合速胜那条线用的是 10~12 能量，本来就在 15 以下；拦的是「攒几轮再一次兑现」。**② 放开无氧上限后大连通块单回合进账可超过 10.0**，但账面被 15.0 压住，净效果仍是削弱。

### 7.7 ~~🔴 【原发灶】：癌细胞出生格开局即为固化癌组织~~ —— ✅ 已取消（口径 #85）

| | 内容 |
|---|---|
| **PRD**（游戏开始 6．） | 「**所有癌组织固化计数初始为0**」—— 开局没有任何固化癌组织 |
| **引擎** | `CWData.SOLID_AT_CANCER_SPAWN := true`：每个癌症玩家的**出生格**开局即为固化癌组织（4 人局 2 格、6 人局 3 格） |
| 定案 | 团队 **2026-08-26**（61 格时代），`CWSetup._place_primary_lesions()` |

**为什么加它**：破解死循环 ——【复活】需要固化癌组织落脚，而固化需要癌细胞在原地停留
到计数满 3，可停留就会被免疫打死。开局给每个癌症玩家一格固化，等于给癌方**一次复活容错**。
注意复活会把固化格降级回癌组织，所以**每格只能救一次**。

**⚠ 这是引擎偏离 PRD 里最重的一处，而它此前完全没有记档。**
[平衡测试报告](平衡测试报告.md) 第一部分一节实测过：把原发灶单独关掉，
「规则原文」在 2 / 4 / 6 人局的癌方胜率**全部变成 0%**、平均 3 个世界回合就结束 ——
棋盘从 61 扩到 127 格带来的全部改善其实都来自它，不是来自棋盘。

**✅ 结果：Kevin 2026-08-31 定案「取消初始的固化癌组织」**，
`SOLID_AT_CANCER_SPAWN` 改为 `false`，引擎与 PRD 就此一致，**PRD 无需改动**。
机制保留成旋钮没有删（它是平衡实验里最重的一根杠杆），并补了测试保证
「旋钮开回来时照旧生效」——否则哪天想开回来会发现代码早烂了而测试全绿。详见口径 #85。

这条是 Kevin 实测时问「为什么一开局就固化了两个癌组织」暴露的，
**不是比对查出来的** —— 方法论上的教训记在 8.5。

### 7.6 2 人局：确认在平衡目标之外（Kevin 2026-08-31）

规则原文只定义 4 人和 6 人局，2 人局是电子版为测试加的（[说明 #5](规则电子化说明.md)）。
实测即便把初始癌组织砍到 8 格（PRD 的一半），蒙特卡洛下仍是 **67% 癌胜**——
它是**空间覆盖问题不是数值问题**（一个免疫细胞只能守一条线），调不平。

**Kevin 2026-08-31 明确：2 人局本来就在平衡目标外，不用管。**
PRD 无需改动（本来就没定义）。引擎里 2 人局沿用 PRD 的 15 格。
本条只是记档，免得以后又有人拿 2 人局的胜率来提问题。

---

### 7.8 【E-癌症胜利】要**连续两个世界回合末**达标（定案 B，2026-09-01）

| | 值 | 引擎位置 |
|---|---|---|
| PRD 正文 | ~~加权占地达到阈值即胜~~ → **已改**（2026-09-01 晚，见 diff）：≥90 且连续两个世界回合末 | —— |
| **引擎现值** | **连续 2 个世界回合末都达到阈值才判胜**；首次达标只记「★ 警报」，中途回落计数归零 | `CWData.CANCER_WIN_HOLD_ROUNDS = 2`、`CWGame.cancer_win_streak`、`check_cancer_win()` |

**为什么**：行动顺序末位是癌方，最后一个癌细胞可以一口气铺到线、紧接着就是 E 阶段判定，免疫零回应。
Kevin 2026-09-01 提出两案（A 行动顺序改 C-I-C-I ／ B 连续达标），启发式各 400 局同种子对照后**定 B**：
6 人 39%→33%、4 人 45%→42%，对局各 +1 回合；A 会让 6 人局癌方再掉 12 点且对局更短。
界面：警报期侧栏「癌性加权」标题换成「★ 警报 1/2」；规则速查文案跟旋钮 `cancer_win_hold_rounds`。

### 7.9 6 人局初始癌组织 21 → **24**（方案乙，Kevin 2026-09-01 晚定案）

| | 值 | 引擎位置 |
|---|---|---|
| PRD 原文 | 「4人局X=15，6人局X=21」 → **已改为 24**（见 diff） | —— |
| **引擎现值** | `INIT_CANCER_TILES = {2: 15, 4: 15, 6: 24}` | `cw_data.gd` |

**为什么**：两种对称 AI 都说 6 人局癌方比 4 人局再弱 6~14 个点；全局杠杆动 4 人的幅度是动 6 人的两倍，
只有这张按人数分档的表能单独补 6 人（启发式 +15 点）。口径 #87 曾否决这根杠杆，否决理由
（「真人打不过 MC 癌」）已被 2026-09-01 的交叉表与四局双边手打推翻。

### 7.10 固化计数阈值 3 → **2**（方案乙）

| | 值 | 引擎位置 |
|---|---|---|
| PRD 原文 | 「计数到达3时癌组织转为固化癌组织」 → **已改为 2**（见 diff） | —— |
| **引擎现值** | `SOLIDIFY_THRESHOLD = 20`（十分整数） | `cw_data.gd` |

**为什么**：3.0 下固化**从不出现**（蹲 3 回合 = 放弃约 30 格扩张；三局手打 36 次盘面快照全 0），
癌方因此没有复活据点、三个细胞一团灭就结束。2 回合让它偶尔值得蹲，又不像 1 回合那样满地据点。
⚠ 连带影响：世界事件【固化加速】的门槛 `SOLIDIFY_ACCEL_AT = 20`（2.0）现在与固化阈值重合，
这张事件只剩「即时转化 vs 等到 E 阶段」的时机差 —— **要不要同步降到 1.0，待团队定**。

### 7.11 癌症占地胜利门槛 85 → **90**（方案乙）

| | 值 | 引擎位置 |
|---|---|---|
| PRD 原文 | ⌈2/3 × 总格数⌉ = 85 → **已改为 90**（见 diff） | —— |
| **引擎现值** | `CANCER_WIN_WEIGHTED = 90` | `cw_data.gd` |

**为什么**：固化在加权占地里算 2 格，固化门槛降了、固化多了，门槛不配套抬就等于「更容易固化 = 更容易占地胜」。

**乙落地后的启发式基线**（400 局 / 档，seed=1，旋钮回显「默认值」）：6 人 **50% / 13.1 回合**、4 人 **52% / 14.4 回合**，
与《平衡方案对比》第三节乙那一行逐位一致。诚实 MC 下乙 ≈ 29% / 24%（专家水平仍免疫占优，见同文第五节）。

### 7.12 巨噬【吞噬】净化不回能 + X 级净化价 0.5 → **0.7**（定案 ①+②，Kevin 2026-09-03）

| | 值 | 引擎位置 |
|---|---|---|
| PRD 原文 | 【I-吞噬】每触发一次【净化】恢复 0.3 → **已改为不恢复**；X 级「【迁移】耗能均改为 0.5」→ **已改为癌性组织 0.7（与 III 级同）**（见 [diff](PRD改动_2026-09-03_定案①+②.diff)） | —— |
| **引擎现值** | `MACRO_HEAL_PURIFY = 0`、`IMMUNE_MOVE_CANCEROUS = [10, 10, 7, 7]` | `cw_data.gd`；balance_scan / play 的 `mheal=3 mvx=5` 可扫回 PRD 原值 |

**为什么**：标尺换成浅层 MC v2 互搏后，乙基线癌胜只有 6 人 15% / 4 人 23%（目标带 40~50）；手打 D 局暴露的雪球是 X 级巨噬净化净成本 0.2/格、一回合清 30 格。
两根杠杆单独都够不到 40（① 约 38/39、② 约 33/34），合起来 6 人 47 / 4 人 46 —— 见《平衡方案对比》§9.3～9.5。攻击成功后的回能（⌈受击方损失 ÷ 2⌉）不动。

**①+② 落地后的基线**（MC v2，168 局 / 档，旋钮回显「默认值」）：6 人 **45%**（76/168，Wilson 95% [38, 53]）· 9.9 回合、4 人 **46%**（77/168，[38, 53]）· 9.6 回合，两档都在 40~50 带内；胜法清场 / 占地各半（6 人 91 / 75，4 人 90 / 77）。片 0～6 与 09-02 网格的 ①+② 分片逐位相同（确定性复核）。启发式地板 6 人 85% / 4 人 62%（新手 PvE 档，见《平衡方案对比》§十）。

## 八、⚠ 2026-08-31 复核新查出的三处不一致 —— 全是【落后】

**本节方向和 〇 / §七 相反**：那两节是「引擎已改、PRD 待改字」，
本节是 **PRD 一直是这么写的、引擎没跟上**，也就是开头分类里的【落后】。

> 📌 **先说定性，免得误会**：Kevin 2026-08-31 同步的 PRD 与本文基准
> **是同一版**（字节数、行数、SHA-256 全同）。所以下面三条**不是 PRD 新改的**，
> 是 **2026-08-28「PRD 完整过了一遍」那次漏掉的** —— 当时的记录写着「25 条编号差异 +
> 7 条结构性 + 4 个歧义」，这三条不在其中。按开头的分类，【落后】**直接改、不需要讨论**；
> 但 8.1 和 8.2 都会动平衡，所以**改代码不用讨论，改完必须重跑平衡**。

**三条已全部按 Kevin 2026-08-31 的裁定实装**（8.1 / 8.2 = 口径 #84，8.3 = 口径 #86）。
（比对方法与已核对无误的部分见 8.4。）

### 8.1 攻击失败时免疫细胞 −0.5 能量 —— ✅ 已实装（Kevin 2026-08-31 定「改」）

| | 文字 |
|---|---|
| 旧规则（61 格版，已作废） | 1/3概率失败，不造成伤害，免疫细胞移动后被癌细胞反弹，弹回原格 |
| **PRD（现行，一直如此）** | 1/3概率失败，不造成伤害，**自身−0.5能量**，免疫细胞移动后被癌细胞反弹，弹回原格 |
| 引擎（改前） | `CWTuning.counter_dmg_on_fail := 0` —— 不扣 |
| **引擎（现值）** | `CWData.COUNTER_DMG_ON_FAIL := 5` —— **已按 PRD 实装**，口径 #84 |

**为什么会漏**：这半句是 61 格旧规则升级到 PRD 时**加进来**的，而引擎那个旋钮是
61 格时代写的，注释还停在旧规则上 ——「规则原文为 0：癌方没有任何伤害手段 →
免疫细胞不可能死亡（见说明 #23）」。**这句现在是假话**，而且正因为它写得斩钉截铁，
08-28 那次比对到这里就跳过去了。口径 #23 已被两处推翻：【E-微环境压迫】早就让免疫会死，
再加上这一条。

影响不小：它给癌方第二条稳定的伤害途径，而且和攻击频率直接挂钩，
**攻击越积极的免疫打法被罚得越重**。改动本身只要把旋钮默认值从 0 改成 5，
但**改完必须重跑平衡**（刚定案的候选乙是在「攻击不自损」的前提下调出来的）。

### 8.2 6 人局行动顺序 —— ✅ 已实装（Kevin 2026-08-31 定「改」）

| | 顺序 |
|---|---|
| 旧规则（61 格版，已作废） | 免疫A - 癌A - **癌B - 免疫B** - 免疫C - 癌C |
| **PRD（现行，一直如此）** | 免疫A - 癌A - **免疫B - 癌B** - 免疫C - 癌C（严格交替） |
| 引擎（改前） | 旧顺序 |
| **引擎（现值）** | `FACTION_ORDER[6] = [免, 癌, 免, 癌, 免, 癌]` —— **已按 PRD 实装**，口径 #84 |

4 人局两版一致（免A-癌A-免B-癌B），**只有 6 人局不同** —— 所以只看 4 人局是发现不了的。

改动只有一行，但**不是小事**：本周实测过席序影响胜率（同为 4v2 的两种席序差 4 个百分点），
改完要重跑 6 人局平衡 —— 而 6 人局初始癌组织 21 格正是刚按旧席序标定的。

### 8.3 E 阶段「其他 E 类效果」的位置 —— ✅ 已实装（Kevin 2026-08-31 定「按 PRD 的顺序改」）

PRD E 阶段第 7 步「结算其他尚未结算的 E 类技能、状态和效果」，第 8 步「更新持续时间类状态」，
第 9 步「移除所有癌组织的新生状态」。引擎把 `world_fx.round_end()`
（【紊乱】返回原位 + 事件倒计时）放在 `_clear_newborn()` **之后**：

```text
引擎：压迫 → 增生 → 侵蚀 → 无氧 → 固化 → 衰减 → 坏死倒计时 → 移除新生 → 紊乱返回/事件到期 → 胜负
PRD ：压迫 → 增生 → 侵蚀 → 无氧 → 固化 → 衰减 → 其他E类(7) → 更新持续(8) → 移除新生(9) → 胜负(10)
```

**具体差别**：【紊乱】返回原位时，若原位已变成健康组织，癌细胞会触发【定殖】造出新癌组织。
按引擎的顺序这些格子是在「移除新生」**之后**造出来的，会**多带一个世界回合的「新生」**，
于是下个世界回合也固化不了；按 PRD 的顺序它们当回合就该被清掉「新生」。

只在【紊乱】这一个世界事件上体现，但确实是行为差异。

**✅ 已按 PRD 改**（口径 #86）：`round_end()` 拆成 `round_effects()`（第 7 步）与
`tick_durations()`（第 8 步），`e_phase()` 每一行都标上 PRD 的步骤号。
判据用**不变量**：跑完 `e_phase()` 盘面上不该剩任何「新生」标记 ——
已验证把顺序改回旧的这条会红。

### 8.4 已逐条核对、结论是**一致**的部分

列出来免得下次重复劳动：

- **66 张卡的身份表**（卡名 / 类别 / 各池权重）：用 `tools/gen_card_data.py` 拿新 PRD
  重新生成一份，与仓库里的 `cw_card_data.gd` **逐行相同** —— 卡池没有增删改。
  （注意这只覆盖「抽到的是哪张卡」，**不覆盖卡牌效果文字**，见下面的说明。）
- **18 个世界事件**名单一致。
- 世界事件触发回合 `[3,6,10,15,20,25,30]`；棋盘 127 格；三条胜利条件
  （免疫全灭且无可复活固化格 / 加权 ≥64 / 30 回合后癌性 ≥42）；S 阶段顺序；
  E 阶段除 8.3 那一处外的顺序。
- 特殊组织：代谢核心上限 2.0、健康每 2 回合 +1.0、癌性每回合 +0.4；
  骨髓上限 1 张、健康每 3 回合、癌性每 2 回合。
- 【E-微环境压迫】(相邻癌性−2)×0.5 且 ≤2 格不损失；【E-增生】相邻癌性数×4%；
  【E-侵蚀】2/3 一格、1/3 两格。
- 【黏液破裂】至少 2.0 能量 / 半径 2 / 最多 8 格 / 范围内免疫损失 2.0；
  【瓦伯格超速糖酵解】110% 向上取整；【骨样硬化】+1.5；固化阈值 3.0、衰减 −0.5。
- 【抗体】费 1.0 / 伤 1.0 / 每世界回合 2 次；【细胞毒素】费 1.0 / 每世界回合 3 次 /
  坏死 2 轮；巨噬【吞噬】净化回 0.3（**2026-09-03 定案①后不回能**，见 7.12）。
- 免疫等级门槛 0 / 6 / 16 / 31，III 级迁移到癌性 0.7、X 级 0.5（**2026-09-03 定案②后 X 级也是 0.7**，见 7.12）；
  抽卡费用免疫 0.5 / 癌症 1.0、每行动回合 3 次；复活能量免疫 1.0 / 癌症 2.0。
- 攻击骰面 1/3 失败、1/2 成功、1/6 大成功（**失败自损那半句除外，见 8.1**）。
- 抽卡合法性：手上已有的同名技能与已装备的同名永久技能移出候选。
- 「非自毁型技能的费用不能使自身能量降至 0」= `CWCost.DEFAULT_PAYMENT_FLOOR`。

> ⚠ **覆盖不到的地方，要说清楚**：**66 张卡与 18 个事件的效果正文**（约 1100 行）
> 这次只做了抽样核对（〇 节涉及的那几张 + 费用相关的六张 + 【免疫抑制因子】），
> **没有逐字通读**。身份表逐行一致只证明「抽到的是哪张卡」没错，**不证明效果文字没错**。
> 8.1 那种「PRD 有半句、引擎没实现」的漏，在效果正文里完全可能还有第二处。
> 要彻底清干净，得把 1100 行效果正文对着 `cw_card_fx.gd` / `cw_world_fx.gd` 逐条过一遍 ——
> 那是独立的一件活，还没做。

### 8.5 这次比对本身的教训

三条全是【落后】，而 08-28 那次是宣称「PRD 完整过了一遍（1462 行）」的全量比对。
为什么会漏，三条各不相同，值得记一笔：

- **8.1**：引擎注释斩钉截铁地写着「规则原文为 0」，比对时信了注释、没回去查 PRD 原文。
  **注释不是证据** —— 这已经是本周第三次被同一类问题咬到。
- **8.2**：4 人局对得上，6 人局才不同；只抽查一个人数就会全绿。
- **8.3**：顺序差异藏在「其他 E 类效果」这种概括性表述里，
  引擎那一行叫 `world_fx.round_end()`，名字看不出它对应 PRD 的第几步。

共同点：**三条都不是「读漏了一句」，而是「读了但对错了地方」**。
下次做全量比对，值钱的做法是**按 PRD 的编号逐条落到引擎的具体函数/常量上**，
而不是通读一遍打勾。

**还有第四处，而且是 Kevin 实测时问出来的，不是比对查出来的**：
7.7 的【原发灶】—— 引擎开局把癌细胞出生格设成固化癌组织，而 PRD 明写
「所有癌组织固化计数初始为 0」。它 2026-08-26 就实装了，本文档里一个字都没有。

**为什么整套比对都查不出它**：我这次（以及 08-28 那次）做的是**单向**比对 ——
拿 PRD 的每一条去问「引擎做到了吗」。这只能查出「PRD 有、引擎没有」（也就是 8.1~8.3 那类），
**查不出「引擎有、PRD 没有」**。【原发灶】正是后者，它在 PRD 里根本没有对应条目可查。

✅ **反向比对已于同日补做**，并做成了常驻工具
[`tools/prd_crosscheck.py`](../tools/prd_crosscheck.py)：把 `cw_data.gd` 的每个常量
对到 PRD 应当出现的那句原文上，去 PRD 里确认它真的存在。
**94 个常量：88 个对上、0 个对不上、5 个有意不映射（都写了理由）、1 个没出处。**
那 1 个就是下面的 8.6。加了新常量要补一条 MAP —— 工具**故意不给默认兜底**，
没映射的会被单独列出来要求人工判断，这正是 `HAND_MAX` 暴露出来的路径。

### 8.6 顺带确认：〇 节那几条待改项，PRD 确实都还没改

〇.1（六张卡「使用时机：任意时间」）、〇.2（【缺氧适应】效果重写）、
〇.5（【囊性护甲】效果重写），以及口径 #79 的 ⏳（【组织驻留】【组织巡航】卡面
「每个行动回合**第一次**」→「**一次**」）—— 在新版 PRD 里全部还是旧文字，
**待改状态正确**。

〇.3 与 〇.4 有变化，已就地标注：〇.3 的**草稿作废需重写**（要按 #79 的语义阶段），
〇.4 **整条撤回**（随定案 A 一起作废）。

### 8.6 ~~🔴 `HAND_MAX = 8`：引擎有手牌上限，**PRD 没有**~~ —— ✅ 已写进 PRD（口径 #90）

| | 内容 |
|---|---|
| **PRD** | 「卡牌规则」四条只写了「同名【技能】在一玩家手牌中最多保留1张」「手牌可随时弃置」「死亡不会弃置手牌」——**没有任何手牌总数上限** |
| **引擎** | `CWData.HAND_MAX := 8`，`CWActions._can_draw()` 里强制执行：手牌满 8 张就抽不了卡 |

**这是本次反向核对唯一抓到的一条，也是【原发灶】的同类** ——
引擎多做了一步，而单向比对（拿 PRD 逐条问引擎）永远查不出来。

**✅ Kevin 2026-08-31 定案「保持 8，在 PRD 里加一条」**（口径 #90）。
PRD「卡牌规则」新增第 5 条，按引擎的**实际**行为落字（不止上限那一个数）：

> 5. 手牌上限8张。手牌已满时无法发动【基因表达】；【骨髓】中存储的卡牌保留在原处，
>    下次经过时再获取（【事件】不进入手牌，不受此限）

其中「骨髓的卡留在原处、不浪费」是团队 2026-08-28 定的行为，**此前也没进过 PRD** ——
顺着这条一起补上了。写完再跑 `tools/prd_crosscheck.py`：**94 个常量全部对上，0 个没出处。**

⚠ **与队友的 Rust 版不一致**：他们 v12 用的是 **6 张**（还配了渐进抽卡费）。
对表时要拿这条出来说——抽卡节奏是他们反复调过的一环
（v10 把抽卡改排最后、v12 加上限与渐进费，4 人局胜率从 74.5% 走到 51.0%）。

### 8.7 修正一处我自己写进 PRD 的错（〇.3 那节通则）

2026-08-31 补进 PRD 的「数值修正的结算顺序」，原本第 8 阶段写的是
「**行动下限**：非自毁型技能的费用不能使自身能量降至0，在此夹钳」。**这句话把两件事混了**：

| 引擎 | 是什么 | 现状 |
|---|---|---|
| `hard_floor` | 行动级最低消耗的**夹钳**（真正的第 8 阶段） | **目前没有任何行动用它**，是个空钩子 |
| `payment_floor` | `energy − cost ≥ 0.1` 的**可负担性判定** | 对应 PRD「费用不能使自身能量降至0」 |

可负担性判定**不参与数值结算**——付不起是不能发动，而不是把费用压低。
而且给一个没人用的空钩子写规则本身就不对。已改：删掉那一阶段，
改在阶段表之后单写一句，并点明它是可负担性判定、不参与结算。

**教训**：新写规范性条文时，「引擎里有这个字段」不等于「这是一条规则」——
空钩子不该进 PRD。

---

## 九、卡牌与世界事件**效果正文**的全量核对（2026-08-31）

§八 收尾时列过一处没覆盖的：66 张卡 + 18 个世界事件的效果正文里
**非数值的行为描述**（PRD 约 1144 行 / 引擎 1301 行）。Kevin 要求「全篇进行检查」，本节是结果。

### 9.1 怎么查的

逐条生成「PRD 效果原文 + 引擎里所有提到这张卡的代码段（带上下文）」的卷宗，
84 条一条一条读。每条查**三件事**，而不是一件：

1. 引擎做的，PRD 说了吗？
2. PRD 没说的地方，有没有记过口径？
3. 那条口径**还有效吗**？（当天就作废了 #23、〇.4，改写了 #73/#79）

第三问是关键——本节 5 条发现里有 2 条正是「注释引用了已作废的口径」。

**79 / 84 完全对得上**，包括全部分期数值、范围与几何条件、目标筛选
（随机 / 无细胞占据 / 普通癌组织排除固化）、时序（立即 / 随后 / 攻击**发动** vs 命中）、
分化分支、时长闸门。

### 9.2 A 类：真的规则不一致 —— ✅ 已按裁定改 PRD 措辞（口径 #91）

两条都落在**同日刚加的收入护栏**（口径 #89）上，而且都是**我写 PRD 时措辞过强**：
黄煦然说的是「免疫下限 2、癌细胞上限 10」，我写成了绝对的**每回合**上下限，
而引擎夹的是**公式算出的那一步**。

| | 引擎实际 | 原 PRD 措辞的问题 |
|---|---|---|
| **A1 有氧下限** | 先夹到 ≥2.0，**之后**才乘【TGF-β释放】的 ×80%（每层） | 「每回合不低于2点」——TGF-β 在场时到手 1.6，下限形同虚设 |
| **A2 无氧上限** | ① 每**次结算**各自夹钳，【糖酵解爆发】另起一次 → 一回合可拿 20<br>② 【瓦伯格】×110% 在夹钳之后 → 单次 11.0<br>③ 【GLUT1高表达】的加成也在之后 → 单次 12.0 | 「每回合不超过10点」——三处都绕开了 |

**Kevin 裁定：上限下限管的是每次结算，引擎不动，改 PRD 措辞。**
PRD 两句已改成「**上式算出的数值**不低于/不超过…」，并写明哪些效果排在其后、
以及「同一世界回合内多次结算时每次各自受限」。

### 9.3 B 类：注释引用了已作废的口径 —— ✅ 已修（行为本来就是对的）

| | 问题 |
|---|---|
| **B1** `cw_card_fx.gd`【炎症趋化】 | 注释说「卡面已由『降为』改成『改为』（定案 A / 口径 #74）…会把压到 0.5 以下的价钱抬回 0.5」。**定案 A 已随 #79 作废**、〇.4 已撤回（PRD 至今写的仍是「降为」），而且 REPLACE（③）排在 FLAT_CUT（⑤）**之前**，根本抬不了价 |
| **B2** `cw_cost.gd`【组织驻留】 | 注释说「比【组织巡航】窄 → **免费竞争时优先**（设计 §九）」，与**同一文件**的 `_free_order` 注释「spec 只留作备查、**不参与排序**，按打出先后」直接矛盾 |

两处都改成了「说清楚**现在**是怎么排的，并点名旧说法已作废」，而不是简单删掉——
删掉的话下一个人还会重新推理一遍。

### 9.4 C 类：未记录的解释 —— ✅ 已补口径 #91

**【基质阻隔】**：PRD 只写「**移动**能量花费翻倍」，没说技能移动算不算；
引擎把小细胞肺癌【转移】与黑色素瘤【早期血行转移】（`Action.CELL_SKILL`）**也翻倍**。
读法合理，但此前**无处可查**——和【原发灶】、`HAND_MAX` 同一类
（引擎多做一步且没记档）。已补进口径并在代码里点名「这是引擎比 PRD 多做的一步」。

### 9.5 顺手修了核对工具的一个致命毛病

`tools/prd_crosscheck.py` 打不符行时用了 `✗`，而 Windows 控制台是 GBK ——
**工具恰好在查到问题时崩掉**（全对时不打这些行，反而看不出来）。
标记改成 ASCII，并给 stdout 兜了一层 `errors="replace"`。
已用「故意注入一处不符」验证过：干净报错、退出码 1、stderr 为空。

### 9.6 至此的覆盖状态

- **数值层**：94 个常量全部对上，有 `prd_crosscheck.py` 常驻守着（§八）。
- **行为层**：84 条效果正文逐条核对完毕，5 条发现全部处理（本节）。
- **⚠ 仍然没有自动化守护**：行为层是人读出来的，没有工具能守。
  PRD 卡面文字若再改动，得重读对应那几条——这也是待办清单里「卡面显示效果」那条
  要求**从 PRD 生成而不是手抄**的原因。