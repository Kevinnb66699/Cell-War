# 内核替换：拍板记录（2026-09-15）

**目标（Kevin 原话）**：把队友写的优化好的 C# 核心引擎**替换掉**现在的核心引擎，
规则等都以**本地最新的正本**为准。

这不是「融合」，是**替换**。GDScript 内核（`game/scripts/core/`，9491 行）最终退役。
路线仍是 A（内核在进程外），但落点比 09-15 早些时候那版更彻底。

---

## 一、十条拍板

| # | 问题 | 拍板 | 代价（Kevin 已知悉） |
|---|---|---|---|
| 1 | 单机要不要能离线玩 | **B：桌面本地 sidecar，网页版连服务器** | 两条分发路径 |
| 2 | 联机改不改服务端权威 | **改** | 老客户端全部作废 |
| 3 | 谁补 C# 的规则缺口 | **我们补**，代码推到**本仓库的新 branch** | 工作量是大头 |
| 4 | PRD 正本 | **以我们本地最新版为准**（不钉死哈希，会更新） | 要队友那边也认 |
| 5 | `UNDEFINED` 四项 | 见下方 §2 | — |
| 6 | 对拍做不做 | **要做，而且测试要迁移** | 18594 行测试要搬 |
| 7 | AI 怎么办 | **重写成 C#**，后续接神经网络模型 | C# 侧现在零 AI |
| 8 | 迁移期线上怎么办 | main 继续维护现版，C# 迁移走独立分支 | 两版并行维护 |
| 9 | UI 的规则数值 | 内核把「预计收入」一起算好发过来，UI 不留规则数值 | 协议要加字段 |
| 10 | 教程 | 放最后；C# 要提供「直接灌一个盘面」的入口 | — |

**决策 1 的理由（Kevin 原话）**：「网页版单机要连服务器不是问题，因为没有网根本进不去网页」。

---

## 二、`UNDEFINED` 四项的裁决

两份 PRD 都没写明、必须拍板的四项，Kevin 09-15 裁决：

1. **2 人局全部数值** → **暂不在考虑范围内**。
   ⇒ 不实现、不测、对拍里标 `OUT_OF_SCOPE` 而不是 `UNDEFINED`。
2. **负数半值的舍入方向** → **参考 GDScript 当前实现**。
   ⇒ 即 `CWData.round_tenth(num, den) = (num + den/2) / den`（**纯整数运算**）。

   **实测（Godot 4.5，别凭印象）**：GDScript 的整数除法对负数**向零截断**
   （`-7 / 2 = -3`，不是 -4），于是这个「四舍五入」公式在负数上**系统性偏向零**：

   | 调用 | 实际 | 数学四舍五入 | |
   |---|---|---|---|
   | `round_tenth(-24, 10)` | **-1** | -2 | ❌ 差 1 |
   | `round_tenth(-26, 10)` | **-2** | -3 | ❌ 差 1 |
   | `round_tenth(-55, 100)` | **0** | -1 | ❌ 差 1 |
   | `round_tenth(25, 10)` | 3 | 3 | ✅ 正数没问题 |

   **但这条目前是空的**：四个调用点（`cw_cost.gd:515`、`cw_world.gd:443/567/1120`）
   实参**全都非负**，其中 `cw_world.gd:1120` 还显式写了 `maxi(raw, 0)`。
   ⇒ 所以它是一颗**没被踩响的雷**，不是现行差异。迁移时照抄公式即可；
   真要改成数学上更对的版本，单独立项 —— 别混在迁移里，否则分不清哪次漂是谁造成的。

   ⚠ **C# 侧和我们不一样，而且差两处**（`Settlement.cs:83`）：
   ```csharp
   public static int RoundTenth(double valueInTenths)
       => (int)Math.Round(valueInTenths, MidpointRounding.AwayFromZero);
   ```
   * **(一) 负数方向不同**：`AwayFromZero` 对 -2.4 给 -2，我们给 -1。（同为空雷，但雷不一样。）
   * **(二) 它收 `double`。** C# 侧能量是**浮点流过管线、末尾才舍入**
     （`RulePolicies.cs:22` `Round(double energyUnits)`、`:274` `income * 0.5`、
     `BoardRules.cs:85` `Math.Max(0, pressure) * 10.0 / 4`）。

   > 🔴 **更正（2026-09-15 晚）**：这里原来写「我们是全程整数、一个浮点都没有」——
   > **那是错的，我没查就写了**。实测内核**早就在用 `pow()`**（`cw_world.gd:805`）：
   > ```gdscript
   > var exp_term := pow(float(plain), exp_pct / 100.0) if plain > 0 else 0.0
   > ```
   > `_anaerobic_pool()` 返回 `float`，`_split_share(pool: float, count)` 里
   > `int(round(scaled / float(count)))` 落回十分位。
   > **也就是说【无氧呼吸】这条路上，两边是同一个形状**（浮点算、末尾 round）。
   >
   > **注意这个更正框本身也不够准**（09-15 深夜第三次修）：我们不止这一条路是浮点（共四座岛），
   > C# 的能量状态也是**整数**十分位、只有五个算式点用 `double`。
   > **以 §三·新三那张表为准**，那是逐行扫出来的。
3. **骰面值域** → **6 面骰子**（1..6）。
   ⇒ 这一条直接判定了 C# 的现成 bug：`CellRules.cs:194/205` 的 `rng.NextInt(6)` 产出 **0..5**，
   而 `RulePolicies.cs:326` 判 `roll == 6` 为暴击 ⇒ 实测 60000 次 **crit 0%**（应 16.7%）。
   **按本条裁决，这是 C# 的 bug，照修。**
4. **11 个特殊格坐标** → **按当前实现**。
   ⇒ 已逐个核对：`cw_data.gd:517-522` ↔ `MatchSetup.cs:15-20` **两边一致**，这条不用动。

---

## 三、两个新问题（由上面的拍板引出，之前没问过）

### 新一：桌面端怎么带 .NET 运行时？

决策 1 选了本地 sidecar，于是桌面客户端必须能跑 .NET。两条路：

* **(a) 自带（self-contained 发布）** ← **推荐**。玩家零安装，双击就能玩。
  代价：客户端体积 **+60~70 MB**（.NET 运行时本体）。
* (b) 要求玩家自行安装 .NET 运行时。体积省了，换来一道安装门槛和一类新的支持问题
  （「我装了怎么还是打不开」）。

推荐 (a)。这个游戏的玩家不是开发者，安装门槛的代价远大于几十 MB。

> ⚠ 注意 (a) 与**增量更新**的张力：self-contained 会把运行时和宿主打成一坨。
> 但**规则 dll（`CellWar.Core.dll`）仍然是独立文件**，所以「换规则」照样只发那一个 dll。
> 只有「换宿主 / 换运行时」才要全量发版 —— 那本来就该全量发。

### 新二：服务端权威之后，服务器要真的跑对局

> 🔴 **更正（2026-09-15 晚）**：这一节原来的第一句是
> 「现在服务器只**转发消息**（客户端各自算）」—— **错的，而且错得不轻。**
>
> 实测：`cw_room.gd:366-388` 服务器上真的 `CWGame.new()` → `init()` → `await game.run_game()`；
> `cw_net_bridge.gd:20-33` 按席位分流询问，AI 席也跑在服务器上；
> 而 `cw_net_client.gd` 的文件头明写影子对局「**没有桥、也永远不 run_game**」——
> 它只 `restore` 服务器发来的视角快照。
>
> **联机今天已经是服务端权威。** 是「影子对局」这个词把我带偏了：它是**只读镜像**，
> 不是并行模拟。
>
> ⇒ **决策 2 的代价大幅下降**：不是「新建服务端权威」，而是
> **「换掉服务器上那个权威内核」**。而且今天的联机路径**就是目标形态的现成原型** ——
> 六个消费者最终都要收敛到它那个形状（UI 提交 + 拉观测 + 只读镜像）。
> 本地单机那条路反而是特例：它今天是「引擎 await 界面桥」，要做一次**控制反转**。

改内核之后服务器仍然要跑真实对局，负载性质不变（本来就在跑）。

要定的是「先按多少并发设计」。**推荐：先不设计，按单机一进程做，测出来再说。**
理由：C# 内核是纯函数 + 结构共享，单局开销极低；现在连真实并发数都不知道，
提前设计容量是凭空想象。**但要在第一版就把「一局 = 一个可序列化的 checkpoint」做对**，
那是以后横向扩容的唯一前提。

---

### 新三：浮点允许出现在哪里？（本节 09-15 晚按实测重写过）

**先把事实摆正**（**这一节我改过两次，前两版都不准，以下是逐行扫出来的**）：

**两边都是「整数状态 + 局部浮点算式」**，不是两种数值模型 ——
C# 的能量同样是整数十分位（`WorldState.cs:240` `public required int Energy` 注释原文
「整数十分能量，如 3.0 = 30」）。

| | 浮点算式出现在哪（剥掉注释逐行扫） |
|---|---|
| **我们：四座岛** | ① 无氧 `cw_world.gd:805-806/822-823`（`pow` + `int(round)`）② 过载 `:471-472`（`pow` + `int(round)`）③ 瓦伯格 `:752` 与 `:1029`（`int(ceil(gain * WARBURG_PERCENT / 100.0))`）④ 吞噬吸血 `cw_damage.gd:558`（`int(ceil(actual / 2.0))`） |
| **C#：五个算式点** | `Settlement.RoundTenth(double)`、`RulePolicies.cs:22/260/274`、`BoardRules.cs:85/98` |

> 此前这张表写的「我们只有【无氧呼吸】这一条路」「C# 是整条管线都收 double」**两头都不准**，
> 而且 `cw_world.gd:749` 那个行号也错了（pow 在 `:805`）。
> 我连着写错两版，根因是同一个：**没有逐行扫就下了全称判断。**

所以不是「整数 vs 浮点」的二选一，而是**「浮点允许出现在哪里」的边界该划在哪**。

### 🔴 真正的浮点差别只有一处，而且是 blocking

**【E-增生】的概率判定**：我们是**纯整数千分位掷点**
（`cw_world.gd:708-720` 算出千分数，再拿 `randi_range(1,1000)` 比），
C# 是 `BoardRules.cs:97-98` 的 `rng.NextDouble() < adjacent.Length * rate`（`rate` 是 `double`）。

这既是跨平台确定性风险，**也是对拍 L1 的硬阻塞** ——
`docs/对拍规格_CWX.md` 已实测：不改这一行，2 人局跑 40 步就撞 **64 条** `RNG_NO_COUNTERPART`，
因为浮点抽取在我们的带子上**没有任何对应物**。E 阶段 100% 走它、每回合约 27 次。
⇒ **这条要排在所有对拍工作之前**。

**推荐：把边界划在「真幂运算」上 —— 只有幂允许浮点，其余全整数。**
理由按重要性：

1. **这正是我们现在的做法**，而且它已经被 3502 项检查盯了几个月。
   照搬一个在跑的口径，比发明一个新的安全。
2. **对拍成本**。整数对整数逐位比；浮点对浮点只能带容差比，
   而「容差内」会吞掉真实的 off-by-one 规则差异 —— 那正是对拍要抓的东西。
   把浮点收窄到两处，就只有那两处需要容差。
3. **神经网络训练要可复现**（决策 7）。浮点在不同 CPU / JIT 上有末位差异。
   收窄到两处之后，要不要再把这两处也查表化，可以**单独**评估，
   不必让整条管线陪着一起改。——**这一条是决策 7 引出来的，之前没人提过。**

**两处真幂运算**：【无氧呼吸】的 `^0.3`（两边都已实现）、
【S-过载】的 `^1.18`（2026-09-15 落进 GDScript，C# 侧待补）。

> 关于这两处要不要预计算查表：**先不做**。
> GDScript 侧 09-15 落地时就是直接 `pow()`，理由是**照抄已有先例**而不是新开一种口径 ——
> 内核里本来就有 `pow()`，为新规则单独发明查表反而会让同一个内核里出现两套数值口径。
> 真要查表，应该**两处一起、两边共用同一张表**（那张表本身就是最强的对拍锚点），
> 作为一个独立议题推进，不要夹在换内核里做。

---

## 四、这次拍板真正的体量（量出来的，不是估的）

| | 我们（GDScript） | 队友（C#） | 缺口 |
|---|---|---|---|
| 内核 | 9491 行 | 5137 行 | 七项状态维度整个不存在 |
| **测试** | **18594 行 / 209 个测试 / 3502 项检查** | **2715 行 / 21 文件 / 141 个测试全绿** | ← **全工程最大的单项** |
| AI | 1576 行（MC / MCTS / 启发式） | **0 行** | 全部重写，且要为神经网络留接口 |
| 联机 | 2241 行 / 7 文件（线上跑着） | `CellWar.Server` **94 行**（壳子） | 从零 |
| 网页版 | 线上 `cellwar.jiling.chat` | 无 | 客户端不变，改连权威服务器 |

**六个地方各自 `new` 一个内核**，换内核要六条路各自重接：
`match.gd`（对局）、`cw_net_client.gd`（联机客户端）、`cw_room.gd`（房间）、
`cw_replay.gd`（回放）、`monte_carlo_bridge.gd`（AI）、`guide_director.gd`（教程）。

---

## 五、已经确定要做的事（不用再问）

> **授权（Kevin 2026-09-15）：「关于阻塞项，在后续迁移过程中，你直接修掉吧」。**
> ⇒ 迁移期 C# 侧查实的 bug **直接改，不用逐个请示**。改动按决策 3 推到本仓库的新 branch。
> 边界照旧：**全量发版仍然要先问**。

**已查实、待修清单**（每条都亲眼看过代码，行号为当时的工作树）：

| # | 问题 | 位置 | 为什么要紧 |
|---|---|---|---|
| 1 | ✅ 已修（`MatchSession.MaskHand`，09-18 核实）**观测无条件泄漏所有人的手牌** | `MatchSession.cs:44` | `c.Hand` 对每个玩家的每个细胞都导出，`authorizedSeat` **只门控 `options`**。我们这边 `cw_net.gd:284-296` 的 `view_for` 是换成 `HIDDEN_CARD` 的。**不报错、不崩，只是牌全开** —— 服务端权威上线前必须修 |
| 2 | **暴击永远掷不出来** | `CellRules.cs:194/205` ↔ `RulePolicies.cs:326` | `NextInt(6)` 出 0..5，而判 `roll == 6`。实测 60000 次 **crit 0%**（应 16.7%）。§2.3 已裁决骰子是 6 面 ⇒ 这是 bug。**修完要补的是「骰面值域」断言，不是结果断言** —— 判定函数本身 16/16 全绿，是值域表才抓到它的 |
| 3 | **静默丢两个字段** | `WorldStateExtensions.cs:18` | `WithOccupyingCell` 手写初始化器漏了 `SolidLockRound` / `ToxinRound`。而「格上有细胞」正是这两个字段唯一起作用的场合 |
| 4 | ✅ 已修（Runtime 以注入实例为原型 Fork，09-15）**rng 注入在执行层无效** | `Runtime.cs:77-78/164` | 每步现场 `new Xoshiro256StarStar(1)`。对拍不阻塞（走纯函数面），但**权威内核必须能控制随机源**，AI 一进来它就从「不阻塞」变成阻塞（推演不能用真实 rng，否则 AI 提前看到自己的骰子 —— 我们 2026-09-01 修过同一个 bug） |
| 5 | ✅ 已修（改读 `RulePolicies.AerobicShare/AnaerobicShare`）**`IncomeFor` 自己抄了一遍规则** | `MatchSession.cs:60-71` | 免疫基数 `20/30/45/50` 与装备 `+5/+8` 是字面量、没走 `RulePolicies`，而癌症那一半走了 —— **同一个函数里两种做法**。这正是决策 9 要消灭的病，只是从 GDScript UI 搬进了 C# 观测层；而它恰好就是 Kevin 点名的那条「预计收入」 |
| 6 | **观测是 `double`** | `MatchSession.cs:29/44/56/70` | `/10.0` 到处都是，与 §三·新三的边界冲突，也让 UI 里 92 处 `CWData.fmt`（整数十分位 → 字符串）没法直接用 |
2. **补 C# 缺的规则**，以本地正本为准，含 09-15 新增的【S-过载】。
3. **迁移测试**：3502 项检查搬到 C# 侧。
4. **对拍**：按 `docs/对拍规格_CWX.md`，作为替换的验收闸。
5. **main 继续维护现版**，包括把【S-过载】落进 GDScript 内核 —— 迁移期线上跑的还是它。

---

## 六、还没定、但不阻塞开工的

* 分支名（建议 `csharp-core`）
* C# 代码在本仓库里的落点（建议 `core/` 与 `game/` 平级，不要塞进 `game/`
  —— 那是 Godot 的项目目录，混进 `.cs` 会被编辑器当成 C# 项目）
* 回放旧档要不要保留（换内核后老回放必然失效；建议不保留，`CWReplay.VERSION` 直接跳号）

---

## 七、2026-09-15 深夜：压测推翻了我给 Kevin 的四条

七路调研之外又跑了一次专项压测（四路取证 + 三路证伪）。以下四条是**我自己说错的**，
以压测实测为准。

### ① 「C# 的 `MatchSetup.BoardRadius` 是 const 6，要新开任意棋盘入口」——**错**

`MatchSession(WorldState initial, ulong seed = 12345)` 是 **public 构造函数**，收任意 `WorldState`；
`MatchSetup.Create` 只是 `Start()` 的便利包装。`BoardRadius` 全仓只有 3 个引用、全在
`MatchSetup.cs` 内部，其余一律读实例字段 `s.Board.Radius`。
⇒ **决策 10 要的「直接灌一个盘面」入口已经存在，不用开。**

### ② 教程的结论对，但**理由要换**，而且方向比我说的更强

我给的理由是「教程留 GDScript 会让内核永远删不掉」。压测判定：**作为技术判断不成立** ——
逐条对照 `guide_director.gd` 那 78 行，C# 只差三样（关胜负判定、观测补 `PlayCounter`、
一个对活对局的受控写入入口），前两样是小时级。

**真正的理由是新 PRD 第一关 GDScript 根本做不出来**：它要「两个横向连接的健康组织」= 两格棋盘。
* **C# 零改动就能跑**（实测：两格 `WorldState` 构造成功、`(0,0)` 在盘邻居数 = 1、
  `QuoteMove` 报 0.5、`MatchSession` 起步给出含 `Move` 的 4 个合法选项）——
  因为 C# 的棋盘形状**就是 `Board.Tissues` 这个字典**。
* **GDScript 做不到**：邻接由**标量半径**算出（`cw_game.gd:34/39`），与 `game.tiles` 解耦；
  `build_board(radius)` 只能整圈铺，最小 radius 1 = **7 格**，没有「两格」这个概念。

而**真正的风险也不是「删不掉」，是「教错规则」**：教程挂在一个正在退役、
且每天还在改（≈310 行/天）的内核上，它会系统性地教过时规则，**而 3502 项检查一条都不会红**。

### ③ 「新 PRD 碰规则的只有三类」——**漏了两类**

补全为五类：① 灌盘面 ② **step 级增量状态改写**（PRD 每个 step 都有「细胞种类变化、
细胞传送到某个格子」，是对**已开局的活对局**逐步改写 —— 今天 GDScript 只有**关级**装配，
零 step 级机制）③ 规则覆盖（「本回合迁移不消耗能量」—— 今天 **GDScript 零实现**，
C# 有 `ModifierStage.Free`，实测能把 `QuoteMove` 从 5 变 0）④ 重置到本关初始状态
（`Save()`/`Restore()` 实测精确复位，连待答请求和选项表都一起回来）
⑤ **关掉胜负判定（PRD 漏写，但必需）**。

**⑤ 是唯一的 blocking**：实测两格盘 + 无存活癌细胞，seat0 提交 `EndTurn` 当场
`Winner=Immune`。GDScript 早就修好了（`cw_game.gd:96` 的 `win_checks`，
注释里写着 Kevin 2026-09-12 截图报的就是这个），C# 侧 grep 全 0 命中。

### ④ 「AI 不进热更 = 平衡迭代速度下降」——**错**

`balance_scan.gd` 在 `game/tests/` 下，导出与补丁**两边都排除它** ——
平衡测量永远在仓库源码树里跑，一辈子不经过热更通道。
正确理由见 `docs/内核替换_迁移计划.md` §六 第 6 条的更正框（「不做例外」）。

### 顺带修掉一条与换内核无关、但今天就是错的

`publish_release.sh` 的 Release notes 与 `docs/联机操作说明.md` 都写着 macOS 包
**「已签名」**。实测：仓库里一行 `codesign`/`notarytool` 都没有，导出预设的
`identity` 与 `apple_team_id` **都是空** —— 跑的是 Godot 内建的**临时签名（ad-hoc）**，
不是 Developer ID、没有公证。玩家照样被 Gatekeeper 拦，而文档写的是「解压后双击」。
已改成真实的右键打开步骤。

### 一条新的、要 Kevin 拍的

**mac 还要不要支持 Intel（x86_64）？** 实测 self-contained 的 170 个托管 dll 里
**90 个两边字节不同**（框架程序集按架构 R2R 预编译），**没有「lipo 成 universal」这条路**。
⇒ mac zip 体积：现 **62.2 MiB** → 仅 arm64 ≈ **91.3 MiB** → 双架构 ≈ **122.0 MiB**。
现在的导出预设是 `universal` + x86_64 低保 10.12。
**「带 sidecar」和「继续支持 Intel Mac」是两笔钱。**

---

## 八、2026-09-19：口径二批 1 与测试迁移的十条 + mac Intel（Kevin 一次拍完）

| # | 事项 | 拍板 |
|---|---|---|
| 批 1 E-1 | 联机四条 query | **(a)** 新增 `query` / `query_result` RPC + 按 rev 缓存 |
| 批 1 E-2 | CWUIBridge 拆不拆 AI 继承 | **(a)** 不拆，`attach_engine` |
| 批 1 E-3 | 老档 / 老回放作废怎么交代 | **直接把所有存档清盘，不用加提示 —— 存档功能本来就没有启用** |
| 批 1 E-4 | 本地观测节拍 | **认下来**（问人之前一份、终局之前一份） |
| 迁移 E-1 | delta 载体 | 按推荐：envelope 子集 |
| 迁移 E-2 | mods 几元组 | 按推荐：四元组 + 前奏重放 |
| 迁移 E-3 | 18 个 C# 没有的旋钮 + 批 2 排期 | 按推荐：补 6 / 判死 5 / 待定 7；批 2 推迟，顺序 0→1→3→4→5a→2→5b |
| 迁移 E-4 | 17 个 private→internal + Produce / ContinueStart 拆步 | **不算动骨架** |
| 迁移 E-5 | 记忆 / 等级阵营级读法 | 好（加在 C#） |
| 迁移 E-6 | 签名不同的探针 | 好（GD 边界权威 / OUT_OF_SCOPE / 不写转换层） |
| §七 | mac 还要不要支持 Intel | **不用继续支持 Intel** |

原文：「1、（a） 2、（a） 3、直接把所有存档清盘，不用加提示，这个功能本来就没有启用 4、认下来 5-7、按你推荐的来 8、不算动骨架 9、好 10、好　mac不用继续支持Intel」。

**同日复议**：队友对批 1 E-4 提出疑问，讨论后 Kevin 拍：「第 4 条决定不变，保留 (a)」。顺带查了联机攻击动画偶尔不完整的报告：不是刷新时机（服务器每次问人前都推 state、AI 席也推；一份 state 3.3 KB、`shadow.restore` 0.25 ms），唯一的截断规则是 `attack_fx.gd:play` 用新一段顶掉同一细胞的旧一段（间隔 < 0.66 s），联机比本地容易撞是因为服务器 AI 没有本地那 220 ms 的每步停顿；「只有 Kevin 机器上看不到」最可能是局域网房主进程内跑服务器、专家档 AI 决策（381～645 ms）堵住渲染循环。两条修法与「按行动打包演出（step_begin / step_end 边界条目）」都等 Kevin 拍。

**同日拍板：演出播放形态选 2。** 问题：「除骰子阻塞外其余演出都不阻塞、同步进行」这套好不好。答：引擎 / 服务器不等演出是对的；客户端照搬「不等」不好（同细胞演出互相顶掉、盘面先于演出落地逼出 `owns()` 一类让位特判、AI 整批到达只靠骰子分隔、回放倍速没有统一把手）。**Kevin 拍：选 2 —— 客户端按条目顺序播、有时长的演出播完再放下一条、一步的 sync 在演出播完后落地、条目流加 `step_begin` / `step_end` 行动边界、队列给「积压快进」把手。** 落在批 1 步 4 与协议 p=2（规格 B-4 已扩）。攻击动画被顶掉的问题随之根治，不单独打补丁。另：AI 思考单开进程 —— Kevin 提了，我建议不单做（批 2 AI 进 C# + 批 3 sidecar 就是这个形态），等他回话；局域网房主看不到攻击动画的那一局**没有 AI**，堵帧假说作废，待他点头后加一行诊断日志复现。

**同日续**：Kevin 拍 ① 诊断补丁要打 —— 已打（build 202609180001，只加 `[fx-diag]` print，从上次发版的提交 a2fa59a 另起分支 `diag-fx-0919` 打的，批 1 的改动一行没进去）；② AI 思考单开进程等批 2 / 批 3 一起做；③ 演出的**播放时长与阻塞时长分开**（消费者只等阻塞那一段，长动画不堵队列）—— 已写进批 1 规格 B-4 与播放队列文件头。

**同日：批 1 步 6+8 合并落地时评委抓出的 7 处跨文件矛盾，我按下面口径处置（都是实现层的取舍，不改任何已拍的决定）**：`refresh / tip_rows / describe_act_for` 的 `q` 必填；`match_started` 仍送客户端、句柄由对局侧自建；`store_pending` 用 `.get` 兜底；收养模式的 `close()` 不置 aborted；`query` 报文免限速；`CWLogStore.of` 搬进测试、白名单回到两条；联机规划器按 E-1 (a) 真做（RPC 未回时「报价中…」，`plan_tick` 每帧补画）。演出阻塞毫秒表（攻击 450 / 扑咬 450 / 长技能 500 / 伪足 400 / 其余 300 / 飞卡 350 / 光束 900）是我按「看清这一下发生了什么就放手」定的，骰子仍整只演完 —— **数值要 Kevin 过目，随时可调**（`ui_bridge.gd` 顶部常量表）。

---

## 九、2026-09-19：我列的八件待拍 → Kevin 一次答完

| # | 事项 | 拍板 |
|---|---|---|
| 1 | 测试迁移 §0.6.7：`anaerobic_pool` / `split_share` / `move_legal` / `settle_loss` 四条要在 C# 开具名入口（E-4 同类） | **接受** |
| 2 | 批 1 步 10 全量发版：合不合 main / 何时 / 等不等 fx-diag | **暂时不合**，等 C# 版测完没问题再说。⇒ 线上继续走 `diag-fx-0919` 热更线，批 1 的发版挂起 |
| 3 | 演出阻塞毫秒表（攻击 450 / 扑咬 450 / 长技能 500 / 伪足 400 / 其余 300 / 飞卡 350 / 光束 900） | **好，有问题再改** |
| 4 | fx-diag 复现日志 | 问「需要我做什么」→ 打一局联机、看到攻击动画不完整就退出，日志在本机我自己读（`%APPDATA%\Godot\app_userdata\Cell War\logs\godot.log`，现在 0 条 `[fx-diag]`：带诊断的客户端还没跑过） |
| 5 | E-3 待定的 6 个旋钮（`world_events_on` / `attack_dmg_success` / `antibody_halve` / `antibody_max_per_round` / `osteo_ossify_cost` / `cancer_win_hold_rounds`） | **补进内核** |
| 6 | E-3「补进内核的 6 个有氧旋钮」原定队友做 | **我做进去**（`aerobic_level_base` / `aerobic_by_level` / `aerobic_split` / `aerobic_split_ref` / `necrosis_aerobic_pct` / `anaerobic_on_turn_end`）。⇒ 12 个旋钮一起进 C# 内核，默认值取 GD 今天的值、零行为改动 |
| 7 | 启发式 v4 去留 | **不重要**，AI 走训练模型路线 |
| 8 | 「树突重做 + 【I-趋化源】引擎未实现」 | 我的记忆过期：09-13 GD 已按新 PRD 重做（协议 v18），C# 侧口径一已对齐（`BoardRules` / `CardRules` / `RulePolicies` 都有趋化源）。**无待办** |

原文：「1、接受 2、暂时不合，等测试完c#版没有问题再说 3、好，有问题再改 4、需要我做什么吗？ 5、补进内核 6、你把旋钮做进去吧 7、不重要，我们在训练AI模型 8、你指哪个引擎未实现？」

**第二轮补充（同日）**：第 4 条 Kevin：「没有攻击动画不完整的问题，大概率是线程阻塞，到时候把 AI 迁出去就好了」⇒ fx-diag 复现不等了、诊断线不再是发版的顾虑；AI 单开进程仍排在批 2/3。第 8 条追问「你确定 C# 端已经实现了吗」→ 核实结果记在开发日志同日条。

**落地安排**：第 1 条 → 测试迁移机件落地后单独一次提交开四个入口（零行为改动，`dotnet test` + L1 三夹具钉），契约表四行从 NOTIMPL 改 OK·deferred 并补空壳；第 5 + 6 条 → 12 个旋钮进 `RuleTuning`、替掉 C# 里写死的同一常数、每个旋钮一条 C# 测试拧一下核公式与 GD 同，`contract_tune.json` 这 12 行从 B 档改 A 档、`WithKnob` 接上；E-3 判死的 6 个（`solid_at_cancer_spawn` / `cancer_types` / `aerobic_mult_growth` / `immune_attack_pct_per_memory` / `immune_attack_pct_growth` / `immune_attack_pct_memory_cap`）登记 `docs/临时下架清单.md`，对应断言留 GD 挂 `OUT_OF_SCOPE`。

**同日 · 树突建源局录成第四条 L1 夹具（Kevin：「树突建源对局可以录一局」）**：`trace_4p_chemo_4242`（279 步）首跑抓到 C# 两处 —— ① 建源时长写死 2 完整回合（GD / PRD 是 1），已修；② 抽到【炎症风暴】/【免疫风暴】时 GD 问 `pick_cell` 选中心细胞，C# 没有这个挂起态、效果静默跳过。②按「保留 C# 架构、问答形状用最小挂起字段解决」的口径做：`PendingPickCell` + `PickCellDecision` + 语义键 —— **同日已做**（开发日志同日条）：L1 重放 279 步整条一致，`pick_cell` 转 OK；envelope 对拍先钉 248、新登记 `chemo-move-quote` 与 `area-damage-batch`；**`chemo-move-quote` 同日合上**（根因 = 0.4-bis #6 借道报价：C# 逐段跑修饰，改成与 GD 同的「累计 raw、落点跑一遍修饰」），四条夹具 L1 与 envelope 都整条一致，`pass_through_cost` 转 OK；`area-damage-batch` 留批 4。

**同日 · 12 个旋钮 + 四个入口落地**（开发日志同日条）。**两件要 Kevin 追认 / 拍**：① 第 13 个属性 `AerobicLevelStep`（GD `aerobic_level_step` = 1.5）—— 不在 12 个名单里，但 `aerobic_level_base` 唯一的读点是 `base + step × 等级`，不加它 `AerobicLevelBase` 就是假旋钮（E-3 口径不许），我先加了；② `cancer_win_hold_rounds ≥ 3` 现在抛 NotSupported：C# 只存「上一次达标的回合号」（`CancerAlarmRound`），数不出三连；GD 是真计数器 `cancer_win_streak`（观测协议 §八 known_divergences #3 正是它）。要真支持得给 `TurnState` 加一个计数器 —— 动骨架，但顺手能把 #3 那条差异消掉（协议规矩「只许减不许加」）。推荐做；拍了就做。

**Kevin 同日拍：「做吧」**（① `aerobic_level_step` 追认；② 癌症胜利改真计数器）。②已落地：`TurnState.CancerAlarmRound` → `CancerWinStreak`（GD `cancer_win_streak` 同义），`OutcomeRules.Evaluate` 任意 `cancer_win_hold_rounds` 都算得出，观测协议 §八 known_divergences #3 收敛、`EnvelopeNormalize` 不再剥 `cancer_alarm.streak`（四条 L1 夹具从此逐步比它）。

## 十、2026-09-19：批 2「AI 进 C#」方案稿 —— 待 Kevin 拍（六条）

方案稿 `docs/口径二_批2_AI进C#_方案.md` §八，每条附推荐：**1** 批 2 交付整只 AI 还是先只交地基（推荐地基，但这是收窄拍板 7「重写成 C#」的交付范围，要重拍）；**2** C# AI 要不要与 GD AI 逐决策一致（推荐不要，接受平衡数字重量）；**3** `CellWar.Ai` 怎么读 Core 的 internal（推荐 `InternalsVisibleTo`；「同一程序集」与 09-18 已拍「`CellWar.Ai` 进热更载荷」冲突，不建议）；**4** AI 进程落哪（推荐现在做 (a3)「两个静态线程入口原样改成子进程入口，一份快照进一个下标出」3～5 人日止血，终局仍是 sidecar）；**4bis** 联机专家档补 `max_sim_steps = 192`（漏设，实测 381～645 ms → 180 ms 量级，0.5 人日；代价是联机专家档弱一档）；**5** 那次动画不完整要不要再量（推荐换计数器量法：`cw_room.push_state` 与 `CWPlayQueue.pump` 各埋一个，服务器侧写出来、不靠复现，0.5 人日；不再走已叫停的 fx-diag）。
同日顺手修掉方案调研抓到的一条活 bug：`CWKernelInProc` 从没调过 `attach_engine`，树搜索档 `mcts.game` 恒 null（a9e18e5）。
