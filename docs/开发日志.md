## 2026-09-19 · 修 issue #62（#44 余账）：掉线期间被代打，重连回来界面当场收掉，不再等下一次轮到自己

现象：掉线时那一问被服务器代打（`_on_seat_offline` 掉线即答），代打广播的 `step_begin` 掉线的人收不到；重连时 `CWRoom.reconnect` 只重推状态 + 重发「此刻正问他的那一问」—— 此刻问的是别人时什么都不发，客户端界面就一直挂在旧一问上，要等下一次轮到自己才被 `_serve_ask` 的 abort 收掉。

修法（**只改服务器侧** `cw_room.gd`）：`_last_ask[pid]` 记每席上一次的 ask_id（`ask_human` 写、一局一清）；`reconnect` 在「此刻没在问他」时补发一条 `step_begin{ask_id: 上一问, seat}`。客户端零改动：#44 的判据 `taken_over(step_ask_id, serving_ask, asking)` 认出「这一问还挂在我手上、边界却报了它」就收界面并提示「这一步由代打接管了」；他自己早答过的话句柄里没有这一条，一比就当没事 —— 报文幂等，多发无害。协议没动（`step_begin` 是 v30 就有的报文）。

护栏 `t_net_takeover_offline`（真服务器 + 真界面：掉线 → 服务器代打去问乙 → 探针「界面还挂着」→ 凭令牌重连 → takeovers 1、行动栏 / 候选格收掉、服务器仍在问乙）；变异验证：去掉补发那一支，「重连回来收到补发的 step_begin」超时红。服务器从 hotfix 树重部署。

## 2026-09-19 · 热更补丁 202609190745（基线 client-2026-09-19-3）：演出阻塞时长全 0；网页版重导

hotfix 分支 cherry-pick f81419d，补丁 202609190745 覆盖 202609190719，整包 11 个文件不变（`ui_bridge.gd` 更新）。网页版从 hotfix 树重导。服务器不用动。收尾脚本 `git add -u game` 时 hotfix 树里那 93 个 `.import` 只是行尾归一的差异，归一后与索引相同、没进提交（核过 `git show --stat`：0 个 .import）；以后收尾还是只 add 点名的文件。

## 2026-09-19 · 演出阻塞时长暂时全部调成 0（Kevin「暂时把所有的阻塞时间都调成 0」）

`CWUIBridge` 加总开关 `BLOCK_MUL := 0.0`，`_block()` 一律乘它：飞卡 350 / 光束 900 / 普通攻击 450 / 扑咬 450 / 归巢等 500 / 缺省 300 那张表一个数不动，眼下全部不等 —— 队列触发即走，各演出在自己的层里照演完（能量飘字、传送溶解这些镜像差分的演出本来就不看阻塞）。**骰子不在此列**：它是 barrier 条目，本地局引擎在等 ack，照旧整只演完（关掉「掷骰动画」设置才跳过）。要恢复原节奏把系数改回 1.0。血行转移那条「离场 / 落场锚在绝对时刻」的纯函数本来就按「桥阻塞多久都对」设计（`t_issue_fx_0919`），0 也成立。

## 2026-09-19 · 新手引导 PRD 第一章「章节设定：本章不设回合/攻击次数上限」（PRD:99）落到第一～三关

Kevin：「看一下现在的 PRD，在第一章这里加了描述」。正本仍是 09-19 06:32 的 534 行版，第一章标题下多的是这一句章节设定（S5 修订时已见过、当时只在第二章落了旋钮，第一章没拧）。现在 `c1_l1` / `c1_l2` / `c1_l3` 的 `base` 世界各加 `tuning.attack_max_per_turn: 0`（同第四 / 五关的写法）：第二关 Step2「击杀癌细胞」、第三关的攻击都可能连打超过 3 次，而第一～三关永不点结束回合、`attacks_used` 一辈子不清零，不拧就是打满 3 次成死局。`t_tutor_c1` 三条断言各钉一关。

「回合上限」这半不拧：`limit_round` 是 tier C 旋钮（关卡数据不许动，装载器当场拒），而第一～三关世界回合停在 1、`cw_world.gd:76` 那条「跑满 limit_round 才终局」到不了 —— 行为上本来就是无上限。教程入口还灰着，不用热更。

## 2026-09-19 · 合并教程 v2 S5 修订（de98c74）+ 两道闸重录

S5 修订（agent 258af7e）合进 main：Step2 关内 `load` 支持 `{"by_player_type": {...}}` 表写法按玩家分化种类挑 world、飘字只静能量落在 ∞ 带里的那只、教程右栏 `guide_layers` 第三层位藏「癌性加权」；`t_tutor_c2` 90 → 109 条；三帧真机图 `截图_2026-09-19_教程v2_S5修订/`。只有开发日志冲突（堆叠）。

合并后全量两处红，都是今晚别的改动欠的账、随本条一起还：① `t_tutor_fx` ⑩「公共特效三支指纹」—— `attack_fx.gd` 今晚为修「第二局起攻击动画消失」动过一行，指纹重录（改的是真人对局的 bug，不是教程去改公共特效）；② 闸三「check 总数涨了 23 条而 covered_sites 没动」—— 23 条全是表现层护栏（`t_attack_fx` ③b、`t_settle_screen` 拆局重开、`t_crit_gold`、`t_hand_swap`），没有规则改动、不欠 L0 用例，`xcheck_report.py --write` 抬基线（total_sites 3605 → 3628，covered_sites 421 没动）。

## 2026-09-19 · 新手教程 v2 · S5 修订：Kevin 五条答复逐条落地（Step2 保留分化种类 / 飘字只静玩家那只 / 右栏去掉「癌性加权」）

S5 合进 main（8bec161）时留了五个问题，Kevin 当天答复。逐条：

| # | 问题 | Kevin | 怎么落的 |
|---|---|---|---|
| 1 | 第五关也拧了 `attack_max_per_turn: 0`，PRD 只在第四关写 | **追认** | 行为不变，只改注释。⚠ 核过剧本正本（09-19 06:32 的 534 行版）：这一条**现文仍只写在第四关** —— PRD:325「玩家本关不设攻击次数上限」，第一章另有章节设定 PRD:99「本章不设回合/攻击次数上限」，第五关 PRD:351-405 通篇没有这一句。`c2_l5.json` 的 `_doc` 与方案 §4 都按现文行号改写 |
| 2 | 玩家挑的种类必然与 Step2 某只 NPC 撞种 | **可以撞种** | 零代码。`c2_l5.json` 的 `_doc` 与方案 §4 里那条「代价 / 待拍」改成「Kevin 2026-09-19：可以撞种，不改」 |
| 3 | Step2 重装把玩家摆回 B 细胞 | **不接受** | **改了**：`state.load` 多一种**表写法**（见下） |
| 4 | 第四 / 五关整关静掉能量飘字 | **只静玩家自己那只** | `match.gd` 那条整关静音的门改成**按细胞判** |
| 5 | 右栏还留着「癌性加权」块 | **关了吧** | `CWMatchPanel.guide_layers` 多一个层位，教程局整块藏掉 |

**③ `state.load` 的表写法（本片唯一的格式扩充）**

```json
"load": { "by_player_type": { "BCell": "b", "TCell": "t", "Macrophage": "macro", "Dendritic": "dc" } }
```

- **键就是 world spec 里 `cells[].type` 那个词**，不另造 `b / t / macro / dc` 那套简写 —— 简写要在数据、导演、校验器三处各记一遍，而种类名的对照表全案只有装载器 `_kind_name` 一处（数据门面新开一个公开 `kind_name()` 转过去）。
- 解析落在**导演**（`cw_tutor_director._world_for` / `_player_kind`）：这一问的答案只有运行期才有（玩家挑了什么），而数据是死的；导演本来就持着取镜像的那条线，调用方（`CWMatch._tutor_load_world`）照旧只收一个 world 名，拆装次序那串一个字没动。四份 world 也一个字没改。
- **「切换种类」的轮转下标随之对到装上的那一份**（`_enter_state` 里 `switch_types().find(wid)`）⇒ 按一下是「从现在这种往下一种」，不是跳回表头那份 `b`。
- 表里没有这一档（剧本写漏 / 玩家还没分化）→ warning + 退回表里第一份：挑错一份还能玩，什么都不装是关卡当场停死。
- 校验器加**判据 ⑮**：写成表就只许 `by_player_type` 这一个键，键在 `PLAYER_KINDS` 里、值是这一关真有的 world；另外钉死 **`flow[0].load` 必须是字符串** —— 「重置本关」退回的就是它（`_tutor_entry_world`），关首也轮不到「按玩家种类挑」。

**④ 飘字的静音判据：`energy_mode() == "infinite"` 且这只细胞的能量在 `INFINITE_MIN` 那条带里**（= 右栏写着 ∞ 的那一只，新旧值任一在带里都算）。给一只写着 ∞ 的细胞飘「-1.0」自相矛盾；而别人的照飘 —— 第五关 Step2 的卖点就是 PRD:403「来场盛大的爆炸吧——」，整关静音的话癌细胞挨打一声不响。

**⑤ 右栏那一块**：`guide_layers(end_turn, round_no, score := true)` 第三个层位，`match.gd:_sync_tutor_layers`（只有教程局每帧跑）写死 `false`。**没给 `CWTutorLayers` 加新层** —— 那文件带 `class_name`、走不了热更（方案 §1.5）。警报期的「★ 警报」字样住在同一只标签里，跟着块一起没了（教程里本来也不会有警报）。**只藏不挪**：让出来的 66px 不给下面的行 —— 九席那套配平（底边 532 ≤ 540、10 席 576 放不下）就是按「藏着也占位」算的，挪了就等于把上限改掉。

**改判表**

| 老判据 | 怎么改的 | 为什么 |
|---|---|---|
| `t_tutor_c2` 的 `loads5 == ["base", "b"]` | 改成 `["base", "t"]` | Step1 分化成 T ⇒ Step2 装的就该是 `t`（这条是③的正面判据） |
| `CWMatchPanel.guide_layers(end_turn, round_no)` | 多一个**带缺省值**的第三参数 `score := true` | 正式局与老调用点一个字不用改 |
| `match.gd` 那条 `CWTutorLayers.energy_mode() != "infinite"` | 改成按细胞判的 `muted` | 见④ |
| `cw_tutor_script` 的十四条判据 | 加第 ⑮ 条 + `flow[0].load` 必须是字符串 | 表写法要有执行机构 |
| `t_tutor_flow` / `t_tutor_view` / `t_match_panel` | **零改动** | 没加新意图、没动排版算法 |

**测试**：`t_tutor_c2` **90 → 109 条**。新增：④′ 四种分化**各跑一次**（Step1 挑哪种 → Step2 就装哪份 world；再按一下「切换种类」→ 从那一份往下轮，实测 `[t, macro, dc, b]`）；⑥′ 在真 `CWMatch` 上走镜像差分（∞ 那只掉能量没条目、癌细胞受击有条目 `[[1, -15]]`；能量层一撤两只都飘）；⑧ 教程局右栏那块 `visible == false`、`guide_layers` 缺省那一档照旧 `true`。全量 `bash tools/run_tests.sh`：两片合计 **4071 项 / 240 个测试**（基线 4052），L0 三段绿（GD 417 / GD 装载往返自检 417 / C# 454），闸三过；`xcheck/COUNT` 的 `total_sites` 3599 → **3605**（+6，全落 `guide` / `testinfra`；`core_sites` 1050 与 `covered_sites` 421 一个数没动）。

**真机三帧**在 `截图_2026-09-19_教程v2_S5修订/`（仓库外）：

1. `1_Step1选T_Step2仍是T.png` —— Step1 真点「分化 → T细胞」，Step2 装出来右栏第一行仍是 **T细胞**（行动栏跟着变成【细胞毒素】/【裂解】，常驻壳「切换种类」已出）。改之前这一帧会是 B细胞。
2. `2_Step2癌细胞受击有飘字_玩家那只没有.png`（+ `2b_放大_飘字只在癌细胞头上.png`）—— 攻击 (5,-1) 那只骨肉瘤 5.0 → 4.0，**癌细胞头顶有「-1.0」、玩家那只（∞）头顶什么都没有**，右栏那一行同拍色闪。
3. `3_第四关右栏_无癌性加权.png` —— 第四关右栏从玩家列表直接起头，没有「癌性加权」那一块。

**步骤串**（前置：`main_menu.gd` 的 Guide `enabled` 临时改 `true`、`guide_progress.cfg` 写成 `done=3` / `done=4`，**两处都已还原、都没提交**）：

```
"D:/Godot/Godot_v4.5-stable_win64.exe/Godot_v4.5-stable_win64_console.exe" --path game \
  --resolution 960x540 --windowed --script res://tests/screenshot.gd -- \
  "user://s5fix.png" "res://scenes/Main.tscn" -1,-1 17.3,17.5,17.7,17.9,18.1 \
  "key:ui_up@0.4;key:ui_up@0.6;key:ui_up@0.8;key:ui_accept@1.0;call:CWTutorViewBubble:advance@4.0;call:CWTutorViewBubble:advance@4.4;call:CWTutorViewBubble:advance@5.0;call:CWTutorViewBubble:advance@5.4;657,504@7.0;348,492@9.0;call:CWTutorViewBubble:advance@12.0;call:CWTutorViewBubble:advance@12.5;489,504@14.0;594,228@15.0;move:350,40@15.3"
```

- **合成鼠标点得动行动栏**（S4 那条口径是对的，S5 记的「点不到」可以翻篇）：`657,504` = 【分化】、`348,492` = 子菜单里的「T细胞」、`489,504` = 【迁移】、`594,228` = 棋盘上 `(5,-1)` 那只骨肉瘤（攻击 = 迁到它那格）。
- **点完要把鼠标挪开**（`move:350,40`）：停在格子上会浮出组织详情框，正好压住玩家那只细胞 —— 「玩家头顶没有飘字」就看不出来了。
- 飘字只活 1.05 s：攻击结算落在点击后约 2.5 s，连拍 0.2 s 一帧才撞得上（17.3 还没扣血、17.5/17.7 有字、18.4 已经没了）。

**留给 S9（间章）的接缝：少了一件**。S5 那条「玩家当时的种类写不死、间章承接活局面时要现取」现在不用管了 —— Step2 之后玩家就是他自己挑的那一种，间章 `flow[0]` 照旧 `load: null` 承接活局面即可；坐标那一条不变（仍由钩子运行期 `ctx.read("cells")` 现取）。第五关 `on_done` 仍是空串，没动。

**要 hxr 的一件**：剧本正本第五关至今没写「不设攻击次数上限」（见上表第 1 条）。Kevin 已追认照拧，但下一次通读 PRD 时顺手补一句，免得后面的人又当成 bug。另外正本 09-19 06:32 那版在第四关的「界面变化 / 游戏状态变化」里多了三个空行，**第五关整段下移 2 行**（现文 PRD:351-405）；`c2_l4` / `c2_l5` 的 `flow[].prd` 仍是 04:08 基线，两关同口径、对账闸（判据 ⑬ 只看单调不减）照样成立，要重编就两关一起编。

---

## 2026-09-19 · 热更补丁 202609190719（基线 client-2026-09-19-3）：手牌抽屉按内容刷；网页版重导

hotfix 分支 cherry-pick ccb544f → dccf34e，补丁 202609190719 覆盖 202609190708，整包 11 个文件（多了 `hand.gd` / `match.gd`）。网页版从 hotfix 树重导。服务器不用动（纯表现层）。

顺带记两条**早就在**的测试小毛病（这次单跑时撞见，与本条改动无关，全套顺序跑时都过）：`t_hand_exit` 单独 `--only` 跑会红三条（离场补间的落点靠前面的测试把帧跑起来，单跑时 y 没动）；`t_hand_play` 单跑打一条 `custom_minimum_size on null instance` 的 SCRIPT ERROR 但断言全过。两条都留着，改日顺手收。

## 2026-09-19 · 「癌症转移落到骨髓抽到一张后打不出癌症转移」根因：手牌抽屉只比张数、不比内容

Kevin 截图：打出【癌症转移】落到骨髓、同一步里骨髓又抽到一张（日志「经由「骨髓」抽到【即时】肿瘤增援（手牌 2）」），抽屉里仍画着【癌症转移】，双击它「还打不出（没有合法目标）」。引擎那边没错：`hand_options` 给的是手里真有的那张（【肿瘤增援】）的选项；【癌症转移】结算后已弃置（`_resolve_played` 之后 `hand.erase`），也不可能再抽到同名（`CWCards.is_legal` 剔除手上已有的）。

真凶在 `match.gd` 的 `_sync_hand()` / `_sync_hand_watch()`：`_hand_seen[pid]` 记的是**张数**，张数没变就不刷抽屉。打出一张 + 同一步抽到一张 = 1 → 1，抽屉永远停在打出去的那张上，玩家看到的牌名和引擎手里的对不上。本地局、观战席同样中招；任何「打出的卡把自己送进骨髓」（【癌症转移】【肿瘤增援】【免疫增援】…）都会触发。

修法：`_hand_seen` 改记**牌名**，新的纯函数 `CWMatch.hand_refresh(seen, was, now)` 决定不动 / 飞入 / 就位 —— 按内容比；「飞入」看有没有**新到的**牌（`CWHand.arrivals`，多重集差），不看张数有没有涨。抽屉本体 `CWHand.sync` 本来就按名字认离场的那张，同张数换牌 = 旧卡飞出、新卡飞入。护栏 `t_hand_swap`（十条：arrivals 四例、hand_refresh 五例含「1 → 1 换牌要刷」、抽屉本体同张数换牌）。`match.gd` / `hand.gd` 都是已有 class_name，能热更。

## 2026-09-19 · 热更补丁 202609190708（基线 client-2026-09-19-3）：攻击大成功金字；网页版重导

hotfix 分支 cherry-pick ae7aac9 → e7b10db，补丁 202609190708 覆盖 202609190659，整包 9 个文件（多了 `toast.gd` / `ui_bridge.gd`）。中间那枚 202609190707 是 cherry-pick 卡在 `headless_test.gd` 冲突时误打的、内容与 0659 相同，已被覆盖。冲突按「两边都留」解 —— hotfix 树的 `headless_test.gd` 因此多了一段没登记的 `t_tutor_c2`（S5 的测试，那棵树上没有它依赖的符号；测试不进包、也没人在 hotfix 树跑套件，不影响发出去的东西）。网页版从 hotfix 树重导。服务器不用动。

## 2026-09-19 · 攻击大成功换金字（Kevin「给攻击大成功加上金字特效」）

文字仍由引擎给（`cw_actions.gd` 攻击结算的 `announce("攻击大成功")`），表现层只认这一句换样式：`CWUIBridge.show_result` 里 `text == CRIT_RESULT` 走新的 `CWToast.bubble_crit_at()`，其余结果照旧 `bubble_at()`。样式：金字 #F6D66A + 2px 深棕描边 #5A3708 + 金边框 #E3C071（调色同 Excalibur 光束的 INK_A / INK_B）；出现时从 1.6 倍「盖章」缩到原大（TRANS_BACK 回弹，0.22 s），字色在金与暖白之间闪三下（0.9 s），气泡四周八粒十字火花错峰迸出（内部类 `CritSparks`，0.8 s 活完自毁）。三段补间绑在气泡自己身上（`box.create_tween`）：`hide_now` free 掉气泡时补间跟着死，不会对着已释放的 Label 写颜色。

护栏 `t_crit_gold`（分派键与引擎原文一致 / 金字描边金边 / 普通结果不受影响 / 盖章起点与 pivot / 火花挂上 / 走完回原大、火花自毁、字色停金 / hide_now 释放）。出图 `tests/preview/preview_crit_gold.gd`，三帧在 `截图_2026-09-19_大成功金字/`。`toast.gd` / `ui_bridge.gd` 都是已有 class_name 的文件，能热更。

## 2026-09-19 · 热更补丁 202609190659（基线 client-2026-09-19-3）：第二局起攻击动画消失的修复；网页版重导

hotfix 分支 cherry-pick 738cd0a → 5f4e4aa，补丁 202609190659 覆盖 202609190629，整包 7 个文件（多了 `attack_fx.gd`）。网页版从 hotfix 树重导。服务器不用动（纯表现层）。

## 2026-09-19 · 「联机里免疫攻击 AI 癌细胞没有攻击动画」根因：普通攻击那一层拆局后再没亮回来（不是线程）

Kevin：「联机模式下，玩家操控的免疫细胞攻击 AI 癌细胞攻击动画会消失，是否是线程阻塞的问题」。**不是线程**：服务器 AI 在主线程同步跑、根本没起线程；客户端也不跑服务器 AI。逐条排过：报文走 `var_to_bytes`（Vector2i / int 原样到）、`CWPlayQueue` 顺序播且没人中途 `clear()` 演出层、`immune_attack` 的 fx 数据（itype / ctype / 双方 id）齐全。

真凶在 `attack_fx.gd`：拆局的 `CWMatch._clear_board_fx()`（2026-09-13「上一局的特效留在等待室的棋盘上」）把所有演出层 `clear()` + `visible = false`，别的层都在 `play()` 里自己 `visible = true`，**普通攻击这一层（同一天合入的 PR #30）漏了这一句** ⇒ 同一次启动里**第二局起**普通攻击一次也不演。Kevin 的房间一晚连打了两三局，看到的正是第二局起「动画消失」；对手是 AI 只是巧合（他的对手本来就都是 AI）。本地局同样中招。

修法一行：`play()` 里 `visible = true`。护栏两条：`t_attack_fx` ③b（`visible = false` 后再 play 必须亮）、`t_settle_screen` 拆局重开后先探针「这一层确实被藏了」再 `_play_attack_animation` 断言亮着。`attack_fx.gd` 没有 class_name 走热更。

## 2026-09-19 · 线上核验：服务器的「AI·专家」确实已是意图级（Kevin「现在线上的专家 AI 真的是意图级吗？检查一下」）

三层证据：① 进程：systemd 里那份 `cellwar` 20:51:27 起跑、cwd `~/cellwar/game`，该树 `scripts/net/cw_net_bridge.gd:20` 是 `MechBridge.new()`，`scripts/ai/mech/` 三个文件在，`.godot/global_script_class_cache.cfg` 里有 MechBridge，重启以来 journal 零 SCRIPT ERROR；② 期间 Kevin 已在上面开了七局（B49BVY / 5QAB9B 等）；③ 端到端：本机一个启发式机器人连线上服务器建 2 人房、另一席放「AI·专家」，打完一局按每次 state 推送的时间戳算专家席每一问的墙钟（含往返）：**143 问，均值 105 ms、中位 90、p95 228、最大 263 ms**（机器人真人席自己 30 问均值 71 ms = 本机启发式 + 往返的底）；旧专家档 MC 在服务器机上一问 0.7～2.6 s。探针脚本是临时的，没进仓库；要复用可照 `tests/net_play.gd` 加 `sync` 时间戳（机器人客户端收到的是 `sync` + `turn`，不是老的 `state`）。

## 2026-09-19 · 热更补丁 202609190629（基线 client-2026-09-19-3）：#47 定稿色（灰蓝加红）；网页版重导

hotfix 分支 cherry-pick c1946b5 → d86216f，补丁 202609190629 覆盖 202609190619。整包仍是 6 个文件。网页版从 hotfix 树重导。服务器不用动。

## 2026-09-19 · issue #47 定稿：Kevin「癌症候选格改成在 #47 改之前（#79849F）的颜色上加红一点点」

前面三版红（提亮红 / 艳红 / 深红）都不是 Kevin 要的：他要的不是红格，是 **#47 之前那块灰蓝往红偏一点**。落法：混合比例照 #47 之前的 0x6E 不动，色标从目标叠色反推（c = 底色 + (目标 − 底色) / 0.43）。出了 R +12 / +21 / +30 三档（`截图_2026-09-19_issue47_加深/对比_灰蓝加红_甲乙丙.png`），+12 几乎看不出、+30 已经发粉，**选 +21**：目标 #8E7E9C ⇒ 色标 **#61C3F3**。与健康候选格 #2F8491 色距 0.38（之前 0.29），与攻击橙叠色 0.41。

`t_ui_bridge` 的 #47 断言整块改钉这个口径：比之前的灰蓝红一点（R +0.05～+0.16）、G / B 基本不动、与健康候选格色距不比之前小、混合比例照旧、与攻击橙分得开。「叠完仍是红」那条删了（现在就不该是红）。

## 2026-09-19 · 热更补丁 202609190619（基线 client-2026-09-19-3）：#47 改回「原版红稍微加深」；网页版重导

hotfix 分支 cherry-pick 70e0c4a → eada663，补丁 202609190619 覆盖 202609190608（后者只活了十分钟：艳红那版 Kevin 没要）。整包仍是 6 个文件（cw_card_fx / cw_net_bridge / board / cw_settings / online_panel / canary）。网页版从 hotfix 树重导一次。服务器不用动。

## 2026-09-19 · issue #47 再续：Kevin 改口「把最原版的红色稍微加深一点就行了」

上一条选的艳红（#FF3D5C @0x8C）Kevin 看过对比图后没要，要的是**第一版那家红、稍微深一点**。落法：色标 #E05A6E → **#D8364F**（G / B 压下去、少一点粉），比例 0x6E → 0x8C 让它不比第一版更淡；叠完 #C63F54（第一版 #C55163）：饱和度 0.59 → 0.68，与未高亮癌格 0.09 → 0.10，与攻击橙叠色 0.22，与健康候选格 0.69。

`t_ui_bridge` 那两条「> 0.15」的断言换成钉与第一版的关系：饱和度高于第一版、与未高亮癌格的色距不小于第一版。和攻击橙分得开那条留着。出图 `截图_2026-09-19_issue47_加深/对比_原版红_加深后.png`。

## 2026-09-19 · 新手教程 v2 · S5：第四关 + 第五关（抗原记忆升级、分化、切换种类、关内换盘、NPC 席位）

第二章两关端到端跑通。盘面照《新手引导v2_盘面提案》**v4**：第四关整盘 127 格 + 四块各 10 格 + 5 只癌，第五关四块各 12 格 + 固化三格 + 各类免疫各一 = **9 席**（右栏到顶）。

**新建 / 改了什么**

| 文件 | 干什么 |
|---|---|
| `data/tutorial/levels/c2_l4.json`（新） | 第四关 抗原记忆（PRD:305-347）。整盘 127 格一次揭完、四个癌块各 10 格（块间最近 3 格）、五只骨肉瘤 5.0/2.0/4.0/3.0/3.0、玩家 `(4,-1)` 起手 **II 级 / 20 记忆 / ∞**、旋钮 `attack_max_per_turn: 0`、任务 `level_at_least: III`、`camera` player/center |
| `data/tutorial/levels/c2_l5.json`（新） | 第五关 分化（PRD:349-403）。Step1 分化按钮慢闪 + 任务 `delta: differentiated` + 图鉴【分化】；Step2 **关内 `load`**（四块各长两格 + 固化 `(6,-1)(1,-5)(2,3)` + 四只 NPC 免疫换成 B / 巨噬 / 树突 / T）+ `ui.switch_type = [b, t, macro, dc]` + 任务 `all_dead: cancer` |
| `data/tutorial/index.json` | 登记第二章两关（`chapter: 2`，章名照 PRD「Immune」） |
| `scripts/kernel/cw_tutor_beats.gd` | `all_dead` 收一个可选的**阵营**参数（`"cancer"` / `"immune"`）。不带参仍是「屏幕前这位真人的敌方」（`foes` 缺省 1 ⇒ 没镜像那一瞬不成立）；写歪阵营名一律不成立 |
| `scripts/tutor/cw_tutor_layers.gd` | **∞ 的显示判据从一个数改成一条带**（见下「一条实测改判」）：新增 `INFINITE_MIN = 90000`，`energy_text` 的两档比它；数据里写的仍是 `INFINITE_AT = 99990` |
| `scripts/tutor/cw_tutor_chrome.gd` | 常驻壳第三颗图标「切换种类」（`SWITCH_RECT (88,46,28,28)`，接在重置 / 目录右边那条缝里）。**懒建**：`ui.switch_type` 非空才建节点；`set_switch / switch_shown / press_switch` 三个尾部追加的独立函数，`Icon._draw` 加一支 `"switch"` 画法 |
| `scripts/tutor/cw_tutor_director.gd` | 尾部追加 `switch_type()`：按一下把 `CWTutorLayers.switch_types()` 的下标 +1、发 `want_load` —— 拆装次序仍归调用方（走舞台的 `reload_world`）。轮转下标随 `invalidate()` 回到第一份 |
| `scripts/ui/match.gd` | 两处接线：`_sync_tutor_layers()` 里跟着 sidebar / round_no 一起把「切换种类」显隐同步一遍；`chrome.switch_pressed → view.switch_type_pressed → _tutor_switch_type → director.switch_type()`（形状照抄目录跳关那三行） |
| `tests/headless_test.gd` | 新测试 `t_tutor_c2`（90 条），已登记进 `_run_all` 手写清单；`_tutor_c1_open` 多收一个可选的 world id（第五关 Step2 要从关内重装那一份起跑） |
| `xcheck/COUNT` | `total_sites` 3487 → **3531**（+44，全落 `guide` / `testinfra` 档；`core_sites` 1035 与 `covered_sites` 416 **一个数没动**） |

**一条实测改判：∞ 的显示判据取一条带宽，不是 `INFINITE_AT` 那一个数**

哨兵 99990 不是真的无限 —— 玩家每走一格就被【迁移】啃掉 0.5~1.0，第四关整盘跑一趟约 100 能量。第一～三关右栏是**关着**的（`ui.sidebar: false`），所以这件事一直没显形；**第四关是第一关「右栏开着 + 无限能量」的关**，拿 `>= 99990` 去判的话玩家走第一步那一刻「∞」就变回「9998.5」，恰好是 PRD:321 要的那一件事没做成。

改法：`energy_text` 的 `infinite` / `null` 两档比 `INFINITE_MIN = 90000`（= 9000.0）。玩家要花掉 999.9 能量才跌出这条带（一整盘的十倍以上），而场上别的细胞最多几十（第五关 9 席里最高 5.0 = 50），够不着。**数据里写的仍是 `INFINITE_AT`**，两个常量各管各的：一个是「数据写什么」、一个是「什么算无限」。

**三条不写死就跑不起来的实现决定（要 hxr / Kevin 追认）**

1. **第五关也要 `attack_max_per_turn: 0`。** PRD 只在第四关写了「不设攻击次数上限」。但 `cell.attacks_used` 只在 `cw_turn` 的回合开始清零，而第一～五关**永远不点结束回合** ⇒ 它一辈子不清零。不拧这一档，玩家打满 3 次就再也攻击不了，「把全部癌细胞杀死」当场变成死局。
2. **Step1 的四只 NPC 免疫必须是未分化的 `ImmuneBasic`。** `_diff_choices()` 按 `game.differentiated` 过滤，而那张表由「`differentiated == true` 且 itype ≠ BASIC 的细胞」现算（`cw_world_loader:_differentiated_of`）。Step1 就摆上四种已分化免疫的话四种全被占，玩家一个【分化】选项都没有。PRD:387 本来也把这四只写在 **Step2 的「游戏状态变化」**里（「生成……优先保证各个种类都各有一个」）。代价：**玩家在 Step1 挑的那一种必然与 Step2 的某一只 NPC 撞种**（截图里 免疫A / 免疫B 都是 B 细胞）。要各不相同就得让钩子运行期现取，数据写不死 —— 要 hxr 拍。
3. **Step2 那次关内重装把玩家摆成 `b`（B 细胞），不保留 Step1 挑的那一种。** 四份 world 是写死的数据，`state.load` 只能点名一份。自愈路径是「切换种类」按钮（按一下换下一份），但玩家确实会看见自己变了一次。要保留就同上：钩子运行期现取。

**第四关起手格 `(4,-1)`（接 S4 留下的账）**

S4 记过：第三关的最省路**两条并列**，走直线的停 `(4,-1)`、绕 `r=-2` 排的停 `(5,-2)`。第四关是**新关、整盘重装**，起手位置本来就按剧本写死 ⇒ 照 v4 Q4-5 钉 `(4,-1)`。走另一条的玩家会在静默切关时看见自己挪一格（PRD 通用规则 2）。要 hxr 过目。

**11 格特殊组织从第四关起不再摊平**（v4 口径 4）：第一～五关从 `PlayerAction` 中途开局、闸里永不放「结束回合」⇒ 进不到 S 阶段 ⇒ 收入与存卡不结算，摊不摊平零影响；而第六关要真结算，那时它们本来就该是特殊格。代价：第三关把 `(3,0)(6,0)(-3,0)(-6,0)` 摊成了 `normal`，换到第四关又变回核心 / 血管 —— 静默换局里看得见。纪律 2 要求活跃格里的特殊格**显式写 type**，本关写的是它**本来的名字**（`core` / `marrow` / `vessel`），不是 `normal`。

**记忆账**：`seats = 6` / `9` 都不是 4 ⇒ 门槛一律回落六人档 `LEVEL_MIN_MEMORY = [0,10,30,120]`（`CWData.level_min_memory` 表里只有四人一档）。第四关起手 20，III 级门槛 30 ⇒ 只差 **+10**；默认盘面可得 40（净化 40 格）+ 17（打掉 17.0 能量）= 57，富余 47（Kevin：富余不用减）。测试里走的是净化 10 格那条最短路，记忆正好 20 → 30。

**右栏 9 席**：让掉回合块那 62px 之后，免疫等级块底边 **532 ≤ 540**，10 席 576 放不下。`CWMatchPanel.level_bottom()` 是纯函数，`t_tutor_c2` 两头都钉（9 席过、10 席不过）。**上限就是 9，别再往上加。**

**没弹结算屏是怎么保证的**：`check_immune_win` 只在 E 阶段最后判（`cw_world.gd:74`），而进 E 阶段要先点「结束回合」—— 第四 / 五关的 `allow` 里没有 `act=end`（测试正面断言，连「敞开的自由段」也没有）。Step2 的闸是方案 §3.2(a) 的**第三态「半开」**：列出除「结束回合」与「基因表达」之外的全部（move / 抗体 / 细胞毒素 / 裂解 / 趋化源 / 效应应答 + 六种子问）。另有一层余量：固化三格都没被免疫占据，就算哪天真跑到 E 阶段也会在「还有可复活据点」那一行早退。

**测试**：`t_tutor_c2` **90 条**。两关逐关真跑（真盘面、真规则、剧本自己的 `allow` 现场过闸）：第四关净化 10 格升到 III；第五关 Step1 四种分化都选得到、真分化一次、图鉴在 O-玩家 之后才解锁；Step2 的 `ui` 层、四份 world 的圆环 + 「抹掉 seat 0 的 type 之后四份逐字相同」、「切换种类」按满一圈回到起点、**同一行动回合里攻击 5 次**（上限 3 的话第 4 次就没选项）、剩三只时不翻页 / 四只全灭才命中 `all_dead: cancer`、`winner` 仍是 -1。另外真起 `Main.tscn` 的教程局断言：9 席、八只非人类席**逐席**装 `Decider` 且 `plan` 全空、棋盘遮罩真的揭到 127 格、`ui.switch_type` 一非空常驻壳那颗按钮就出来。

**改判表**

| 老判据 | 怎么改的 | 为什么 |
|---|---|---|
| `cw_tutor_layers.energy_text` 的 `v >= INFINITE_AT` | 改比 `INFINITE_MIN`（90000） | 花掉的能量会把哨兵啃下去；第四关是第一关右栏开着的关，不改就看不到 ∞ |
| `cw_tutor_beats.all_dead` 无参 | 加可选阵营参数 | 第五关 Step2 场上五个免疫席，「我的敌人」要看问的是哪一只 |
| `_tutor_c1_open(lv)` | 多一个可选 world id | 第五关 Step2 要从关内重装那一份起跑 |
| `t_tutorial_npc` | **零改动** | S1 就已经改判成「只剩纯 decider 那三段」，本片只在 `t_tutor_c2` 里补「接到席位上」那一段 |
| `t_tutor_flow` / `t_tutor_view` 的意图序列 | **零改动**（中途试过加第十类意图 `switch_types`，当场把这两条打红 ⇒ 撤回，改走 `_sync_tutor_layers` 那条每帧全量刷的老路） | 「切换种类」是一个显隐开关，与 sidebar / round_no 同类，不该占一条导演 → 皮的意图 |

**留给 S9（间章）的接缝**

- 间章从「上一关最后一个癌细胞被杀死」那一刻起（PRD:393）：第五关 Step2 的 `until` 命中那一帧就是那一刻，`on_done` 眼下是空串，**S9 落地时改成间章的 id**。
- **玩家当时的坐标写不死**：Step2 是自由游玩，最后一击落在哪一格、玩家停在哪都随玩家。按 v4 §6.1 方案甲，间章 `flow[0]` 承接活局面（`load: null`）、冲击波在**原地**起，直到分镜 6 那次完整换局才把玩家搬到 `(6,-2)`。所以本片**不预留任何坐标**，间章要的那两个量（玩家坐标 / 最后一击的格）由钩子运行期 `ctx.read("cells")` 现取。
- **世界半径不用在这里预留**：v4 口径 3「活跃集只增不减」，第四关已经一次揭到整盘 127 格，第五关 / 间章 / 第六关都是 `+0`。间章分镜 2「补齐缺失格子」按 v4 §6.2 的路 A 退化成纯镜头动作（改动为零）—— 路 B / 路 C 要动的是第六关的世界重心，与本片两关无关。
- 第五关的**固化三格 `(6,-1)(1,-5)(2,3)` 到第六关还在**（v4 Q5-2），是「复活窗口必须有一格不被免疫占据的固化格」那条硬约束的余量，而且都避开了第六关围圈的那一环。间章那两次重装别把它们抹掉。

**真机四帧**：`截图_2026-09-19_教程v2_S5/`（第四关关首 / 免疫等级高亮 / 第五关 Step1 分化按钮慢闪两帧对拍 / Step2 九席 + 切换种类）。Step2 那一帧靠**临时**把 Step1 的 `until` 改成一条当场成立的谓词才走得到（合成鼠标点不到行动栏的 Control，`screenshot.gd` 只认 `key:` 与 `call:`），拍完已按 `gen_s5.py` 原样重生成；`main_menu.gd` 的 Guide `enabled` 与 `guide_progress.cfg` 都已还原。

---

## 2026-09-19 · 热更补丁 202609190608（基线 client-2026-09-19-3）：#47 红色标加深 + 服务器专家档换意图级；网页版重导

hotfix 分支 cherry-pick 31e0b66 → 966d72b（前一条 fd5e49d → 378889f 只动服务器侧，当时没打补丁），补丁 202609190608 覆盖 202609190536（整包 6 个文件：cw_card_fx / cw_net_bridge / board / cw_settings / online_panel / canary）。探针导包第一趟段错误、原样重跑过（`build-patch-probe-flaky`）。网页版不吃补丁，`deploy_web` 从 hotfix 树重导（38 MB → 预压 9.3 MB）。服务器不用动（cw_net_bridge 那条已随上一次 deploy_server 上线）。

## 2026-09-19 · issue #47 续：癌性候选格的红色标加深（Kevin「滤镜太淡了，稍微加深一点」）

第一版 `MARK_MOVE_SICK` = #E05A6E @0x6E 是「癌组织贴图那块红提亮一档」，叠完 #C55163 —— 和健康候选格分开了（色距 0.75），可和**没高亮的**癌格 #B04A5A 只差 0.09：高亮压在癌组织上几乎看不出来，正是 Kevin 说的「滤镜太淡」。同色只抬混合比例没用（0.55 也才 0.12，色标和底色本来是一家），得把色标从底色上挪开。

出了五档候选帧（`截图_2026-09-19_issue47_加深/`，同一块棋盘 / 同一批候选格）：现状、同色 @0x8C、艳红 #FF3D5C @0x8C、艳红 @0xA0、深绯 #8C1428 @0x8C。**选乙：#FF3D5C @0x8C ≈ 0.55**，叠完 #DB435B，与未高亮癌格 0.17、与攻击橙叠色 0.21、与健康候选格 0.75。深绯那档叠完比底色还暗，「高亮反而更暗」读反了，没选。

`t_ui_bridge` 原来那条「混合比例不动」的断言撤了（比例就是这次要动的东西），换成三条：探针（第一版与未高亮癌格只差 0.09，闸认得出「太淡」）、现色标 > 0.15、与攻击橙叠色 > 0.15。`preview_move_marks.gd` 副标题里的叠色改成现算。只动 `board.gd` 一个常量 + 测试与出图工具，能热更。

## 2026-09-19 · 服务器 AI 主线程阻塞：换意图级之后量了一遍（没上线程，但一问从秒级降到 0.1 s 级）

Kevin 问「服务器的 AI 单线程计算优化了吗」。答：**没有把 AI 决策挪到工作线程**（`CWNetBridge` 里的 MechBridge 与原来的 MC 一样在主线程同步跑；MC 桥虽有 `use_threading` 那条路，服务器从来没开过，默认 false）。2026-09-02 记的「单线程坑」两道缓解（决策前 `next_frame` 让一帧 + `poll` 里 `STALL_FORGIVE_MS`=1000 的免罚）原样还在。变的是**每问的算量**：意图级只枚举一两步的迁移路径并在副本上试走，不做 rollout。

新基准 `tests/bench_mech.gd`（每席都换 MechBridge 整局逐问计时 + 同一局面对比旧专家档 MC rollouts=2 · horizon=40 同步路径）。**在服务器那台机上跑的**（Xeon 8255C 2.5 GHz × 4，`~/cellwar/godot`，工程复制到 /tmp 跑完即删）：

| 局 | 行动问次数 | 均值 | p95 | 最大 |
|---|---|---|---|---|
| 4 人 seed 99 | 135 | 67 ms | 96 ms | 123 ms |
| 4 人 seed 7 | 176 | 62 ms | 108 ms | 127 ms |
| 6 人 seed 99 | 301 | 74 ms | 118 ms | 130 ms（候选 42） |

同一局面旧专家档 MC：早 683 ms / 中 875 ms / 晚（候选 33）2585 ms；意图级同三个局面 63 / 62 / 68 ms。本机（Ultra 7 356H）约为服务器的 0.6 倍。三局 612 次问没有一次超过 200 ms，离 1 s 的免罚阈值和心跳超时都远。**结论：主线程阻塞这件事靠算量降下来先够用了，线程化不做**；将来要做的话 MC 桥的 `_threaded_eval`（快照 → 线程 → 每帧轮询 holder）可以照搬。

## 2026-09-19 · 服务器专家档 AI 换成意图级 MechBridge（Kevin「把意图级 AI 部署到服务器上，代替目前的专家级 AI」）

`cw_net_bridge.gd` 的 `mc` 从 `CWMonteCarloBridge.new()`（rollouts=2 · horizon=40，2026-09-02 定）换成 `MechBridge.new()`（PR #59 的第四档：迁移走意图规划器、其余回落启发式；试走在独立副本上，见下面「动画乱套」那条根因）。席位 tier 键仍是 `"mc"`、显示名仍「AI·专家」—— 那是建房报文里的键，客户端还在发，改名要升协议。

耗时：MechBridge 在服务器**主线程同步**跑、不起线程，一问的耗时就是所有房间一起卡的时长。`t_mech_bridge_quiet` 加了一段四人局计时（第一次轮到癌方、12 个候选）：**59 ms**（上一趟 46 ms；粗闸 < 1500 ms），远低于原来 MC 的秒级。同一条测试加了源码闸：`cw_net_bridge.gd` 里不再 new 扁平蒙特卡洛。`t_net_game` 原来把 `bridge.mc.rollouts / horizon` 调浅省时间，MechBridge 没这两个属性（跑出 SCRIPT ERROR），改成断言桥是 MechBridge。跑了 `--only=t_mech_bridge_quiet,t_net_lobby,t_net_game,t_net_takeover,t_net_resume`：92 项全过、零 SCRIPT ERROR。

部署：只动 `game/scripts/net/`（服务器侧）与测试，没有新 class_name、协议没动 ⇒ 不用发版、客户端也不用补丁；cherry-pick 到 hotfix 分支后 `deploy_server` 从 hotfix worktree 跑（= tag + #46 + #54 + 这条）。

## 2026-09-19 · 热更补丁 202609190536（基线 client-2026-09-19-3）：#46 落盘 + #54 放疗固化不转；服务器与网页从 hotfix 树重部署

hotfix 分支上 cherry-pick 9536b60 → 228e34c，补丁 202609190536 覆盖上一枚 202609190534（补丁是「自 tag 起全部改动」的整包，后一枚含前一枚）。#54 动了 `game/scripts/core/cw_card_fx.gd` 的规则 ⇒ `deploy_server` / `deploy_web` **从 hotfix worktree 跑**（= tag + 两条修复，与客户端逐字一致；不从 main 跑，main 上教程 v2 在制）。协议号没动。

## 2026-09-19 · issue #54 追加拍板：【放疗】区域里的**固化癌组织不转、也不坏死**（两侧同改 + L0）

**出处**（Kevin 原话，本文件上一条日志里那句的展开）：「#54【放疗】区域内**只转癌组织、固化不转**（现实现把固化也转了，要改，两侧 + L0）」。

**改前**：区域里每一格都走 `to_necrotic` —— 普通癌、**固化癌**、健康三种一律转健康 + 进「坏死」，打一张就能把一块固化墙洗平（`cleared` 还是用 `is_cancerous` 数的，把固化也计进了「%d 格转健康」）。

**改后**：翻格那一轮遇到 `Tissue.SOLID` **整格跳过** —— 不转健康、也不进「坏死」，原样留着；普通癌组织转健康 + 坏死、健康组织照旧坏死。选项面**没动**（固化格照旧可以当起点，只是起点自己原样），区域生长也没动（从来就不看 tissue）。日志 / 通报的「坏死」格数从 `region.size()` 改成真翻了的 `burned`。

- GD：`game/scripts/core/cw_card_fx.gd` `_radiotherapy()`；C#：`core/CellWar.Core/CardRules.cs` 的「放疗」一支（回放对拍用，L0 是硬闸）。两侧一字不差。
- 卡面文案本来就写的「区域内所有**癌组织**转为健康组织」（PRD:1245 现文）—— `cw_card_data.gd` **不改**，`gen_card_data.py --check` 继续绿。
- `docs/规则电子化说明.md` 第 55 条改字；顺手把同一格里遗下的两个旧值修了（区域 15 → 10、`NECROSIS_RADIO=5` → 2）。

**改判表**：

| 断言 | 改前 | 改后 | 理由 |
| --- | --- | --- | --- |
| `t_card_choices` ⑩「起点固化癌组织转健康并坏死 2 轮」 | 起点摆成固化、断言它转健康 | 改名「起点普通癌组织转健康并坏死 2 轮」，起点改回普通癌 | 拍板后固化不转了；固化格摆到距起点 9 格的 `-6,3`（选项数照旧盖到「固化也算癌性组织」），不再混进区域搞乱「整块连通」那一条 |
| `t_card_choices` ⑩ 续二（新增五条） | 无 | 固化一格没少 / 固化不进坏死 / 坏死格数少于区域格数 / 普通癌照旧转 / 健康格照旧坏死 | 起点六个邻格全摆固化，区域长到 10 格必然吃进几格 |
| L0 `execute/play/radiotherapy/necrotic_region` | covers 指旧名 | covers 改指新名；`source` 里把「1,0（固化）」的错字改成「普通癌组织」（盘面一直就是 `cancer`） | 同上 |
| L0 `execute/play/radiotherapy/solid_kept`（新增） | 无 | 同一盘面、同一条带子（区域生长不看 tissue ⇒ 区域是同一批 10 格），只把区域内的 `2,0` / `1,1` 改成固化：这两格在 delta 里**一个字段都不动** | 固化不转不坏死的两侧契约 |

**测试**：`--only=t_card_choices,t_card_fx_hooks,t_tissue_transitions` 220 项绿；`tools/run_l0.sh` 五段全绿（GD 417 条 / 自检 417 条 / C# 454 / 闸三 `covered_sites` 416 → **421**，已 `--write`）；`dotnet test core/CellWar.Core.Tests` 1177 项绿。`pre_envelopes.jsonl.gz` 随新增用例重录（417 条）。GD 全量未跑（Kevin 09-19：只在推 main / 发版前跑）。

## 2026-09-19 · 热更补丁 202609190534（基线 client-2026-09-19-3）：#46 回程票落盘

Kevin：「后续如果可以热更就尽量热更哦」。从此小修走补丁：从最近发版 tag 另起 `hotfix-client-2026-09-19-3` 分支（worktree `../Cell-War-hotfix`）cherry-pick 修复提交（5402eeb → 6f3d9da），`tools/build_patch.sh client-2026-09-19-3` 打出 3 个文件 72 KB 的补丁，跨基线闸过。**别从 main 打**：main 上教程 v2 在制的活会一起进包。能热更的判据：只动 `game/**` 脚本 / 数据、没有新 `class_name`、没动 boot / patch_state / project.godot、没升协议；规则改动另跑 `deploy_server`；网页版不吃补丁、要更新就从 main `deploy_web`。

## 2026-09-19 · issue #46 续：回程票落盘（Kevin「落盘吧」）

回程票原先只记在 `CWOnlinePanel._resume` 上 —— 面板跟着主菜单活着、比那条连接长命，可**比不过一次运行**：
关掉客户端再开票就没了，而服务器那一席的令牌（`CWRoom.seats[i].token`）还好端端留着。

- **落在 `CWSettings.resume`**（`settings.cfg` 的 `[online]` 段加一键 `resume`，值就是那张票 `{code, token, url}`）。
  **旧 settings 没这一键也能起**：`get_value` 缺省 `{}`；存成了别的类型也当没有票 —— 宁可少一张，别拿垃圾去 `reconnect`。
- **读回来那一下就是「进联机页」**：联机面板是点「联机对战」才 `new` 的（`CWMainMenu._open_online`），
  所以 `_ready()` 里一句 `_resume = CWSettings.resume.duplicate()` 正好赶在任何一处读票之前。
- **写票收在一处 `_set_resume()`**：抄席（`_remember_seat`）/ 用掉（`"room"` 分支回到自己那一席）/
  作废（自己发起的 `reconnect` 被答 `bad_token` / `no_room`）三处都走它，盘面跟着一起改。
  漏一处，下次开客户端就会照着盘上那张过期票预填房间码。
- **读票那四处一个字没改**（`_hint_resume` / `_lobby_join` / 大厅行文案 / `"room"` 分支）：判据仍是 `_resume_here()`
  —— 票非空 **+ 服务器地址对得上**。落盘只是让票活得久一点，认不认还是认地址；
  票在盘上躺一整晚、第二天连到别的服务器，照样不预填、不改行文案、也不作废。

**怎么验的**：`--only=t_net_resume,t_settings` —— 52 项检查全绿（`t_net_resume` 单跑 24 项也绿，不靠测试顺序）。
`t_net_resume` 加三条：抄席之后把 `CWSettings` 的内存值打乱再 `load_prefs()`（= 关掉客户端再开那一下），
新建的面板从盘上读回同一张票；票用掉之后再读盘是空的；从盘上读回来的票换一台服务器同样不认（`_resume_here()` 为 false）。
收尾照 `t_settings` 的规矩把 `settings.cfg` 删掉 —— 留着会让后面建面板的测试莫名其妙预填一个房间码。
反向验过：`_ready()` 那句读盘换成 `_resume = {}` + `_set_resume` 不落盘 ⇒ 前两条当场红；
只把 `"room"` 分支那一句换回 `_resume = {}` ⇒「盘上也清了」当场红。

协议件与 `game/scripts/core/**` 一字未动，`CWNet.NET_VERSION` 仍是 **31**。

## 2026-09-19 · 意图级 AI「动画乱套 / 重复播放」根因：试走跑在真 game 上（不是线程）

Kevin：「现在的意图级 AI 有问题，会导致动画乱套或重复播放。我怀疑是 AI 线程阻塞的问题」。查下来**不是线程**（MechBridge 在主线程同步跑，没有线程）：PR #59 的 `MechBridge.ask` 把 `MechIntent` 的「快照 → 试走 → 读数 → 回滚」直接跑在**真 game** 上（`intent.best_by(game, …)`），每个候选路径的每一步 `g.step()` 都是真步 —— 内核消费者把它推成 roll / result / fx / feed 条目、界面照演，回滚之后真的那一步又演一遍 ⇒ 乱套、重复；日志与出牌列也被假动作填满（变异实测：一问 25 条 log_line）。MC / MCTS 桥从来是从快照造一份 `sim_quiet` 的独立副本（`CWMonteCarloBridge._build_image_static`，陪练全同步启发式）再推演。改法：`MechBridge.ask` 同一条路 —— 造副本、`best_by(image, …)`、`image.dispose()`；确定性不变（副本 rng 随快照复原）。护栏 `t_mech_bridge_quiet`（8 项：合法下标 / 真局面哈希不变 / 日志与 log_line 零 / feed_log 与 feed_seq 不变 / pending 仍是同一问 / sim_quiet 没被波及 / 两次答案一致），变异（改回真 game）当场红。只跑了相关测试（Kevin 09-19 口径），随 client-2026-09-19-3 出去。

## 2026-09-19 · 修 #61：坏死膜不再盖住特殊组织的图标

**现象**（Kevin 报）：「坏死贴图盖住特殊组织纹路，当骨髓 / 代谢核心进入坏死状态后，骨髓的卡牌 icon
和代谢核心的闪电 icon 会被盖住」。那一格当场退化成一块没有身份的灰地 —— 「这里原来是什么」读不出来了。

**根因**：特殊组织的图标是**烤进地块贴图里**的（`energy_normal.png` / `marrow_normal.png` 那几张，
闪电 74 px、卡牌框 66 px），而坏死膜（`board.gd:_necrosis_film`，issue #15 起）是**整格一张**
盖在地块贴图上的 Sprite。于是膜一盖，图标连同底色一起没了。
（积累进度那圈反倒还在：它是格子的**子节点**、相对 z = 1，本来就压在膜上面 —— 这也正是「只有 icon 没了」的原因。）

**改法**：在膜上给图标**留个洞**（按像素位置镂空），而不是另起一层把图标重画一遍。
`_necrosis_film()` 加一个 `special` 参数，核心 / 骨髓各烤一张镂空版，`set_necrosis()` 按
`CWData.special_of(c)` 挑张；洞底下露出来的就是地块贴图自己的那几十个像素 ——
所以**空仓 / 待结算 / 固化七档**的图标各是什么样，这里一概不必知道，永远跟着 `set_tissue()`
当前那张走，也不必每帧同步。膜照旧盖满其余部分（顶面 + 两片侧面），坏死一眼还是认得出来。

图标的像素位置从**健康版**贴图上量（`_icon_pixels`）：顶面上凡是与该格顶面底色不同的像素就算图标，
底色取顶面上最多的那个颜色。**不能拿当前那张贴图现量** —— 固化族的顶面是一片结晶花纹，
现量会把花纹也当成图标镂掉。各变体的图标位置与健康版逐像素一致，本次逐张核过。

**只镂核心和骨髓**：血管那个菱形图标占顶面 144 px（四分之一张脸），镂了就不像坏死了；
而 #61 点名的正是这两种。要不要连血管一起镂，**等美术 / Kevin 说**。

**改的文件**：`scripts/ui/board.gd`（`_necrosis_film` 分张 + 新增 `_icon_pixels`，`set_necrosis` 挑张）。
`scripts/core/**` 一字没动，`match.gd` 不用改（膜是棋盘自己的事）。

**怎么验的**：
- 无头 3 条断言，补进 `t_issue31_fx` ③（坏死膜口径本来就钉在这条里）：① 膜按格子的特殊组织挑张
  （核心 / 骨髓各一张、普通格还是原来那张）；②③ 核心 / 骨髓各一条逐像素 ——
  图标那些像素膜里一笔不画（且**原来那张确实盖着**，这一半是防回归的），其余像素与普通格那张逐像素相等。
  「洞里露出来的确实是图标」用的是独立判据：癌变只换底色和侧面、图标色一点不动（2026-09-14 定的口径），
  所以「健康版与癌变版同色」把图标和其余部分分得干干净净 —— 核心 83 个镂空像素里 74 个、骨髓 73 里 66。
  只跑了相关的 7 支（`t_issue31_fx` / `t_skill_fx` / `t_solid_tissue_art` / `t_board_*` / `t_store_ring` /
  `t_board_active_tiles`），134 项全过；按 Kevin 09-19 的口径没跑全量。
- 真机一帧在 `截图_2026-09-19_issues_fx/issue61_坏死不盖图标.png`：代谢核心与骨髓各一格癌变 + 坏死，
  闪电与卡牌框照常露在灰地上；旁边两格普通坏死整格灰（没变），再给两格没坏死的核心 / 骨髓做对照。
  出图脚本 `tests/preview/preview_necro_icon.gd`（新增，**不能加 --headless**）。

## 2026-09-19 · 全量发版 client-2026-09-19-2（914c1f4）：删旋钮 + 协议 p=3 / NET_VERSION 31、13 条 issue、三个 PR、教程 v2 底座（入口仍灰）

Kevin：「重新发版吧，我们现在要进行测试」「能否把当前规则先热更或发版，我们急着测试」。补丁走不了（PR #59 与教程 v2 都带新 class_name，跨基线闸拦），所以全量：`BASE_BUILD` 202609182322 → **202609190519**；发版前全量一次（3938 项，唯一红 = `t_tutor_hooks` 的 class_name 例外表没登记 S3 的皮 A，加一行）；Win / mac 导出 → `publish_release.sh` 打 **client-2026-09-19-2**（第一次被仓库根下 S3 agent 误放的截图目录判「工作树不干净」拦下，挪到仓库外再发）→ `deploy_web.sh` → `deploy_server.sh`（排空后切 v31；老 v30 客户端从此被拒，必须换包）。以后的热更补丁从 **client-2026-09-19-2** 这个 tag 另起分支打。教程入口仍灰（新版只做到第一～三关 + 常驻壳 + 皮 A，第四关起在做）。

Kevin 同日对 issues 遗留的四答：#46 回程票**落盘**进 CWSettings；#44 掉线期间被代打那一路**另开 issue**（已开）；#54【放疗】区域内**只转癌组织、固化不转**（现实现把固化也转了，要改，两侧 + L0）；#53 分化交叉淡入 1.65 s 与 #52 叠演**暂时不动、先看效果**。前两条本日落地。

## 2026-09-19 · 新手教程 v2 · S3：皮 A 贴身气泡落地 + 教程镜头（PRD 04:08 版新增）

Kevin 09-19「**整体走 A**」—— S2 文件头里那句「两个都不好 ⇒ 两版皮都否」是误读，实际只否了自动重置那一帧。本片把方向 A 从出图脚本搬成产品代码，并接下 PRD 04:08 版新加的**镜头变化**与**通用规则 13**。

**`cw_tutor_view_bubble.gd`（新，皮 A）**

- **台词贴着细胞说**：`say` 每帧重算锚点 —— `who == "ui:<id>"` 挂控件矩形上方（问提亮层的 `rect_of`），其余问装配方注入的 `speaker_of`（`who` → `{at, immune}`，**皮不认识镜像**，同 `reveal_tiles` 那条 Callable 的办法）。描边取说话人的阵营色，尾巴是 13×7 的像素三角（烤图 + NEAREST，`flip_v` 换朝向），上面塞不下就翻到目标下方。
- **一句一泡**（真机改的）：第一版按占位皮的口径把一段 `lines[]` 摞进一只泡，第一关那个 4 倍机位下气泡有 175px 高，**把主角细胞整个盖住** —— 方向稿 A 的第 ② 帧只画了一句，所以纸面上看不出来。改成一句一泡、原地换（A §1「不摞第二个泡」），`auto:true` 的一段仍整段画完（玩家没有翻页的机会）。
- **气泡高度按 Label 真排出来的行数算**：`get_multiline_string_size` 的折行标志与 `AUTOWRAP_WORD_SMART` 不是同一套，估少了「继续 ▸」会压在末行上（真机第一版就是）。
- **`hint()` 头一行就是 `_clear_say()`** —— Kevin 那条「提示行一出现就把台词收掉」。提示行是一条居中的垫块（88,424,600,34，下缘让开 `CWActionBar.PROMPT_RECT`），**不做成气泡**：气泡是「有人在说话」，提示行是「轮到你动手」，同一种画法玩家会分不清谁在等谁。
- **控件旁 tip 小气泡只给 `tip` 非空的那种**：`point` 覆写把 `tip` 从提亮层摘过来自己画（提亮照旧交回提亮层，按钮**保持单线原样只慢闪**）。剧本里只有第六关「点击结算【微环境压迫】」是这一种。
- **「……」= 三颗渐显的点**（一拍 0.35 秒、停 0.6 秒）。这一段是协程，而下一句 / 提示行 / 拆局随时会把它掀掉 —— 每段一个代号（`_clear_say` 换号），号对不上就自己退场；**`is_instance_valid` 必须排在 `as` 转型前面**，对着已经 free 的对象做 `as` 本身就报错。
- **图鉴解锁 = 右上角滑入的小卡**：196×46，滑入 0.25 / 停 2.2 / 滑出 0.22，连着几条往下摞，走掉一张下面的补位；右缘跟着右栏走（无右栏 950、有右栏 686）。卡面的名字走图鉴现成的对照表（`codex_map.json` → `CWCodex`），表里没有就原样写 id —— **不自己编名字**，图鉴映射是 S6 的活。
- 换皮开关 `CWSettings.tutor_skin`（`bubble` 缺省 / `plain` / `tally`），**不上设置页**：它是做教程时现场 A/B 切的旋钮，不是玩家偏好。`toast.keep_out` 归皮挂（接口纪律 3），贴身气泡那版只有提示行一条常驻件。

**S2 留的两问，本片定了**

1. **章节提示补一条整幅横带 + 幕布 0.88 → 0.94**（`cw_tutor_chrome.gd` 三行常量）。B 的两道横线没有解决「棋盘正中的主角细胞从大字底下透出来」，A 的解法是横带；两条都要 —— 只抬 alpha 仍压不住。横带上下缘正好落在 B 那两道横线上，所以**看起来仍是 B**。对拍见 S2 的 ① 帧（「一」整个糊掉）与本片的 ① 帧。
2. **副标照 B 的表落 silkscreen 不动**：Kevin 拍板「章节提示用 B 的样子」时看的就是那张图，理由那句对不上不改变他看到的东西。

**教程镜头（PRD:9-22「镜头变化」，S3 接）**

数据字段 `ui.camera = {"anchor": "map"|"player", "align": "center"|"left"|"right"}`，缺省「地图调中」；两个枚举与缺省值住在 `cw_tutor_beats.gd`（**校验器与层表读的是同一份**），层表因此从 11 层变 12 层，`off_value` 认 Dictionary ⇒ **`"*": false` 关不掉镜头**（镜头永远存在，只会退回缺省）。校验器加第 ⑭ 条，装载期就红 —— 写错一个字在真机上只表现为「镜头没反应」。

机位算法在 `cw_view.gd`（全工程唯一一台相机的那个文件）：zoom 按**活跃格集合**的包围盒塞进镜头可用区，封顶 4.0（再近能看出贴图插值）、不低于对局机位；**整盘直接给 `GAME_*` 那三个数**，第三关（推近）切第四关（整盘）不漂一两像素。看点按 anchor 取地图包围盒中心或玩家那一格，align 把它放在镜头横向的 1/3、1/2、2/3，**竖直恒取 `GAME_ANCHOR.y`** —— 小棋盘与整盘机位竖向对齐，切关时不上下跳。关首 / 重置直接就位（通用规则 2：关间静默切换，没有过场可给镜头飞），关内换 step 与地图浮现补间 0.45 秒，插的是**取景参数**不是相机 position（`blend_to`，理由同 `blend`）。

**通用规则 13（PRD:65，禁止向镜头外 / 被镜头框切割的格子迁移）**：纯函数 `CWView.tile_fully_visible(camera, board, at, view_rect)` —— 这一格顶面的外接矩形要**完全**落在镜头矩形里，擦着边也算切到。两个消费点共用它：`match.gd` 合并 `bridge.marks` 时滤掉（**不画成可选**），`ui_bridge` 的两处点击闸再问一次（**每次现问** ⇒ 镜头补间没走完就还没放开）。正式局不注入那条 Callable，一格都不滤。

**改判表**：一条 `check()` 都没删、没改判。新增全是新增断言。`CWTutorLayers.DEFAULTS` 多一层 `camera` ⇒ `t_tutor_flow` / `t_tutor_data` 里按层数写的断言全部原样绿（它们数的是 `on()` 的结果不是键数，已确认）。`cw_tutor_view.gd` 加了一个注入位 `speaker_of`，**九个方法的签名一个字没动**，`t_tutor_view` 那条源码扫描照旧成立。

**测试**：新增 `t_tutor_view_bubble`（25 项）/ `t_tutor_camera`（15 项），已登记进 `_run_all` 那份手写清单；`t_tutor_data` 加 7 项（第 ⑭ 条镜头判据的正反两面）。全量 **3654 项绿 / 231 个测试**（基线 3607 / 229）。

**真机八帧**在 `截图_2026-09-19_教程v2_S3/`：① 章节提示（横带版）② 关首台词气泡 ③ 图鉴解锁小卡 ④「……」三拍 + 第二张卡 ⑤ 提示行 + tip 小气泡 + 【迁移】亮相 ⑥ 同一颗【迁移】的暗相（相隔 1.6 秒 = `HALO_PERIOD` 的半周期，轮廓线宽与颜色一字不差）⑦ 镜头「角色调左」⑧ 镜头「角色调中」。**步骤串**（前置：`main_menu.gd` 的 Guide `enabled` 临时改 `true`、`guide_progress.cfg` 的 `done` 改 0 并清空 `unlocked`，**三处都还原了、都没提交**）：

```
"D:/Godot/Godot_v4.5-stable_win64.exe/Godot_v4.5-stable_win64_console.exe" --path game \
  --script res://tests/screenshot.gd -- "user://s3.png" "res://scenes/Main.tscn" -1,-1 \
  3.6,5.2,6.9,7.6,10.2,11.8 \
  "key:ui_up@0.4;key:ui_up@0.6;key:ui_up@0.8;key:ui_accept@1.0;call:CWTutorViewBubble:advance@5.8;call:CWTutorViewBubble:advance@6.1;call:CWTutorViewBubble:advance@6.4;call:CWTutorViewBubble:advance@6.6;call:CWTutorViewBubble:advance@7.0;call:CWTutorViewBubble:advance@7.3"
```

- **一句一泡 ⇒ 六次 `advance`**（第一关三段台词共 6 句），不是 S2 那样三次。
- ④ 帧的「……」与 ⑤⑥ 帧的 tip 小气泡**在今天的关卡数据里都还没有**（「……」属间章 S9、`tip` 属第六关 S10/S11），拍图时给 `c1_l1.json` 打了临时补丁（加一条 `say:["……"]` 与 `point.tip`），**拍完原样还原、没提交**；⑦⑧ 帧同理（临时把活跃集铺成两排 10 格、`ui.camera.anchor` 改 `player`）。
- **清 `unlocked` 才拍得到图鉴小卡**：解锁点是集合语义，第二次跑同一关 `added` 是空表、一张卡都不出。

**留给后面的**

- **S4**：`c1_l2` / `c1_l3` 的 `ui.camera` 按 PRD 写（第二关 Step1/Step2「地图调中」PRD:155/:203，第三关「角色调左」PRD:249）；字段名与本片一致，别另起一个。
- **S12 收口**：① 方向稿 A 自己预告的风险一 ——「棋盘大、细胞密时气泡会压到格子」在第七关（zoom 1.6、细胞在正中）必然发生，一句一泡只缓解不根治；② 长句仍会折两行（剧本侧的 14 字上限没人管）；③ 占位皮 P 留不留当兜底；④ `ui_bridge` 那条 `tile_visible` 在第七关的技能目标上要再核一遍（`skill:<名>` 的矩形还是零矩形）。

## 2026-09-19 · 新手教程 v2 · S4：第二关 + 第三关（净化 / 攻击判据、自动重置与劝重置、能量 6.6 引擎现算复核）

第一章三关端到端跑通。数据照《新手引导v2_盘面提案》**v4**，能量从 v3 的 6.1 重算成 **6.6**，两支新测试把这个数钉死。

**新建 / 改了什么**

| 文件 | 干什么 |
|---|---|
| `data/tutorial/levels/c1_l3.json`（新） | 第三关 ATP（PRD:239-301）。37 格三排横带 + 9 格「凸」癌块、能量 66、带子 `[[1,6,3]][[1,6,1]][[1,6,6]]`、`reset_when: stuck`、`advise_when: low_energy_beside:35` |
| `data/tutorial/levels/c1_l2.json` | S1 的占位关整份重写成第二关 癌（PRD:149-237）。Step1 亲手净化两格、Step2 **预置 + `reveal`** 揭第 7 格与那只 1.0 能量的癌细胞，**零关内重装** |
| `data/tutorial/levels/c1_l1.json` | 只改坐标：`(-1,-1)(0,-1)` → v4 的 `(-5,-1)(-4,-1)`（理由见下「一处越界改动」） |
| `data/tutorial/index.json` | 登记第三关；`_doc` 补一条「三关连着一条教学带」的坐标口径 |
| `scripts/kernel/cw_tutor_beats.gd` | **修一条哑谓词**：`tile_healthy` 读的是 `m.tile(at)["state"]`，而镜像格字典的键叫 `tissue`（`cw_mirror.gd:124` 一带）⇒ 这条谓词**永不成立**。改读 `tissue` 并加了盘外保护（`m.tiles.has(at)`，取不存在的键会当场报运行时错）。钩子那一侧的 `ctx.read("tile")` 是**另建**的 `{state,type,solid}` 投影，两处不是同一张字典 |
| `scripts/tutor/cw_tutor_layers.gd` | 加第 12 层 `camera`（PRD 04:08 版新增的镜头一族）。值 `{anchor:"map"|"player", align:"center"|"left"|"right"}`，缺省 map/center；通配 `"*": false` 把它铺回**缺省**而不是 false —— 镜头没有「关」这一档。**实装在 S3**，这里只开白名单 + `camera()` 读口 |
| `tests/headless_test.gd` | 两支新测试 `t_tutor_c1`（75 条）/ `t_tutor_energy_formula`（10 条），已登记进 `_run_all` 手写清单；`t_tutor_view` 那条闸的坐标跟着第一关改 |
| `docs/新手引导v2_实现方案.md` | §2.8 样例 B 与 §4 / §6.1 的 61 → 66、2.5 → 3.0、6.1 → 6.6；§4 抬头加 PRD 行号基线说明；§6.3 实测表第 4 / 7 两格从「待 S4」改成实测 |

**能量账（v4，逐项由引擎现算复核）**

```
6.6 = 3.0 最省路 + 3.0 攻击三次 × 1.0 + 0.5 失效自损 + 0.1
      └ 两条并列：直线 (1,-1)(2,-1) 健康 + (3,-1)(4,-1) 癌 → 落 (4,-1)
                  绕 r=-2 排 4 格健康 + (5,-2) 癌        → 落 (5,-2)
```

- 最省路不是人算的：`t_tutor_energy_formula` 用 `game.tune.immune_move_healthy/cancerous` 跑一遍 Dijkstra，得 **3.0 且恰好两条并列**，再用引擎的 `quote_path` 复核这两条（都 3.0、都走得通、路上一分核心收入都没有），`pass_through_map` 为空 ⇒ 借道给不出更便宜的走法。
- **判据判「到相邻格」不判路径**（`state: beside`）：两条并列都算对。
- 劝重置阈值 **35** = 攻击三次 30 + 反弹 5，把最省（剩 36，不劝）与次优 3.5（剩 31，当场劝）分得干干净净。
- 伤害 1.0 + 0 + 2.0 = 3.0 第三次刚好打死；`ATTACK_MAX_PER_TURN = 3` 锁死带子长度 ⇒ 打不出第四次、带子永不 overrun。

**实跑出来的两条引擎口径**（写代码前没人算到，这里记死）

1. **攻击得手会把攻击方挪进那一格**，并顺手【净化】那一格（`cw_actions._do_move` 的致死分支 → `enter_tile`）。于是第二关打死癌细胞之后玩家站在 `(1,-1)`，而第三关起点照 v4 是 `(0,-1)` —— **切关时退一格**。改成 `(1,-1)` 起的话最省路少一步、能量回落到 6.1（= v3 那个数），与 v4 拍的 6.6 冲突 ⇒ 先照 v4 走，这一格记在账上（要 hxr 答）。
2. **攻击成功本身也给 +1 抗原记忆**。第二关跑完 `memory == 4`：亲手净化两格 +2、攻击得手 +1、击杀进格再净化 +1。

**一处越界改动（请复核）**：第一关那两格由 `(-1,-1)(0,-1)` 改成 v4 的 `(-5,-1)(-4,-1)`。S1 照的是方案 §2.7 样例 A（v4 之前的稿），而第二关起整条教学带都长在 v4 那套坐标上（`r=-1` 排 `q -5…1`、免疫承接第一关终点 `(-4,-1)`）。不改的话关间**静默**切关那一瞬间玩家细胞要跳 4 格，而第一关的终点 `(0,-1)` 在第二关是**癌组织**。改动只有三个坐标 + `t_tutor_view` 里那条闸的字符串。

**PRD 04:08 版又动了**：hxr 在本片开工后推了一版（514 → 532 行），给关卡模板加了镜头一族（PRD:9-22「地图调中 / 左 / 右」「玩家调中 / 左 / 右」），并在各关「界面变化」里各插一行（第二关 :155 / :203「地图调中」，第三关 :249「角色调左」）。⇒ **所有旧行号整体下移**（第二关 139→149、第三关 223→239）。本片两关的 `prd` 按**新基线**写，方案文档与第一关的行号仍是旧基线，已在 §4 抬头标明「下一次通读时整批重编」，对账闸（判据 ⑬）等那一次一起实现。

**测试**：`t_tutor_c1` 75 条 / `t_tutor_energy_formula` 10 条。前者三关逐关真跑（真盘面、真规则、真带子，玩家那几步由剧本自己的 `allow` 现场过闸），盖住：带子双向归零 ×3、Step1 净化的两格在 Step2 仍健康、**Step1 期间那只癌细胞节点 `visible == false`**（这一条真起 `Main.tscn` 的教程局正面断言）、`reset_when` 命中时 `reset_anim` 排在 `want_reset` **之前**、重置回关首那份 world（`dump_world ≡ minify`、人回 `(0,-1)`、能量回 6.6）、劝重置命中时**不重置也不翻页**、第一～三关的 `allow` 里没有 `act=end` 也没有敞开的自由段。闸三 `total_sites` 3292 → 3332（+40，全落 `testinfra`/`guide` 档，`core` 分子分母一个数没动）。

**真机三帧**在 `截图_2026-09-19_教程v2_S4/`：① 第二关净化（踩上癌组织当场转健康）② 第三关攻击失效 + 反弹（能量 2.6 → 1.1）③ 第三关劝重置（走次优路剩 3.1，提示行换成劝退句、左上角「重置本关」慢闪）。前置：`main_menu.gd` 的 Guide `enabled` 临时改 `true`、`guide_progress.cfg` 的 `done` 临时改 1 / 2，**两处都还原了、都没提交**。

**踩到的三个坑（下次照这个走）**

- `guide_progress.cfg` 用 python 改完**必须留结尾换行**：少了那一个 `\n`，Godot 的 `ConfigFile.load` 直接失败 ⇒ `done` 读成 0 ⇒ 教程从第一关开，而且**一声不吭**。前后两小时里因为这个白跑了五六趟。
- 行动栏那颗按钮**合成点击点得动**（`screenshot.gd` 的 `x,y@t`，2026-09-05 那条「点不到 Control」的账可以翻篇了），但**一步一等**：说话那一拍要两次 `advance`（`say` 与随后的 hint 各一拍），按钮要等行动栏真建出来（≈1 秒），迁移落点在棋盘上点、隔 0.8 秒一步，攻击结算要等 3~4 秒。
- 棋盘格 → 屏幕坐标是**固定**的（`_look(1.0)` 的镜头与关卡无关）：`(0,-1)` = (365, 240)，同排每 +1 q 加 46.2px，每 +1 r 加 (23.1, 25.4)。

**留给 S5 / S3 的接缝**

- **S3**：`ui.camera` 的白名单与读口（`CWTutorLayers.camera()`）已开，第二 / 三关的数据也按 PRD 现文填好了，剩镜头本身与通用规则 13（禁止往镜头外的格迁移）。
- **S5**：第四关起手格照 v4 是 `(4,-1)`（第三关走直线那条最省路打完站的那格）—— 但**走另一条并列路的玩家停在 `(5,-2)`**，Q4-5 那条要 hxr 定一个。
- **右栏还多一块**：第三关的右栏除了 PRD:247 要的「状态框 + 抗原记忆框」之外，还留着「癌性加权」那条胜负进度（见真机第 ②③ 帧）。要藏它得加第 13 层 + 动 `match_panel` 的版面算法（`_score_top` / `rows_top` / `level_bottom` 三处连着 `t_match_panel` 的护栏），**本片没动，单列请示**。

﻿## 2026-09-19 · issue #48 / #52 / #53 回炉：交叉淡入淡出补上、血流对时改锚绝对时刻、遮罩外不演

复核把上一版的六条挑出来，逐条落地（分支先 `git rebase origin/main`，教程组把 `CWGuideLayers`
改名成了 `CWTutorLayers`，`match.gd` / `match_panel.gd` 两处调用跟着改）。

### ① #53 ⑤「和 html 中的虚化效果不符」上一版读反了方向（high）

- **现象**：上一版在 `CWPix.burst` 上加了一档柔边外圈（`soft_px`），并在日志里写「仓库里没有那张原型页」。
- **根因**：**原型页就在仓库里** —— `tools/art-preview/index.html` + `common-skills.js`，`skill_fx.gd`
  自己的头注第 22 行就写着 `differentiate → common-skills.js differentiate v2「粒子重组」`。
  原型第 67 行那口粒子 `burst(c,a.x,a.y-5,p,cyan,9,20,true)` 走的是 `pixel()` **硬方块**
  （`draw.js:64-68`），和改动前的 GD 版逐参数一模一样；真正「虚化」的是第 59 行 ——
  **细胞贴图的交叉淡入淡出** `fade(c,1-p,()=>cell(c,'immune',q,0)); fade(c,p,()=>cell(c,name,q,0))`
  （`fade` 就是 `globalAlpha`，`draw.js:8`）。加外圈反而比改前更偏离原型，而原型真正有、GD 没有的
  那件事（分化时旧形态淡出、新形态淡入）一行都没做 —— `_apply_immune_art` 一直是瞬间换图。
- **改法**：
  - `CWPix.soft_px` / `SOFT_A` / `burst(..., soft)` 整条撤掉，三处调用（`_differentiate` /
    `_revive_immune` / `_revive_cancer`）回到硬像素。
  - 新 `CWMatch.cross_fade_art(s)`（静态）：旧贴图挂成真身的**子节点**（位置 / z / 同格错位 /
    被伪足拉着走全自动跟随），两层各走 **`self_modulate`** —— 父节点的 `modulate` 会乘到子节点上
    （`_pop_in` 用的就是它），拿它做交叉会把旧层一起压暗、交叉变成一起淡出。时长 `ART_FADE` 1.65 s
    对齐 `CWSkillFx.DURATION["differentiate"]`（原型里粒子与交叉共一条 `p`）。
    `_animate_breath` 一并喂淡出层的呼吸帧，否则那 1.65 秒两张图各呼各的。
  - `_apply_immune_art(s, itype, cross)` 多一个参数：`cross = not became_alive` ——
    刚落子 / 刚复活那一次直接上图（那是「凭空出现」，走 `_pop_in`）。
- **怎么验的**：`t_issue_fx_0919` 新 7 项（旧层规格 / 起手 alpha / 中途两层之和恒为 1 / 淡完收起 /
  真 `CWMatch` 上改 `itype` 后长出 `ArtFade` 子节点）；动图 `issue53_05_分化_交叉淡入淡出.gif`。

### ② #53 ④ 的「到目标后放大出现」时刻算错（medium）

- **现象**：细胞比血流的落地爆晚 0.45 s 才凝出来，中间空一拍。
- **根因**：`lag = HOMING_LAND - HOMING_LAUNCH`（固定 1.25 s）是**相对残影开始**的偏移，
  而残影开始的那一刻并不是血流出发那一刻：`cw_actions.gd` 先 `game.fx("homing")` 入队 →
  桥 `BLOCK_FX_MS["homing"]` 阻塞 500 ms → 才轮到 step_end 的 sync，镜像差分认出这一跳时
  `homing_elapsed` 已经 ≈0.5 s，`delay` 被 clamp 成 0，真身在 0.5+1.25=1.75 s 凝出，而血流
  落地爆在 1.30 s。问得越晚偏得越多。
- **改法**：两个量都锚到**绝对时刻**，抽成静态纯函数 `CWMatch.homing_teleport_timing(homing)`
  → `[delay, lag, shrink]`：`delay = maxf(HOMING_LAUNCH - homing, 0)`、
  `lag = maxf(HOMING_LAND - homing - delay, CWTeleportFx.LAG)`；不是血行转移时第一个返回 -1
  （别动外面按环算好的那个 delay）。
- **怎么验的**：`t_issue_fx_0919` 四项取值断言（`homing=0` → `delay+lag == HOMING_LAND`；
  `homing=0.5` → `delay+lag == HOMING_LAND-0.5` 且 `delay==0`；血流早落地了至少留 `LAG`），
  不再靠源码文本 grep。

### ③ #48 能量飘字不受活跃格遮罩约束（medium）

- **现象**：教程里遮罩外那只还没揭示的细胞一有收入 / 伤害，空白遮罩区就飘出一个 ±数字
  （`_energy_fx.z_index = Z_OVER_BOARD`，压在所有东西之上）。`_sync_tiles` 的
  `solid_form` / `solid_break` 同理。
- **改法**：飘字那条加 `and board.is_active(pos)`；`solid_changed` 收集那条加 `and board.is_active(c)`。
  两处的 `_last_energy[i] = e` / `_last_tissue[c] = now_solid` 都**照旧在条件外更新** ——
  跳过差分基准的话，揭示那一刻会把攒下的那一跳补演出来。
  `solid_changed` 那条用 `and` 而不是复核建议的 `continue`：这个循环下面还要画这一格。
- **和复核建议的一处出入**：飘字判的是 `board.is_active(pos)` 而**不是** `node.visible`。
  后者还含「扑咬 / 普通攻击由演出层代画」那一段（`node.visible = false`），而
  `BLOCK_FX_MS["immune_attack"]` 只阻塞 450 ms、动画要演 0.66 s —— 镜像差分正好落在代画那一段里，
  拿 `node.visible` 去判会把**攻击伤害**这条最该演的飘字整条吃掉（而且 `_last_energy` 照旧更新，
  吃掉就是永久丢失）。要的是遮罩，就只判遮罩。
- **怎么验的**：`t_issue_fx_0919` 里一只真 `CWMatch`（`Main.tscn` + 镜像差分）跑六项：
  首帧只记不演 → 揭示过的格飘一条 → 遮起来再改能量不长第二条 → 揭回来也不补演；
  固化同法两项。

### ④ 交给 Kevin 的动图里 ⑧ 演的不是实装行为（low）

`preview_fx_0919.gd` 的起手偏移写的是 `get_height() / 12.0`（≈2.8 px），实装走
`CWUIBridge.show_beam → CWMatch.cell_half_height` = `get_height() / 2.0`（≈17 px）——
动图里光芯几乎贴着胞体中心出，实装是从轮廓上离开，正好是这条 issue 要看的那一点。改成 `/ 2.0`
并重出 `issue53_特效八条.gif`；`t_issue_fx_0919` 加一条「预览里不许再出现 `/ 12.0`」的回归闸。

### ⑤ #53 ④ 那半条既没有图也没有测试（low）

`fx53` 场景补一只 `CWTeleportFx`：血流在 1.20 s 入队、1.70 s（= 桥阻塞 0.5 s 之后）才认出这一跳，
`delay` / `lag` / `shrink` 三个参数**全部走实装的 `CWMatch.homing_teleport_timing`**，动图里
看到的缩小 / 放大时刻和真机一致。⑤ 那一拍也从「手点一个 `play`」改成真换图（2 号细胞起手是
通用免疫细胞，当场分化成 B 细胞）。新出两张动图：`issue53_04_血行转移_缩小消失与放大出现.gif`、
`issue53_05_分化_交叉淡入淡出.gif`。

### ⑥ 基线落后 + 两支重复的拼图工具（low）

- `git rebase origin/main`（`e492f9a`）：五处冲突全是教程组那批 —— `match.gd` / `match_panel.gd`
  的 `CWGuideLayers` → `CWTutorLayers`、`headless_test.gd` 的 `_run_all` 清单、`xcheck/COUNT`、
  开发日志各取一边。
- `tools/make_gif.py` 与教程组 `e4ed4e9` 的 `tools/make_fx_gif.py` 是同一件事（都是 Pillow 拼 GIF，
  只差**按目录**还是**按帧前缀**找帧）。**留 `make_fx_gif.py`**（origin/main 上已有、教程组的文档在引它），
  把 `--fps` / `--crop` / `--hold` 与 `--from-prefix` 并进去，`make_gif.py` 删掉。
- 工作树里 30 个 `*.import` 的 CRLF/LF 噪声（`--import` 留下的，无内容差异）`git checkout --` 还原。

### 上一版日志里的两处事实错误（已改）

- 「仓库里没有那张原型页」→ 改成指向 `tools/art-preview/common-skills.js:59`。
- ⑤ 的改判表那一格「`CWPix.soft_px` 柔边」→ 改成「贴图交叉淡入淡出，粒子照原型是硬像素」。

### 验的

`--only=t_issue_fx_0919` 55 项绿（上一版 38 项）；收工前 `bash tools/run_tests.sh` 全量 + 闸三。
`xcheck/COUNT` 跟着 rebase 后的基数重写（`total_sites` 3251 → 3268，`core_sites` / `covered_sites`
一个没动 —— 新断言全在 ui 档）。

## 2026-09-19 · 新手教程 v2 · 盘面提案 v3（Kevin 第二轮四条意见）：**第四关一次揭到整盘**

## 2026-09-19 · issue #54 / #55 / #56 复核回炉（六条 findings 逐条落地）

上一轮（`6aa9ad9` / `2337e07` / `ec37b36`）交付后复核提了六条：两条 medium、四条 low。
这一轮逐条改掉，**规则数值一个没动**（`prd_crosscheck` 仍是 154 全对、`gen_card_data --check` 仍绿）——
改的是越界的文件、AI 侧漏掉的一处镜像、四处与代码对不上的说明文字。

### 一、越界：碰了禁区 `game/data/tutorial/**`（medium · #55）

**现象**：`2337e07` 把五份教程关卡 json 的 `expect_level_tiers` 从 `[0,10,30,70]` 改成 `[0,10,30,120]`。
任务书明写「教程数据（`data/tutorial`）正在重做、别碰」。
**根因**：那五份数据自带一条校验（`cw_tutorial_data.gd` 的判据 ⑧）拿 `expect_level_tiers` 现对
`CWData.level_min_memory(seats)` —— #55 一抬门槛，五关的 `validate()` 当场全红。
当时为了让**本分支**的套件绿，顺手把改动做进了禁区。
**为什么非退不可**：`origin/main`（9a20167）已经把老教程整批推倒 ——
`c1_l3` / `c2_l4` / `c2_l5` 在 main 上**已删除**，`c1_l1` / `c1_l2` 被整份重写，
`git grep expect_level_tiers origin/main -- game/` **零命中**，`cw_tutorial_data.gd` 本身也已删除。
留着就是 2 个 modify/modify + 3 个 modify/delete 冲突，解完还是一堆没人读的字段。
**改法**：`git restore --source=fa4f2b0 -- game/data/tutorial/`，五份 json 回到基线原样。
**代价（写下来，别让下一个人以为是新 bug）**：本分支全量跑完红 **13 项**，全部落在老教程那三条测试里 ——
`t_tutorial_data`（6 项：五关的「九条纪律」+「校验只读」）、`t_tutorial_c1`（3 项）、`t_tutorial_c2`（4 项）。
红因只有一个：老教程数据钉着旧门槛表，而本分支把门槛抬了（`--only` 单跑这三条：98 项里红 13 项，别的全绿）。
这三条测试在 `origin/main` 的 `headless_test.gd` 里**同样已经不存在**（老教程整套推倒，换成了 `t_tutor_*` 那一族），
所以合 main 之后数据、校验、测试三者一起消失，这 13 项红也随之消失。
**合并时 `data/tutorial` 整个子树一律取 main 侧。**

### 二、AI 侧漏了无氧分期增益的镜像（medium · #56）

**现象**：`cw_world._anaerobic_pool` 末尾套了新的 `_stage_boost()`（II 期 ×1.2、III 期 ×1.5），
而它的逐位镜像 `game/scripts/ai/mech/mech_value.gd:block_pool` 一字未动 —— 从肿瘤 II 期起两边差 20% / 50%。
这条路真的在跑：`mech_intent.gd` 的 `cancer_supply` 走 `MechValue.total_supply → cell_income_layout → block_share → block_pool`。
**根因**：现成的对拍 `t_mech_anaerobic` 盖不住 —— 它跑三局 4 人各 60 步，**一步都跨不到第 6 世界回合**，
全程停在肿瘤 I 期（增益 ×1.0），漏了乘数也照样 342 采样 0 偏差。
**改法**：
- `mech_value.gd` 加 `static func _stage_boost(g, pool)`，位置与写法逐位照抄引擎的 `CWWorld._stage_boost()` ——
  乘在**池子**上、不取整，`block_pool` 的两条 return（指数式与 coef == 0 的线性对照档）都套上。
  **放 `block_pool` 而不是 `block_share`**（复核建议的位置）：`block_pool` 在本仓库只有 `block_share` 一个调用点，
  两处都不会双乘；而这个文件的契约就是「`block_pool` ≡ `_anaerobic_pool`」，放 pool 里两边才继续逐行对得上。
- 头部口径注释同步，并加一句「这份镜像不会自己跟着引擎走」的警告。
**怎么验的**：`t_mech_anaerobic` 补一段：另起一局跑 40 步，把 `g.round_no` 依次钉到 1 / 6 / 11
（`tumor_stage()` 只看它；这一段只调 `anaerobic_gain_for` / `cell_income` 两个纯查询，不推进引擎、不消耗 rng），
三档逐位对拍 + 「总供给严格递增」的正面断言。
**反证跑过**：把 `_stage_boost` 摘掉重跑，立刻 7 条红（II 期 26 vs 31、III 期 26 vs 39）；装回去 115 项全绿。
**没重录 AI 基线**：`ai_same_hash.json` 的六个用例是 level 0/1/2（启发式 / 扁平 MC / MCTS），
`MechValue` 只在第四档「意图」（`MechBridge`）那条路上，基线一步都碰不到它 —— `t_ai_same_hash` 全量里绿，是这条的实证。

### 三、C# 两处 doc 注释还写着本批改掉的旧数（low · #56）

`BoardRules.cs:345` 的 `SolidifyThreshold` summary 还写「默认 I 期 3.0，II/III 期 2.0」（III 期已是 1.5）；
`CellRules.cs:218` 的 `AddMemory` summary 还写「四人 `[0,10,20,50]` / 其余 `[0,10,30,70]`」，
而它正上方 212–215 行的两张表已经是 100 / 120。`ec37b36` 专门修了 GD 侧三处同类失真注释，C# 这两处漏了。
顺手把 `CellRules.cs` 那行写到 `</summary>` **外面**的 issue #55 说明收回标签里。纯注释，不动行为，不重录任何夹具。

### 四、`prd_crosscheck.py` 这把尺被说过头了（low · #56）

**现象**：`2337e07` 修好扫描器（`:?=` 那条正则）之后，新注释给人的印象是「这些分期表现在有人盯了」。
**事实**：`main()` 只做一件事 —— `missing = [w for w in want if 去空白(w) not in prd_flat]`；
`consts[k]` 的值**只拿来打印，从不参与比对**。把 `PROLIFERATE_BASE_BY_STAGE` 改回 `[30,35,40]` 而不动 MAP，
脚本照样全绿。**它能发现「PRD 改了、引擎没跟」，发现不了「引擎单方面改数」**，后者只有无头测试挡得住。
**改法**：把措辞改准 —— 模块头 docstring 加「这把尺只有一个方向」一段，扫描器那条 ⚠ 注释、
`docs/架构说明书.md` 的工具表格、以及上一节日志里那句一并改成「**PRD 把那几句改了**也不会红」。
**没有改成「值也参与比对」**：七张表各有各的换算（门槛 `[30,20,15]` 对「3 / 2 / 1.5」、增生 `[30,40,50]` 对「3% / 4% / 5%」、
无氧 `[100,120,150]` 对「增益 20% / 50%」、根深蒂固 `[0,1,3]` 对「一格 / 三格」的中文数字），
要一张表一个格式化函数；换来的护栏无头测试已经有了（`t_tumor_stages` 五档正面断言）。写成能挡引擎改数，
下次就会有人靠它而不写测试 —— 这正是复核那条的原话。

### 五、`cw_net.gd` 的 v31 条目没记本批规则改动（low · #56）

本批不升 `NET_VERSION`（已是 31 且未发版，brief 允许），但动了 `RULE_FIELDS` 里
`solidify_threshold` / `proliferate_*` 三张表的**值** —— 形状没变、条数没变，
`tune.signature()` 与 `state_hash` 的**取值**却变了：打了这批补丁的人和没打的人协议号一样、哈希对不上。
v31 条目里补一段把 #55 / #56 的四处数值列清楚，照协议号回溯的人才看得到。

### 六、【放疗】卡面文案以孤立的「· 」开头（low · #54）

**现象**：`cw_card_data.gd:107` 的 effect 是 `"· 选择全图任意1格癌性组织。…"`，69 张卡里独此一张行首带点。
**根因**：PRD:1245 这条效果现在写成了 markdown 列表项 `- 选择全图…`，
`gen_card_data.py:_flush` 的 `re.sub(r"^- ", "· ", …)` 把它统一换成了 `· `。
生成器逐字跟 PRD 是对的（`--check` 绿），但圆点是给「多条并列」用的（对比【迁移】【增生】那几张），
单条效果上屏就是一个没有同伴的点。
**改法**：走复核给的第 ② 条 —— `_flush` 里「整段只有一条列表项」时直接去掉 `- ` 前缀，不换圆点。
**为什么不走第 ① 条**（请团队改 PRD）：PRD 正本在仓库外、本批口径是「以 PRD 现文为准、不出 diff 不改正本」。
**怎么验的**：重跑 `python tools/gen_card_data.py`，`cw_card_data.gd` **只有【放疗】这一行变**（圆点去掉），
`--check` 绿；全仓 `grep "选择全图任意1格癌性组织"` 只有这一处，没有测试钉这段文案。

### 七、改判表

| 位置 | 改前 → 改后 | 为什么 |
|---|---|---|
| `game/data/tutorial/levels/*.json`（5 份） | `[0,10,30,120]` → 回到基线 `[0,10,30,70]` | 禁区，且 main 上这五份已删 / 已重写 |
| `t_tutorial_data` / `t_tutorial_c1` / `t_tutorial_c2` | 绿 → **红 13 项（预期）** | 老教程数据钉旧门槛；三条测试与那批数据都已在 main 上删除，合并即消失 |
| `t_mech_anaerobic` | 112 项 → 115 项（+3） | 新增 I / II / III 三档对拍与「总供给严格递增」 |
| `cw_card_data.gd`【放疗】 | `"· 选择全图…"` → `"选择全图…"` | 孤立圆点 |

`mech_value_test.gd` **仍然不进** `tools/run_tests.sh`：《架构说明书》写明它「不塞主套件」，
是团队有意的（研究模块快速迭代）。这一轮是手工跑的（115 项 0 失败）。要不要接进去，留给 Kevin 拍。

### 八、这一轮跑了什么

| 命令 | 结果 |
|---|---|
| `bash tools/run_tests.sh` | 3932 项 / 239 个测试，**红 13 项**（全部是上面那三条老教程测试）；L0 两侧 416 条全过、闸三过 |
| `dotnet test core/CellWar.Core.Tests --nologo` | 1176 / 1176 全绿 |
| `mech_value_test.gd`（手工） | 115 项 0 失败（原 112 项）；三档总供给 I 78 < II 93 < III 117 |
| `python tools/prd_crosscheck.py` | 154 个常量全对 |
| `python tools/gen_card_data.py --check` | 绿（重生成后【放疗】只差那个圆点） |
| `dotnet build core/CellWar.Core` | 0 warning 0 error（C# 只改了注释） |

**规则数值这一轮一个没动**，所以 L0 / L1 / pre 夹具、AI 基线、PRD 全部**没有重录、没有改**。

## 2026-09-19 · issue #55 免疫等级门槛只抬 X 级 + issue #56 环境恶化加强（两侧同改，全套夹具重录）

两条都以**玩法 PRD 正本现文为准**（Kevin 09-19：PRD 已更新，issue 只是摘录）；逐句对完 PRD「免疫记忆」
与「环境恶化」两节，六项里**三项引擎本来就对**（压迫倍率 / 侵蚀格数与概率 / 根深蒂固），三项要改。
本批**不升 `CWNet.NET_VERSION`**：现在已是 31 且未发版。

### 一、issue #55：X 级门槛四人 50 → 100、六人 70 → 120

**现象**：X 级一到，抗原记忆就清零改记效应记忆，整局后半截等于没有升级目标 —— 门槛来得太早。
**改法**：只动第四档，II / III 两档一字不动。

| 表 | 改前 | 改后 | 出处 |
|---|---|---|---|
| `CWData.LEVEL_MIN_MEMORY`（六人兼缺省） | `[0,10,30,70]` | `[0,10,30,120]` | PRD:361/365「6人30-119 / 6人≥120」 |
| `CWData.LEVEL_MIN_MEMORY_BY_PLAYERS[4]` | `[0,10,20,50]` | `[0,10,20,100]` | PRD:361/365「4人20~99 / 4人≥100」 |
| C# `CellRules.LevelMinMemory` / `…ByPlayers` | 同上 | 同上 | 逐字镜像 |

II 级「到癌性组织的【迁移】耗能降为 0.8」、X 级「效应记忆从零计数」两条既有机制**核过、没动**。

### 二、issue #56：环境恶化加强

| 项 | 改前 | 改后 | 出处 / 结论 |
|---|---|---|---|
| 【E-微环境压迫】倍率 `PRESSURE_MUL_BY_STAGE` | `[10,15,20]` | 不动 | PRD:411/431「1.5 倍 / 2 倍」——**本来就对** |
| 【E-增生】基数 `PROLIFERATE_BASE_BY_STAGE` | `[30,35,40]` | `[30,40,50]` | PRD:413/433「4% / 5%」 |
| 【E-增生】每固化 `PROLIFERATE_SOLID_BY_STAGE` | `[5,10,10]` | `[5,10,15]` | PRD:413/433「+1% / +1.5%」 |
| 【E-侵蚀】格数 `EROSION_TILES_BY_STAGE` | `[(2,3),(2,3),(3,5)]` | 不动 | PRD:417/419/437/439——**本来就对** |
| 【E-侵蚀】概率 2/3 : 1/3 | `roll_d3() <= 2` | 不动 | `cw_world.gd:617`——**本来就对**，rng 消耗没变 |
| 【固化】门槛 `SOLIDIFY_THRESHOLD_BY_STAGE` | `[30,20,20]` | `[30,20,15]` | PRD:441「III 期降低为 1.5」 |
| 【根深蒂固】`ROOTED_BY_STAGE` | `[0,1,3]` | 不动 | PRD:423/443「一格 / 三格」——**本来就对** |
| 【E-无氧呼吸】分期增益 | **没有这张表** | 新增 `ANAEROBIC_STAGE_MUL_BY_STAGE = [100,120,150]` | PRD:425/445「增益 20% / 50%」 |

**1.5 落得上**：`SOLIDIFY_STEP` 是 +1.0，但计数本来就带 −0.5 衰减，粒度是 0.5。

**无氧增益乘在哪**：`CWWorld._stage_boost()` 乘在 `_anaerobic_pool()` 的**返回值**（池子）上、**不取整** ——
人数系数 k、均分、四舍五入、兜底 2.0、封顶一律排在它之后，整条算式仍然只取整一次（`_split_share`）。
放池子有两个有意的后果：卡【糖酵解爆发】走同一个口子（`anaerobic_gain_for`）自动跟上；
兜底 2.0 **不跟着涨**（PRD 写的是 `max{2, …}`，2 是绝对地板不是基础数值）。线性对照档也照吃这一刀。

### 三、顺手修掉的两处旧账（都是这批数值一改才暴露的）

1. **C# `RulePolicies.SolidFraction` 的分母写死**：原来是 `Stage(s) == 1 ? 30 : 20`，此前正好等于门槛表，
   III 期降到 1.5 之后当场与 GD 分叉（GD 走 `CWGame.solidify_threshold()` 喂给 `CWData.solid_progress`）。
   改成 `BoardRules.SolidifyThreshold(s)`。L0 `rooted/t_tumor_stages/002` 与 `/004` 是抓到它的两条。
2. **`tools/prd_crosscheck.py` 整类扫不到**：扫描器只认 `const X := [...]`，而带类型标注的常量在 GDScript 里
   只能写 `const X: Array[int] = [...]` —— **七张 `_BY_STAGE` 分期表连同四张癌种分期表从来没被核过**
   （MAP 里写着锚点，**PRD 把那几句改了**也不会红）。与 2026-09-09 那次字典常量是同一类盲区：写法不同 = 悄悄跳过一整类规则数值。
   ⚠ 修好拿回来的**只有这一个方向**：`consts[k]` 的值从不参与比对，「引擎单方面改数」这一类它依然发现不了（见顶上那节 §四）。
   改好之后当场抓出三条失配：增生两张表的 II/III 锚点还停在旧 PRD；`PRESSURE_MUL_BY_STAGE` 第一条锚点
   多带了一个右括号（PRD 那行 LaTeX 是 `…-相邻健康组织})`，花括号写在括号里面）—— 这条从写下那天起就没真跑过。
   同时给 `EROSION_TILES_BY_STAGE`（四条锚点 = 两档 × 两个概率分支）、`ANAEROBIC_STAGE_MUL_BY_STAGE`、
   四张癌种分期表补了映射，`CORES` / `VESSELS` 进 INTENTIONAL（PRD 只给个数不给坐标）。核对数 141 → 154。

### 四、改判表（测试 / 夹具）

| 位置 | 改前 → 改后 | 为什么 |
|---|---|---|
| `t_tumor_stages`「III 期门槛」 | 20 → 15 | #56 |
| `t_tumor_stages` 固化边界 | `raise_solid(c2, 10)` → `5` | 1.0 + 0.5 = 1.5 恰好到门槛（旧门槛 2.0 下这一步不会转），正面测新值 |
| `t_tumor_stages` ⑤（新增） | 无 → 三档无氧增益 + 严格递增 | #56 新表要有正面断言；递增那条保证没被兜底 2.0 吃掉 |
| `t_immune_level_rules` 三张门槛断言 + 边界表 | 70/50 → 120/100，`[69,2],[70,3]` → `[119,2],[120,3]` | #55 |
| `t_proliferate_tiers` 四条 | 35/40 → 40/50，`2×(40+10)` → `2×(50+15)` | #56 |
| `t_card_choices` ⑩ 续（新增，随 #54 提交） | 无 → 三条 | 见上一条日志 |
| C# `K5Batch2RuleResultTests` 门槛用例 | 50/70 → 100/120 | #55 |
| C# `ObservationV1Tests` | `[30,20,20]` → `[30,20,15]` | #56 |
| C# `GdScriptParityTests`（新增） | 无 → `无氧分期增益三档等于GDScript数组` | 新表两侧各一份，得有一条对拍钉住 |
| L0 9 条用例 | 逐条按 GD runner 实算值改判 | 见下 |
| `game/data/tutorial/levels/*.json` 的 `expect_level_tiers` | `[0,10,30,70]` → `[0,10,30,120]` | 教程数据自带的校验就是拿它对 `CWData.level_min_memory()`，不改则五关全红 |
| L1 水位线 `Ratchet2p` | 173 → 172 | 夹具重录后 2p 终局提前了一步，整条仍一致 |
| L1 水位线 `RatchetChemo` | 279 → 282 | 夹具重录后 343 步里前 282 步一致（往上拧） |

**L0 九条**：`solidify/threshold_stage_III`（20→15）、`const/data/proliferate_base_by_stage`、
`const/data/proliferate_solid_by_stage`、`ossify/t_batch2_rules/050`（`d.proliferate_chance` 45→50）、
`rooted/t_tumor_stages/002`（`d.solid_fraction` 500→667）、`/003`（`d.proliferate_chance` 60→80 / 120→160）、
`/004`（500→667）、`execute/play/stroma_harden/late_reaches_threshold`（50→65）、
`enter_tile/purify/memory/level/x_level_resets_memory`。
**最后那条是唯一动了盘面的**：X 级门槛抬到 120 之后，原来的 `memory: 69` 再也跨不过去，
用例要还能测「升级 + 就地清零」就必须把起点跟着抬到 119 —— 期望值一个字没改。

**重录的夹具**：`l0/pre_envelopes.jsonl.gz`（416 条）、`l1/trace_*.jsonl` 与 `l1/env_*.jsonl.gz` 四条
（4p 200 步 / 2p 终局 172 步 / 6p 200 步 / 树突建源局 343 步）、`baseline/ai_same_hash.json` 六例。

⚠ **树突建源局重录后第 283 步新暴露一处分叉**（`cells[1]` 的 `marked` / `mark_left` / `mark_round`：
GD 在第 12 世界回合又标了一次、C# 让标记过期）。**与本批三条规则无关** —— 旧夹具停在 279 步时够不到它，
是轨迹变长才露出来的老账。本批不碰，另开工单。

### 五、胜率扫描（改前 / 改后，**只量不调平**）

`game/tests/balance_scan.gd`，同种子同参数各跑两遍：改前 = `fa4f2b0` 的 `cw_data.gd` / `cw_world.gd`
（只换这两个文件），改后 = 本批。**这批数不做调平依据**，只是把方向量出来。

| 席位 | AI | 局数 | 改前癌胜 | 改后癌胜 | 改前平均回合 | 改后平均回合 |
|---|---|---|---|---|---|---|
| 4 人 `ICIC` | 浅层 MC v11 | 40 | **5%**（2/40） | **10%**（4/40） | 9.1 | 9.3 |
| 4 人 `ICIC` | 启发式 v11 | 200 | **29%**（59/200） | **44%**（89/200） | 11.4 | 11.3 |
| 6 人 `ICIICI` | 浅层 MC v11 | 40 | 0%（0/40） | 0%（0/40） | 3.6 | 3.6 |
| 6 人 `ICIICI` | 启发式 v11 | 200 | 0%（0/200） | 0%（0/200） | 4.8 | 4.8 |

**方向明确**：四人局癌方增强 —— 启发式那把尺 +15pp、MC 那把 +5pp（40 局的 ±1σ ≈ 5pp，只看方向）。
主要来源是增生那两张表 —— 每局【增生】造出来的格数：四人 MC **21.0 → 24.3**、四人启发式 **30.8 → 38.3**（改前 → 改后，同两轮扫描的「每局占地」行）。

⚠ **六人局这两把尺量不到本批的改动，一个数都别往结论上放**：两把尺的六人局都在
**第 3.6 / 4.8 世界回合**就结束（胜法 100% 是 `immune_clear`，三只癌细胞被团灭），
而**环境恶化第 6 回合才进 II 期** —— 整局停在肿瘤 I 期，#56 改的三张表一次都没被读到。
改前 / 改后逐项相同（6 人 MC 终局癌性 16.1 vs 16.3 格）正是这件事的证据，不是「改动无效」。
要量六人局的环境恶化，得先有一把打得到第 6 回合之后的尺（记忆 `cell-war-balance-target`：
启发式 v4 Kevin 09-19 判不做、AI 走训练模型）。

#55 的门槛改动在这两把尺上**几乎量不到**：X 级原本 70 / 50 就很少在一局里摸到，抬到 120 / 100 之后
更是到不了；真实影响在「真人长局的后半段还有没有升级目标」，尺子里看不见。

### 六、怎么验的

- `bash tools/run_tests.sh` 全量绿：3929 项检查 / 239 个测试 + L0 两侧（GD 416 / 自检 416 / C# 453）+ 闸三
- `dotnet test core/CellWar.Core.Tests --nologo` 1176 / 1176 绿
- `python tools/prd_crosscheck.py` 154 个常量全部对上；`python tools/gen_card_data.py --check` 绿

## 2026-09-19 · issue #54【放疗】逐字复核：实现已符合，补上「可以包括健康组织」那条断言 + 卡面跟 PRD 重生成

**现象**：issue #54 把【放疗】的效果重写了一遍（「共 10 格且彼此连通的组织区域（可以包括健康组织）」）。
要判的是引擎到底符不符，而不是照着抄一遍。

**根因（逐字对完的结论：四条全中，一条没断言）**

| issue 原文 | 引擎 | 出处 |
|---|---|---|
| 共 10 格 | `CWData.RADIO_REGION = 10` | `cw_data.gd:483`（PRD 2026-09-09 由 15 改 10） |
| 彼此连通 | `_radiotherapy` 从起点按邻接长，frontier 只收已入选格的邻居 | `cw_card_fx.gd:989-1000` |
| 可以包括健康组织 | 长区域时**不挑 tissue**，只要连通 | 同上（此前**没有断言**盯着） |
| 区域内癌组织转健康 + 全部进「坏死」 | `CWTissue.to_necrotic` = `to_healthy` + 计时 | `cw_tissue.gd:24-30` |

所以 issue #54 **不需要改规则**。真正缺的是第三条的测试：老用例把癌块铺成半径 2 的 19 格，
10 格区域整个落在癌区里 —— 挑不挑 tissue 都看不出来，这条一直是「碰巧对」。

**改法**

- `headless_test.gd:t_card_choices` ⑩ 后面加一小段：癌块只有 3 格，区域要长到 10 格就**必须**吃进健康组织。
  三条断言 = 照样 10 格坏死 / 至少 7 格本来就是健康（原样留健康，但照样坏死）/ 仍是一整块连通区域。
  下界写成「10 − 癌块大小」而不是等号：区域是随机长的，那 3 格癌组织不保证全被吃进去。
- `cw_card_fx.gd:_radiotherapy` 的注释：① 把残留的「坏死 5 轮」改成 `CWData.NECROSIS_RADIO`
  （2026-09-07 坏死收成通用状态时就回到 2 轮了，注释漏跟）；② 把「可以包括健康组织」写进去。
- `cw_card_data.gd` 按现 PRD 重生成（`tools/gen_card_data.py`）：【放疗】卡面补上「（可以包括健康组织）」，
  并跟着 PRD 把「所有癌性组织」改回 PRD 的用词「所有癌组织」。
- `tools/gen_card_data.py` 的 `OUT` 从写死主仓库绝对路径改成**跟着脚本自己走**。
  这不是顺手改：在 worktree 里跑 `--check` 校的是主仓库那一份、`gen` 改的也是主仓库那一份 ——
  于是 worktree 里的改动既验不到也落不下，还顺手把别人的工作目录弄脏了。`prd_crosscheck.py` 同理
  （PRD 在「仓库的上一级」，而 worktree 的上一级是 `.claude/worktrees/`，找不到就退到写死的那份）。

**怎么验的**：`--only=t_card_choices` 50 项绿；`python tools/gen_card_data.py --check` 绿。

**改判表**

| 断言 | 改前 | 改后 | 为什么 |
|---|---|---|---|
| 「起点固化癌组织转健康并坏死 5 轮」 | 名字写着 5 | 名字改成 **2**（仍是字面量），L0 `play_radiotherapy.json` 的 covers 同改 | 断言体本来就在比 `CWData.NECROSIS_RADIO`，只有名字还写着 09-07 之前的旧值 |
| （新增）「癌块只有 3 格时区域照样长到 10 格」 | 无 | 新增 | issue #54「可以包括健康组织」此前没有断言 |
| （新增）「区域里至少 7 格本来就是健康组织」 | 无 | 新增 | 同上 |
| （新增）「含健康组织的区域也是一整块连通区域」 | 无 | 新增 | 同上 |

⚠ **这条踩了一次坑**：第一版把名字改成了 `"…坏死 %d 轮" % CWData.NECROSIS_RADIO`，**闸三当场红** ——
`tools/xcheck_report.py` 只认**字符串字面量**的断言名，拼出来的名字 covers 指不到，`covered_sites` 416 → 415。
断言名要跟着常量走的时候只有两条路：写死字面量（本条选它），或者把这条 covers 一起删掉。

**没改的**：`RADIO_REGION` / `NECROSIS_RADIO` 两个常量、`_radiotherapy` 的实现、C# `CardRules` 那一支 —— 都已符合。

- `game/scripts/tutor/cw_tutor_fx.gd`：`GLITCH_MODE = "blocks"` / `RESET_VARIANT = "rewind"` 两个常量顶掉原先写死在 `_probe_glitch` / `_probe_reset` 里的 `"jitter"` / `"rewind"`（后者本来就是 rewind，这回钉成常量并在闸里正面断言）。
- **马赛克串台**：blocks 每帧在**错开的那几条**里随机挑 1~3 块，那一小片画的不是自己的像素，而是从 `pool` / `morph_to` 的贴图**同一位置**（按比例对位，不是按像素 —— 小细胞肺癌那张只有别人一半大）采过来的，连着 1~3 帧再换一批。于是间章「分化→普通」「免疫→小细胞肺癌」和第七关「随机切换→定格印戒」这三处用它时，串台来源正好就是**将要变成的样子** —— 马赛克本身成了变身预告。没给 `pool` / `morph_to` 的场合退回**同阵营另一种细胞**（`MOSAIC_KIN` 按贴图名分阵营，演出层照样不认识内核的类型枚举）；已经定格成 `morph_to` 之后不再串它自己（串了也看不出来）。
- 参数：`MOSAIC_MAX` 轻 2 / 剧烈 3 块，`MOSAIC_W` 轻 0.34 / 剧烈 0.58（占胞宽），`MOSAIC_HOLD` 3 帧封顶 —— **强度跟着 `intensity` 走**。画的时候走新加的 `_blit_part()`（源按比例取、落点按自己那张的尺寸算），`_blit()` 一个字没动，所以 jitter / scanlines / dissolve 那几支的画面一帧不变。
- **三条纪律照旧**：随机仍在 `begin()` 里用自带 rng 一次摇完存进 `_plan.mosaic`（每帧一张查表），`probe(t)` 只查表 —— 整段还是时间的纯函数；而且**串台只在 blocks 模式摇**，另两种模式的随机流一颗不动，旧的 `glitch_jitter.gif` / `glitch_scanlines.gif` 逐帧仍然对得上。
- `game/tests/preview/preview_tutor_fx.gd`：新增一段 `glitch_blocks_morph`（blocks + 剧烈 + 随机切换 + 定格印戒细胞癌，第七关那一处的参数），静帧 27 → **30 张**，`dump=glitch_blocks_morph` 同样认。

**数字**：`t_tutor_fx` 19 → **25 项检查全绿**（`--only=t_tutor_fx`，单跑 1 个测试）。新增的六条：两处缺省（blocks / rewind）；blocks 24/24 帧都带串台且来源全在 `pool` / `morph_to` 里（采错 0 块、采到自己那张 0 块）；同一块最长连着 3 帧就换走、没有哪一条从头盖到尾（这就是「闪一下又回自己的」）；黑色素瘤与印戒细胞癌同位置 1084/1088 个像素不同（不然串了也看不出来）；无 pool 时只串到同阵营的 `Dendritic`/`Macrophage`/`BCell`/`TCell`；强度剧烈 28 块 / 轻 13 块。全量按 Kevin 09-19 的口径**没跑**。

**动图**（仓库外 `特效动图_教程v2_2026-09-19/`，README.txt 同步改了）：`glitch_blocks.gif` 覆盖重出（21 帧 / 1.75s，轻强度，串台采的是 morph_to 的普通免疫细胞），新增 `glitch_blocks_morph.gif`（30 帧 / 2.50s，剧烈 + 随机切换 + 定格印戒）。两张都 zoom 2.8、真渲染（**不能 `--headless`**）：

```
godot --path game --script res://tests/preview/preview_tutor_fx.gd -- dump=glitch_blocks_morph out=<帧目录>/glitch_blocks_morph
python tools/make_fx_gif.py <帧目录>/glitch_blocks_morph <输出>/glitch_blocks_morph.gif
```

⚠ 动图末尾定格那半秒画的是**演完之后的盘面**（演出把代画的真细胞还回去了），所以定格帧上是那一格原本的细胞、不是 `morph_to` 那张 —— 旧的 `glitch_scanlines.gif` 也是这样，不是这次改出来的毛病。

## 2026-09-19 · 教程演出库出动图：`preview_tutor_fx` 加逐帧倾倒 + `tools/make_fx_gif.py`

Kevin 09-19：「这两个表现请做成动图给到我」。静帧挑不出好坏 —— 自动重置提示三候选差在**动**（撕裂带往上扫 / 红光绕屏一圈 / 细胞错峰溶解），像素错误三模式也一样，一张定格看不出抖没抖。所以给出图脚本加一档逐帧模式，再合成 GIF。

- `game/tests/preview/preview_tutor_fx.gd`：新增 `dump=<kind>[:<variant>] out=<目录>`（`kind:variant` 与 `kind_variant` 两种写法都认，对的是 `SHOTS` 里的名字）。一次倒一段，从 0 到「时长 + 收尾定格 `DUMP_HOLD` 0.5 秒」，每 `1/12` 秒一张 960×540 PNG。时间用 `seek(t + 0.001)` 给（**微推一下**：不微推的话 `24 * (1.0/12)` 这种浮点误差会被 `_quant` 掉到上一格，末尾少一帧），超过时长那一帧才 `skip()` 收尾 —— 代画藏起来的真节点还回去，剩下的帧就是「演完之后的盘面」定格。原来的 27 张静帧模式一行没动。
- `tools/make_fx_gif.py`：PNG → GIF（12 fps、循环）。**调色板全片共用一张**（先把所有帧竖着拼成一张量化出 256 色，再拿它套每一帧，dither 关掉）—— 逐帧各量化各的，像素块会一帧一个色、糊成一片。GIF 的帧间隔只能是 10ms 的整数倍，12 fps 对不上，按累计时间取整排成 80/80/90 的节奏，整段总时长才不偏。
- 出的 6 张在 `特效动图_教程v2_2026-09-19/`（仓库外，附 README.txt 写清每张是什么、请谁挑）：`reset_hint_rewind/edge/dissolve`（各 24 帧 / 2.00s）给 Kevin 挑，`glitch_jitter/blocks`（各 21 帧 / 1.75s）、`glitch_scanlines`（30 帧 / 2.50s，顺带演了随机切换 + 定格印戒细胞癌）给 hxr 挑。glitch 三份照静帧的做法把机位推到 zoom 2.8，reset_hint 三份用对局机位。

命令：

```
godot --path game --script res://tests/preview/preview_tutor_fx.gd -- dump=reset_hint:rewind out=<帧目录>/reset_hint_rewind
python tools/make_fx_gif.py <帧目录> <输出目录> --batch
```

**不能加 `--headless`**（要真渲染才有画面）。`t_tutor_fx` 单跑过一遍 19 项全绿，演出库本体一个字没改。
## 2026-09-19 · issue #48 / #52 / #53：能量增损通用动画、固化生成的像素弥散、特效 bug 八条

三条都是**纯表现**：`game/scripts/core/**` 与协议件（`cw_obs_*` / `cw_net*`）一字未动，`CWNet.NET_VERSION` 不升。能量变化与固化变化都**从镜像差分里取**（`CWTeleportFx` 那条先例：传送也是靠差分认出来的，引擎零改动、联机影子对局同样触发）。

动图（真渲染连拍 + `tools/make_fx_gif.py`）在 `截图_2026-09-19_issues_fx/`：`issue48_能量增损.gif` / `issue52_固化生成与解除.gif` / `issue53_特效八条.gif`（回炉又加了 `issue53_04_血行转移_缩小消失与放大出现.gif` / `issue53_05_分化_交叉淡入淡出.gif`）。

---

### #48 能量增损通用动画

- **现象**：HXR-I「对于所有使细胞能量产生改变的事件，细胞贴图和右侧细胞状态栏都应有相应动画来提示」。此前能量只是右栏一个静默变化的数字，打了一下、收了一笔，画面上什么都没发生。
- **根因**：没有任何一处在「能量变了」这件事上挂演出。逐条去接演出报文也接不全 —— 改能量的路有收入 / 伤害 / 反弹 / 迁移费 / 卡牌 / 过载 / 复活……十几条，接漏一条就静默失效。
- **改法**：
  - 新建 `game/scripts/ui/energy_fx.gd`（**无 `class_name`**，热更装不上新全局类，同 `attack_fx.gd`；`CWMatch.ENERGY_FX` preload）。细胞头顶飘一个 ±数字，`LIFE` 1.05 s 里缓出上升 13 px，透明度三档。
  - 字**不走 `CWStyle.FONT`**：棋盘层带相机缩放（zoom 1.27），10px 点阵过去会被重采样磨出灰边（约定 #13）。用一张手写的 3×5 像素字模（`GLYPH`，只有 `0-9 + - .`），按整数倍放大 `SCALE` 2 倍画，和盾的 `SHIELD_HALF` 一个路子 —— 纯整数像素、纯函数、无头直接核。
  - 判据是 **`CWMatch._sync_cells` 的镜像差分**：`_last_energy[i]`（`ENERGY_FX.UNSEEN` = 还没见过）对上这一帧的 `cell["energy"]`。刚复活那一跳不演（那是「凭空出现」，走 `_pop_in`），教程的无限能量不演。
  - **别刷屏**：同一只细胞 `MERGE` 0.22 s 内的多次变化合并成一条（攻击扣血 + 反弹常常同一拍落地），一进一出抵消为 0 就整条撤掉；合不进去的按 `SLOT_DX` 左右错峰，最多同时留 3 条。
  - 右栏同步：`CWMatchPanel.bump_energy(pid, up)` 让那一行的能量数字色闪 `ENERGY_FLASH` 0.55 s（`energy_color` 是纯函数，时间从外面进来）。**只闪色不滚数字** —— 那一行每帧全量刷，滚数字要另记一份显示值，那份值一旦和引擎错开就是两套真相。两支笔 `CWStyle.ENERGY_GAIN` / `ENERGY_LOSS`（配色只许有一处）。
- **怎么验的**：`t_issue_fx_0919` 的头 9 项（文案带符号、字模齐、上升缓出、透明度三档、合并 / 错峰 / 上限 / 抵消 / 拆局清干净、右栏色闪五种取值、差分接线在位）；动图 `issue48_能量增损.gif`。
- **改判**：无既有测试改判。`CWStyle` 只加了两个常量，既有色一个没动。

### #52 固化癌组织生成的像素弥散 ＋ #53 ⑥ 解除时取消长方形马赛克

这两条是**一对**：生成 = 粒子聚拢成固化的六边形纹理，解除 = 纹理散成同样的粒子飞开。

- **现象**：固化生成时画面上什么都没有；解除时（癌方复活踩掉依托的那格）出现「一个长方形的马赛克」。
- **根因**：那个长方形是 `CWSkillFx.stone_patch` —— 「碎石重生」里的一层，`solid_mosaic()` 当年只取贴图中间 27×18 那一块（还夹了一道 `|x| + 0.6|y| ≤ 17` 的方框），散开时外形就是个长方形。而且它画在**落点**，真正降级的是 `anchor`（`CWWorld.revive_cancer`，两格可以不同）—— 位置本来就常常是错的。
- **改法**：
  - `solid_mosaic()` 改成按**贴图自己的 alpha** 取满张（块心对齐不动：`(x+13)%3 == 0`、`(y+9)%3 == 0`，`t_issue31_fx` 钉着它，所以范围从 `MOSAIC_X0` −16 / `MOSAIC_Y0` −12 起）。94 块，外形就是那块六边形组织本身。
  - 新增纯函数 `solid_block(idx, cell, p)`：p=1 全就位、p=0 全散开（黄金角方向、y 压 0.7 的贴地透视），每块按原来那条带交叉项的散列错峰（线性式会排成斜条，issue #27 栽过）。还在飞的块提亮到 `SOLID_SPARK`，落位才退回贴图本色 —— 深红矿物色落在深绿组织上，不提亮整段演出在一屏尺度上看不见（第一版出图就是这么丢的）。
  - 两条新 kind `solid_form` / `solid_break` 由 **`CWMatch._sync_tiles` 的镜像差分**开演（进 / 出 `Tissue.SOLID`）。差的是**引擎的** `tissue` 而不是这一帧画出来的那个 —— 开场绽开期间画的是健康组织，拿画面差分会在绽开放开那一刻满盘齐炸。一帧变超过 `SOLID_FX_MAX` 3 格（联机快照回灌 / 存档 restore）只记不演。
  - `_revive_cancer` 里那层 `stone_patch` 整条删掉（函数一并删）。解除现在演在**真正降级的那一格**上。
- **怎么验的**：`t_issue_fx_0919` 的固化五项（两条 kind 在表、马赛克铺满六边形且越出旧方框、p=0/1 两端、`stone_patch` 删干净、差分接线 + 上限）；`t_issue31_fx` 原样通过（块心对齐与取色口径没变，块数 60 → 94）；动图 `issue52_固化生成与解除.gif`。
- **改判**：`t_issue31_fx` 那句文案里的块数从 60 变 94（断言是 `> 50`，没改）。

### #53 特效 bug 八条

| # | 改前 | 改后 | 落点 |
|---|---|---|---|
| ① | B 细胞抗体从脚底射到脚底 | `from_body` / `targets_body` —— 中心射到中心；命中后受击者横抖 `HIT_SHAKE_FOR` 0.28 s | `skill_fx._antibody` / `hit_shake` / `shake_offset`；`ui_bridge.FX_BODY_CENTER` |
| ② | T 细胞裂解也从脚底 | `from_body`（落点仍是那格地面） | `skill_fx._lyse` |
| ③ | 细胞毒素画在 `Z_OVER_BOARD`，压在细胞之上 | 整只演出改走**地面贴花** `GroundDot`：一格一个节点、各拿 `tile_z(格, Z_MARK)` | `skill_fx.GroundDot` / `draw_toxin`；`ui_bridge` 另给 `tiles_z` |
| ④ | 血门 squash 0.85 近正圆；癌细胞的传送与血流各演各的 | 血门压扁 0.42 + 倾斜 `HOMING_TILT`，下方再淡画一圈当厚度；传送按血流对时（流出发才缩小消失、流落地才在目标放大出现） | `skill_fx._homing` / `homing_elapsed`；`teleport_fx.play(..., lag, shrink)`；`match._play_teleports` |
| ⑤ | 分化等粒子对格心上方 5px；分化是**瞬间换图** | `at_body` 胞体中心；换图改成旧形态淡出 / 新形态淡入（照原型 `common-skills.js:59`）。粒子照原型仍是硬像素 | `skill_fx._differentiate` / `_revive_*`；`match.cross_fade_art` |
| ⑥ | 解除固化时一个长方形马赛克 | 见上一节（`solid_break`，六边形纹理散成粒子） | — |
| ⑦ | 突变的双股钉在脚底往上 23px 起、往下压到脚底 | 以胞体中心为准上下各 12px | `skill_fx._mutate` |
| ⑧ | Excalibur 从格顶面中心往外 16px 冒出来 | 起点 = 胞体中心，起手偏移 = 胞体半径（`play` 多一个 `start`），光芯正好从细胞轮廓上离开 | `beam_fx.play`；`ui_bridge.show_beam` |

- **①④ 的共同做法**：细胞的 `position` 每帧被 `CWMatch._sync_cells` 覆写，演出层想让它动就只能把偏移 / 时序交出去 —— 和伪足的 `carry_pos` 同一个道理。震动走 `shake_offset(细胞位)`，传送对时走 `homing_elapsed(from_px, to_px)`。
- **⑤ 的「和 html 中的虚化效果不符」**：原型页就在仓库里 —— `tools/art-preview/index.html` + `common-skills.js`（`skill_fx.gd` 头注第 22 行就写着这条对应 `differentiate v2`）。第 59 行 `fade(c,1-p,immune)` + `fade(c,p,新形态)` 是**贴图的交叉淡入淡出**，第 67 行那口粒子 `burst(...,cyan,9,20,true)` 走的是硬像素。所以虚化落在 `CWMatch.cross_fade_art`，粒子一个参数都不改（本条 2026-09-19 回炉时改正，见顶部那一段）。
- **怎么验的**：`t_issue_fx_0919` 的 ①~⑧ 共 19 项；动图 `issue53_特效八条.gif`（抗体中心命中 → 细胞毒素落在细胞之下 → 血门躺在地上 → 分化柔边 → 突变对中 → Excalibur 从细胞表面推出）。
- **改判**：`t_tutor_fx` 的 `PUBLIC_FX_MD5` 重录了 `beam_fx.gd` / `teleport_fx.gd` 两支的指纹（`attack_fx.gd` 未动）。那道闸认的是「**教程**没去改真人对局也在用的那几支」—— 这次改的人是表现组、按 issue 改的，重录是对的。

### 新增 / 改动的文件

- 新增：`game/scripts/ui/energy_fx.gd`、`game/tests/preview/preview_fx_0919.gd`（拼图走 `tools/make_fx_gif.py`；本批一度新建过 `tools/make_gif.py`，回炉时并进前者并删掉）
- 改动：`skill_fx.gd`、`pix.gd`、`beam_fx.gd`、`teleport_fx.gd`、`ui_bridge.gd`、`match.gd`、`match_panel.gd`、`cw_style.gd`（只加两个常量）、`headless_test.gd`
- 闸三（`tools/xcheck_report.py`）：`t_issue_fx_0919` 按 §0.6.5 第 6 条逐名归 **ui**——整支是演出层的纯函数与接线断言、零规则量，不归的话兜底的 `^_?t_` 会把它扫进 core、凭空把分母抬 32 站点（core 覆盖率 40.4% → 39.2%）。
- `xcheck/COUNT` 抬了一次（rebase 到 `e492f9a` 之后重算：`total_sites` 3219 → 3268、`total_funcs` 241 → 242）：新增的 32 条断言**全在 ui 档**，`core_sites` / `covered_sites` 一个没动 ——闸三那句「新断言要同批补用例」说的是 **core 规则覆盖**，这批没碰规则，按脚本头注的「人工抬 COUNT」走 `--write`。

⚠ **一条留给 Kevin 的措辞题**：PRD:1245 写「区域内所有**癌组织**转为**健康组织**」，而 PRD:217 明确
「癌组织与固化癌组织统称为**癌性组织**」，PRD:651（【细胞毒素】那条）还特意写了「**固化癌组织**不被转化」。
按最严的读法，【放疗】不该清固化；但同一句又说「使区域内**全部**组织进入「坏死」状态」，
而 PRD:257「坏死为**健康组织**的状态」—— 固化格留着就进不了坏死，那一句自相矛盾。
引擎现在是**连固化一起清**（`to_necrotic` 一律走 `to_healthy`），本批**不动**，写在这里等拍板。

## 2026-09-19 · 新手教程 v2 · S7：教程演出库 `cw_tutor_fx`（六种演出 / 零内核 rng / 时间的纯函数）

方案 §1.4 第 ② 类演出落地。新建三份文件，**一份既有文件都没改**（S1 正在并行重建骨架，`match.gd` / 舞台 / 老 guide\* 一律不碰；公共特效 `beam_fx.gd` / `attack_fx.gd` / `teleport_fx.gd` 一字不动，并加了指纹闸盯着）：

- `game/scripts/tutor/cw_tutor_fx.gd` —— 演出库本体（**无 `class_name`**，preload 用；方案 §1.5）
- `game/tests/preview/preview_tutor_fx.gd` —— 真机对照图（真 Board.tscn + `CWView` 的对局机位 + 真细胞贴图）
- `headless_test.gd` 的 `t_tutor_fx`（19 项检查，已登记进 `_run_all` 的清单 —— 那是手写清单，不登记就永远不跑也不报错）

**六种 kind 与签名**（S2/S3 接线照这个写）：

```
fx.attach(board)                       ## 认棋盘 + 设 z 序；再 board.add_child(fx)
fx.seat_at = Callable                  ## 席位 -> 格坐标，由导演注入（演出层不认识席位）
await fx.play(kind, args)              ## 协程，播完才返回
fx.skip()                              ## 一步到位（loop 的那种也靠它停）
fx.begin(kind, args) / advance(dt) / seek(t) / clear()     ## 无头测试与真机的分开入口
fx.probe(t) -> Dictionary              ## 这一刻的画面（纯函数）；current_tex() 问末帧贴图
```

| kind | args | PRD |
|---|---|---|
| `shockwave` | `at`(Vector2i) / `radius`(缺省 3) | 393 / 411 |
| `glitch` | `at`\|`target` / `mode` ∈ `jitter`\|`blocks`\|`scanlines` / `intensity` `light`\|`heavy` / `tex` / `pool`+`shuffle` / `morph_to` / `seed` | 399 / 403 / 487-489 |
| `knockback` | `from` → `to`（一格或两格）/ `tex` / `node` | 411 / 469 / 473 |
| `beam_hit` | `from` → `to` / `loop` | 463-465 / 483 |
| `reveal` | `coords` | 45 |
| `reset_hint` | `variant` ∈ `rewind`\|`edge`\|`dissolve` / `cells` / `shake_node` | 47 |

公共约定：`seed` 缺省 0（可复现）；`secs` 覆盖时长；`node` / `nodes` 交进来的真细胞节点由演出库**代画**（演出期间藏起来、收尾自动还回去，同 `CWAttackFx` 的路数）。

**三条纪律怎么落的**

1. **零内核 rng**。随机在 `begin()` 里用自带的 `RandomNumberGenerator` **一次摇完存进 `_plan`**，`probe(t)` 只查表。理由不是洁癖：教程那几关的骰子是预设带子，多掷一次整条错位，而 `cw_roll_tape.gd:26-28` 的 overrun 是**静默**回落真 rng —— 错了不报。源码闸扫 `scripts/tutor/**`：零 `CWGame` / `CWWorld` / `CWActions` / `CWSetup` / `CWKernel` / `CWMirror` / `CWRollTape` / `game.`，零裸 `randf(` / `randi(`，零 `class_name`。
2. **时间的纯函数**。`probe(t)` 把这一刻算成一个字典，`_apply(t)` = `probe` + 落盘，时间量化到 1/12 秒（`PIX_FPS`，同 `CWSkillFx` / `tutorial_opening.gd:459`），alpha 也量化到 1/6。于是「跳过 = `seek(末刻)`」成立、无头测试自己喂时间不用真等、同一个 t 喂两遍结果一定相同（闸里搅了 64 颗全局随机数再比一次）。
3. **教程的变体归教程**。PRD:465「效应应答命中于癌细胞而非贯穿」做成本库自己的 `beam_hit`：螺旋包络的尖**收在 tip 上**，射程封死在目标胸前 `BEAM_HALT = 10px`，任 t 多大都越不过去（闸里正面断言）。`beam_fx.gd` 那支真人对局也在用，改它回归面会溢出教程之外 —— 三支公共特效的源文件 md5 钉进 `PUBLIC_FX_MD5`，真要动它们就把数更新掉并在本日志说明。
4. `reveal` 是**转调**不是重写：`board.set_active_tiles()` 本来就按 `ring_delays` 错峰淡入（`board.gd:650-700`），演出库只负责算时长（错峰上限 + `ACTIVE_FADE`）。

**两处等拍板的候选**（真机截图在 `截图_2026-09-19_教程v2_S7/`，27 张）

- **像素错误三种表现**（hxr 未答口径，三种都做了，各 3 帧 起/中/末；为看得清把机位 zoom 推到 2.8，取景参数其余照旧）：
  `jitter` = 整只按帧抖 + 冷暖两道色差重影；`blocks` = 胞体横切 5 条、每条各自横移；`scanlines` = 一道往下滑的横向撕裂 + 几条暗带（`heavy` 档另加整只抖动与过曝闪）。末帧都验了 `morph_to` 确实换贴图；`scanlines` 那份顺带演了 PRD:487 的「混乱像素中随机切换、最终定格印戒细胞癌」（随机只在 pool 里挑，**定格那一张不许提前抽到**，闸里有一条正面断言）。
- **自动重置提示三个候选**（Kevin 09-19「两个都不好，重做」，这是重做的三版，各 2 帧）：
  `rewind` = 全屏 VHS 扫描条 + 棋盘上几条撕裂带从右往左刷过 + 左向双三角 ◀◀ 与一行「本关重置」；
  `edge` = 屏幕四边红光绕一圈扫过 + 棋盘抖一下（抖的是 `shake_node`，缺省就是棋盘，收尾还原）；
  `dissolve` = 细胞自上而下溶解成碎粒、隔 0.55s 再自下而上浮现回来。
  三版共用同一行字、同一字号同一位置，**差别只在动画** —— 就是为了对拍着挑。

**数字**：`t_tutor_fx` **19 项检查全绿**（单跑 `--only=t_tutor_fx`；顺带核过 `t_font_coverage` / `t_no_engine_in_ui` / `t_xcheck` 三条会被新文件波及的闸，28 项绿）。全量按 Kevin 09-19 的口径**没跑**（推 main / 发版前再跑）。闸三 `total_sites` 3437 → **3458**（`--write` 抬一次）：涨的 21 条里 19 条是本片新增、另 2 条是上一片（S13 补 ∞ 字形）留下没抬的；**全落 `guide` 档 376 → 395，`core` 四个数 1029 / 85 / 1009 / 416 一个没动**。⚠ `tools/xcheck_report.py` 的 `FUNC_SUBSYSTEM` 补了两行：v2 这批测试叫 **`t_tutor_*`**（不是 `t_tutorial_*`，少一个 ial），不加就会掉进兜底的 `core` 档、把闸三的分母搅了 —— 后面几片的 `t_tutor_data` / `t_tutor_beats` / `t_tutor_flow` 都靠这两行，别再各加一行。

**没做的 / 留给别人的**：演出库不依赖导演也不依赖皮，`flow[].play` 与钩子怎么调它是 S2/S3 的事；`docs/新手引导v2_实现方案.md` 此刻还没进 main（S1 在做），所以本片的落地记录先写在这儿，等那份方案合进来再把这一段搬进它的 §5 之后；`docs/架构说明书.md` 里 `game/scripts/tutor/` 这一层由 S1 统一补（本片只是在那个目录下放了第一份文件，两边都写必冲突）。

## 2026-09-19 · 新手引导 v2：七关盘面示意图 + 盘面提案（★Q-05 交给 hxr 圈格子）

方案 §8.3 的 ★Q-05「第七关整张地图与 T 细胞站位要具体格子；第二、三关的『凸的癌组织连通块』『向前 / 向周围延伸』也要圈定」一直卡在「没人给得出具体坐标」。Kevin 09-19 的答复是「按默认走：我方出示意图给 hxr 圈」，于是把方案默认摆成七帧**真渲染**图，配一份可以直接在上面改的坐标表。

- `game/tests/preview/preview_tutor_boards.gd`（新）：真 `Board.tscn` + 真机位（按**活跃格集合**推近，照 `CWMatch.tutorial_active_tiles` / `board.set_active_tiles` 的口径，不重铺格网）+ 真组织 / 细胞贴图。第一～五关各一帧，第七关两帧（初始 / 第 23 步边缘再生）。
- `docs/新手引导v2_盘面提案.md`（新，CRLF + BOM）：每关一小节 —— 图、方案默认坐标表（格 / 细胞 / 席位 / 能量）、要 hxr 答的问题清单、几何约束；末尾一张一次性勾选表。
- 图落在仓库外的 `截图_2026-09-19_教程v2_盘面示意图/`（七帧 960×540）。

**默认摆法一句话**：第一关 `(-1,-1)`→`(0,-1)` 两格；第二关 `r=-1` 排 `q=-1…5`，癌 `(3,-1)(4,-1)` + Step2 揭 `(5,-1)`；第三关 `q=0…6` + `(5,-2)` 做「凸」，免疫 `(0,-1)` 能量 6.1；第四关以 `(0,-1)` 为心半径 2 的 19 格、两个癌块 4+5 格、四只癌细胞；第五关同 19 格、两块各扩到 6 格 + 两格固化 + 四种免疫各一，9 席正好到顶；第七关 `r=-1/-2` 走廊 25 格，玩家 `(-3,-2)`、T `(6,-1)`，再生 T/B 预置在 `r=-3` 的三格活跃集之外。

两个坑值得记一笔：

1. **坐标标签的 z 不能按 `tile_z` 排。** 标签一开始挂在 `tile_z(c, Z_MARK)`（比同格细胞低一档），第四 / 五关密排时**斜下方**那只细胞的贴图正好长到上一排的标签上，把「-1,-1」啃成「1,-1」——渲图逐帧才看出来。改成整层 `Z_DICE_TOP`（同骰子层的理由：它要盖住的是**别人格子**上的东西，按排比大小根本表达不了），再靠「标签落在格心下方 8px、细胞贴图下边缘在 +6」保证自己那格的细胞永远不被自己的标签压到；标签用 `scale = 1/zoom` 拉回屏幕上恰好 10px（CWStyle.SIZE_LABEL）。
2. **方案里写的「第七关玩家 `(-3,-1)`」是围完一圈之后的位置，不是开局位置。** 照字面把开局放 `(-3,-1)`，围 `(-4,-1)` 那一环走完 5 步会停在 `(-4,0)`，到 T 细胞 10 格，`5k+1` 的转移几何当场散架。开局要放 `(-3,-2)`：从它起步 5 步正好把环走完**并停在 `(-3,-1)`**，距离 9 →「进 1 退 2」两轮 → `(-5,-1)` 距离 11 = 5×2+1 → 两次【转移】`(0,-1)`→`(5,-1)` 正好邻接 T。这一条已写进提案的「几何约束」一节。

只出图、不动引擎：`scripts/**` 与 `data/**` 一个字没改，也没跑测试套件。

## 2026-09-19 · 合并 PR #60（Ydee1208）：意图 AI 热路径去掉 state_hash（with_hash 可选）+ 单卡估值表改杠杆口径

`mech_intent.gd` 的 `evaluate_path` / `_read_metrics` 加 `with_hash := false`，逐候选评估不再全盘编码取哈希（作者实测快 64%），回放 / 排错时显式传 true；`mech_card_value.gd` 从「能量当量」改成「单杠杆归一化的杠杆向量 + 参考倍数 q」（只在单杠杆内排序、不跨杠杆比），`docs/平衡_单卡估值.md` 同步。无冲突，`mech_card_value.gd` / `mech_value_test.gd`（112 项）单跑绿；按 Kevin 09-19 的口径没跑全量（只动 AI 研究脚本与一个默认参数，对局逻辑零改动）。

## 2026-09-19 · 点阵字库补一个 ∞（U+221E）字形（Kevin「补字形，如果没有相似字形，就用 INF」）

缝合像素 10px 原版没有 ∞，也没有 ≈ / ∝ 能顶替（教程 S4 的「能量 ∞」正是被字形闸 `t_font_coverage` 拦下的）。按 Kevin 那句的**前一半**办：`tools/add_infinity_glyph.py`（fontTools）手画点阵补进 `fusion_pixel_10px.ttf` —— 9×5 像素、advance 1000（全角，和 → / ○ 一档）、纵向 y 100..600 与数字（y 0..700）同基线齐腰，每个亮像素写成一条顺时针方形轮廓（与数字 0 的方块同向）；`cmap` 三张子表 / `glyf` / `loca` / `hmtx` / `vmtx` / `maxp` / `post` 一并更新，字形 24803 → 24804、码位 25070 → 25071。`.import` 一个字没动，Godot 按路径重导（跑过一次 `--import`；它把所有 `.import` 的行尾从 CRLF 压成 LF，内容零变化，已 `git checkout` 还原）。

- **许可证**：OFL 1.1 准改准分发；这份字体的版权行 `Copyright (c) 2022, TakWolf` **没有声明 Reserved Font Name**，所以家族名 `Fusion Pixel 10px Prop zh_hans` 原样不改，处置记在 `game/assets/fonts/README.md`。
- **真渲染对照图**：新建 `tests/preview/preview_infinity.gd`（照 `preview_config.gd` 的做法，**不能 --headless**），真 Board.tscn + GAME 机位、右栏 PANEL_WIDTH 的真位置上按 CWStyle 的 30 / 20 / 10 三档各画「∞」「∞ 3.0」「能量 ∞」，外加一行「0123456789 ∞」对高低 → `截图_2026-09-19_教程v2_S13/infinity.png`。三档都清爽不糊、两个环的交叉看得清、与数字齐腰。
- **闸**：`t_font_coverage` 在判定器自检后面加一条 `supported.has(0x221E)` —— 字体被上游原版覆盖回去就先红，不用等玩家看见方框。

## 2026-09-19 · 删 `world_events_on` 旋钮 + 物理删除七个冻结协议字段（观测协议 p=3、NET_VERSION 31）

**为什么现在删**：09-19 上一批「删世界事件」（9eb70a1）把旋钮 `world_events_on` 与六个观测协议字段**冻成恒定值**、登记「随下一次协议升号一并物理删除」（拍板记录 §十五再续六 ①）。Kevin 今天答「删」，本批就是那次升号 —— 一次删干净、只升一次号。

**删了什么**（七个字段）

- 旋钮 `CWTuning.world_events_on`：出 `RULE_FIELDS`（63 → 62 键）；旋钮台账 `contract_tune.json` 65 → 64 条（tier A 36 → 35，`in_rule_fields` 63 → 62）。
- 观测协议六键：`events.pool` / `events.double_next` / `events.active[].doubled` / `events.active[].d.is_world_event`（`EFFECT_D` 整张常量表删、`CWMirror` 那一支校验同删）/ `g.d.is_world_event_round`（tier A）/ `g.d.next_event_round`（tier B，连同 `CWObsCodec.next_event_round()` 函数）。加上 `tune.world_events_on` 正好七个。
- 建房链路：C→S `create_room` 的 `world_events`、`CWRoom.world_events` 字段与 `configure()` 形参、S→C `room` 报文与大厅列表行里的 `world_events`。⚠ 删形参会**静默改掉位置参数语义** —— `headless_test.gd` 两处 `configure(srv, "TESTxx", 4, 0, true, true)` 的第 6 个实参本来是 `p_world_events`，同批改成 5 实参，否则那个 `true` 会落到 `p_watch_hands` 上、测试照绿而验的东西变了。
- 演出条目 kind `world_event`（全仓零生产者）：`cw_kernel.gd` / `cw_kernel_remote.gd` / `cw_net_client.gd` 三处 `STREAM_KINDS` 与 C# `SessionV1Tests` 白名单同批删。
- 世界文件：`cw_world_loader.gd` 的 `EVENTS_KEYS` 3 → 1、`EFFECT_KEYS` 5 → 4；写了 `pool` / `double_next` / `doubled` 的旧世界段从此 UNLOADABLE ⇒ 不兼容 ⇒ 格式号 `cwxworld/2` → **`cwxworld/3`**（`cwxcase/2` 不动，用例自己的 13 键一字未改）。九份用例 / 夹具 JSON 共 20 处跟着改；`tools/migrate_l0_cases.py` 不可复用也没重写（它是 `cwxworld/1 → 2` 的一次性手术，四件事写死，改它会毁掉它的幂等自证）。

**升了两个号**：观测协议 `P` 2 → **3**（`cw_obs_proto.gd` + C# `ObservationV1Codec.Protocol`）；`CWNet.NET_VERSION` 30 → **31**。`NET_VERSION` 非升不可 —— `RULE_FIELDS` 少一项 ⇒ `rules_state()` / `tune.signature()` / `state_hash` 三者形状同时变，加上建房报文与房间行也少一个键，老客户端必须被服务器拒。`CWSave.VERSION` / `CWReplay.VERSION` **不动**：回放按 `RULE_FIELDS` 逐项 `if state.has(field)` 取，旧带子里多出来的那一项被静默忽略，`valid()` 用带子自己的 rules 重算指纹，而那个旋钮本来就不改结算。

**夹具重录**：四条 L1 trace（2p/2222、4p/4242、6p/6666 各 steps=200，4p/chemo/4242 steps=400）—— **除 header 的 `proto` 与 footer 的 `state_hash` 两行外逐字节相同**，draws / kinds / rounds / steps / winner 逐条对过；四份 `env_*.jsonl.gz`、`l0/pre_envelopes.jsonl.gz`、AI 基线 `ai_same_hash.json`。⚠ **标尺没动** —— AI 基线六个 case 的 `winner` / `round_no` / `steps` 逐位不变，只有 `hash` 变（`events` 的两个空壳字段退出快照）。**别把这次重录当成动了平衡标尺。**

**保留**（Kevin 同日另外三答）：PRD 的来源层级 `Settlement.SourceLayer.WorldEvent` / `CWCost.Source.WORLD` 保留；`cw_damage.gd:315`【抗原丢失】死代码暂时不动；建房页行距的意见已提、HEAD 已按 42 落地。卡牌的全局修饰容器 `events.active`（【基质稳定】【TGF-β释放】【TNF-α局部炎症】）与 `install_event` / `tick_durations` / `event_stacks` **一行没动**，只拿掉条目上的 `doubled` 与 `d` 两键。

---

## 2026-09-19 · 「新手引导」入口再灰回去；老教程整套推倒、基于剧本从 0 重做（Kevin）

Kevin 原话「把之前教程的 UI 等设计全部删掉，基于脚本从 0 构建一个全新的新手教程出来！现在的开场动画可以保留」+ 四答：① 界面 + 关卡数据 + 流程全重来，只留内核侧机件（关卡棋盘装载器 / 骰子带子 / NPC 席位驱动）② 先出两版界面方向稿挑一版再实装 ③ 重做期间入口先灰回去（老教程「退出后再启动会崩溃」我用八条路径没复现：暂停菜单返回 / 保存并退出再开 / 继续对局 / 跳过引导 / 首次含开场 / 发行版 exe 同样跑法，全能重开；顺带抓到一条真 bug：教程局「保存并退出」后「继续对局」会把教程世界当正式局载入 —— 满盘、无引导壳、免疫 9999 能量，随老教程一起删）④ 间章与第三章一起交、期限顺延。`main_menu.gd` Guide enabled 回 false，`t_tutorial_pick` 改判回「灰的」。线上：桌面端补丁只能从发版 tag 另起分支打（main 已合 PR #59 的三个新 class_name，跨基线闸拦 match.gd），网页版从 main 重导。S9 间章 agent 已停（老架构上的活作废）。

## 2026-09-19 · 合并线上两个 PR（Ydee1208）：#57 编辑器跳过热更、#59 意图级 AI（第四档）

- **#57**（`boot.gd`）：`_ready()` 开头 `OS.has_feature("editor")` 直接进主场景，本地 F5 不再被线上旧补丁盖掉；与 09-19 的网页版早退并列、都在查更新之前。`boot.gd` 打包器拒收 ⇒ 这条只随下一次全量发版进客户端。基线落后 main 383 个提交，只这一处冲突，两段都留。
- **#59**（`game/scripts/ai/mech/` 三件 + 六个研究测试脚本 + `match.gd` 第 4 档「意图」+ `ui_bridge.gd` 的 `mcts` 槽泛化成 `ai_bridge` + 单卡估值表 `docs/平衡_单卡估值.md`）：只 `开发日志` 冲突（堆叠）。`--import` 生成的五个 `.uid` 补进提交。**三个新 `class_name`（MechValue / MechIntent / MechBridge）⇒ 从此到下一次全量发版之前，任何碰 `match.gd` 的热更补丁都会被 `build_patch.sh` 的跨基线闸拦住**（引用了基线 client-2026-09-19 没有的类）—— 删旋钮那批协议升号本来就要三件套，届时改成全量发版。
- **改判**：`t_config_panel` 的「三档循环，拨回普通」→ 四档（树搜索后多一档「意图」，档数钉 `AI_LEVEL_NAMES.size() == 4`）。PR 只跑了 `mech_value_test`（112 项）与冒烟，没跑全量，这一条是合并后全量抓到的唯一红。

## 2026-09-20 · 意图级 AI（MechValue + MechIntent + MechBridge）落到 main + 单卡估值表

**背景**：第四档（MCTS 叶子换 NN）弃用后，改走「意图级规划」—— 目标练训练 + 研究平衡，不做公平对战强 AI。先前这批工作在 csharp-core 上以 TDD 做出（20 个提交），本片把它**移植到最新 main**（origin/main 已被 csharp-core 快进、领先旧引用 378 提交；mech 模块与测试全是新文件，只有 match.gd / ui_bridge.gd 需在最新代码上重接，`mcts` 槽泛化重命名为 `ai_bridge` 并把 `ai_baseline_case.gd` / `headless_test.gd` 的 `.mcts` 引用一并改）。

- **MechValue**（杠杆库）：单操作解析期望价值，与引擎逐位对拍、数千采样零偏差（免疫攻击期望 / 无氧连通块供给 / 小细胞跳块 / 净化断供 / 固化生灭 / 定殖能量与成本杠杆）。
- **MechIntent**（意图评估器）：迁移路径 → 快照试走 → 读「做完后的地图能量」→ 回滚；candidates 含「不动」基线 + 2 步；best_by 注入 scorer。
- **MechBridge**（意图 AI 桥）：action 用 best_by 选意图（癌方 scorer 含击杀/压迫/封骨髓战略三维），其余回落启发式；第 4 档「意图」（`CWMatch.AI_INTENT`）接入人机对局，与 MCTS 共用 `ai_bridge` 槽。
- **单卡估值表**：`game/tests/mech_card_value.gd`（遍历 `CWCardData.CARDS` 断言全覆盖，50 张非事件卡 → 能量当量）+ `docs/平衡_单卡估值.md`；发现免疫卡系统性高于癌卡、离群卡（效应记忆形成/组织驻留）。
- **验证**：mech_value_test 112 项绿、`t_kernel_attach_engine` 6 项绿、入口冒烟（意图档人机装配）过、单卡覆盖断言过。
- **诚实强度**：意图癌 ≈ 普通癌（~20-30%），瓶颈在「突变等技能不在迁移意图空间 + 规则层面免疫占优」，非迁移打分（B+A 式调权重已证无效）；意图免 82.5-85% 胜普通癌（靠主动击杀）。
- ⚠ 含新 `class_name`（MechValue/MechIntent/MechBridge）→ 进客户端只能全量发版，不能热更。

---

## 2026-09-19 · 主菜单「新手引导」开回来（Kevin「把新手教程启用」）

`main_menu.gd` 的 `ITEMS` 里 Guide `enabled` false → true，别的一行没动（09-14 停用时就是按「开回来只改这一处」做的）。现在玩家能玩到的是新版教程第一、二章五关（S0～S8 落地的 c1_l1～c2_l5），关数由 `index.json` 定，五关看完 `CWGuide._turn_page` 记 `set_all_done`、图鉴全解锁、下次进教程先挑对手癌种；间章（S9，agent 在做）与第三章（S10）落地后自动接上。`t_tutorial_pick` 改判：原「灰的、点下去什么都不发生」→「亮的、点下去直接开教程」（菜单那一下的请求清掉后，后面「挑对手癌种」那套断言原样）。真机图 `截图_2026-09-19_删世界事件/主菜单_新手引导已启用.png`。走热更补丁 + 网页重导。

## 2026-09-19 · 建房页行距 34 → 42（Kevin「建房界面的行间距可以调大一点」）

删掉「世界事件」行之后建房页只剩四行，`CREATE_ROW_H` 不必再压到 34：改成 `ROW_H`（42，与连接页 / 局域网页 / 配置页 / 主菜单项一致），251 起四行到 377，末行底离「建房」按钮 40 多像素（护栏「末行底 < 建房按钮顶」照过）。真机图 `截图_2026-09-19_删世界事件/联机建房页_4行_行距42.png`。发版刚出去，这条走热更补丁（基线 client-2026-09-19）+ 网页重导，不动服务器。**从这条起改动直接推 `main`**（Kevin 同日），`csharp-core` 不再是工作分支。

## 2026-09-19 · 全量发版 client-2026-09-19（993d384）：main 快进到 csharp-core、删世界事件随包出去、网页 / 服务器同版

**做了什么**

- `main` 两次快进到 `csharp-core`（先 fd9045c 让主链先同步，删世界事件落地后再到 9eb70a1 / 993d384）；从此两条分支同一个提交，产品分支 = main（拍板记录 §十六）。
- `BASE_BUILD` 202609140141 → **202609182322**（发版时刻，`publish_release.sh` 闸 ⑤）。
- Win / mac 包导出后 `tools/publish_release.sh client-2026-09-19` → https://github.com/Kevinnb66699/Cell-War/releases/tag/client-2026-09-19（win 96M / mac 63M，`gen_card_data.py --check` 过）。
- nginx（域名块）：撤掉 `/cellwar/latest.json(.sig)` 两条故意 404，改成 `location /cellwar/ { alias /var/www/cellwar/; }` —— 域名块的 root 是 `/var/www/cellwar/web`，manifest / 签名 / 补丁包在上一级，没有这条 alias 新 `SELF_HOST`（https 域名）一样取不到 manifest。`nginx -t` 过、reload 后三条 curl 200；备份 `cellwar.conf.bak-20260919`。reload 后头几百毫秒老 worker 还会回 404，探测要等一下再看。
- `tools/deploy_web.sh`（不再注入 shim；wasm 37M → 8.9M gz，线上 MIME 核过）→ https://cellwar.jiling.chat/。
- `tools/deploy_server.sh`：排空 → 14:26 切到新版，协议 v30 监听 8611、反馈口 8612 正常。
- 线上热更线 `diag-fx-0919`（补丁 202609180255，`min_base` 202609140141）就此作废：新客户端 `base` 202609182322 > 补丁号，`decide()` 第三道闸「build <= base → skip」会跳过它；以后的补丁一律从 993d384 另起分支打。

**顺手核过的**

- 删世界事件的两页（单机配置页 / 联机建房页）5 行变 4 行的真机截图在 `D:/Projects/SpringSense/2026-2027/Cell War/截图_2026-09-19_删世界事件/`，给 Kevin 看一眼（`BTN_Y` 480 → 438）。
- `preview_online_lan.gd` 的建房页焦点行 `_create_sel` 4 → 3（行数变了），注释同步。

**还没做 / 等 Kevin**：`world_events_on` 冻还是删、PRD 来源层级末项、【抗原丢失】死代码、两页排版认不认（拍板记录 §十五再续六）。新手引导 S9（幕间）/ S10（第三章）/ S11（收口）继续在 csharp-core 上做，随做随快进 main。

## 2026-09-19 · 删世界事件（Kevin：「我们不加入世界事件了」）—— GD 侧：15 个事件的内容与抽取整块拆掉，容器留给三张卡

**范围**：Kevin 09-19 拍「世界事件直接删了吧」，`main` 与 `csharp-core` 两条分支都删。这一条记 GD 侧（`game/**` + `tools/` + docs + PRD diff）；C# 侧（`core/**`）同批另记。

**删掉的**

- `cw_world_fx.gd` **364 行 → 36 行**：`EVENTS` / `BLURB` / `DURATION` / `STACKABLE` 四张表、`trigger()`（抽取 + 三档加倍 + 通报 + 广播）、`on_round_start()`、`round_effects()`、`_resolve()`、【紊乱】整块（`_chaos` / `_chaos_return` / `_chaos_fallback`）、`on_vessel_pass()`、`free_move_available` / `consume_free_move`、`_discard_random()`、`is_world_event()` 全删。**只留 `tick_durations()`** —— 它是三张卡（基质稳定 / TGF-β释放 / TNF-α局部炎症）与 `mods until=round` 的唯一回合时钟，录制代理 `cw_rec_worldfx.gd` 与契约表 `tick_durations` 行都钉在它上面。
- 调用点：`cw_world.gd` 的 S 阶段整步（`on_round_start` + `is_world_event_round` + `trigger`）、E 阶段第 7 步的 `round_effects`、`_tissue_production` 的【营养缺乏】早退、`_vessel_teleport` 两处 `on_vessel_pass`、`_proliferate` 的【增殖抑制】早退与【异常增殖】翻倍。
- `cw_data.gd:is_world_event_round()`；`cw_cost.gd` 的 `TEMPLATES` 世界事件段（4 个键 5 条模板：细胞应激 / 免疫伪装 / 基质阻隔 ×2 / 迁移激活）+ `Store.EVENT_FREE` 那一路 + `cond: free_move_left`；`cw_game.gd` 的 `signal world_event` / `broadcast_world_event()` / `events["pool"] = CWWorldFx.EVENTS.duplicate()` / `gain_memory` 开头的【抗原暴露】加成；`cw_actions.gd` 的 `attack_outcome` 两句并给、【抗原变异】两处抽牌、`core_gain` 的【代谢加速】翻倍、`_spend_snapshot` / `_restore_spend` 的 `free_used`；`cw_card_fx.gd` 的 `_stress_fee()` 与两个调用点、`_amp()` 的【信号放大】循环。
- 桥与分派：`cw_bridge.gd` / `cw_kernel_bridge.gd` / `cw_net_bridge.gd` / `ui_bridge.gd` 的 `show_world_event()`、`cw_play_queue.gd` 的 `"world_event"` 分支、`ui_bridge.gd` 的 `fx_world_event`。**流式条目白名单里的 `"world_event"` 与 feed 的 `kind="world"` 保留**（协议容器，永不产生）。
- UI：`feed.gd` 的 `WORLD_WHO` / `add_world_event()`；`match_panel.gd` 的事件行 + 悬浮框 + `active_events_text` / `doubled_line` + `EVENT_*` 排版常量（阶段行不再倒计时，只写阶段 + 分期）；`cw_codex.gd` 的「世界事件」章与 `event_rounds_text()`（知识之书 13 章 → 12 章）；`config_panel.gd` 的「世界事件」行（普通对局 5 行 → **4 行**，`BTN_Y` 480 → **438** 回到加行之前的排版）；`online_panel.gd` 建房页 5 行 → **4 行**；`guide_spotlight.gd` 的 `world_event` 提亮目标与教程剧本 13.1。
- 测试：13 个函数整删（`t_ev_*` 10 个 + `t_world_events_draw` / `t_world_events_off` / `t_doubled_marker` / `t_event_rounds` / `_t_no_fake_double_trigger` 与助手 `_install`），2 个削薄改名 —— `t_ev_attack_mods` → **`t_attack_verdict_base`**（只留纯 `base_verdict` 那条，4 条 L0 用例指着它）、`t_ev_proliferate` → **`t_proliferate_plain`**（留两条，两条 L0 用例指着）；`t_ev_chaos` 的「E 阶段跑完不留「新生」标记」不变量**搬进 `t_phase_order`**，构造改用癌细胞走进健康组织触发【定殖】造 newborn（不再用【紊乱】）。L0：`batch5b/` 整目录 + `batch3/tick_durations.json` 删除，另删 `batch0/const_data.json` 与 `batch3/proliferate.json` 各若干条，**431 → 416 条**；契约表 `contract_ops.json` **48 → 45 行**（删 `trigger` / `on_round_start` / `chaos_return`），6 个 `event:*` 档与 `const` 的 `cw_world_fx` 档同删；两侧常量表 42 → **39 键**（GD 半边 `l0_runner.gd:_build_consts`，C# `Probes.cs` 由 P-CS 同批逐字删）。

**冻结不删的七个（删它们要升协议号 + 全量发版，登记到批 1 那次升号）**

`state.g.events.pool` 恒 `[]`、`double_next` 恒 false、`active[].doubled` 恒 `""`、`active[].d.is_world_event` 恒 false、`g.d.is_world_event_round` 恒 false、`g.d.next_event_round` 恒 0、`tune.world_events_on` 恒 false（仍在 `CWTuning.RULE_FIELDS` 里，元素个数与顺序一字不变）。**观测协议 P 仍 = 2、`cw_obs_proto.gd` 零改动、`NET_VERSION` 仍 = 30、`cw_room.gd` 零改动**；建房报文的 `world_events` 形参也不动，`online_panel.gd` 改传字面 `false`。

**夹具重录（轨迹一步没动）**

`events.pool` 退出快照 ⇒ `state_hash` 变。四条 L1 trace **除最后一行 footer 的 `state_hash` 外逐字节相同**（`draws` / `kinds` / `rounds` / `steps` / `winner` 全同），四条 `env_*.jsonl.gz` 与 `l0/pre_envelopes.jsonl.gz` 重录；**AI 基线 `ai_same_hash.json` 六个用例的 winner / round_no / steps 逐位相同、只有 hash 变**，按 `tests/record_ai_baseline.gd` 重录 —— 这条是「平衡标尺没动」的护栏，重录的理由写在这里以免以后被当成偷偷动了标尺。

**xcheck**：`covered_sites` **427 → 416**（`xcheck_report.py --write` 重算；`core_sites` 1110 → 1029、`total_sites` 3433 → 3324），`--check` 绿。`tools/xcheck_report.py` 的 ui 档正则去掉 `doubled_marker`。**`tools/gen_card_data.py:32`** 原先拿 PRD 的「# 世界事件」当免疫卡池节的结束锚点，团队删章之后会 StopIteration —— 改成 `next((...), len(L))`，删章前后都跑得通。

**PRD**：`docs/PRD改动_2026-09-19_删世界事件.diff`（只出 diff，不改正本）：删 :59 触发回合那行、:63-65 的「世界事件阶段」整节并把后面四个阶段重新编号、:195 的免费额度入场顺序去掉「世界事件」、:1597 到文件末尾整章。**来源层级那一行（现 :175）没动**，两版任选留给 Kevin（原样 / 末项改「其他来源」）—— 引擎两侧的 `Source.WORLD` / `SourceLayer.WorldEvent` 按 PRD §181 保留。

**验证**：副本 `scratchpad/pGD_rmwe/` 上 `bash tools/run_tests.sh` 两片合计 **3733 项全过**、`tools/run_l0.sh` 的 GD 侧（runner 416 / `--selfcheck` 416）绿、闸三「✔ 闸三过」。C# 侧的 `PreParityTests` 要等 P-CS 把 codec 的 `pool` / `is_world_event` / `is_world_event_round` 一并冻住才绿（本组不碰 `core/**`）。

## 2026-09-19 · 新手引导 S6b（S6 的补片）：图鉴闸只在教程进行中生效 + 未解锁条目改「灰显」

Kevin 09-19 答完 S6 回传①「等 hxr 拍」的那一条，本片照办他拍的两条，别的一个字没动
（同期 S8 在改 `guide_data.gd` / `guide_watch.gd` / `guide_layers.gd` / `match.gd` / 第二章关卡数据，本片一行没碰）。

**改了什么**

- **`cw_guide_progress.gd`：多一个键 `skipped` + 一个只读谓词 `codex_gated()`**。三态各一句 ——
  没进过教程（没有这份 cfg，或者一点进度都没有）→ **不闸**；教程开着、还没跳过也没通关 → **闸**；
  按过「跳过引导」/ 全部通关（`set_all_done`）→ **不闸**。**判断只有这一处**，`cw_codex.gd` 只问它一句
  （S6 那会儿面板自己读 `unlocked` 就完事，口径一改就得改两处）。
  「进过教程」按进度算（`done` / `unlocked` / `at` 任一非空），**不按 `opening_seen` 算** ——
  那是 S7 写在同一份 cfg 里的另一个键，看了开场就退出去的玩家一课都没上，图鉴不该因此对他关门。
  `at` 是 `guide.gd:_render()` 在「这一步成为当前」时落的，所以「教程开始了」= 第一步渲染过了，判得住。
- **`guide.gd` 新 `skip()`**（4 行），「跳过引导」那颗按钮从 `dismiss()` 改接它：先 `set_skipped()` 再收面板。
  **和 `dismiss()` 分开是故意的** —— `dismiss()` 的口径是「只收面板、不改进度」，剧本走空那一处与老测试还在那么用，
  给它加副作用会顺手把进度也改了。`set_skipped()` **不动 `done`**：跳过不是看完，主菜单的完成标记不该因此变绿。
- **`cw_codex.gd`：`_filter` 改成 `_mark_locked`** —— 不再删条目，只给「受闸而还没解锁」的那些打一个
  `locked: true`。于是 `chapters()` / `search()` 返回的**条目一条不少、位置一格不挪**，
  `search()` 的每条命中也跟着带 `locked`。就地打标不另建一份：唯一的调用方是 `chapters()`，而它每次现建整本书。
- **渲染两处照 `locked` 画**：正文页标题 `TEXT_HI → TEXT_OFF`、正文 `TEXT → TEXT_OFF_DIM`
  （CWStyle 现成的「灰掉」那一对，**不新造色值**，同 `_paint_arrow` 压暗翻页箭头的写法）；
  结果页整行降一档、命中的字**不**标阵营色（标了就是这一行唯一的亮点，等于反过来替玩家指路）、
  不收点击、不换鼠标形状、悬停也不提色。回车（`_on_submit`）跟着改成「跳到第一条**点得动的**」。
- **解锁那一刻的慢闪原样保留**：`_fresh` 只收已解锁的点，所以 locked 的条目永远进不来，渲染那头不必另判。
- **面板只在 `open_to()` 问一次**：`_refresh_unlocked()` 里 `_gate = _unlocked if codex_gated() else null`，
  其余三个调用点（`_rebuild_page` / `_on_query` / `_goto`）统一传 `_gate`。

**为什么「灰显」比「隐藏」对**（Kevin 的原话是结论，这是落地时想明白的）：隐藏的代价是玩家**不知道有这一条**，
翻到「棋盘与地形」只看见三条，以为书就这么薄；灰显是「这里有东西，你还没学到」——同一块闸，一个是删内容、一个是给目标。
搜索那一半同理：条目在页面上本来就看得见，再从搜索结果里剔掉只会让玩家以为书里根本没这个词。

**改判表**（原 → 新 → 依据）

| # | 原断言 | 新断言 | 依据 |
|---|---|---|---|
| 1 | `t_codex`「零解锁：受闸条目**一条都不出现在页面上**，但章一章不少」 | 「受闸条目**照样在页面上**、一条不少地打上 `locked`，章一章不少」 | Kevin 09-19 拍灰显（S6 回传①「等 hxr 拍」的那一条） |
| 2 | `t_codex`「解锁【净化】→「净化与记忆」**回到页面上**；没解锁的「健康组织」仍在闸后」 | 「「净化与记忆」**转亮**；「健康组织」**灰着但还在**；没点名的常驻条目从不灰」 | 同上 |
| 3 | `t_codex`「不传解锁集 = **不过滤**」 | 「不传解锁集 = **一条都不打标**（整本书全亮）」 | 同上（`_filter` → `_mark_locked`） |
| 4 | `t_codex`「**未解锁的搜不出来**，解锁之后搜得到」 | 「未解锁的**仍搜得到、结果带 `locked`**；解锁之后同一条转亮」 | 同上 |
| 5 | `t_codex`「零进度开书：「健康组织」**在闸后**」 | 「零进度 = 从没进过教程 ⇒ **连解锁集都不往下传**，一条都不灰」 | Kevin 09-19「从没进过教程的玩家也全解锁」 |
| 6 | `t_codex`「解锁【健康组织】后重开书：**它回到页面上**」 | 「闸开着 —— 刚解锁的转亮，**同章没解锁的「固化癌组织」仍灰显**」 | 同上两条合并 |
| 7 | `xcheck/COUNT` 的 `total_sites` 3488 | 3502 | 新增 14 条断言，`core` 分子分母一个数没动 |

**老 check 一条没删**。新增 14 条：`t_guide_progress` 7 条（`codex_gated()` 三态各一条 + 「跳过不动 `done`」
+ 「解锁过一个点也算进行中」+ 与 S7 的 `opening_seen` 互不吃掉那两条）、`t_codex` 7 条
（灰显的色值与不收点击、亮的那条一档没动、结果页命中带 `locked`、结果页那一行不收点击也没挂回调、
回车不跳灰显的、**按下「跳过引导」按钮后图鉴闸当场放开**、跳过后重开书一条都不灰）。
「跳过」那一条是**真按那颗按钮**（新建一只 `CWGuide`、`_build()`、往 `_skip` 上 `gui_input.emit` 一次左键），
不是直接调 `set_skipped()` —— 接线断了当场红。

**真机截图步骤串**（前置：`main_menu.gd:81` 的 Guide 临时改 `true`、**别提交**；
删 `%APPDATA%/Godot/app_userdata/Cell War/guide_progress.cfg`；**不能加 `--headless`**）：

```
## ① 教程进行中：灰显条目与解锁条目并排（截到的是「棋盘与地形」那一章）
"D:/Godot/Godot_v4.5-stable_win64.exe/Godot_v4.5-stable_win64_console.exe" --path game \
  --script res://tests/screenshot.gd -- user://s6b_gated.png res://scenes/Main.tscn -1,-1 15.6 \
  "key:ui_down@0.5;key:ui_down@0.7;key:ui_down@0.9;key:ui_accept@1.1;call:CWGuideShell:close_banner@13.6;call:CWGuide:open_codex_at_current@14.2;key:ui_right@15.0"

## ② 从没进过教程：主菜单直接开「知识之书」，整本全解锁（同一章逐条比）
"D:/Godot/Godot_v4.5-stable_win64.exe/Godot_v4.5-stable_win64_console.exe" --path game \
  --script res://tests/screenshot.gd -- user://s6b_all.png res://scenes/Main.tscn -1,-1 2.4 \
  "key:ui_down@0.5;key:ui_down@0.7;key:ui_accept@0.9;key:ui_right@1.6"
```

**时间点是量出来的**：①「开始对局」往下三下到「新手引导」（Guide 临时开着时，没存档 ⇒「继续对局」「对局回放」
是灰的、键盘自动跳过，所以三下不是五下），回车进去 **开场动画整段要演 10.75 秒**（S7；`guide_progress.cfg`
一删 `opening_seen` 就没了，它必播），**章节提示 13.4 秒才立起来** —— S6 那会儿的 `close_banner@5.6` 是开场落地前的数，
照抄会点在幕布上。想省这 11 秒就别整份删 cfg，只写一个 `opening_seen=true` 进去：那仍然是「没有任何进度」，
`codex_gated()` 照样返回 false，等第一步渲染完 `at` 落盘才转成闸。
**`key:` 走的是 `InputEventAction`，跳不掉开场**（开场的「按任意键跳过」认的是真键盘事件），别在这儿浪费时间。
② 里两下 ui_down 到「知识之书」，`ui_right` 翻到第 2 章 —— 两张截同一章才好逐条比。
**操作串别断行**：`\` 接下来那一行的前导空格会原样进到字符串里，`call:` 前面多几个空格就认不出来了（解析只 `split(";")`，不 strip）。
**往下数几下要看有没有存档**（灰掉的项键盘会自动跳过，数目跟着变）——S7 用的「往上数三下」那招（`ui_up` ×3 = 退出游戏 → 设置 → 新手引导）跟存档无关、更稳；本条两串是 09-19 当天在**无存档**的机器上量的。

**两张都核过**：①「一块蜂窝棋盘」（没点名，常驻亮）／「健康组织」（刚解锁，亮且在慢闪）／
「癌组织」「固化癌组织」（标题与正文都灰）／「三种特殊组织」（常驻亮）—— 灰的亮的**穿插排着、位置一格没挪**；
② 同一章五条**全亮**。

**数字**：`tools/run_tests.sh` 3918 项绿 / 249 个测试（+14 项 / +0 个测试函数 —— 加的判据都挂在现有两个测试里）；
`run_l0.sh` 五段绿（GD 431 / 自检 431 / C# 468）；闸三 `total_sites` 3488 → **3502**（`--write` 抬一次：
涨的 14 条全在 `guide` 与 `ui` 档，`core` 分子 427 / 分母 1110 一个数没动）。

**没做 / 要拍的三件**：

1. **「不可展开」今天是空话**：Kevin 那句里的「不可展开 / 不响应点击」来自 hxr Q-08 的**拆条目**提案，
   而图鉴条目从来就不能折叠（正文一直是整块画出来的，S6 也没拆条目）。本片把它落成「**不响应点击**」：
   页面上的标签一律 `MOUSE_FILTER_IGNORE`（`CWStyle.label` 本来就是），结果页那一行不收点击、回车也不跳。
   **要真做成「灰显只给标题、点一下才展开」得先拆条目**（Q-08 的前半条，S6 也没做）。
2. **灰显的正文是真的画出来的**（Kevin：「标题与正文降成灰色」）。也就是说这道闸从此是「你还没学到」的标记，
   不再挡内容 —— 配合口径③（闸只在教程进行中生效）其实是一致的：只有正在上课的那几十分钟里才有灰的。
   S6 回传里猜的是「正文不画」，**按 Kevin 的字面办了**，要收紧就是 `_rebuild_page` 少画一段。
3. **跳过之后再回来接着看教程，闸不会自己关回去**（`skipped` 不会清）。这是「跳过之后全解锁」的字面读法；
   要改成「重进教程就重新闸上」，是在 `CWGuide.setup()` 里清一次 `skipped`，一行。**等一句话。**

## 2026-09-19 · 还掉网页版的「发版侧猴补丁」：地址改 https 域名、网页版不查更新、shim 删除

**这笔账是什么**

09-14 网页版上线（https://cellwar.jiling.chat/），Kevin 第一句是「为什么 cellwar 显示不安全」。
不是证书 —— 是 `boot.gd` 的热更 manifest 和 `feedback.gd` 的反馈口都写死成
`http://124.221.78.13/cellwar/`：https 页面发 http 请求 = **混合内容**，浏览器拦掉、整页标「不安全」，
拦掉之后还要干等满 `CHECK_BUDGET`（10 秒）才进得去游戏。

根治要改 `boot.gd` 的一行常量，而**它读在挂载补丁之前、热更不了**，一改就必须全量发版
（而且从改完那一刻起到发版为止，桌面端的补丁打包器会整个拒绝出包）。所以 09-15 只能在**发版侧**
顶一个猴补丁：`tools/web/same_origin_shim.js` 由 `deploy_web.sh` 注入 `index.html` 的 `<head>`，
包住 `fetch` 与 `XMLHttpRequest.open` 把地址改写成同源路径。它赌的是 Godot 的 web 版 HTTPRequest
走 fetch/XHR —— 引擎换实现就**静默失效**：页面变回「不安全」，而且不报任何错。
提醒因此挂在 `tools/publish_release.sh` 的**所有闸之前**（放后面的话，看到它时两个包已经导完了，
照着改完还得再导一遍），全量发版是还它的唯一时机。**这次就是。**

**改了什么**

- **`game/scripts/boot.gd`**：`SELF_HOST` → `https://cellwar.jiling.chat/cellwar/`
  （`MANIFEST` / `MANIFEST_SIG` / `PCK_HOSTS[0]` 随之）；`_ready()` 在**查更新之前**加
  `if OS.has_feature("web"): get_tree().change_scene_to_file(MAIN_SCENE); return`。
  文件头那段「为什么不上 HTTPS」改成「当初的理由 + 这次为什么改 + 代价」。
- **`game/scripts/ui/feedback.gd`**：`URL` → `https://cellwar.jiling.chat/cellwar/feedback`。
- **删 `tools/web/same_origin_shim.js`**；`tools/deploy_web.sh` 里注入它的那 18 行整段删掉
  （脚本其余一行没动，`bash -n` 过）。`publish_release.sh` 那段提醒按 `[ -f ]` 打印，
  **跟着自动消失，不用改**。
- **`game/tests/headless_test.gd`（`t_hot_patch`）**：两条改判 + 两条新增，见下表。
- 文档：`docs/网页导出.md` §① 改写成「已根治」、§④ 的反馈地址跟着改；本条日志。

**两件故意没做**

① **`PCK_HOSTS` 留着明文的老地址**（`[SELF_HOST, "http://124.221.78.13/cellwar/"]`）。
`tools/build_patch.sh` 的 `HOST` 仍是 `http://124.221.78.13/cellwar`，而它不在这次动的范围里。
去掉老地址 = **每个补丁都被 `decide()` 静默跳过**：不下载、不报错、没人发现 —— 正是 `decide`
头注里写的「漏装」那一档。补丁包走明文本来就是有意的（完整性靠 manifest 里的 SHA-256，
不靠传输层），而混合内容只是**网页**的事，网页版现在根本不查更新。新加的护栏直接抠出
build_patch.sh 的 `HOST` 去核 `Boot.pinned()`，两边再漂就红。

② **`BASE_BUILD` 没动** —— 那是发版脚本那一步的事，`publish_release.sh` 第 ⑤ 项闸着
「必须比上一次发版大」。

﻿## 2026-09-19 · 联机三条 issue：#44 代打接管卡住 · #45 Esc 菜单阻塞演出 · #46 退出房间后回不去

一组三条，都在「客户端与服务器对同一件事的理解不一致」这条线上。`game/scripts/core/**` 与协议件一字未动，
`CWNet.NET_VERSION` 仍是 **31**（报文形状没改：#44 用的 `step_begin` 是早就在发的那一条，#46 用的 `reconnect` 也是既有报文）。

### #44 移动时被接管会卡住

**现象**（Kevin 的截图：右栏写着「剩 0 秒」、`xuan` 标着「离线代打」，而左下角还摆着「选择要移动到的组织 · 高亮 6 格可达」，
点哪一格都没反应）。**根因**：服务器超时 / 掉线替我答完这一问（`cw_room.gd` 的 `_auto_answer`）之后
**不会再给我发一条 ask** —— 它答完就广播 `step_begin` 接着往下走。界面这边还挂在上一问的 `await` 上：
行动栏、可达高亮、路径规划器全留在屏幕上，倒计时停在 0，而对局早就走到下一位了。
要等**下一次轮到我**才被 `CWMatch._serve_ask` 开头那句 `bridge.abort()` 收掉 —— 中间那一整段就是「卡住」。
迁移尤其难看：`_sticky_move`（上一步选的是迁移）与 `_plan`（规划好的路线）是**跨问留存**的，
下一问会直接跳回选目标态、甚至照着一条按旧盘面算出来的路 `_plan_take_step` **不问自答**。

**改法**：`step_begin{ask_id, seat}` 本来就是「这一问已经答了」的那一拍（`answer` 与 `_auto_answer` 都在 emit 之前广播它）。
`CWMatch._on_step` 见到它就问一句「这一问还挂在句柄手上吗」——`CWKernelRemote.asking(ask_id)`
（`answer()` 里已经 `erase`，所以**我自己答过的一定是 false**）。判据提成纯函数 `CWMatch.taken_over(step_ask_id, serving_ask, asking)`。
是接管就 `_serving_ask = -1`（排在前面，旧协程醒来一比就知道这个答案不该发）→ `CWUIBridge.taken_over()`
（= `abort()` 再多清 `_sticky_move` / `_plan`；`abort()` 自己不清是对的，迁移的每一步都要经过它，清了就不再是切换式的）
→ 停倒计时 → 一句「这一步由代打接管了」。

顺带修掉一处被它引出来的老坑：`CWMatch._error_at()` 在**落子阶段**会 `cells[-1]` 越界（那时 `cell_id` 还是 -1）。
从前走不到：调它的只有「对局中冒出来的服务器拒绝」，都在开局之后；接管提示在第一问就可能弹，于是撞上了。

**怎么验的**：`t_net_takeover`（真 socket + 真 `Main.tscn`）—— 摆成「正在迁移」的状态，
让服务器走一次 `_auto_answer`，断言提示栏收掉、可达高亮清空、`_plan` 与 `_sticky_move` 作废、倒计时停掉。
反向验过：把 `_on_step` 里那一句换成 `pass`，这条测试当场红 4 项。

### #45 联机对战中 Esc 菜单阻塞动画播放

**根因**：`CWPauseMenu.open()` 一律 `get_tree().paused = true`。本地局这是对的（AI 的行动间隔是 `SceneTreeTimer`，
停下来才是玩家要的那个「暂停」）；联机局由服务器驱动、客户端只是镜像 + 播放，冻了树，演出层挂在节点上的
补间全停住 —— 而骰子那一条是 **barrier** 条目，`CWPlayQueue.play_one` 正 `await consumer.show_roll(...)`，
于是整条条目流跟着卡住，菜单一关又一口气补播。服务器可不等人：计时照走、别人照打。

**改法**：`freezes_tree(p_online)` —— 联机局不冻树，改成「只挡输入、不停播放」。
鼠标本来就被本节点整屏的 `MOUSE_FILTER_STOP` 挡着；键盘由 `CWPauseMenu.modal()` 挡，
对局里那四个单键快捷键各自先问一句（`CWActionBar` 数字 / `CWMatchPanel` 空格 / `CWLogPanel` 的 L / `CWChatBox` 回车），
判据和它们早就在问的 `CWChatBox.typing()` 同一路数。聊天那一句**必须排在 `_input` 最前面** —— 它比谁都早，
不让路的话 Esc 会被它先吃掉、菜单关不掉。

⚠ `modal()` 是**现问现答**（活着 + 在树上 + 可见），不是一面 `static var modal := false` 旗子。
第一版就是旗子，套件当场红 9 项：`t_chat_box` 里有一处 `pause.open()` 之后直接 `queue_free()` 整层、没走 `close()`，
旗子从此永久钉在 true 上，**之后所有测试的单键快捷键集体失灵**。生产里同一类路（对局 `teardown`）一样存在。

**怎么验的**：`t_pause_and_teardown` 补「本地冻 / 联机不冻 + 摘出树就自愈」；`t_chat_box` 补「菜单压着时四个键全让路、一关就恢复」；
`t_match_online` 里加了整局那一条 —— 菜单开着时服务器推一份状态，出牌列照样长（冻了树 `_process` 就停，这一条必红，验过）。

### #46 联机对战退出房间后无法重连继续对战

**根因不在服务器**：`CWRoom.leave` 在对局中只把席位标成「已离开」，令牌原封不动（`reconnect` 也不挑主动离开还是掉线）。
丢掉回程票的是**客户端**：`CWNetClient.leave()` 顺手把房间码与令牌一起清了（`_clear_room`），
那条连接又被 `CWOnlinePanel.leave_online()` 整个丢掉 —— 于是大厅里只剩「进去当观众」这一条路：
看得见盘面、答不了任何一问，自己的席位还空着。

**改法**（只动 `online_panel.gd`）：离开之前把这一席抄在**面板**上（`_resume = {code, token}`，
面板跟着主菜单活着、比那条连接长命），条件是「局在打 + 我有席位 + 手里有令牌」。
回到大厅：房间码预填进输入框、状态行写清「你还有一局没打完（房间 XXXX）—— 点「加入」回去接着打」；
「加入」与大厅里自己那一行的点击都改走 `_lobby_join`，认出是自己那一局就发 `reconnect` 而不是 `join`
（差的不只是一个报文：`join` 进去是观众）。自己那一行的文案也从「观众 2/8」改成「← 回到对局」。
票在回到席位时用掉（room 视图 `you_seat >= 0`），在**自己发起的那一次**被答 `bad_token` / `no_room` 时作废并说明
（`_resume_tried` 防连坐 —— 随手输错一个房间码同样答 `no_room`）。暂停菜单的确认页小字也跟着补了后半句
「可凭房间码回来接着打」，不说清楚玩家只会读到「走了这局就没了」，于是宁可挂机干等。

**私密房**是这条预填的正主：它在大厅列表里根本不出现（`lobby_view` 只列公开房），房间码是唯一入口。

**怎么验的**：`t_net_resume`（真 socket）—— 离开 → 席位标「已离开」、客户端票没了、服务器令牌还在 →
大厅改口 + 预填 → 点「加入」回原席 → 服务器又开始问我；再加两条负面：令牌对不上要作废票，输错别的房间码不能连坐。
排版走真渲染：`tests/preview/preview_online_resume.gd`（新），图在 `截图_2026-09-19_issues_net/lobby_before.png` / `_after.png`。
第一版行文案写成「← 回到对局（你的席位还留着）」，被那一行定宽 400 px 截成「你的席…」——该说的话交给状态行，行里只留「← 回到对局」。

**改判表**

| 断言 | 改前 | 改后 | 为什么 |
|---|---|---|---|
| `confirm_hint("menu", online)` | 离开后本局由 AI 代打 | 离开后本局由 AI 代打，**可凭房间码回来接着打** | #46 把那条路补通了，后半句才成立 |
| `confirm_hint("quit", online)` | 退出后本局由 AI 代打 | 退出后本局由 AI 代打，**可凭房间码回来接着打** | 同上 |
| 大厅里自己那一局的行 | `XXXX  4 人局  观众 2/8  甲 的房间` | `XXXX  4 人局  ← 回到对局` | 写「观众」会让人以为席位没了 |

**数字**：`bash tools/run_tests.sh` 全量绿 —— 两片合计 **3967 项检查**、L0 两侧同跑全绿（GD 416 条 / C# 453 项）。闸三 `total_sites` 3458 → **3501**（`--write` 抬一次）：新增 43 条断言分在 `net` 484 → 520、`ui` 1145 → 1152，**`core` 四个数 1029 / 85 / 1009 / 416 一个没动**。⚠ `t_feedback` 抢 18612 端口那 4 条第一遍红了两次（别的 agent 同时在跑，netstat 里全是 TIME_WAIT），端口放开后单跑与全量都绿 —— 和这批改动无关，见记忆里那条。

**没做 / 留给后面的**：① `_resume` **不落盘** —— 关掉游戏再开就没了（要不要进 `CWSettings` 等 Kevin 拍）；
② 掉线期间收不到 `step_begin`，重连回来那一问若已被代打答掉，界面仍要等下一次轮到我才收（今天的兜底路），
要彻底得让 `reconnect` 的 `push_state_to` 带一句「你手上那一问的 ask_id 是多少」；
③ `CWNetServer._reconnect` 是**先 `unbind` 再验令牌**，令牌错了会把人从当前房间踢出来 —— 老坑，本批没碰。

### 复核修订（同日回炉，评审 findings 逐条）

**① #45 的「只挡输入」这条契约漏了 `CWHand`**（medium，真会打出牌）。`hand.gd` 的 `_input` 是 **Node 级回调**、
排在 GUI 派发之前，暂停菜单整屏那道 `MOUSE_FILTER_STOP` 根本拦不住它 —— `chat_box.gd` 那一支正是因为这个才被打上 `modal()` 闸，
手牌被漏了。从前靠 `get_tree().paused` 把它一起停掉，现在联机局不停了，于是：拖起一张手牌 → 按 Esc 弹暂停菜单
（此时 `can_cancel()` 为 false，Esc 归菜单）→ 卡继续跟着光标画在模态菜单**之上** → 在抽屉外松手，
`hand.gd` 照样 `emit play_requested`，这张牌就在菜单开着的时候被真打出去了，且不可撤销。
改法照 `action_bar` / `log_panel` / `match_panel` 同一路数，闸装在 `_input` 第一句，**并且把半途的拖动当场作废**
（`_drag = -1` + `_layout()`）—— 只 `return` 不清 `_drag`，卡就停在菜单上面不动了（`_layout()` 碰都不碰拖动中的那张）。

**② `_on_step` 头上的文档注释与实现打架**（low）。本仓把注释当契约用，架构说明书多处引「`step_begin` 一个闸都不装」这一句，
而这批恰恰在 `step_begin` 上装了 #44 的接管判定。改成「`step_begin` 只做演出分组（快进 / 跳过）与 issue #44 的接管判定，
教程的装闸时机仍只在 `step_end`」。

**③ #46 的回程票不记服务器地址**（low）。`_resume = {code, token}` 只认房间码，可**房间码只在它自己那台服务器上唯一**：
离开 A 服的对局改连 B 服，`hello` 分支照样 `_hint_resume()` —— 大厅预填 A 服的房间码、状态行写「你还有一局没打完」，
点下去在 B 服答 `no_room`，而 `_resume_tried` 为真，会被解释成「那一局已经结束，或席位已被收回」：
**A 服那一局其实还在好好打着，票却被无辜作废了**。改法是 `_remember_seat()` 连 `client.url` 一起抄，
判据收在一处 `_resume_here()`（票非空 + 地址对得上），四个读票的地方各问一句：`_hint_resume()`（不预填）、
`_lobby_join()`（走普通 `join`，不拿票去 `reconnect`，所以 `_resume_tried` 保持 false、答了错也不连坐）、
大厅列表行文案（不写「← 回到对局」）、`"room"` 分支用掉票那一下（不作废）。地址对不上就**当没有票**：
不预填、不改行文案、也不作废 —— 回 A 服它照样能用。

**不改的两条（点名交出去）**

- `_on_step` 里老代码那句 `if str(e.get("kind", "")) != "step_end": return` 读的 `kind` 在 step 条目里**根本不存在**
  （服务器发的是 `{"t":"step_end","rev":N}`，`cw_room.gd:691`；`kind` 只是 fx / game_over 的载荷字段），
  也就是说 **step_end 分支一直是死的**：`_step_rev` 不更新、`_guide.rebase_watch()` 与 `_apply_guide_step()` 在这条路上从不执行。
  这是 `d20ced2`（教程 S4）带进 main 的老坑、不是本批造成的，改法是把 `e.get("kind", "")` 换成 `e.get("t", "")`
  并补一条直接喂 `{"t":"step_end","rev":N}` 的测试 —— 但它会**唤醒教程的装闸路径**，属教程组的禁区文件范围内的行为变更，本组不碰。
- #44 只补通了「客户端在线、计时到点被代打」这一路（Kevin 的截图正是这一路：延迟 25 ms 还在、剩 0 秒）。
  「掉线期间被代打」那一路仍然卡，原因见上面「没做 / 留给后面的」②：要动 `push_state_to` 的报文形状 = 升 `NET_VERSION`。
  **issue #44 别当成完全关闭**。

**改判表（复核这一轮）**

| 断言 | 改前 | 改后 | 为什么 |
|---|---|---|---|
| `t_chat_box` 的 #45 那一段 | L / 数字 / 空格 / 回车四支让路 | 多一支：菜单压着时手牌在抽屉外松手**不打牌**、`_drag` 归 -1；菜单一关同样的松手照常打出 | finding ① |
| `t_net_resume` 抄票那一条 | 票 = {code, token} | 票 = {code, token, **url**} | finding ③ |
| `t_net_resume` 尾部（新） | —— | 换一台服务器：不预填房间码、行里不写「回到对局」、点加入走普通 `join`、票仍在；地址换回来又照样预填 | finding ③ |

**怎么验的**：`--only=t_chat_box,t_hand,t_hand_play,t_hand_index_after_exit,t_net_resume,t_net_takeover,t_match_online,t_online_panel`
—— 181 项检查全绿。反向验过：把 `hand.gd` 那道闸换成 `pass`，「菜单压着不打牌」当场红。

## 2026-09-19 · 界面四条 issue（G-UI 组）：#47 迁移对比度 · #49 升级规则悬停 · #50 肿瘤分期悬停 · #51 聊天「己方」变白

分组任务书 G-UI。**只动 `game/scripts/ui/**` 与测试 / 预览脚本** —— `scripts/core/**`、协议件（`cw_obs_*` / `cw_net*`）、`boot.gd` / `patch_state.gd`、以及正在重做的教程那一摊（`guide*.gd` / `cw_tutorial_*` / `data/tutorial`）一个字没碰。四条都是**只新增断言、零改判**（下面每条各自写明）。

真机 / 出图一律落在 `截图_2026-09-19_issues_ui/`。

### issue #47 · 迁移模式下，癌性组织和健康组织的对比度太低

- **现象**（Kevin 附图）：迁移模式下一片候选格里，站在癌组织上的那一格和健康格几乎是一个色，看不出「这一步会不会净化」。
- **根因**：候选格的高亮是**叠在地块贴图上的半透明剪影**（`silhouette.gdshader`，混合比例 `0x6E` ≈ 0.43），两种组织共用同一抹免疫青 `MARK_MOVE` #30D1FA。算一下就明白了：健康组织主色 #2E4A41 叠完是 #2F8491（设计稿 `board_pick.png` 里那个青），而癌组织主色 #B04A5A 叠完是 **#79849F** —— 一块灰蓝，与 #2F8491 的色距只有 0.30，正是附图里那一格。**不是判层级、不是 alpha 调错，是两种底色被同一层色标洗到了一起。**
- **改法**：`board.gd` 加一条 `MARK_MOVE_SICK = #E05A6E6E`（取普通癌组织贴图那块红 #B04A5A 提亮一档，和地块贴图同一家；**混合比例照旧 0x6E**，逐环亮起那个节奏一个数没动），`ui_bridge.gd` 的 `_repaint_marks()` 在 `else` 那一支按 `mirror.is_cancerous(c)` 二选一。叠完是 #C55163，与健康格的色距 0.65 —— 拉到两倍以上。
  - **不能用癌方橙**：那是「可攻击」（`MARK_ATTACK`）的词，占了就再也读不出「格里站着敌人」。`MARK_ATTACK` 那条 `elif` 排在换红的 `else` 之前，攻击格照旧是橙。
  - 判据走 `mirror.is_cancerous`（内核的同名查询，固化癌组织也算），界面不自己数格子（架构约定 #10）。全仓只有 `_repaint_marks` 这一处配可迁移色，教程小棋盘走的是同一只 `CWBoard.set_marks`，跟着一起变。
- **怎么验的**：`t_ui_bridge` 加 6 条 —— 叠完仍是红（R−G / R−B 都 > 0.25）、**探针**（旧色标下色距只有 0.30，闸认得出 issue 报的那个现象）、新色距 > 旧的两倍、混合比例没动、健康 / 癌组织 / 固化三格分色正确。钉的是**对比度关系**不是某个色值，将来换色标它自己跟着走。真机出图 `tests/preview/preview_move_marks.gd`：**同一块棋盘**（种子写死 20260919）跑出改前 / 改后两帧，`issue47_改前.png` / `issue47_改后.png`。
- **改判表**：无。只新增断言。

### issue #51 · 聊天切换到「己方」之后字变白

- **现象**（5yntaxEr）：等待室聊天右上角那两个字，切到「己方」时是蓝的，**鼠标移开后变白，再移回也是白**。
- **根因**：那个标签是 `CWOnlinePanel._clicky` 建的，身上挂着通用悬停白光。`CWStyle.link_hot(label, true)` 会把**当时**的字色记进 meta `"rest"`（全体 = `TEXT_HI`），移开时 `link_hot(false)` 按 `rest` 还原。而 `_repaint_chat()` 里直写 `add_theme_color_override("font_color", 阵营色)` —— **看着是对的，可 `rest` 还是白的**，鼠标一移开就被还原回去，而且此后每一轮悬停都从白色重新记一遍，再也回不来。
  - 这条规矩本来就写在 `CWStyle.link_hot` 头上：「列表是随时重画的，重画时直接写 `font_color` 会把正悬停着的那一行的白光盖掉，**所以定静止色一律走 `paint_link`**」。这儿是漏了。
  - **对局内的那只聊天框（`chat_box.gd`）没有这个洞**：它的 `_scope` 只设 `MOUSE_FILTER_STOP` + 手型光标，根本没挂 `link_hot`，没有 `rest` 可被还原。所以本条只改等待室那一处，`chat_box.gd` 一个字没动。
- **改法**：`online_panel.gd` 的 `_repaint_chat()` 那一行换成 `CWStyle.paint_link(_chat_scope, …)` —— 它改的是**静止色本身**，正悬停着就只记下来、等移开再生效。一行。
- **怎么验的**：`t_online_panel` 把 5yntaxEr 那条路径原样走一遍（移上去 → 悬停着点一下换到己方 → 移开），加 4 条：悬停照旧转白（辉光没丢）、悬停着切过去白光还压着、移开还原成阵营色、再移回再移开还是阵营色，外加换回「全体」同理。出图 `tests/preview/preview_chat_scope.gd`：真等待室页（棋盘 + 菜单机位一起画，平底色会骗人）两帧 `issue51_改前.png` / `issue51_改后.png`，右下角把那两个字放大三倍 —— 差的只是 #EAF8FC 与 #30D1FA，整屏图上瞄不出来。
- **改判表**：无。只新增断言（原有那三条「坐癌症席 / 坐免疫席 / 观众」的颜色断言照旧绿）。

### issue #49 · 悬停抗原 / 效应记忆 → 浮出升级规则；issue #50 · 悬停「肿瘤 n 期」→ 浮出当期效果

两条一起做：都是右栏的一块**悬停浮窗**，共用同一只节点、同一套排版。

- **现象**：#49（HXR-I）鼠标悬停在抗原 / 效应记忆那一块时没有任何升级规则；#50（Kevin）右上角只写着「肿瘤II期」，具体效果一个字都没有 —— 而分期表改的是压迫倍率 / 增生概率 / 侵蚀格数 / 固化门槛 / 根深蒂固这一整排数，玩家没处查。
- **改法**（`match_panel.gd`，新增 150 行，既有的一个数都没改）：
  - 两块**只感应悬停的透明区**（`_put_hover`，`MOUSE_FILTER_PASS`，不吃点击，同玩家行那只）：阶段行整条（`PAD, PAD+36, W, 16`）与免疫等级整块（范围同 `rect_of("level")`）。做成一块区而不是给四个标签各挂一次 `mouse_entered` —— 那等于把「这一块」散写六遍。
  - 内容是两个**纯函数**：`level_rules_rows(tiers, level)` 与 `stage_rows(stage, solidify)`，返回 `[{text, color}]`。
  - **一个数都不写死**：门槛取内核按人数算好的 `d.level_thresholds`（同 `memory_text`，四人 0/10/20/50、六人 0/10/30/70 自动跟着走），每级收益取 `CWData.AEROBIC_BY_LEVEL` / `IMMUNE_MOVE_CANCEROUS`，解锁档位取 `DIFFERENTIATE_MIN_LEVEL`，X 级那一行的名字取 `CWData.memory_name`；分期五项取 `PRESSURE_MUL_BY_STAGE` / `PROLIFERATE_BASE_BY_STAGE` / `PROLIFERATE_SOLID_BY_STAGE` / `EROSION_TILES_BY_STAGE` / `ROOTED_BY_STAGE`，固化门槛取**内核算好的那一个数** `mirror.solidify_threshold()`。G-RULES 组改门槛表 / 分期表的话这两块自己跟上。
  - `tiers` 不全（tier B 缺席时是空表）就返回空表、干脆不画 —— 同 `memory_text` 的退路：半张门槛表比没有更糟，玩家会照着它算。
  - 浮窗摆在右栏左边 8px（`INFO_W` 252，比技能详情框宽 52 —— 要把「门槛 + 收益」并排写在一行里）。技能详情框**钉住**的时候正巧也在这条竖带上，竖向真撞上就再往左让一格（Kevin 2026-09-06 报过「两个框叠在一起」）。
- **两条已知口径**（写在代码注释里，不是遗漏）：① 按等级的那两张收益表（`aerobic_by_level` / `immune_move_cancerous`）**不在观测协议的 tune 里**（`CWObsProto.TUNE` 只有八个键），所以读的是 `CWData` 常量 —— 正式局两者恒等，只有平衡扫描改旋钮时这儿会跟不上；② **无氧呼吸的分期增益**（PRD 环境恶化那条 II +20% / III +50%）今天引擎里还没有任何 `_BY_STAGE` 表，所以 #50 不写这一行。G-RULES 组的 #56 落地后照 `stage_rows` 的样子补一行即可。
- **无头怎么验悬停**：无头视口不跟踪悬停控件（`mouse_entered` 一次都不会发，2026-09-06 探过），所以悬停做成**可注入的 Callable** `info_hover_probe`（返回 `"level"` / `"stage"` / `""`），`refresh()` 里探针在场就它说了算；真机一个字也不碰它，照旧走 `mouse_entered` / `mouse_exited`。同 `CWBoard.pointer_on_control` / `CWMainMenu.guide_done_check` 那一套。
  - ⚠ 写测试时踩到一次：GDScript 的 lambda **按值捕获**局部变量，`var zone := "stage"` 之后再改 `zone` 传不进闭包 —— 要装在数组里。
- **怎么验的**：`t_match_panel` 加 14 条 —— 四人档四级门槛逐条现算、收益现算、两处解锁、X 级换名并从零重数、当前档用免疫青标出、六人档自己跟着换、门槛表空时不画；三期五项逐项对照 `_BY_STAGE` 表、根深蒂固 I 期「未生效」/ III 期写格数、固化那一行照 `solidify_threshold()` 给的数写；**排版**（最长的一行 200 px，装得进 252 宽的浮窗）；**悬停这条路本身**（注入探针 → 浮窗出来 / 换一块改画 / 移开就收 / 整只浮在右栏左边）。
- **真机截图**（`--resolution 960x540 --windowed`，观战局让 AI 自己打）：`issue49_升级规则.png`（四人局 III 级、抗原记忆 27/50，当前档那一行是青的）、`issue50_肿瘤I期.png`、`issue50_肿瘤II期.png`（压迫 ×1.5 / 增生 3.5%+1.0% / 固化计数满 2.0 / 根深蒂固每块助推 1 格 —— 逐项跟着分期变）。步骤串见本节末。
- **改判表**：无。只新增断言。

**真机截图步骤串**（**不能加 `--headless`**；`--resolution 960x540 --windowed` 是必须的 —— 不钉的话窗口会被拉到显示器原生分辨率，`move:` 的坐标就全错位）：

```
"D:/Godot/Godot_v4.5-stable_win64.exe/Godot_v4.5-stable_win64_console.exe" --path game \
  --resolution 960x540 --windowed --script res://tests/screenshot.gd -- \
  "<输出>.png" "res://scenes/Main.tscn" -1,-1 60.0,72.0 \
  "key:ui_accept@0.6;key:ui_down@1.0;key:ui_right@1.3;key:ui_right@1.6;key:ui_down@1.9;key:ui_down@2.2;key:ui_down@2.5;key:ui_accept@2.8;move:828,352@55.0;move:828,60@66.0"
```

- 配置面板打开时焦点停在**第一行「人数」**；`ui_down` 一下到「我的阵营」，`ui_right` 两下把它从「免疫细胞」拨到**观战**（`FACTION_STEPS` = 免疫 / 癌症 / 观战 / 本地多人）—— 观战 = 全 AI，不然开局落子那一问会卡住等真人点棋盘。再 `ui_down` 三下到「进入棋盘」，回车开局。
- `move:828,352` = 免疫等级那一块（四人局 `_level_y` = 144 + 4×44 + 10 = 330，块高 44）；`move:828,60` = 阶段行（`PAD + 36` 起、16 高）。**人数一变纵坐标就变**：六人局免疫等级在 418。
- 肿瘤II期要等到第 6 世界回合，观战局大约 85~95 s（`ai_delay_ms` 400）；再晚就可能撞上结算屏。

### 收工验收

- `bash tools/run_tests.sh` 全量：两片合计 **3956 项**（基线 3929），L0 五段绿（GD 416 条 / GD 装载往返自检 416 条 / C# 453 条）。
  - **`t_feedback` 那 4 条是抢 18612 端口的假红**（任务书里点名的两个已知不稳项之一）：`--only=t_feedback,t_observe_budget` 单跑 28 项全绿。
- **闸三 `xcheck/COUNT` 抬一次**：`total_sites` 3458 → 3485（+27）。**涨的 27 条全在 `ui` 档**（1140 → 1167），`core` 的分子分母（已覆盖 416 / 候选 1029）与 `total_funcs` 255 **一个数没动** —— 没有新增测试函数，四条都是往既有的 `t_ui_bridge` / `t_online_panel` / `t_match_panel` 里加断言。理由同 S5 / S7 那两次。
- 结构闸 `t_no_engine_in_ui`、字形闸 `t_font_coverage` 都绿（浮窗里的 `×` `、` `　` `【】` `（）` `·` 字库全有，没引进新字形）。

## 2026-09-19 · 新手教程 v2 · S7：教程演出库 `cw_tutor_fx`（六种演出 / 零内核 rng / 时间的纯函数）

## 2026-09-19 · 新手教程 v2 · S6：进度四键 + 常驻目录面板（间章平级单列）+ 图鉴去闸只留通知

**三件**：存档换口径、目录面板从占位板变成真的、图鉴的闸整条拆掉。Kevin 09-19 的四条拍板逐条照做 —— 目录里**间章是独立条目、与三个主章节平级**；加一行「**Cell War**」重看开场（Q-21：按钮名就叫片名，不写「重看开场」四个字）；图鉴**去闸只留通知**；第七关全文改称**第六关**。

**① 进度四键**（`cw_guide_progress.gd`，方案 §3.6）

`done` / `at:{level, beat}` / `unlocked` / `opening_seen`。`at` 从 `{chapter, level, step}` 三个整数换成 **`{关 id, flow 下标}`** —— 关下标会随关表增删漂移，关 id 不会；`set_at()` 在导演的 `open()`（关首 beat=0）与 `_finish()`（关末）各写一次，**续读只认「关」不认「步」**（Q-11），挑关那侧的唯一入口是新加的 `at_level()`（`match.gd:_tutor_pick_level` 先认它，认不出来退回按 `done` 数挑 ⇒ **旧档只有 `done` 也能起**，仓库里我那份真存档正是老形状的 `at`，实测走的就是这条退路）。**`opening_seen` 一个字都不写**（归 `tutorial_opening.gd`），硬口径是「写盘一律 `ConfigFile.load` 之后 `set_value`」—— 整份覆盖会把它抹掉、开场每次重播，而这种错**只能正面测**（写完三个键之后 `OPENING.seen()` 仍为真），反面测不了：抹掉之后一切照跑，只是玩家每次都要再看一遍开场。`skipped` 键与 `set_skipped()` 随图鉴去闸一起**删掉**。

**② 常驻目录面板**（`cw_tutor_chrome.gd`，PRD:43）

数据由导演现拼（`menu_rows()`：`index.json` + `done`），壳**只会画** —— 不读关表、不读存档。每行 `{id, title, kind, unlocked}`，`kind` 三档：`chapter`（章标题行，现插的 —— `index.json` 里没有「章」这个东西，章是关的属性，`(chapter_kind, chapter)` 二元组一变就插一条）/ `level`（缩进 24）/ `interlude`（**不缩进，与章标题同一个 x**，而且**打断分组**：间章之后那一章要重新出一条章标题）。未通关的灰显不收点击、连回调都不挂；已通关的点了 → `menu_goto` → 皮 → 导演 `goto_level()` → `want_goto` → `match.gd` 走 `on_done` 同一条拆装序列换局，**差别只有一处：跳关不记「通关」**（`mark_done = false`）—— 否则点一下目录就把没打的关记成过了，进度越点越前。

**落地时改了方案原文两个数**（原文那两个算不过来，照实记）：面板 **480×360**（§3.6 的排版上限）居中摆在 960×540 的屏上 ⇒ `Rect2(240, 90, 480, 360)`；**行高 26 不是 34** —— 满编是「3 个章标题 + 6 关 + 1 个间章 = **10 行**」（原文写「7 行数据行 + 4 行标题行」是把间章数了两遍：间章自己就是一行，不另插章标题），10 × 34 = 340 再加表头与底部那行就装不下，26 正好（`48 + 10 × 26 = 308 ≤ 316` 页脚横线）。`capacity()` / `row_y()` 做成**纯函数**，判据逐关算一遍 —— 排版溢出这种错在真机上要一直翻到最后一章才看得见，**再加关就得先加高面板**。

底部「**Cell War**」常驻可点（不是一关，不受「未通关灰显」管）→ `replay_opening` → `match.gd` 调开场脚本**现成的** `clear_seen()`（开场三件一个字没改）→ `main.gd._replay_opening()` 照 `_back_to_menu` 那三拍收摊，最后一步不是把菜单放出来而是径直 `_begin_tutorial(0)`（`seen()` 为假 ⇒ 开场重播）。

**③ 图鉴去闸只留通知**（`cw_codex.gd` + `codex_map.json`，方案 §3.8）

PRD 全文只写「图鉴解锁：【X】」，一句「没解锁就看不到」都没有 ⇒ `_refresh_unlocked()` 里那句 `_gate = _unlocked if codex_gated() else null` 改成**恒 `null`**（那一行就是闸的全部开关），全书常驻可读。解锁时只剩两件：导演发 `codex_unlocked(ids)`（**通知小卡的画法归皮 A / S3**）+ 图鉴里那一条标题**慢闪一轮**（`_fresh` / `_glow` 那套一个字没动）。`_mark_locked` 与 `chapters(unlocked)` / `search(query, unlocked)` 的可选参数**留着不删**（§3.8 明令）：三条纯函数判据还在守着它，将来要重新上闸就是把那一行改回去。

`codex_map.json` 按剧本 **03:41 版**重写：12 个解锁点 → 9 个条目，三处不是一一对应（净化 / 抗原记忆 / 免疫等级共用一条；攻击一对二；压迫 / 增生 / 侵蚀共用一条），**照实登记**；行号跟着 03:41 走（压迫三条 451 → **455**、固化癌组织 497 → **501**），第七关改称第六关。新增 **`names` 段**（解锁点 → PRD 那个【X】）+ `CWCodex.unlock_name()` / `unlock_names()`：**通知小卡上写的是【攻击】，不是条目标题** —— 【攻击】一条落到「怎么打」「攻击骰」两条上，拿标题拼会当场多出一行。这一支是留给 S3 画卡的取字口。

**改判表**（8 条，全在 `t_codex` / `t_tutor_chrome`；一条都没有静默删）

| # | 原判据 | 改判后 | 依据 |
|---|---|---|---|
| 1 | 零进度开书：没进过教程 ⇒ 不传解锁集 | 零进度开书：`_gate` **恒 null**，一条都不灰 | §3.8 去闸；`codex_gated()` 已删 |
| 2 | 教程进行中重开书：闸开着，没解锁的仍灰显 | 解锁一条之后重开书：**全书照样常驻可读** | 同上 |
| 3 | 未解锁条目：标题 `TEXT_OFF` / 正文 `TEXT_OFF_DIM` | **与解锁了的长得一模一样** | 同上 |
| 4 | 结果页那一条灰显、命中带 `locked` | 搜得到且**不带 `locked`** | 同上 |
| 5 | 灰显那一行不收点击、没挂回调 | **收点击、挂着回调**、「章 > 条目」标阵营色 | 同上 |
| 6 | 回车不跳到灰显的那一条 | 回车**照常跳**到那一条所在的章 | 同上 |
| 7 | 跳过引导 ⇒ 闸放开（`set_skipped` / `codex_gated` 各一条，共 3 项） | **整段作废**（没有被判对象了） | `skipped` 键退役 |
| 8 | 目录占位板：表头 + 每关一行 = 3 个 Label | 真面板：表头 + 2 关 + 底部「Cell War」= **4 个** | S2 那条明写「真面板 S6 做」 |

**`chapters([])` / `search(词, [])` 那三条纯函数判据一个字没改**（`_mark_locked` 留着，它们守的就是它）。

**测试**：新增 `t_tutor_progress`（**24 项**，已登记进 `_run_all` 那份手写清单）—— 四键互不覆盖 / `opening_seen` 正面断言 / 旧档只有 `done` 也能起 / 续读认关不认步 / 满编 10 行不溢出 / 间章与章标题同级 / 灰显不可点 / 跳关代际 +1 且不记通关 / 「Cell War」发信号。`t_codex` 新增 2 项（`names` 段两个方向对扫 + 一对二只出一个名字）。全量 **3631 项绿 / 230 个测试**（基线 3607 / 229），`run_l0.sh` 五段绿。闸三 `total_sites` 3261 → **3285**、`total_funcs` 243 → 244（`--write` 抬一次）：**24 条全落 `guide` 档（200 → 224），`core` 分子分母一个数没动**。

**真机两帧**在 `截图_2026-09-19_教程v2_S6/`：① 目录（间章那一行与章标题同一个 x、「Cell War」在页脚横线底下）② + ③ 图鉴解锁那一条的**亮相 / 暗相**对拍（相隔 1.6 秒 = `HALO_PERIOD 3.2` 的半周期 —— 单帧证不了「在闪」）。**步骤串**（前置：`main_menu.gd` 的 Guide `enabled` 临时改 `true`、`index.json` 临时铺满 6 关 + 1 间章、`guide_progress.cfg` 临时写 `done=3` / `opening_seen=true` / `unlocked=["tissue_healthy"]`，**三处都还原了、都没提交**）：

```
# ① 目录
... --script res://tests/screenshot.gd -- "user://s6menu2.png" "res://scenes/Main.tscn" -1,-1 11.6 \
  "key:ui_up@0.4;key:ui_up@0.6;key:ui_up@0.8;key:ui_accept@1.0;call:CWTutorViewPlain:advance@5.4;call:CWTutorViewPlain:advance@5.8;call:CWTutorViewPlain:advance@6.2;call:CWTutorViewPlain:advance@6.6;call:CWTutorChrome:toggle_menu@11.2"
# ②③ 图鉴慢闪（四次 ui_up 从「开始对局」绕到「知识之书」；`_next_page` 翻到「棋盘与地形」那一章）
... -- "user://s6codex.png" "res://scenes/Main.tscn" -1,-1 2.6,4.2 \
  "key:ui_up@0.4;key:ui_up@0.6;key:ui_up@0.8;key:ui_up@1.0;key:ui_accept@1.2;call:CWCodex:_next_page@1.8"
```

- **目录那一帧里棋盘是空的**：满编关表把第四关指到了第一关那份数据上（临时铺的），而第一关起手就是空画布；面板居中又正好压住盘心。**要看「目录压在真盘面上」得等 S4 的关卡数据齐了再补一帧**。
- 慢闪那两帧**不是**「解锁前 / 解锁后」，是同一条标题的一个呼吸周期的两端 —— 解锁动效本来就是 `modulate.a` 在 `HALO_ALPHA_LO..HI` 之间走。

**留给 S9（间章）的接缝 —— 两件**

1. **间章怎么进目录**：在 `index.json` 里写一行 `chapter_kind: "interlude"` 就够了，目录会把它画成**顶层一行**（与章标题同 x、不缩进、不另插章标题行），`title` 那一栏写什么屏幕上就是什么（我截图时临时写的是「间章 癌变」）。**别给间章单独插一条 `chapter` 行** —— `menu_rows()` 见到 `chapter_kind != "main"` 会主动跳过插标题这一步，手写一条会变成两行。
2. **「看过」怎么记**：间章在目录里和别的关**同一套口径** —— `unlocked = 关下标 < done`，而 `done` 只由 `level_done` 那条路推进。所以间章必须像一关一样**走完 `flow` 发 `level_done`**（哪怕它整关只有一条 `play`），否则它后面那一关永远解锁不了。它自己在目录里亮起来，也是靠 `CWGuideProgress.set_done(间章的下标)`。间章「所有 UI 消失」那一条走 `shell()` 的 `can_reset: false`（壳已经认这个键了 —— 那颗「重置」整颗不出）。

**留给 S3 / S12 的**

- **S3**：通知小卡的字走 `CWCodex.unlock_names(ids)`（按次序去重），导演发的 `codex_unlocked(ids)` 里是**解锁点 id** 不是条目 id；小卡形状与位置是皮的语言，我这边一笔没画。
- **S12**：「Cell War」这条重看开场的链路已经端到端通了（壳 → 皮外的 `match.gd` → `main.gd`），但**入口那颗菜单项仍灰着**（`main_menu.gd` 的 Guide `enabled=false`，重做期间不开）—— 全量发版时把它改回 `true` 的那一刀，连带着这条链路才真到得了玩家手上。

## 2026-09-19 · 间章分镜 2 拍板：地图可超 127 格、补全缺格（路 C）

Kevin：「对地图的格数上限没有要求，可以超过 127 格，补全缺格即可」。落法写进 `docs/新手引导v2_盘面提案.md` §6.3：教程世界半径放大（建议 12），第四关整盘仍是原点 127 格，间章镜头居中玩家后揭示以玩家为心的半径 6 圆盘（可见集可超 127），第六关布局改成相对玩家的偏移、完整换局时平移装入；`board.gd` 的画格半径要从写死改成按镜像半径。归 S9。

## 2026-09-19 · 新手教程 v2 · S8：钩子层（`ctx` 九方法 + epoch 取消语义 + `t_tutor_hooks` 护栏）

把方案 §3.7 的「承重件图纸」落成代码。S1 在导演文件末留的那段接缝注释换成真实现，`flow[].hook` 从「warning 再往下走」变成**真的加载关卡脚本并当协程跑**。

**新建 / 改了什么**

| 文件 | 干什么 |
|---|---|
| `scripts/tutor/cw_tutor_ctx.gd`（新） | 钩子 → 导演 的唯一接口。九个方法签名**逐字照 §3.7**：`beat` / `until` / `read` / `alive` / `frame` / `rng` / `state` / `log` / `fail`。成员**全是私有的**，九个方法的返回类型只有 void / bool / Variant / Dictionary / RandomNumberGenerator 五种 —— 拿不到 kernel / mirror / game / view / stage 的任何原始句柄 |
| `scripts/tutor/levels/demo.gd`（新） | 示例钩子：`noop` / `peek_cells` / `tick_forever`。证明「数据点名 → 导演 → ctx」这条线通，也是护栏的正面样本（零成员变量、`while` 条件含 `ctx.alive()`） |
| `scripts/tutor/cw_tutor_director.gd` | `hook` 动词真分派（`_run_hook` + `_hook_of`）；`ctx` 的三条回调 `run_beat` / `run_until` / `next_frame`；`hook_state()` / `hook_fail()` / `hook_log`；`active_of`（`read("active")` 那条线）；`open()` 补 `invalidate()`；`invalidate()` 顺手清 `ctx.state()` |
| `scripts/ui/match.gd` | 一行：把棋盘的活跃格取法接到 `_director.active_of` |
| `tests/headless_test.gd` | 新测试 `t_tutor_hooks`（31 条），已登记进 `_run_all` 手写清单 |

**九个方法的最终签名**（S9～S11 照这个写钩子，一个字都别改）

```gdscript
func beat(row: Dictionary) -> void                            ## 协程：跑一条与 flow[] 同构的条目
func until(pred: Dictionary, timeout_secs := 0.0) -> bool     ## 协程：等谓词，超时 false
func read(q: String, arg = null) -> Variant                   ## 只读白名单八项，给副本
func alive() -> bool                                          ## 代际闸（每个 while 的条件都得含它）
func frame() -> void                                          ## 协程：让一帧
func rng() -> RandomNumberGenerator                           ## 演出随机，种子来自数据
func state() -> Dictionary                                    ## 钩子唯一合法的状态落点
func log(msg: String) -> void                                 ## 记一笔（不上屏）
func fail(why: String) -> void                                ## 协程：warning + 挂起
```

`read` 的八项：`cells`（`{seat, at, type, alive, energy}`，含死者）/ `tile`（`{state, type, solid}`，`state` = `CWData.Tissue`、`type` = `CWData.Special`，都是枚举整数）/ `dist`（两种坐标写法都认）/ `beside` / `alive_count`（`"immune"|"cancer"`）/ `round` / `energy` / `active`。白名单之外一律 `null` + warning。

**三条与方案的偏差，都已在 §3.7 改字标明**

1. **对策① 不成立，闸按实测重写。** §3.7 原话是「挂死的协程持 ctx / 导演引用 ⇒ 导演每关一只、关末显式 `dispose()`」就能回收。Godot 4.5 实测：20 代挂死的协程约 **280 只对象**，`queue_free()` 导演之后只掉 **39 只**。所以「跑 20 次重置 `OBJECT_COUNT` 不单调上涨」这条原判据写不出真判别力 —— 改成**两轮 churn 对比**：让钩子分别跑 3 帧与 12 帧，各做 20 次重置，两轮涨幅必须一样大（实测 281 vs 280）。要钉的性质是「挂死的是**每代一只**，不是每帧一只」；每代约 14 只对象，与钩子跑多久无关。副作用：这条测试跑完，进程退出时会多一句 `ObjectDB instances leaked at exit` 的**警告**（不是红，`run_tests.sh` 只认 SCRIPT ERROR / Parse Error）。**给 S10/S11 的口径：钩子里的 await 原语越少越省。**
2. **`rng()` 的种子**：方案只写「种子来自数据」，没定从哪个字段读。落法 = `hook.args.seed`，没写就按**关 id** 定死（`hash(level.id)`）。两种都与运行期无关，「同一关跑两遍逐帧复现」成立。
3. **`open()` 也算一代**。§3.7 列的是「重置 / 目录跳关 / 换局各调一次 `invalidate()`」，但仓库里换关走的是 `director.open()`，那儿原先不拨代际 —— 不补的话上一关挂着的钩子协程会在新关里继续跑（真机上就是「上一关的钩子还在改这一关的盘面」）。现在 `open()` 第一行就是 `invalidate()`。

**两件实现上的坑**

- **`_hook_depth` 用计数不用布尔**：`ctx.beat({"do":"hook"…})` 会让钩子里再点一支钩子，布尔的话内层跑完外层就被主游标当成完事了。另外 `open()` / `reset_level()` 都要把它清零 —— 挂死的那只永远不会自减，不清就再也翻不过 `hook` 那一条（`ctx.fail()` 之后按「重置」拉不回来，就是这一条漏了）。
- **`ctx.log()` 走 `print_verbose`**：账本是 `director.hook_log`（测试读它），控制台只在 `--verbose` 下出声。第七关的钩子一帧一笔，真打出来会把套件输出淹了。

**`t_tutor_hooks` 的 31 条**：签名逐字（1）/ 公开面只有九个方法、零公开成员、返回类型全是值类型（3）/ 钩子文件零成员变量、`while` 含 `alive()`、`ctx.` 后只许那九个、零引擎符号、闸自检（5）/ `scripts/tutor` 零 `class_name`（1）/ 剧本点名的钩子函数都在（1）/ 行为：`hook` 真跑起来、主游标按住不翻页、`invalidate()` 之后旧协程零流水账、`read` 八项 + 给的是副本 + 白名单外给 null、`state()` 同一只字典且随代际清空、`rng()` 同种子复现 + 零裸随机、`until` 超时 false、`fail` 挂起后「重置」拉得回来、两轮 churn 的对象账（19）。

**留给 S9～S11 的接口示例**（第七关那三只钩子照这个形状写）

```gdscript
## 围一圈（PRD:443，运行期才知道亮哪一格）
func encircle(ctx) -> void:
    while ctx.alive() and int(ctx.read("alive_count", "immune")) > 1:
        var me: Dictionary = ctx.read("cells")[0]
        await ctx.beat({ "do": "point", "hex": ["%d,%d" % [me["at"].x, me["at"].y]] })
        if not await ctx.until({ "delta": "moved" }, 30.0):
            await ctx.fail("围一圈：等了 30 秒玩家没走")   ## 必须 await：不等它挂住，钩子就返回了，主游标会把 hook 那一条翻过去
        ctx.state()["ring"] = int(ctx.state().get("ring", 0)) + 1
        ctx.log("围了第 %d 格" % int(ctx.state()["ring"]))
```

**全量**：`tools/run_tests.sh` 两片合计 **3595 项 / 228 个测试**全绿（含新增 31 条），L0 两侧同跑全绿（GD 416 / C# 453）。闸三：`total_sites` 3219 → **3250**（+31 **全落 `guide` 档**，158 → 189），`core_sites` 1029 / `covered_sites` 416 **一个没动**；已 `--write` 抬过 `xcheck/COUNT`。

## 2026-09-19 · 新手教程 v2 · S2：常驻壳补全 + 提亮层 + 通用规则实测表

S1 留的三处接缝（`chrome.show_chapter` 只有 1.8 秒没画那一屏、`view.point/clear_point/reset_anim` 是空实现、`chrome.sync` 只认 `can_reset`）这一片全接上。**Kevin 09-19 的界面拍板逐条照做**：章节提示用**方向 B 的样子**、重置 / 目录是**左上角两颗小图标**（方向 A 的画法，悬停出字）、提亮 = **控件保持单线原样只慢闪**、自动重置提示走 `cw_tutor_fx` 的 **rewind（倒带）**、禁操作期光标变「…」。

**`cw_tutor_chrome.gd` 补全**（91 → 约 320 行）

- **章节提示那一屏**（PRD:35，B 的 ① 帧）：`#0a0d14` a=0.88 全屏幕布（同 `tutorial_opening.SKY`，半透明 —— 通用规则 1 要的就是「看得见自己刚才站在哪」）+ 两道 360×1 横线 @ y=222/298 夹住 `SIZE_HERO` 大字 + 英文副标 @ y=310。文案与亮度都做成**纯函数**（`chapter_text()` / `chapter_alpha()`），测试逐字核。**总时长仍是 S1 的 `CHAPTER_SECS = 1.8`**，只是切成淡入 0.30 / 停 / 淡出 0.35 三段。这一屏排在壳的最顶、自己也 `MOUSE_FILTER_STOP`（通用规则 9：这 1.8 秒连常驻两颗一起罩住）。
- **左上角两颗图标**（PRD:41/43）：**位置没照抄方向 A 的 (12,12)** —— 那儿被迷你日志占着（`CWLogPanel.RECT` 起的 300×22），S1 就是在这儿栽过一次。往下让过 22+8 的行距是 y=46，再往下 y=76 是出牌列 `CWFeed` 的顶 ⇒ 图标由 A 的 32×32 收成 **28×28**，卡在这条缝里；`t_tutor_chrome` 有一条硬断言盯着这两条边界，谁把它挪回 (12,12) 当场红。图标是画出来的（重置 = 缺口回转环 + 箭头，目录 = 三条横杠），悬停出字、悬停换底与描边 alpha。
- **z 序**（PRD:51 第 2 层）：加进来的先后就是上下 —— 全屏 STOP 层最底、两颗按钮在它之上（**提示期照常可点**）、章节提示最顶。挡着的时候指针旁边跟着三颗「…」点（方向 A 第 7 条：降灰只说「这颗按钮不可用」，光标说的是「整个画面不接受操作」）。
- **目录占位面板**：点图标开一块 424×388 的板，`shell()` 给了关表就列（未解锁的灰着点不动），点已解锁的那一关发 `menu_goto` 并收面板。**真面板（章-关两级 / 间章平级单列 / 焦点菱形 / 跳关本体）仍是 S6 的活**，本片只把这条线接通。

**`cw_tutor_spot.gd`（新）—— 提亮层**

★ 这一片最要紧的一条是 **Kevin 的提亮口径**（方案 §5.5 第 1 条「迁移按钮改为单线原样，用闪烁来提示用户」）：**拿得到本体的目标一笔都不画**，只推拉它自己的 `modulate.a`（曲线 = `CWStyle.HALO_*` 那三个常量，与劝重置慢闪同一条）。不加描边、不换底色、不套第二层框。为此给 `action_bar.gd` 加了一支 `button_node(title)`（`button_rect` 改成转调它）—— 光有矩形改不动控件本身的亮度。拿不到本体的（右栏的块、手牌抽屉、棋盘格）才退到「描一圈」，那些地方本来就没有自己的外框。`mode` 三档：`soft`（缺省）/ `arrow`（像素箭头）/ `fullscreen`（PRD:445 暗幕留洞 + 箭头）；`tip` 挂一块最小的牌（气泡形状与尖角是 S3 皮的语言）。几何三支（`hex_points` / `hand_rect` / `board_rect`）从 `git show 35e6c91:game/scripts/ui/guide_spotlight.gd` 原样搬；**`place_tiles` / `purify_tiles` / `cell_tiles` 没搬** —— 那三支是老 `FLAGS` 语义转译表的配件，方案 §5.4 已经把那张表作废（剧本直接给 id 与坐标）。**目标每帧现算**：行动栏那排按钮是「问的时候才建」的，而 `point` 排在 `player` 前面，发意图那一帧栏里还没有【迁移】。

**装配与接线**（`match.gd`，只加不改）：提亮层压在皮之下、HUD 之上，四个句柄（行动栏 / 右栏 / 棋盘 / 相机）由装配方注入；**演出库 `cw_tutor_fx` 也在这里建并 `attach(board)`**（S7 做完一直没人接），`_tutor_view.fx` 指过去。拆局时提亮层先 `clear()` 把借走的按钮亮度还回去再销毁 —— 不还的话下一局别人的按钮停在半亮上。基类 `cw_tutor_view.gd` 的默认实现这下真的转调了：`point/clear_point` → 提亮层，`block/chapter/urge_reset/shell` → 常驻壳，`reset_anim` → `fx.play("reset_hint", {variant: rewind})`。

**真机踩到的一件**：皮原先「没注入就自己建一只 fx」，因为我只看了 `_paint_screen` 那一半就断定「倒带全画在屏幕层上」。实际 `_draw_rewind` 要 `board.tile_center`，没接棋盘时**每帧一条** `Nonexistent function 'tile_center' in base 'Nil'`。改成只认装配方注入的那一只，皮不自建。

**改判表**：一条 `check()` 都没删、没改判。两支新测试全是新增断言；`t_tutor_view` 里「基类给齐了九个方法」那条源码扫描照旧成立（本片只填了实现，签名一个字没动）。`action_bar.button_rect` 的行为不变（改成转调 `button_node`），老断言原样绿。

**测试**：新增 `t_tutor_chrome`（23 项）/ `t_tutor_spot`（20 项），已登记进 `_run_all` 那份手写清单。全量 **3607 项绿 / 229 个测试**（基线 3564 / 227），`run_l0.sh` 五段绿。闸三 `total_sites` 3219 → **3261**、`total_funcs` 241 → 243（`--write` 抬一次）：**42 条全落 `guide` 档（158 → 200），`core` 分子分母一个数没动**。

**方案 §6.3 改成实测表**：原表是开工前的计划，本片按仓库里真有的断言逐格重写，没归宿的明写「待 S4 / S6」——
规则 1 `t_tutor_flow`+`t_tutor_view`+`t_tutor_chrome`；2 `t_tutor_director`；3 `t_tutor_data`⑧+`t_tutor_flow`；4 `t_tutor_chrome`（「重置回关首那份 world」待 S4 的 `t_tutor_c1`）；5 `t_tutor_chrome`（真面板与存档待 S6 的 `t_tutor_progress`）；6 `t_tutor_flow`/`t_tutor_view`+`t_board_active_tiles`；7 `t_tutor_spot`（「排在 `load` 之前」待 S4 的 `reset_when`）；8 `t_tutor_flow`+`t_tutor_spot`+真机对拍；9 `t_tutor_gate`+`t_tutor_flow`+`t_tutor_chrome`；10/11/12 `t_tutor_data` / `t_tutorial_npc`（「非人类席都有 decider」待 S5）/ `t_tutor_data`。

**真机六帧**在 `截图_2026-09-19_教程v2_S2/`：① 章节提示（B 样式）② 提示期（禁操作层开着）③④ 慢闪对拍 ⑤ 目录占位 + 左上两图标（悬停出字）⑥ 重置动画（倒带）。**步骤串**（前置：`main_menu.gd` 的 Guide `enabled` 临时改 `true`、`guide_progress.cfg` 的 `done` 临时改 0，**两处都还原了、都没提交**）：

```
"D:/Godot/Godot_v4.5-stable_win64.exe/Godot_v4.5-stable_win64_console.exe" --path game \
  --script res://tests/screenshot.gd -- "user://s2.png" "res://scenes/Main.tscn" -1,-1 \
  3.6,5.0,7.0,8.6,9.6,11.0 \
  "key:ui_up@0.4;key:ui_up@0.6;key:ui_up@0.8;key:ui_accept@1.0;call:CWTutorViewPlain:advance@5.4;call:CWTutorViewPlain:advance@5.8;call:CWTutorViewPlain:advance@6.2;call:CWTutorChrome:toggle_menu@9.0;move:66,60@9.2;call:CWTutorChrome:toggle_menu@10.0;call:CWTutorViewPlain:reset_anim@10.4"
```

- **三次 `ui_up` 比数着往下点稳**：从「开始对局」往上绕回去依次是 退出游戏 / 设置 / **新手引导**，这三项的 `enabled` 不随存档与回放变（「继续对局」「对局回放」会）。
- **慢闪那两帧是同一枚按钮的亮相与暗相**（7.0 / 8.6 相隔 1.6 秒 = `HALO_PERIOD 3.2` 的半周期）。**不是「未提示 vs 提示中」** —— 【迁移】那颗是这一问才建的，而 `point` 与 `player` 是同一步，按钮存在的每一帧都在被提亮，拍不到「同一颗按钮不被提亮」的样子。「轮廓线宽与颜色一字不差」由 `t_tutor_spot` 正面断言（提亮前后逐项比 `border_width_left` / `border_color` / `bg_color` / 字色）。
- `call:CWTutorViewPlain:reset_anim` 只是把那段演出播一遍**给人看**，不真重置（真重置走导演的 `reset_when`，S4）。

**要 Kevin / hxr 拍的两件**

1. **章节提示那一屏，棋盘正中的主角细胞会从大字底下透出来**（见 ① 帧：「第」字后面那团青色）。这正是方向 A 文档预告过的那个坑，A 的解法是在幕布上再压一条整幅横带；B 的解法是两道横线，**没有解决透出来这件事**。Kevin 定的是 B 的样子，所以照 B 落了。要不要往 B 上补一条横带（或把幕布 alpha 从 0.88 抬到 0.94）？一行常量的事。
2. **副标那支字**：方向 B 的表写「silkscreen_bold 20 + `spacing_glyph` 2」，理由却是「照抄开场页」；而开场页副标（`tutorial_opening.gd:57-60`）用的是**点阵字**、silkscreen 是那一页的大 Logo 那支。本片按 B 的**表**落了 silkscreen（① 帧里那行 `CHAPTER I - CELL`）。要换成点阵只改一行常量。

**留给 S3 ~ S12 的接缝**

- **S3（皮）**：`say` / `hint` / `codex_unlocked` 仍是占位皮那三种最小画法（文字行 + 「继续」）。气泡的形状、尖角、图鉴解锁小卡是皮的语言；提亮层只出一块最小的 `tip` 牌，皮要自己那套的话覆写 `point` 即可。
- **S4（自动重置 / 劝重置）**：`reset_anim` 这一跳已经通到演出库，缺的是 `reset_when` 命中 → `await view.reset_anim()` → 重装关首那份 world 的**顺序断言**（§6.3 规则 7 那一格）。
- **S6（目录）**：`chrome.sync()` 已经认 `state["menu"]`（`[{id,title,unlocked}]`），但**导演今天还不发这个键**（`cw_tutor_director.gd:98` 只发 `chapter/level/can_reset`）；`menu_goto` 从壳到皮的线已接（`match.gd` 一行转发），皮那头到导演的跳关还没人接。真面板照方向 A 的 `_toc` 几何（占位板已经按这个矩形摆着）。
- **S10/S11（第七关）**：`spot.rect_of()` 的 `skill:<名>` 分支今天返回零矩形（技能栏是第七关才出现的件），PRD:479/491 要用到它时在那儿接。

## 2026-09-19 · 新手教程 v2 · 盘面提案 v3（Kevin 第二轮四条意见）：**第四关一次揭到整盘**

## 2026-09-19 · 新手教程 v2 · 盘面提案 v4（Kevin 第三轮 + PRD 03:41 版）

对齐 PRD **03:41 版**（整份重读）。与 02:12 版只有两处 diff，**都是盘面相关**：PRD:395-399 间章分镜 2 从一行扩写成三行（镜头居中 + 地图以免疫细胞为中心补齐成完整棋盘的中央 + 镜头后续不再移动）；PRD:419「第七关」→「**第六关**」。其余逐字未变（12 条通用规则、第一～五关正文、第六关 25 步流程）。

① **第三关把 (3,0)(4,0)(5,0) 也变成癌组织**（r=0 排右段并入右端那个外突连通块，块 6 → **9 格**）。癌细胞 (5,-1) 一环里只剩 (6,-2) 一格健康，而它只能穿过癌组织到达（3.5）⇒ **最省从 2.5 涨到 3.0、且变成两条并列**：直线 (1,-1)(2,-1) 健康 + (3,-1)(4,-1) 癌；绕 r=-2 排 4 格健康 + (5,-2) 癌。**能量 6.1 → 6.6**；劝重置阈值仍 **35**，照样把最省（剩 36）与次优 3.5（剩 31）分得开。新增一问 Q3-6：并列两条要不要改成唯一。

② **第四关四个癌块各放大到 10 格**（共 40 格），块间仍 ≥3 格；癌细胞 4 → **5 只**（两两 ≥2 格）；记忆账重算 = 净化 40 + 打掉 17.0 = **57**，升 III 只需 +10（富余 47，已列为 Q4-3 问 hxr 要不要减）。玩家起手改 **(4,-1)**（第三关直线最省路打完站的那格，已被净化成健康组织）。

③ **第五关与第四关同规格**：Step2 四块各 **12 格**（PRD:361 扩大）+ 固化 3 格 (6,-1)(1,-5)(2,3)（都避开第六关围圈那一环）+ 各类免疫各一；癌细胞收成 **4 只**给四个免疫席让位（9 席到顶）。五只免疫两两 ≥3 格。

④ **全文「第七关」改「第六关」**（标题 / 帧名 / 文档 / 勾选表）。**第六关盘面暂不动**（Kevin 原话）—— 06/07 两帧与 v3 逐格相同，只换关号，几何（距 9 → 11 = 5×2+1 → 两次转移邻接）原样。

⑤ **新开 §6.2：PRD 03:41 分镜 2 与「第四关起整盘」的矛盾**，两条路写清代价、列为 Q6-2 等 Kevin/hxr 拍板。路 A：第四关已把 127 格揭完、没有缺失格子可补，这一条退化成**纯镜头动作**（改动为零）。路 B（协调者候选）：间章分镜 6 那次完整换局把世界**重心平移到玩家**，老盘里离玩家超过半径 6 的自然掉出、新露出的用浮现补进。**路 B 的代价是第六关整套几何作废** —— 玩家站盘心时到任何格最远只有 6，距离 9 / 11 物理上不存在；重摆版已写成表（玩家/复活固化 (0,0)、巨噬 (-2,0)、围圈终点 (-1,-1)、T (-5,-1)、距离 **4** → 两轮进 1 退 2 落 (1,-1) 距 **6 = 5×1+1** → **只需一次【转移】**到 (-4,-1) 邻接），并点名 PRD:483「点击**若干次**转移」在路 B 下只剩一次、要重读。

`preview_tutor_boards.gd`：新增 `_check_blocks()`（癌块两两 < 3 格当场 push_error，顺带打每块格数），帧名与 tag 全部改第六关。实测：第四关 A/B/C/D 各 10 格、块间 3 格、癌细胞最近 2 格；第五关各 12 格、块间 3 格、免疫最近 3 格、癌细胞最近 5 格。

图在 `截图_2026-09-19_教程v2_盘面示意图v4/`（八帧）。勾选表 37 条。

