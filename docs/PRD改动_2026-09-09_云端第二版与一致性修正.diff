# PRD 云端第二版（Kevin 2026-09-09 深夜再拉一次）+ 我的六处一致性修正
#
# 我上一版 SHA-256: df31d06357a9d0208767c97f58c63d92596d77afa373dd464af5010f39cffdf2
# 云端原样 SHA-256: 3a8e9909d389df620f9854f6726ef3992903e5e6349aba907205703b2547e27b
#   （备份 Cell_War PRD.备份_2026-09-09_云端第二版原样.md）
# 修正之后 SHA-256: ca61952f4ab2cd7716ad8f23f227073b1827941408d79e7556fc67f64a415937
#
# ============ 云端带进来的改动 ============
#
# 一、**通用规则 3 去掉「n世界回合后」**，只留「持续n世界回合」。
#     连带【骨样硬化】改写成「标记持续3世界回合，然后转为固化癌组织并移除标记」
#     = 第（当前+3−1）= **当前+2** 回合 E 阶段 = **引擎现状**（OSTEO_OSSIFY_ROUNDS=2）。
#     这一条正是 Kevin 回答「第三点：不是的」—— 上一版写「2世界回合后」会被通用规则 3
#     读成当前+1、悄悄快一回合，团队这次改措辞把它堵死了。**引擎不用动。**
#
# 二、**新增「负担性判定」一节**：「算出最终数值之后，再判断付不付得起，
#     非自毁型技能的费用不能使自身能量降至 ≤0」；主动技能一节补了同样一句，
#     并加上「由技能效果造成能量降至≤0时，细胞死亡」。
#     ⭐ 这正是同日修的「规划器把自己走死」所依据的规则 —— 引擎 payment_floor = 0.1
#     （十分能量下「>0」⟺「≥0.1」），两边一致；【突变】自扣致死也对得上。
#     规则从此有明文依据，不再是口径。
#
# 三、**癌症【S-复活】两条路合并成一条**：
#     「每个癌细胞可以在**固化癌组织1环内**无细胞占据的**癌组织**复活，获得2能量，
#       随后该固化癌组织转为癌组织」
#     Kevin：「云端文档现在更新了，保留这条链路」→ 队友旁复活保留。
#     ⚠ 合并后的**字面读法**与引擎有两处对不上，已列进开发日志问 Kevin，**引擎暂不动**：
#       · 落点写的是「癌组织」，而棋盘设定里「癌组织」**不含**「固化癌组织」——
#         字面读就是**不能落在固化格自己身上**（旧规则的第一条路）。只差一个「性」字。
#       · 对依托的固化格**被谁占据**只字未提。引擎现在「被免疫占着的两条路都不给」，
#         那是 Kevin 2026-08-31 的裁定（「免疫踩着固化格堵复活位…请保留」）。
#         沉默不等于推翻，所以保持引擎行为，但 PRD 该补一句。
#
# 四、免疫等级按人数分档、黏液 0.2 —— 团队把我同日的改动收进去了（黏液只收了一处，见 ①）。
#
# 五、特殊状态一节**新增「骨样硬化」条目**，与骨肉瘤卡面那处逐字相同。
#     ⚠ 这是 PRD 里**第四类**「同一条规则重复出现」的源头（前三类：免疫卡按池子重复、
#       黏液侵染两处、癌种技能与卡面两处）。这几天反复翻车的根因就是它 —— 改一处漏两处。
#
# ============ 我的六处修正（都是一致性缺陷，不是设计改动）============
#
# ① **黏液加费两处不一致**：特殊状态那处仍是 0.5、卡面那处已是 0.2 → 统一到 **0.2**（Kevin 定的数）。
# ② **【免疫增援】三处三种写法**（第 4 次撞上）。其中一处
#    「传送至其**相邻2格范围内**的随机健康组织」把「无细胞占据」漏掉了 ——
#    这不只是措辞，是**漏了一条约束**。统一到最精确的那一版。
# ③ **【补体级联】三处两种写法**（语义相同）→ 统一到出现两次的那一版。
# ④ **【应激蛋白】两处差一个句号** —— 又是它。补上。
# ⑤ **【BCL-2抗凋亡】卡面出现「≤」**（第 3 次）。像素字库没有这个字符，上屏是豆腐块，
#    改回「0 或更低」。**规则正文里的四处「≤」保留** —— 那些不上屏，而且更精确。
#
# ②③④ 任一不修，`gen_card_data.py` 就跑不通 → 发版守卫（publish_release.sh 的 ⓪）会直接拦下。
# 这次守卫如期生效，**是它加进去之后第一次真的挡住东西**。
#
# ============ 引擎侧同批改动 ============
#
# 【增生】/【侵蚀】：增生新造的格子**不作侵蚀的来源**
#   （PRD 的「注」，Kevin 定「只把新格排除在『来源』之外」）。
#   `_proliferate()` 改成返回本轮新造的格子，`e_phase()` 写成 `_erosion(_proliferate())`；
#   「完全包围」仍按实时盘面算 —— 新格已经不是健康组织了，连通块本来就变了，不去还原。
#   ⚠ 这是**削癌方**：从前增生刚铺的一圈当场就能给侵蚀当跳板。
#
--- "Cell_War PRD.\345\244\207\344\273\275_2026-09-09_\344\272\221\347\253\257\347\254\254\344\272\214\347\211\210\345\216\237\346\240\267.md"	2026-09-09 10:16:18.699045700 -0700
+++ "Cell_War PRD.md"	2026-09-09 10:25:37.404421200 -0700
@@ -262,7 +262,7 @@
 
 - 「黏液侵染」状态无法被技能清除，免疫细胞进入该格后立即消失
 
-- 免疫细胞迁移进入「黏液侵染」格时，迁移耗能\+0\.5
+- 免疫细胞迁移进入「黏液侵染」格时，迁移耗能\+0\.2
 
 骨样硬化
 
@@ -810,7 +810,7 @@
 
 权重：4
 
-效果：自身本回合下一次攻击成功后，随机从目标癌细胞相邻的无细胞占据的癌组织中选择最多2格，将其转为健康组织。
+效果：自身本回合下一次攻击成功后，随机从目标癌细胞相邻的普通癌组织中选择最多2格没有细胞占据的癌组织，将其转为健康组织。
 
 
 
@@ -976,7 +976,7 @@
 
 权重：2
 
-效果：选择1个其他免疫细胞，传送至其2环内无细胞占据的随机健康组织，若无合法健康组织则无法使用
+效果：选择1个其他存活的免疫细胞，传送至其2环内无细胞占据的随机健康组织，若无合法目的地则无法使用
 
 
 
@@ -1152,7 +1152,7 @@
 
 权重：3
 
-效果：选择1个其他免疫细胞，传送至其相邻2格范围内的随机健康组织，若无健康组织则无法使用
+效果：选择1个其他存活的免疫细胞，传送至其2环内无细胞占据的随机健康组织，若无合法目的地则无法使用
 
 
 
@@ -1172,7 +1172,7 @@
 
 权重：3
 
-效果：下一次受到微环境压迫或癌细胞技能造成的能量损失\-1
+效果：下一次受到微环境压迫或癌细胞技能造成的能量损失\-1。
 
 
 
@@ -1488,7 +1488,7 @@
 
 效果：
 
-自身受到能量损失后若能量降至≤0，则自身不死亡，能量改为0\.5 / 0\.8 / 1。
+自身受到能量损失后若能量降至0或更低，则自身不死亡，能量改为0\.5 / 0\.8 / 1。
 
 触发后本牌被弃置，可重新抽取
 
