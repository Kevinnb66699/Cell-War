# PRD 改动 2026-09-19（删世界事件）—— **只出 diff，不改 PRD 正本**
#
# 正本：D:/Projects/SpringSense/2026-2027/Cell War/Cell_War_玩法PRD.md（仓库外，云端是权威）
#
# ## 为什么
#
# Kevin 2026-09-19：「我们不加入世界事件了」。引擎侧（main 与 csharp-core 两条分支）
# 同批把 15 个世界事件的内容与抽取容器整块删掉，PRD 这边也该跟上 ——
# 否则下一次拉云端 PRD 回来，`tools/gen_card_data.py` 还会照着「# 世界事件」那一章找锚点，
# 而引擎里已经没有这些机制了。
#
# ## 四处改动
#
#  1. :59   删「第3、6、10、14世界回合触发**世界事件****（暂时停止维护）**。」整行
#  2. :63-65 删「1. **世界事件阶段**」与其子条；后面的阶段**重新编号**
#            （S阶段 2→1、玩家行动阶段 3→2、E阶段 4→3、世界回合结束 5→4）
#  3. :195  「按给予此效果的卡牌、世界事件等的入场顺序」→「按给予此效果的卡牌等的入场顺序」
#  4. :1597-1706 删整章「# 世界事件（暂时停止维护…）」到文件末尾（15 个事件条目）
#
# ## 留给 Kevin 的一条（Q2）：来源层级那一行（现在是 :175）
#
#     同一阶段内有多个效果时，先按**来源层级**（细胞自带被动 → 卡牌 → 技能 → 世界事件）
#
# 这一行**下面的 diff 里没有动**。它是排序契约、不是世界事件的内容，引擎两侧都按它实现着
# （GD `cw_cost.gd:enum Source { PASSIVE, CARD, SKILL, WORLD }` ↔ C# `Settlement.cs:SourceLayer.WorldEvent`），
# 删掉末项会改枚举取值、动整条排序契约与 L0 的 move_cost 期望。两版任选：
#
#   (a) 原样保留（本 diff 的选择）—— 引擎零改动，末项暂时没有来源住进去；
#   (b) 末项改成「其他来源」：
#
#       -同一阶段内有多个效果时，先按**来源层级**（细胞自带被动 → 卡牌 → 技能 → 世界事件）
#       +同一阶段内有多个效果时，先按**来源层级**（细胞自带被动 → 卡牌 → 技能 → 其他来源）
#
#       选 (b) 的话引擎侧只改注释字句，枚举成员名保持 WORLD / WorldEvent 不动
#       （改名会动 `Settlement.cs` 的序列化与 GD 的 enum 值 —— 那要跟协议升号一起走）。
#
# ## 连带（已随本批落地，不在这份 diff 里）
#
#   · `tools/gen_card_data.py:32` 原先拿「# 世界事件」当免疫卡池节的**结束锚点**，
#     团队按本 diff 删章之后会当场 StopIteration。已改成
#     `end = next((i for i, l in enumerate(L) if l.startswith("# 世界事件")), len(L))`
#     —— 删章前后两种 PRD 都跑得通。
#   · 引擎侧的三个观测协议字段（`events.pool` / `double_next` / `active[].doubled`）
#     与旋钮 `world_events_on` **本批只冻结不删**：删它们要升 NET_VERSION + 全量发版，
#     登记到批 1 全量发版那次协议升号一并物理删除。
#
# ---------------------------------------------------------------------------

--- Cell_War_玩法PRD.md（2026-09-19 现状，1706 行）
+++ Cell_War_玩法PRD.md（删掉世界事件之后）
@@ -56,15 +56,9 @@
 
 游戏的进程由**世界回合**决定。
 
-第3、6、10、14世界回合触发**世界事件****（暂时停止维护）**。
-
 每一世界回合按照以下顺序结算：
 
-1. **世界事件阶段**
-
-    - 若当前世界回合达到世界事件触发回合，则结算1次【世界事件】。
-
-2. **S阶段**
+1. **S阶段**
 
     1. 结算特殊组织资源生产
 
@@ -86,13 +80,13 @@
 
     7. 结算其他尚未结算的S类技能、状态和效果。
 
-3. **玩家行动阶段**
+2. **玩家行动阶段**
 
     - 按照本局行动顺序，所有未死亡的玩家依次完成自己的行动回合。
 
     - 玩家死亡时跳过其行动回合。
 
-4. **E阶段**
+3. **E阶段**
 
     1. 所有存活的癌细胞结算【无氧呼吸】。
 
@@ -112,7 +106,7 @@
 
     9. 进行胜利条件检查。
 
-5. **世界回合结束**
+4. **世界回合结束**
 
     - 若当前为第15世界回合，在完成上述全部结算及胜利检查后进行15回合终局胜负判定。
 
@@ -192,7 +186,7 @@
 
 - 其余各类：只要适用条件成立（目标类型正确、次数未用尽），即便本次没有改变数值，其次数与每行动回合额度**照常消耗**
 
-- 若存在多个作用范围等大或不等的免费额度，按给予此效果的卡牌、世界事件等的入场顺序进行结算
+- 若存在多个作用范围等大或不等的免费额度，按给予此效果的卡牌等的入场顺序进行结算
 
 ### **抽卡合法性**
 
@@ -1594,113 +1588,3 @@
 
 - 本世界回合自身前两次向癌性组织发动【移动】时，该次移动不消耗能量；
 
-# 世界事件（暂时停止维护，正常平衡性测试和对局不考虑世界事件）
-
-随机抽取，同一对局中不会出现重复的世界事件。
-
-**【营养输送】**
-
-持续2回合，2回合中每个细胞首次通过血管时，获得2能量，抽1张牌。
-
-
-
-**【异常增殖】**
-
-持续2回合，组织增生概率翻倍。
-
-
-
-**【代谢加速】**
-
-持续2回合，进入代谢核心的细胞获得的能量翻倍。
-
-
-
-**【紊乱】**
-
-本回合，所有细胞随机传送至各自阵营组织内除血管外的随机位置。回合结束时，返回原位置。
-
-若返回原位置时组织不属于当前阵营：
-
-- 免疫细胞触发【净化】
-
-- 癌细胞触发【定殖】
-
-
-
-**【****抗原引导****】**
-
-持续2回合。免疫细胞攻击不会无效。
-
-攻击时：
-
-- 5/6 概率成功
-
-- 1/6 概率大成功
-
-
-
-**【免疫伪装】**
-
-持续2回合，免疫细胞攻击不会大成功。癌细胞移动具有\+0\.2能量消耗。
-
-攻击时：
-
-- 1/3 概率无效
-
-- 2/3 概率成功
-
-
-
-**【抗原变异】**
-
-持续2回合，免疫细胞若攻击无效，被攻击的癌细胞抽1张牌。若攻击大成功，免疫细胞抽一张牌。
-
-
-
-**【抗原暴露】**
-
-持续2回合，免疫细胞获得抗原记忆时，获得\+1抗原记忆。
-
-
-
-**【增殖抑制】**
-
-本回合，组织无法增生。所有免疫细胞失去0\.5能量。所有癌细胞弃1张牌。
-
-
-
-**【营养缺乏】**
-
-本回合，代谢核心清空并不产生能量，骨髓清空并不产出抽卡机会。
-
-
-
-**【双重触发】**
-
-若可能，下一个世界事件触发两次。
-
-
-
-**【细胞应激】**
-
-本回合，使用卡牌花费0\.5能量。
-
-
-
-**【基质阻隔】**
-
-持续2回合，癌细胞移动能量花费翻倍。
-
-
-
-**【迁移激活】**
-
-持续2回合，回合开始时，每个免疫细胞首次移动能量花费设为0。
-
-
-
-**【信号放大】**
-
-持续2回合，卡牌效果中能量的增益或减益数值翻倍。
-
