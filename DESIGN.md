# Cell War 设计系统（DESIGN.md）

> 从现有实现**提取**的规范（extraction 分支）：令牌一律以 `game/scripts/ui/cw_style.gd`（颜色/字号/控件底座）
> 与 `board.gd` / `match.gd` / `guide_spotlight.gd`（动效节拍）为准。本文件是教程重构及一切后续 UI
> 改动的契约：**新增颜色、字号、间距、动效先记进这里再写代码**；两处不一致时以代码为准并回改本文。

## 1. 氛围与识别点

深色显微视野里的像素棋盘：组织块贴图 + 点阵字 + 2px 单色描边构成全部层级，不用阴影。
阵营即语义——免疫青、癌方橙各占一个心智位，其余文字按亮度分四档。动效语言统一为
「同一坐标格上的状态变化」：高亮淡入、同心环排队、脉冲描边、六帧呼吸，全部服务可读性，无装饰动效。

## 2. 颜色

| 角色 | 令牌（CWStyle 常量） | 值 | 用途 |
|---|---|---|---|
| 画布底 | `GROUND` | `141f2e` | 游戏画布 |
| 面板底 | `PANEL` | `0f1822` | 面板 |
| 按钮底 | `BTN_BG` | `0a1018e6`（半透明） | 压在棋盘上的按钮/面板 |
| 免疫/强调 | `IMMUNE` | `30d1fa` | 免疫方、可交互强调 |
| 癌方 | `CANCER` | `ffb03a` | 癌方、警示数值 |
| 描边基色 | `LINE` | `3fa5b6`（用时带 alpha） | 2px 描边 |
| 文字四档 | `TEXT_HI` / `TEXT` / `TEXT_DIM` / `TEXT_OFF` | `eaf8fc` / `cfe2e6` / `7b929b` / `5c737c` | 主要/常规/次要/灰掉 |
| 灰掉次要 | `TEXT_OFF_DIM` | `44565e` | 灰按钮的费用行 |

### 棋盘格染色（board.gd，混合比例即语义强度）

| 令牌 | 值 | 语义 |
|---|---|---|
| `MARK_MOVE` | `30d1fa6e`（0.43） | 可迁移/可移动 |
| `MARK_ATTACK` | `ffb03a6e` | 可攻击 |
| `MARK_HOVER` | `eaf8fc8f`（0.56） | 鼠标所在格 |
| `MARK_PLAN` / `MARK_PLAN_BAD` | `30d1fabf` / `ffb03abf`（0.75） | 路径规划器：走得通/走不通 |
| `MARK_SELF` | `eaf8fc47`（0.28） | 当前行动细胞脚下 |
| `MARK_SOLID`（match.gd） | `0000004d` | 固化癌组织压暗色标 |
| `MARK_OSSIFY`（match.gd） | `e8d9a0` 脉冲 | 骨样硬化倒计时格 |

规则：**格上交互态用免疫青系、敌意/危险用癌方橙系、中性提亮用白**；新增状态先在此表登记再使用。

## 3. 字体与字号

- 唯一字体：`fusion_pixel_10px`（`CWStyle.FONT`）。字号只有四档：`SIZE_HERO 40`、`SIZE_BIG 30`、
  `SIZE_BODY 20`、`SIZE_LABEL 10`。
- **字号必须是 10 的整数倍**（点阵重采样约束，架构约定 #13）；新字号先进本表再使用。
- 全角字符宽度按实测（`CWStyle.FONT.get_string_size`）预算，不估——悬浮框/面板正文都按最长行实测撑宽。

## 4. 间距与布局

- 画布逻辑分辨率 960×540（架构约定 #12）；右侧竖条 `CWView.PANEL_WIDTH` 一条、左侧 `CWView.LEFT_STRIP` 留给出牌列。
- 面板内边距用 `CWStyle.box(border_alpha, bg, pad_v, pad_h)` 的参数给，不散写 magic px。
- 常用基准：引导浮层 `CWGuide.ZONE = Rect2(88, 16, 600, 200)`（与机位锚点 388 同心、无可见框）、
  `PAD 16`，纵向节奏 `ROW_*`：眉行 4 / 标题 22 / 正文 58 / 行动提示 92 / 尾巴 150 / 按钮行 164；
  按钮/区块间距 `GAP 8`（浮层按钮行 16）；描边外让 `GROW 4`；按钮高度 52（行动栏）。
  同屏可点控件彼此 ≥16px 空档。

## 5. 组件

| 组件 | 出处 | 状态 |
|---|---|---|
| `CWStyle.label / clickable_label` | cw_style.gd | 常态/悬停白（文字按钮） |
| `CWStyle.plate / keycap / box` | cw_style.gd | 快捷键垫块、2px 描边框 |
| 引导浮层 | guide.gd | **无框沉浸式**（2026-09-10 Kevin 拍板取消窗口）：说明文字浮在棋盘上方居中，白雾横带托可读性；「继续 / 下一章 / 完成引导」用免疫青（悬停提白），「跳过引导」「知识之书」暗色次级 |
| 引导白色柔光 | guide.gd `_halo` / `_fog` | 行动提示底下的椭圆白光（3.2s 慢呼吸）+ 整块白雾横带；**全游戏唯一柔光/渐变破例**，仅此一层 |
| 行动栏按钮 | action_bar.gd | 灰掉（不消失只变暗）/常态/悬停三态；数字键 1..9 |
| 棋盘高亮剪影 | board.gd `set_marks` | 每帧全量替换；淡入 + 同心环排队 |
| 引导提亮描边 | guide_spotlight.gd | HUD 矩形 / 六边形脉冲描边；FLAGS 注册表是唯一入口（新 flag 必须登记，测试盯着） |
| 悬停详情卡 | tile_info.gd / card_info.gd | 贴格子/贴卡摆放的纯函数布局 |

## 6. 动效与交互

| 令牌 | 值 | 语义 |
|---|---|---|
| `MARK_FADE` | 0.22s | 高亮淡入/淡出（整批「啪」出现是反例） |
| `MARK_RING_DELAY` | 0.045s/环 | 新高亮按重心同心环由内向外亮起 |
| 提亮脉冲 | 周期 1.2s，alpha 0.35→0.95，线宽 2 | 引导「看这里」；醒目但不报警 |
| 柔光慢呼吸 | 周期 3.2s，modulate alpha 0.35→1.0 | 引导行动提示的白色柔光（guide.gd `_halo`）；比提亮脉冲慢一截，读起来是「呼吸」不是「报警」；**唯一的装饰性动效破例**（浮层是画外音，不映射状态） |
| 呼吸 | 6 帧 @6fps，按细胞编号错相位 | 静息生命感 |
| 出牌三拍 | 蓄力 0.07 + 0.16/0.30/0.36 + 留拍 0.08 | 卡牌往返（抽到 = 倒放 + 吸收 0.14） |
| 细胞入场 | 0.32s 淡入 + 0.7→1.0 缩放 | 「就位」而非凭空出现 |

- **动效必须映射状态变化**（可迁移、可攻击、翻面、倒计时），禁止装饰性动效。
- **可中断/可跳过**：演出不锁输入；结果由引擎先定，表现层只演（约定 #11）。
- **减弱动效开关先例**：`CWSettings.dice_anim` / `teleport_anim`——关掉只跳演出、结算与判定不变。
  教程引导动效将沿用同一模式（开关名进本表后再接线）。

## 7. 深度与表面策略

**2px 单色描边 + 底色透明度**是唯一层级手段（`CWStyle.box` 的 border_alpha 分档：1.0 悬停 / 0.5 常态 /
0.22 灰掉 / 0.42~0.45 面板；描边色一律 `LINE`）。无阴影、无圆角分层；
**唯一破例是引导浮层**（2026-09-10）：白雾横带 + 白色柔光是软渐变（GradientTexture2D 就地线性采样，
全局默认最近邻会把渐变采成硬带），其余界面不得引入柔光/渐变/阴影。
棋盘上的东西用画家算法 z 序（`board.tile_z` + Z_MARK/Z_CELL/Z_DICE）。

## 8. 可及性约束与已接受债

- 10px 点阵字是可读下限；正文 20px、标签 10px，不再更小（像素字体特性，非 Web 标准）。
- 键盘可达：行动栏数字键、Esc/右键=取消、L=日志；焦点态 = 悬停同款提亮。
- 已接受债：固化癌组织外壳为压暗色标（美术未交付）；癌变血管与健康血管已分图（2026-09-08 结清）。
  新债在此登记，不默默接受。
